File: File_26
Title: The Precessional Bridge — The 430/432 Shift from 18726 BC
Status: Final; pressure tested
Pressure-test status: Current; publication-clean verification complete; May 28 2026 pressure-tested status retained.
Register Agreement status: Checked against State_Vocabulary_Register v1.17 and active companion controls; convergence / pointer update only; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries changed.
Publication-cleanup status: Public-clean Markdown source; archival amendment history routed to Repository_Change_Archive.
Primary domain: SKL; Prophetic; Comparative; Calendar; Theological; Mirror; Deep Time
Traditions: SKL; Hebrew Bible; MT; Exodus / Mosaic; Abrahamic; Danielic / prophetic; Egyptian calendar comparison
Canonical source: Markdown
Primary anchors: 434886 BC; 29516 BC; 27366 BC; AD 27366; 20916 BC; 20886 BC; 18766 BC; 18756 BC; 18726 BC; 2166 BC; 10136 BC; 10126 BC; 10086 BC; 1846 BC; 1526 BC; 1486 BC; 14466 BC; 1446 BC; 1406 BC; 586 BC; 2906 BC; 2886 BC; 2856 BC; AD 29515; AD 33 by dependency
Related files: File_00; File_01; File_03; File_05; File_12; File_15; File_16; File_17; File_19; File_21; File_24; File_25; File_30 by dependency; File_51a; File_54
Major operators: same-side BC span; civil cross-axis span `BC + AD − 1`; SKL rounded mod-10 Mirror display `BC + AD − 2`; authorial forward-time `+` display; SKL `+30` rail-label addition; Pillar Mirror counterpart display label; `18726 − 2166 = 16560`; `18726 − 1526 = 17200`; `18726 − 1446 = 17280`; `27366 − 18726 = 8640`; `18726 − 10086 = 8640`; `10086 − 1446 = 8640`; `27366 − 1446 = 25920`; `18726 − 10126 = 8600`; `10126 − 1526 = 8600`; `10086 − 1486 = 8600`; `29516 − 27366 = 2150`; `29516 − 20916 = 8600`; `29516 − 18766 = 10750`; `29516 − 14466 = 15050`; `1446 + 29515 − 1 = 30960`; `586 + 29515 − 1 = 30100`; `434886 − 2886 = 432000`; `434886 − 1446 = 433440`; `434886 + 29515 − 1 = 464400`; `434886 + 27366 − 2 = 462250`; `430 × 40`; `432 × 40`; `120 × 72`; `120 × 71⅔`; `240 × 72`; `240 × 71⅔`; `360 × 72`; `360 × 71⅔`; `1290 × 336`; `1290 × 360`; `215 × 2150`; `430 × 1008`; `430 × 144 × 7`; `6450 × 72`; `6480 × 71⅔`; `18 × 25800`
Major modal states: File_24 extra state; Nangishlishma-start / Abraham lock state; pre-branching `18726 BC` state; Balih standard / hidden-`50` start state; Historical Lock / `430` state; Cosmic Lock / `432` state; Standard `8640` precessional-season triad; Alternate `8600` precessional-season triad; `2160/2150` precessional-month dual state; Moses three-`40` life-division state; Egyptian three-season comparison state; Famine Window integration state; `1876 BC` Nexus 10x rail comparison state; Pillar Mirror counterpart display state; `AD 29515` actual Pillar state by File_19 / File_21 dependency; remote `434886 BC` Danielic / adjusted Pre-Creation circuit state; Priestly `1290 × 336` Exodus state; Prophetic `1290 × 360` Pillar state; SKL rounded mod-10 Mirror `AD 27366` counterpart state; Decimal Invariance / `10x` display state by File_25 dependency; no-formal-Mirror local state except explicitly bounded Pillar / SKL mod-10 Mirror display; no-Residue local state; Rounded Scaffold non-retrojection state
Revision basis: 490d Repository Style Guide v2.5; Restart Capsule v11.15; State Vocabulary Register v1.17; Project Procedures v3.2; prior finalized File_26 source; File_00; File_01; File_03; File_05; File_12; File_15; File_16; File_17; File_19; File_21; File_24; File_25; File_51a; File_54; Repository_Change_Archive (non-controlling history).

Current refresh note: Public-clean trust-status equalization aligns active control pointers to 490d Repository Style Guide v2.5, Restart Capsule v11.15, State_Vocabulary_Register v1.17, and Project Procedures v3.2. Register Agreement status is recorded in the header; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed. Detailed pass logs, pressure-test records where applicable, pointer-refresh history, and replacement-workflow notes are preserved in Repository_Change_Archive.
# File_26 — The Precessional Bridge: The 430/432 Shift from 18726 BC

## Related Repository Files

| File | Function for `File_26` |
|---|---|
| `File_00` | Controls source-map discipline, same-side versus cross-axis arithmetic, day-year / scale-state vocabulary, and Mirror / civil / inverse / Residue firewalls. |
| `File_01` | Controls Abraham birth at `2166 BC`, the landing point for the pre-branching `18726 BC → 2166 BC = 16560` display. |
| `File_03` | Controls Joseph biography and the `1876 BC` Entry field where Joseph-related homage or nations-bowing comparison language is later indexed. |
| `File_05` | Controls the primary Exodus anchor `1446 BC`, Moses / Exodus state language, the `430/432` precessional pair, and the Famine-window paired target field. |
| `File_12` | Controls the Priestly `336`-day Ledger where the remote `1290 × 336` Exodus route is opened by comparison. |
| `File_15` | Controls Moses' `40`-year generational unit and the larger `40/70/100` generational-unit register where Moses' three `40` divisions are invoked. |
| `File_16` | Controls the `1876 BC` Nexus, the `1878–1871 BC` Famine Window, the `1846 BC` Affliction-start state, the `430/400` distinction, and `10x` scaling where the Nexus is compared. |
| `File_17` | Controls prophetic span-component vocabulary, especially `400`, `430`, `432`, `72`, `71⅔ = 215/3`, `2150`, `2160`, `25800`, `25920`, `1290`, and `336/360` prophetic-register distinctions by dependency. |
| `File_19` | Controls the actual `AD 29515` Pillar state by dependency and prevents local BC-side Pillar display labels from replacing the actual AD Pillar node. |
| `File_21` | Controls SKL scalar, SKL `+30` rail, SKL Short / Long terminology, SKL rounded mod-10 Mirror display `BC + AD − 2`, Jachin `AD 29515`, and the remote `434886 BC → AD 29515 = 464400` Pillar route by dependency. |
| `File_24` | Immediate parent file. Controls the First Dynasty of Kish `23`-kings cipher, Nangishlishma hidden-`50`, Balih standard / hidden-`50` dates, En-tarah-ana coordinate states, `18726 BC → 2166 BC = 16560`, `27366 BC → 1446 BC = 25920`, and the downstream `2906/2856 BC` terminal field. |
| `File_25` | Controls Decimal Invariance, 10x display vocabulary, decimal-shadow display, biblical 10x Mirror-scale display, SKL / Rounded mod-10 display, and BC/AD counterpart display-label handling. |
| `File_30` | Dependency-controlled target file for the AD 33 convergence claim. `File_26` preserves the target vector but does not locally verify File_30's `490/4900` prophetic count. |
| `File_51a`; `File_54` | Provide Rounded Scaffold non-retrojection and later-file claim-status restraint. |

External source-control note:
The Egyptian-calendar comparison uses the author-supplied reference:

```markdown
https://en.wikipedia.org/wiki/Egyptian_calendar
```

This external source-control note is used only for the calendrical analogy that the ancient Egyptian civil calendar had three `120`-day seasons within the `360`-day body of the year, with five epagomenal days outside the year proper. It does not supply the repository's SKL, biblical, precessional, or theological interpretation.

Dependency note:
`File_26` is a companion to `File_24`. It does not rederive the full First Dynasty of Kish cipher, the SKL rail system, the En-tarah-ana coordinate quartet, the File_21 Pillar framework, or the File_30 AD 33 target. It isolates the pre-branching value of `18726 BC` and tests its `430/432`, `8640/8600`, `1876 BC` Nexus, Pillar, and remote Pre-Creation interfaces.

## 0. File-function

`File_26` demonstrates the importance of `18726 BC` before the downstream `+50` parallel-column dates branch through En-tarah-ana and toward the terminal anchors `2906 BC` and `2856 BC`.

Active state:
File_24 extra state; Nangishlishma-start / Abraham lock state; pre-branching `18726 BC` state.

Publication-cleanup note:
This public-clean source preserves the Final and pressure-tested status in the header. Detailed pass logs, pressure-test records, and replacement-workflow notes are archived rather than repeated in the public body.

In `File_24`, Nangishlishma is the third king in the First Dynasty of Kish `23`-kings arrangement. He carries the visible `670` and the hidden-completion grammar:

```markdown
670 + 50 = 720
```

In `File_26`, however, the first active anchor is not the later En-tarah-ana `+50` branch. The active anchor is the start of Nangishlishma at `18726 BC`, before the parallel `+50` column becomes visible through En-tarah-ana.

The file therefore tests six pre-branching and extension relations:

```markdown
18726 − 2166 = 16560
18726 − 1526 = 17200
18726 − 1446 = 17280
27366 − 18726 = 8640
18726 − 10086 = 8640
29516 − 18766 = 10750
```

The first relation ties `18726 BC` to Abraham birth at `2166 BC`. The second and third relations show how Moses' birth-to-Exodus span bridges the Historical `430` state and the Cosmic `432` state. The fourth and fifth relations place `18726 BC` inside a threefold `8640` precessional-season structure from the SKL Long variant `27366 BC` to the primary Exodus at `1446 BC`. The sixth relation introduces the Pillar Mirror-side extension from `29516 BC` into the same `2150/8600/10750` system.

Argument-control rule:
Arithmetic facts carry the local argument. Structural inferences explain why the pre-branching `18726 BC` anchor matters inside the broader File_24 Kish-cipher field. Theological and providential language is preserved where it explains meaning, but it does not replace arithmetic or dependency control.

Modal-state rule:
A shared number, reign value, date-label, SKL rail, biblical anchor, harmonic, Mirror display, or precessional display does not create identity unless state, node-class, tradition, and operator agree.

## 0.1 Working state register

| State | Primary values / forms | Node-class / function | Handling in `File_26` |
|---|---:|---|---|
| File_24 extra state | whole file | companion to `File_24` | `File_26` extends one File_24 point rather than replacing the parent Kish-cipher file. |
| Nangishlishma-start / Abraham lock state | `18726 BC`; `2166 BC`; `16560` | pre-branching SKL-to-Abraham display | Core local anchor. Same-side BC arithmetic: `18726 − 2166 = 16560`. |
| Nangishlishma hidden-`50` grammar | `670`; hidden `50`; `670 + 50 = 720` | third-king hidden-completion grammar | Preserved by dependency from `File_24`. In this file, it explains why `18726 BC` matters before the later `+50` branch appears. |
| En-tarah-ana downstream `+50` state | En-tarah-ana coordinate states by `File_24` dependency | later branch-state | Not locally rederived. En-tarah-ana is the downstream branch where the hidden `50` grammar becomes visible in the coordinate system. |
| Short / Long terminal rail state | `2906 BC`; `2856 BC`; related `2936/2906 BC` and `2886/2856 BC` rail-pairs | First Dynasty of Kish terminus field | Dependency-controlled by `File_24` / `File_21`. `File_26` names the terminus field but does not rederive it. |
| Balih standard / hidden-`50` start state | `10136 BC`; `10086 BC`; Balih reign `400` | Etana endpoint / Balih-start boundary | Imported from `File_24`. `10086 BC` becomes the central standard-precessional `8640` landing in this file. |
| Historical Lock / `430` state | `18726 BC → 1526 BC = 17200`; `430 × 40 = 17200` | Moses birth / historical generation lock | Core local state. |
| Cosmic Lock / `432` state | `18726 BC → 1446 BC = 17280`; `432 × 40 = 17280` | Exodus / cosmic generation lock | Core local state. |
| Moses birth-to-Exodus bridge state | `1526 BC → 1446 BC = 80` | bridge from `430` to `432` | The bridge is Moses' first `80` years, not Moses' whole lifespan. |
| Moses three-`40` life-division state | `1526 → 1486 → 1446 → 1406 BC` | Moses life pattern by three `40` spans | Used as typological / structural comparison for the three `8640` precessional-season segments. |
| Standard Precession Year state | `72`; `360 × 72 = 25920`; `120 × 72 = 8640`; `240 × 72 = 17280` | standard precession display | Exact state. |
| Alternate Precession Year state | `71⅔ = 215/3`; `360 × 71⅔ = 25800`; `120 × 71⅔ = 8600`; `240 × 71⅔ = 17200` | alternate precession display | Exact state. Do not decimalize as executable arithmetic. |
| Standard `8640` precessional-season triad | `27366 → 18726 → 10086 → 1446`; each segment `8640` | three `120`-day precessional seasons under `72` | Divides `25920` into three equal `8640` segments. |
| Alternate `8600` precessional-season state | `18726 → 10126 → 1526`; `10086 → 1486`; `120 × 71⅔ = 8600` | alternate `25800` precessional-season display | Shows how the alternate precession register intersects Moses' age-40 and birth states. |
| `2160/2150` precessional-month dual state | `2160 = 30 × 72`; `2150 = 30 × 71⅔`; `4 × 2160 = 8640`; `4 × 2150 = 8600` | standard / alternate precessional-month comparison | Explains the `10`-year offset between related rail displays. |
| Famine Window integration state | `1878–1871 BC`; `1876 BC`; `1846 BC`; `1446 BC` | Famine / Entry / Affliction / Exodus field by File_16 dependency | Uses `1876 BC` as Nexus label and `1846 → 1446 = 400` as Affliction state. |
| `1876 BC` Nexus 10x rail comparison state | `18766/18756 BC`; `1876.6/1875.6`; `+30` SKL rail | local 10x comparison to the Nexus / Joseph homage display | Bounded comparison only; not ordinary civil chronology and not a new event-node. |
| Pillar Mirror counterpart display state | `AD 29515`; counterpart label `29516 BC` | Mirror-side display label / 10x structural extension | Author-supplied File_26 state. It tests the same `2150/8600/10750/15050` structure from the BC-side counterpart label. It does not replace the actual `AD 29515` Pillar state. |
| `AD 29515` actual Pillar state | `AD 29515`; `1446 BC → AD 29515 = 30960`; `586 BC → AD 29515 = 30100` | actual civil Pillar node by File_19 / File_21 dependency | Dependency-controlled. File_26 may cite the actual Pillar state but does not turn it into ordinary historical chronology. |
| Projective Pillar / Temple-Time state | Jachin `AD 29515`; `1446 BC → AD 29515 = 30960`; `430 × 72` | Projective Boundary / Temple-Time coordinate by File_21 dependency | Used as bounded Pillar extension. This is not ordinary historical chronology. |
| Decimal Invariance / `10x` Pillar extension state | `4300`; `8600`; `10750`; `15050`; `29516 BC`; `AD 29515` | File_25-style `10x` structural display | Extends File_25 `10x` vocabulary into File_26. It does not collapse biblical 10x display, SKL / Rounded mod-10 display, and ordinary civil cross-axis arithmetic. |
| Remote `434886 BC` Danielic / adjusted Pre-Creation circuit state | `434886 BC`; `2886 BC`; `1446 BC`; `AD 27366`; `AD 29515` | remote Pre-Creation / Danielic / Pillar extension | Demonstrates that the `430`, `2150`, `1290`, `336`, `360`, and Pillar patterns extend beyond the local Flood / Pillar field. Provenance remains dependency-controlled by File_21. |
| Priestly `1290 × 336` Exodus state | `434886 − 1446 = 433440 = 1290 × 336` | Priestly Ledger route to the Exodus | Uses the File_12 Priestly `336`-day Ledger as the active unit. This is not the Prophetic `360` route and not a Rounded Scaffold state. |
| Prophetic `1290 × 360` Pillar state | `434886 + 29515 − 1 = 464400 = 1290 × 360` | Daniel 12 / Projective Pillar route | Uses ordinary civil cross-axis arithmetic to the actual Pillar `AD 29515`. The Pillar remains Projective Boundary / Temple-Time, not ordinary historical chronology. |
| SKL rounded mod-10 Mirror `AD 27366` counterpart state | `434886 + 27366 − 2 = 462250 = 215 × 2150` | SKL Mirror-side counterpart display | Valid only under File_21's SKL rounded mod-10 Mirror display. Ordinary civil cross-axis arithmetic would give `434886 + 27366 − 1 = 462251`, so this row must be explicitly state-labeled. |
| Egyptian three-season comparison state | three seasons of `120` days each | calendrical analogy | External source-control comparison only. Does not prove the SKL or biblical arithmetic. |
| File_30 AD 33 target vector | AD 33 by dependency | Scripture / Stars convergence claim | Preserved as dependency-controlled target vector. Not locally verified. |
| No-formal-Mirror local state | no formal Mirror protocol active except bounded display rows | firewall | `File_26` does not open Protocol 1, Protocol 2, or inverse-number reversal. The bounded Pillar and SKL mod-10 display rows are explicitly labeled. |
| No-Residue local state | no Residue Protocol active | firewall | Do not treat the `80`, `120`, `240`, `8600`, `8640`, or `6480` displays as residue arithmetic. |
| Rounded Scaffold non-retrojection state | clean values such as `8600`, `8640`, `16560`, `17200`, `17280`, `25800`, `25920` | actual / local-state harmonic displays | Clean values are not File_51a Rounded Scaffold values unless a later section explicitly opens that state. |

## 0.2 File map

| Section | Function | Active state / operator |
|---|---|---|
| §0 | Defines file function, working states, and Machine Guards | File_24 extra state; pre-branching `18726 BC`; bounded Pillar / mod-10 display firewalls |
| §1 | Identifies the pre-branching `18726 BC` anchor | Nangishlishma-start / Abraham lock state |
| §2 | Records the 430/432 bridge from `18726 BC` to Moses' birth and the Exodus | `430 × 40`; `432 × 40`; same-side BC arithmetic |
| §3 | Highlights the two Precession Year states | `25800`; `25920`; `71⅔`; `72` |
| §4 | Adds the `8640` standard precessional-season triad | `27366 → 18726 → 10086 → 1446`; `3 × 8640 = 25920` |
| §5 | Adds the `8600` alternate precessional-season / Famine Window integration | `120 × 71⅔ = 8600`; `2150/2160`; `1876` 10x Nexus comparison |
| §6 | Adds the Pillar / `10x` Mirror-side extension | `29516 BC`; `AD 29515`; `4300`; `10750`; `15050`; `30960` |
| §7 | Adds the remote `434886 BC` Pre-Creation / Danielic circuit | `1290 × 336`; `1290 × 360`; `215 × 2150`; `BC + AD − 2` |
| §8 | Records cross-file target vectors and claim-status controls | File_24; File_05; File_16; File_17; File_21; File_25; File_30 dependency |
| §9 | Synthesizes the enlarged argument | `18726 BC`; `8640/8600`; `430/432`; Pillar; `434886 BC` |
| Appendix A | Preserves the local File_24 parallel matrix reference | standard / hidden-`50` First Dynasty of Kish columns |

## 0.3 Machine Guards

Machine Guard [FILE_26 SAME-SIDE BC SPANS]:
All local BC-to-BC spans in `File_26` are same-side BC spans. Compute by subtracting the lower BC date from the higher BC date. No ordinary civil cross-axis span is active unless an AD target is explicitly present.

Machine Guard [FILE_26 AUTHORIAL FORWARD-TIME PLUS]:
When the author says that a BC date “+” a span reaches a later BC date, the statement is forward-in-time language. The executable same-side BC display subtracts. For example, authorial `18726 BC + 8640 = 10086 BC` is checked as `18726 − 8640 = 10086`.

Machine Guard [FILE_26 PRE-BRANCHING 18726]:
`18726 BC` is the pre-branching Nangishlishma-start anchor. It must not be collapsed with the downstream En-tarah-ana coordinate quartet, the hidden-`50` En-tarah-ana branch, the terminal `2906/2856 BC` anchors, or the SKL Short / Long total states.

Machine Guard [FILE_26 NANGISHLISHMA HIDDEN-50]:
The display `670 + 50 = 720` is the Nangishlishma hidden-completion grammar inherited from `File_24`. It does not correct the visible reign value `670`, does not replace En-tarah-ana's coordinate states, and does not create a new local `+50` column inside `File_26`.

Machine Guard [FILE_26 BALIH STANDARD / HIDDEN-50 NON-COLLAPSE]:
Balih has two relevant start labels by File_24 dependency: `10136 BC` in the standard column and `10086 BC` in the hidden-`50` variant column. `File_26` may use both. Do not collapse them into one Balih date.

Machine Guard [FILE_26 430/432 NON-COLLISION]:
The `430` and `432` locks are simultaneous states. The `430` state reaches Moses' birth at `1526 BC`. The `432` state reaches the primary Exodus at `1446 BC`. Neither state corrects the other.

Machine Guard [FILE_26 PRECESSIONAL YEAR STATES]:
The alternate Precession Year uses `71⅔ = 215/3` years per degree, giving `25800`. The standard Precession Year uses `72` years per degree, giving `25920`. Do not decimalize `71⅔` as executable `71.66` when exact arithmetic is being checked.

Machine Guard [FILE_26 8640 / 8600 NON-COLLAPSE]:
`8640` belongs to the standard `72`-year precessional-day state: `120 × 72 = 8640`. `8600` belongs to the alternate `71⅔`-year precessional-day state: `120 × 71⅔ = 8600`. These are parallel states, not corrections of one another.

Machine Guard [FILE_26 2160 / 2150 MONTH-DUALITY]:
`2160 = 30 × 72` is the standard precessional month. `2150 = 30 × 71⅔` is the alternate precessional month. Their `10`-year difference may generate a local rail offset, but it must not be treated as an arithmetic error or as a replacement of one month-state by the other.

Machine Guard [FILE_26 1876 10X NEXUS COMPARISON]:
The `18766/18756 BC` display is a local `10x` scale comparison to the `1876 BC` Nexus / decimal Nexus field. It is not ordinary civil cross-axis arithmetic and not a new historical date-state.

Machine Guard [FILE_26 FAMINE / AFFLICTION DISTINCTION]:
`1878–1871 BC` is the Famine Window. `1876 BC` is an internal Year-2 / Entry / Nexus label. `1846 BC` is the Affliction-start state. The span `1846 − 1446 = 400` must not be collapsed with the MT Sojourn span `1876 − 1446 = 430`.

Machine Guard [FILE_26 PILLAR MIRROR COUNTERPART]:
`29516 BC` is a bounded Pillar Mirror counterpart display label for the actual Pillar `AD 29515`. It is not the active File_19 local Pillar node, not an ordinary historical anchor, and not a replacement for `AD 29515`.

Machine Guard [FILE_26 PILLAR CIVIL / MIRROR FIREWALL]:
When `AD 29515` is used as an actual Pillar state, civil cross-axis spans use `BC + AD − 1`. When `29516 BC` is used, it is a Mirror-side counterpart display label. Do not mix these operators inside one calculation.

Machine Guard [FILE_26 4300 / 2150 / 8600 / 15050 NON-COLLAPSE]:
`4300 = 10 × 430` belongs to the local `10x` display state. `2150 = 5 × 430 = 30 × 71⅔` belongs to the alternate precessional-month state. `8600 = 2 × 4300 = 4 × 2150`; `10750 = 2.5 × 4300 = 5 × 2150`; `15050 = 3.5 × 4300 = 7 × 2150`. These values interact in File_26 but are not one generic operator.

Machine Guard [FILE_26 REMOTE 434886 PRE-CREATION]:
`434886 BC` is a Danielic / adjusted Pre-Creation anchor by dependency. It is not the Table 6.1 SKL Short Pre-Creation base `452886 BC`, not the SKL Long Pre-Creation base `459366 BC`, and not a replacement for the local `18726 BC` pre-branching Nangishlishma anchor.

Machine Guard [FILE_26 434886 CIVIL / MOD-10 FIREWALL]:
`434886 BC → AD 29515` uses ordinary civil cross-axis arithmetic: `434886 + 29515 − 1 = 464400`.

`434886 BC → AD 27366` uses File_21 SKL rounded mod-10 Mirror display: `434886 + 27366 − 2 = 462250`.

Do not mix these operators.

Machine Guard [FILE_26 336 / 360 NON-COLLAPSE]:
`1290 × 336 = 433440` belongs to the Priestly 336-day Ledger route from `434886 BC` to the Exodus at `1446 BC`. `1290 × 360 = 464400` belongs to the Prophetic / Danielic route from `434886 BC` to the Jachin Pillar at `AD 29515`. The two routes are complementary, not interchangeable.

Machine Guard [FILE_26 CLAIM-STATUS FOR LARGE-SUM PERSISTENCE]:
The persistence of the pattern at `434886 BC` may be classified as structural inference and providential synchronization. Do not word it as a formal anti-randomness proof unless a defined statistical model is supplied.

Machine Guard [FILE_26 NO ROUNDED / NO RESIDUE]:
`File_26` does not open the File_51a Rounded Scaffold or Residue Protocol. Clean values such as `8600`, `8640`, `16560`, `17200`, `17280`, `25800`, `25920`, `30960`, `433440`, and `464400` remain actual / local-state harmonic displays unless a later section explicitly opens another state.

## 1. The pre-branching `18726 BC` anchor

Active state:
Nangishlishma-start / Abraham lock state; File_24 extra state.

The first purpose of `File_26` is to isolate `18726 BC` before the downstream `+50` parallel-column branch appears through En-tarah-ana.

The Abraham lock is:

```markdown
18726 − 2166 = 16560
```

This is also:

```markdown
16560 = 46 × 360
16560 = 10 × 1656
```

Structural inference:
The `18726 BC` anchor stands before the En-tarah-ana branching field and already locks to Abraham birth at `2166 BC`. This makes `18726 BC` a pre-branching control point for the later `16560` triad in `File_24`.

Dependency note:
`File_24` controls the larger Abraham / dual-Exodus `16560` triad. `File_26` foregrounds the first member:

```markdown
18726 BC → 2166 BC = 16560
```

The later File_24 members remain dependency-controlled:

```markdown
18056 BC → 1496 BC = 16560
18006 BC → 1446 BC = 16560
```

## 2. The 430/432 bridge from `18726 BC`

Active state:
Historical Lock / `430` state; Cosmic Lock / `432` state; Moses birth-to-Exodus bridge state.

`File_26` next tests the same pre-branching anchor against Moses' birth and the primary Exodus.

| Target | Same-side BC calculation | Generational display | Precessional display | Active state |
|---|---:|---:|---:|---|
| Moses birth | `18726 − 1526 = 17200` | `430 × 40 = 17200` | `240 × 71⅔ = 17200` | Historical Lock / `430` |
| Primary Exodus | `18726 − 1446 = 17280` | `432 × 40 = 17280` | `240 × 72 = 17280` | Cosmic Lock / `432` |
| Moses birth-to-Exodus bridge | `1526 − 1446 = 80` | `(432 − 430) × 40 = 80` | `240 × (72 − 71⅔) = 80` | shift bridge |

The exact precessional calculation for the Historical Lock is:

```markdown
71⅔ = 215/3
240 × 215/3 = 17200
```

The exact precessional calculation for the Cosmic Lock is:

```markdown
240 × 72 = 17280
```

The bridge between them is:

```markdown
17280 − 17200 = 80
1526 − 1446 = 80
432 − 430 = 2
2 × 40 = 80
72 − 71⅔ = 1/3
240 × 1/3 = 80
```

Conclusion:
Moses' birth-to-Exodus span functions as the clutch between the Historical `430` state and the Cosmic `432` state. The bridge is not Moses' whole lifespan. It is the `80`-year span from Moses' birth to the Exodus.

## 3. Two Precession Year states

Active state:
Alternate Precession Year state; Standard Precession Year state.

`File_26` highlights two Precession Year states.

| Precession Year state | Years per degree | Full 360-degree cycle | `240`-unit bridge | `120`-unit season |
|---|---:|---:|---:|---:|
| Alternate / Historical | `71⅔ = 215/3` | `360 × 71⅔ = 25800` | `240 × 71⅔ = 17200` | `120 × 71⅔ = 8600` |
| Standard / Cosmic | `72` | `360 × 72 = 25920` | `240 × 72 = 17280` | `120 × 72 = 8640` |

The difference between the two full Precession Year states is:

```markdown
25920 − 25800 = 120
```

The difference across the `240`-unit bridge is:

```markdown
17280 − 17200 = 80
```

The difference across the `120`-unit season is:

```markdown
8640 − 8600 = 40
```

Structural inference:
The `240`-unit bridge is two-thirds of the full 360-degree precessional cycle. Therefore the `120`-year difference between the two full Precession Year states appears as an `80`-year difference across the File_26 bridge:

```markdown
120 × 2/3 = 80
```

The `120`-unit season is one-third of the full 360-degree precessional cycle. Therefore the same `120`-year difference appears as a `40`-year difference across one season:

```markdown
120 × 1/3 = 40
```

This is why the Moses birth-to-Exodus span can mediate the shift from the `430` historical register to the `432` cosmic register, and why the same system can also touch Moses' three `40`-year life divisions.

## 4. The `8640` standard precessional-season triad

Active state:
Standard `8640` precessional-season triad; Balih hidden-`50` start state; SKL Long Flood-to-Exodus precessional-year state.

The first addition extends the File_24 `27366 BC → 1446 BC = 25920` display.

The standard Precession Year is:

```markdown
25920 = 360 × 72
```

One `120`-day precessional season is:

```markdown
120 × 72 = 8640
8640 = 24 × 360
8640 = 4 × 2160
2160 = 30 × 72
```

The SKL Long variant `27366 BC` to the primary Exodus at `1446 BC` is:

```markdown
27366 − 1446 = 25920
```

This full precessional year divides into three equal `8640` segments:

| Segment | Same-side BC calculation | Precessional form | Local meaning |
|---|---:|---:|---|
| Segment 1 | `27366 − 18726 = 8640` | `120 × 72` | SKL Long variant to pre-branching Nangishlishma-start |
| Segment 2 | `18726 − 10086 = 8640` | `120 × 72` | Nangishlishma-start to hidden-`50` Balih-start |
| Segment 3 | `10086 − 1446 = 8640` | `120 × 72` | hidden-`50` Balih-start to primary Exodus |

Therefore:

```markdown
27366 → 18726 → 10086 → 1446
8640 + 8640 + 8640 = 25920
```

Structural inference:
`18726 BC` is the first internal joint in the three-season precessional year from the SKL Long variant to the Exodus. `10086 BC`, the hidden-`50` Balih-start state, is the second internal joint. The primary Exodus at `1446 BC` completes the third season.

Balih reign note:
Balih's reign is `400` years in both File_24 columns:

```markdown
10136 − 9736 = 400
10086 − 9686 = 400
```

Typological note:
Because Balih's reign is `400`, the hidden-`50` Balih-start at `10086 BC` may be compared with the `400` years in a land not their own:

```markdown
1846 − 1446 = 400
```

This is a comparison to the Covenant-of-Pieces / Affliction state. It does not replace the File_16 distinction between `1876 BC` Entry / Nexus and `1846 BC` Affliction-start.

Moses-life comparison:
Moses' life is divided into three `40`-year sections by dependency:

```markdown
1526 − 1486 = 40
1486 − 1446 = 40
1446 − 1406 = 40
```

The three `8640` precessional-season segments correspond to the three Mosaic `40`-year divisions at a `216:1` scale:

```markdown
8640 ÷ 40 = 216
25920 ÷ 120 = 216
```

Calendrical comparison:
The Egyptian civil calendar comparison is external source-control only. It supplies the analogy that a `360`-day year can be grouped into three `120`-day seasons. This supports the readability of the threefold `8640 + 8640 + 8640` display, but it does not prove the SKL or biblical chronology.

## 5. The `8600` alternate precessional-season and Famine Window integration

Active state:
Alternate `8600` precessional-season state; `2160/2150` precessional-month dual state; Famine Window integration state; `1876 BC` Nexus 10x rail comparison state.

The second addition runs beside the `8640` state and uses the alternate Precession Year:

```markdown
25800 = 360 × 71⅔
71⅔ = 215/3
```

One alternate `120`-day precessional season is:

```markdown
120 × 71⅔ = 8600
8600 = 4 × 2150
2150 = 30 × 71⅔
2150 = 5 × 430 = 10 × 215
```

The `18726 BC` pre-branching anchor reaches a Moses-birth bridge by two `8600` segments:

```markdown
18726 − 10126 = 8600
10126 − 1526 = 8600
18726 − 1526 = 17200
17200 = 2 × 8600
17200 = 430 × 40
```

The `10126 BC` point is a `10`-year downstream overshoot from the standard Balih start `10136 BC` under forward-time BC movement:

```markdown
10136 − 10126 = 10
```

This `10`-year displacement is explained by the difference between the standard and alternate precessional month-states:

```markdown
2160 − 2150 = 10
```

### 5.1 The `2150/2160` month-pair from the SKL Flood / Kish beginning

Active state:
`2160/2150` precessional-month dual state; SKL `+30` rail state.

The standard precessional-month route is:

```markdown
20886 − 18726 = 2160
2160 = 30 × 72
```

The alternate month-state may conceptually stand beside it:

```markdown
2150 = 30 × 71⅔
```

From the SKL Flood / Kish beginning to the standard Balih start, the alternate sequence is:

```markdown
2150 + 8600 = 10750
10750 = 430 × 25
20886 − 10750 = 10136
```

This explains why `10136 BC`, the standard Balih-start state, remains meaningful inside the alternate precession system.

With the SKL `+30` rail active, the same month-pair appears as:

```markdown
20916 − 18756 = 2160
20916 − 18766 = 2150
18766 − 18756 = 10
```

The `18756 BC` rail-label is generated from the `18726 BC` pre-branching anchor under the local `+30` rail-label state:

```markdown
18726 + 30 = 18756
```

This is a rail-label addition, not a same-side BC span.

### 5.2 Balih to Moses: attempted and successful deliverance

Active state:
Balih hidden-`50` start state; Moses three-`40` life-division state; alternate / standard precessional-season comparison.

The hidden-`50` Balih-start at `10086 BC` reaches Moses' attempted deliverance at age `40` through the alternate `8600` season:

```markdown
10086 − 8600 = 1486
```

It reaches the successful deliverance / Exodus at age `80` through the standard `8640` season:

```markdown
10086 − 8640 = 1446
```

The difference is one Mosaic `40`-year section:

```markdown
1486 − 1446 = 40
8640 − 8600 = 40
```

Structural inference:
The same Balih hidden-`50` start therefore distinguishes Moses' attempted deliverance from Moses' successful deliverance by the same `40`-year difference that separates the alternate and standard `120`-unit precessional-season states.

### 5.3 The `1876 BC` Nexus 10x comparison

Active state:
Famine Window integration state; `1876 BC` Nexus 10x rail comparison state.

`File_16` controls `1876 BC` as a shared year-label inside the Famine Window. It may function as Entry, Call, return, death, prophetic target, or convergence label depending on active state. `File_26` uses only the bounded Famine Window / Nexus comparison.

The local 10x rail display is:

```markdown
18756 ÷ 10 = 1875.6
18766 ÷ 10 = 1876.6
```

The pair `18766/18756 BC` therefore brackets the `1876 BC` Nexus at the `10x` scale.

Author-supplied interpretation:
`1875.6 BC` is retained as the Joseph homage / nations-bowing display one year after the `1876 BC` Entry into Egypt. Under this typological reading, the SKL field represents the nations, and the `18756 BC` rail-label becomes a `10x` witness to the `1876 BC` Nexus field.

Claim-status restraint:
This is a local `10x` comparison and typological / providential synchronization note. It is not a new historical anchor, not a formal statistical proof, and not a replacement for File_16's Famine Window controls.

### 5.4 Summary of the dual season system

The standard route is:

```markdown
120 × 72 = 8640
2160 = 30 × 72
3 × 8640 = 25920
27366 − 18726 = 8640
18726 − 10086 = 8640
10086 − 1446 = 8640
```

The alternate route is:

```markdown
120 × 71⅔ = 8600
2150 = 30 × 71⅔
3 × 8600 = 25800
18726 − 10126 = 8600
10126 − 1526 = 8600
10086 − 1486 = 8600
```

The difference between the two season-states is one Mosaic generation section:

```markdown
8640 − 8600 = 40
```

The difference between the two month-states is one local `10`-year rail offset:

```markdown
2160 − 2150 = 10
```

Structural inference:
The complexity arises because both month-states are active simultaneously: `2160` / `8640` under the standard `72` register, and `2150` / `8600` under the alternate `71⅔` register. These intercept Moses' life at the `40`-year divisions and intercept the `1876 BC` Nexus at the `10x` scale.

## 6. The Pillar `AD 29515` and the `29516 BC` Mirror-side extension

Active state:
Pillar Mirror counterpart display state; `AD 29515` actual Pillar state by File_19 / File_21 dependency; Projective Pillar / Temple-Time state by File_21 dependency; Decimal Invariance / `10x` display state by File_25 dependency.

The third addition places the actual Pillar `AD 29515` inside the same `10x` and alternate-precessional structure already opened by the `8600` state.

The active actual Pillar is:

```markdown
AD 29515
```

Its Mirror-side counterpart display label is:

```markdown
29516 BC
```

Dependency-control note:
`AD 29515` remains the active actual Pillar node by File_19 / File_21 dependency. The BC-side `29516 BC` label is used here only as a bounded File_26 Mirror-side counterpart display. It does not restore a BC-side Pillar proof into File_19.

### 6.1 The `29516 BC` counterpart and the `2150/8600/10750/15050` ladder

Active state:
Pillar Mirror counterpart display state; `2150/8600` alternate precessional-season state; `10x` display state.

From the Mirror-side counterpart label `29516 BC`, the ladder is:

| From | To | Same-side BC calculation | Factor form | Active state |
|---|---:|---:|---:|---|
| `29516 BC` | `27366 BC` | `29516 − 27366 = 2150` | `5 × 430 = 10 × 215 = 30 × 71⅔` | alternate precessional month |
| `29516 BC` | `20916 BC` | `29516 − 20916 = 8600` | `2 × 4300 = 4 × 2150` | alternate `120`-day precessional season |
| `29516 BC` | `18766 BC` | `29516 − 18766 = 10750` | `2.5 × 4300 = 5 × 2150 = 430 × 25` | Flood-to-Balih / Nangishlishma rail comparison |
| `29516 BC` | `14466 BC` | `29516 − 14466 = 15050` | `3.5 × 4300 = 7 × 2150 = 35 × 430` | cumulative Exodus-shadow / Pillar counterpart display |

The ladder shows that `29516 BC` is not an isolated Mirror label. It sits above the same `2150` and `8600` machinery already used in the File_26 alternate precessional-season state:

```markdown
2150 = 30 × 71⅔
8600 = 4 × 2150
10750 = 5 × 2150
15050 = 7 × 2150
```

The same ladder may also be stated at the `10x` scale of the Sojourn `430`:

```markdown
4300 = 10 × 430
8600 = 2 × 4300
10750 = 2.5 × 4300
15050 = 3.5 × 4300
```

Structural inference:
The Pillar counterpart label `29516 BC` extends the File_26 `8600` system upward into the same `10x` register controlled by File_25. The values `2150`, `8600`, `10750`, and `15050` are not corrections of the earlier `8640` standard state. They belong to the alternate `25800` / `71⅔` precessional register.

### 6.2 The actual `AD 29515` Pillar and the Exodus / Exile checks

Active state:
`AD 29515` actual Pillar state; Projective Pillar / Temple-Time state; civil cross-axis span.

The actual Pillar state is checked from the primary Exodus by ordinary civil cross-axis arithmetic:

```markdown
1446 + 29515 − 1 = 30960
30960 = 430 × 72
```

This is also:

```markdown
71⅔ = 215/3
432 × 215/3 = 30960
```

Therefore:

```markdown
1446 BC → AD 29515 = 30960
30960 = 430 days of standard precession
30960 = 432 days of alternate precession
```

The same actual Pillar reaches the Fall of Jerusalem / Exile state at `586 BC` by:

```markdown
586 + 29515 − 1 = 30100
30100 = 430 × 70
```

This creates a clean relation between the `15050` Mirror-side counterpart span and the actual Pillar-to-Exile span:

```markdown
15050 × 2 = 30100
30100 = 430 × 70
```

Structural inference:
The Pillar `AD 29515` completes the File_26 bridge at the far end of the same `430` system. The Mirror-side counterpart display `29516 BC → 14466 BC = 15050` gives the half-scale form:

```markdown
15050 = 3.5 × 4300
```

The actual civil Pillar span to the Exile gives the doubled form:

```markdown
30100 = 7 × 4300 = 430 × 70
```

The actual civil Pillar span to the Exodus gives the precessional form:

```markdown
30960 = 430 × 72
```

### 6.3 Integration with the earlier File_26 states

Active state:
File_26 `8640/8600` bridge; Pillar Mirror counterpart display; Decimal Invariance / `10x` display state.

The earlier File_26 standard route was:

```markdown
27366 → 18726 → 10086 → 1446
8640 + 8640 + 8640 = 25920
```

The earlier File_26 alternate route was:

```markdown
18726 → 10126 → 1526
8600 + 8600 = 17200
```

The Pillar counterpart route adds:

```markdown
29516 → 27366 = 2150
29516 → 20916 = 8600
29516 → 18766 = 10750
29516 → 14466 = 15050
```

This means the File_26 structure now has three visible layers:

| Layer | Main values | Function |
|---|---:|---|
| Standard precessional season | `8640`; `25920`; `72` | Three `120`-day seasons from `27366 BC` to the Exodus. |
| Alternate precessional season | `8600`; `25800`; `71⅔`; `2150` | Intercepts Moses' `40`-year divisions and the Balih / Famine Window interface. |
| Pillar / `10x` Mirror extension | `29516 BC`; `AD 29515`; `4300`; `10750`; `15050`; `30960` | Carries the same `430` and `2150/8600` grammar to the Projective Boundary / Pillar state. |

Claim-status restraint:
The Pillar material is a bounded structural extension. Arithmetic facts establish the local spans. The identification of the Pillar as a providential synchronization belongs to the File_21 / File_19 dependency field. `File_26` may cite the extension, but it does not turn the Pillar into ordinary historical chronology and does not make `29516 BC` a new independent anchor.

## 7. The remote `434886 BC` Pre-Creation circuit

Active state:
Remote `434886 BC` Danielic / adjusted Pre-Creation circuit state; Priestly `1290 × 336` Exodus state; Prophetic `1290 × 360` Pillar state; SKL rounded mod-10 Mirror `AD 27366` counterpart state; Decimal Invariance / `10x` display state by File_25 dependency.

The fourth addition places `434886 BC` above the Pillar and Flood fields. This shows that the pattern does not stop at `AD 29515`, `29516 BC`, or the SKL Flood variants. It branches outward into a remote Pre-Creation circuit while preserving the same `430`, `2150`, `1290`, `336`, and `360` span families.

The remote anchor is generated from the Babel / terminal rail at `2886 BC` by the full `432000`-year SKL expansion:

```markdown
434886 − 2886 = 432000
432000 = 1200 × 360
432000 = 120 × 3600
```

Claim-status:
The arithmetic is exact. The classification of `434886 BC` as adjusted Pre-Creation / Danielic circuit remains dependency-controlled by File_21.

### 7.1 `434886 BC` to the Exodus: the Priestly `1290 × 336` route

Active state:
Priestly `1290 × 336` Exodus state; primary Exodus state.

From `434886 BC` to the primary Exodus at `1446 BC`:

```markdown
434886 − 1446 = 433440
```

This is:

```markdown
433440 = 1290 × 336
433440 = 430 × 1008
1008 = 144 × 7
433440 = 430 × 144 × 7
```

Structural inference:
The same Danielic `1290` that reaches the Pillar through the Prophetic `360` register reaches the Exodus through the Priestly `336` register.

Claim-status:
The arithmetic is exact. The interpretation that this creates a Priestly / Prophetic complement is structural inference.

### 7.2 `434886 BC` to the Pillar: the Prophetic `1290 × 360` route

Active state:
Prophetic `1290 × 360` Pillar state; actual `AD 29515` Projective Pillar state.

From `434886 BC` to the Jachin Pillar at `AD 29515`, use ordinary civil cross-axis arithmetic:

```markdown
434886 + 29515 − 1 = 464400
```

This is:

```markdown
464400 = 1290 × 360
464400 = 6450 × 72
464400 = 6480 × 71⅔
464400 = 18 × 25800
```

The exact alternate-precessional calculation is:

```markdown
71⅔ = 215/3
6480 × 215/3 = 464400
```

Structural inference:
The `434886 BC → AD 29515` span carries the Daniel 12 `1290` through the Prophetic `360` register, the standard precessional `72` register, and the alternate `71⅔` / `25800` register at once.

### 7.3 The Priestly / Prophetic complement

Active state:
Priestly `336`; Prophetic `360`; Exodus-to-Pillar span.

The two remote routes differ by the known Exodus-to-Pillar span:

```markdown
464400 − 433440 = 30960
```

This is also:

```markdown
360 − 336 = 24
1290 × 24 = 30960
30960 = 430 × 72
30960 = 432 × 71⅔
1446 BC → AD 29515 = 30960
```

Structural inference:
The `434886 BC` circuit does not merely add a large remote number. It shows that the Priestly `1290 × 336` route to the Exodus and the Prophetic `1290 × 360` route to the Pillar are separated by the same `30960` span already carried by the File_26 Pillar extension:

```markdown
1446 BC → AD 29515 = 30960
```

This makes the remote circuit complementary rather than merely additive.

### 7.4 The SKL Long Flood variant in the Mirror: `AD 27366`

Active state:
SKL rounded mod-10 Mirror `AD 27366` counterpart state; File_25 Decimal Invariance / `10x` display state.

The SKL Long Flood variant is:

```markdown
27366 BC
```

When this value is viewed as the Mirror-side display label:

```markdown
AD 27366
```

the relation to `434886 BC` checks under the File_21 SKL rounded mod-10 Mirror display:

```markdown
434886 + 27366 − 2 = 462250
462250 = 215 × 2150
```

This row must not be processed by ordinary civil cross-axis arithmetic. Under ordinary civil cross-axis counting it would be:

```markdown
434886 + 27366 − 1 = 462251
```

Therefore the active operator must be named:

```markdown
SKL rounded mod-10 Mirror display: BC + AD − 2
```

Structural inference:
The `AD 27366` Mirror-side display shows that the SKL Long Flood variant participates in the same `2150` cycle-family that governs the `8600`, `10750`, and `15050` states already opened in File_26.

The factor display is:

```markdown
215 × 2150 = 462250
2150 = 5 × 430
462250 = 1075 × 430
```

Claim-status:
This is a bounded Mirror-side display and structural inference. It is not ordinary civil chronology and not a new independent historical anchor.

### 7.5 Summary of the remote circuit

Active state:
Remote `434886 BC` Danielic / adjusted Pre-Creation circuit state.

The remote circuit is:

| Relation | Operator | Calculation | Result / function |
|---|---|---:|---|
| `434886 BC → 2886 BC` | same-side BC | `434886 − 2886` | `432000 = 1200 × 360` |
| `434886 BC → 1446 BC` | same-side BC | `434886 − 1446` | `433440 = 1290 × 336 = 430 × 1008` |
| `434886 BC → AD 29515` | civil cross-axis | `434886 + 29515 − 1` | `464400 = 1290 × 360` |
| `434886 BC → AD 27366` | SKL mod-10 Mirror display | `434886 + 27366 − 2` | `462250 = 215 × 2150` |
| Priestly / Prophetic difference | subtraction of spans | `464400 − 433440` | `30960 = 430 × 72` |

Conclusion:
The `434886 BC` extension demonstrates that the File_26 pattern persists at a larger scale. The same structure links Babel / Pre-Creation, the SKL Long Flood variant, the Exodus, and the Jachin Pillar through `430`, `2150`, `336`, `360`, `1290`, `25800`, and `25920`-family precessional logic.

Claim-status restraint:
This persistence is structurally significant and may be treated as providential synchronization. It should not be worded as a formal probability result unless a later file supplies a statistical model.

## 8. Cross-file target vector

Active state:
Dependency-controlled File_30 target vector; claim-status restraint.

`File_26` preserves the source claim that the `432` / precessional Cosmic Count should be cross-verified against `File_30`, where the `490/4900` prophetic count targets AD 33.

Dependency note:
`File_30` is not opened in this file. The AD 33 convergence remains dependency-controlled until `File_30` supplies the local `490/4900` calculation.

Claim-status note:
The convergence of `File_26`'s precessional bridge and `File_30`'s scriptural / prophetic count may be described as providential synchronization or a theological integrity witness. It should not be worded as independent proof inside `File_26` unless `File_30` is opened and the local arithmetic is checked.

## 9. Synthesis

Active state:
File_24 extra state; pre-branching `18726 BC`; Historical `430`; Cosmic `432`; standard `8640`; alternate `8600`; Famine Window integration state; Pillar extension; remote Pre-Creation circuit.

`File_26` demonstrates that `18726 BC` is not merely another date inside the File_24 Kish-cipher field. It is the pre-branching Nangishlishma-start anchor that first locks to Abraham by `16560`, then to Moses' birth by `17200`, then to the Exodus by `17280`.

The local `430/432` bridge is:

```markdown
18726 BC → 2166 BC = 16560
18726 BC → 1526 BC = 17200 = 430 × 40
18726 BC → 1446 BC = 17280 = 432 × 40
```

The standard precessional-season triad is:

```markdown
27366 BC → 18726 BC = 8640
18726 BC → 10086 BC = 8640
10086 BC → 1446 BC = 8640
27366 BC → 1446 BC = 25920
```

The alternate precessional-season state is:

```markdown
18726 BC → 10126 BC = 8600
10126 BC → 1526 BC = 8600
10086 BC → 1486 BC = 8600
10086 BC → 1446 BC = 8640
```

The transition from `430` to `432` is carried by Moses' `80`-year birth-to-Exodus span:

```markdown
17280 − 17200 = 80
```

The transition from alternate season to standard season is carried by a single Mosaic `40`-year section:

```markdown
8640 − 8600 = 40
```

The standard and alternate month-states explain the local `10`-year offset:

```markdown
2160 − 2150 = 10
```

The `10x` rail comparison then touches the `1876 BC` Nexus:

```markdown
18756 ÷ 10 = 1875.6
18766 ÷ 10 = 1876.6
```

The Pillar extension adds the far-side `10x` boundary:

```markdown
29516 BC → 27366 BC = 2150
29516 BC → 20916 BC = 8600
29516 BC → 18766 BC = 10750 = 430 × 25
29516 BC → 14466 BC = 15050 = 3.5 × 4300
1446 BC → AD 29515 = 30960 = 430 × 72
586 BC → AD 29515 = 30100 = 430 × 70
```

The remote Pre-Creation circuit extends the Pillar material beyond the local Flood / Kish field:

```markdown
434886 BC → 2886 BC = 432000
434886 BC → 1446 BC = 433440 = 1290 × 336
434886 BC → AD 29515 = 464400 = 1290 × 360
434886 BC → AD 27366 = 462250 = 215 × 2150
```

The complement between the Priestly and Prophetic routes is exact:

```markdown
464400 − 433440 = 30960
30960 = 430 × 72
1446 BC → AD 29515 = 30960
```

Final claim-status:
Arithmetic facts establish the local bridge. Structural inference explains why the `18726 BC` pre-branching anchor matters before the downstream En-tarah-ana `+50` column and the `2906/2856 BC` terminal rails. The `8640` triad shows the standard `25920` Precession Year divided into three equal `120`-day seasons. The `8600` state shows the alternate `25800` Precession Year intersecting Moses' birth, attempted deliverance, and the Famine Window / `1876 BC` Nexus at the `10x` scale. `AD 29515` / `29516 BC` extends the same `430`, `2150`, `8600`, and `10x` grammar into the Pillar / Projective Boundary field. `434886 BC` shows that the circuit continues into remote Pre-Creation / Danielic scale without changing the local operators. Providential synchronization may be named, but the file does not convert the pattern into a formal statistical probability proof.

## Appendix A — File_24 parallel matrix reference

Active state:
File_24 standard / variant parallel matrix; hidden-`50` branch control.

This appendix preserves the author-supplied table for File_26 retrieval clarity. The full derivation remains File_24-controlled. The table is not a new local proof of the First Dynasty of Kish cipher.

| Ruler | Reign length | Standard timeline | Variant timeline |
|---|---:|---:|---:|
| Jushur | 1200 | `20886–19686 BC` | `20886–19686 BC` |
| Kullassina-bel | 960 | `19686–18726 BC` | `19686–18726 BC` |
| Nangishlishma | 670 | `18726–18056 BC` | `18726–18056 BC` |
| En-tarah-ana | 420 | `18056–17636 BC` | `18006–17586 BC` |
| Babum | 300 | `17636–17336 BC` | `17586–17286 BC` |
| Puannum | 840 | `17336–16496 BC` | `17286–16446 BC` |
| Kalibum | 960 | `16496–15536 BC` | `16446–15486 BC` |
| Kalumum | 840 | `15536–14696 BC` | `15486–14646 BC` |
| Zuqaqip | 900 | `14696–13796 BC` | `14646–13746 BC` |
| Atab | 600 | `13796–13196 BC` | `13746–13146 BC` |
| Mashda | 840 | `13196–12356 BC` | `13146–12306 BC` |
| Arwium | 720 | `12356–11636 BC` | `12306–11586 BC` |
| Etana | 1500 | `11636–10136 BC` | `11586–10086 BC` |
| Balih | 400 | `10136–9736 BC` | `10086–9686 BC` |
| En-me-nuna | 660 | `9736–9076 BC` | `9686–9026 BC` |
| Melem-Kish | 900 | `9076–8176 BC` | `9026–8126 BC` |
| Barsal-nuna | 1200 | `8176–6976 BC` | `8126–6926 BC` |
| Zamug | 140 | `6976–6836 BC` | `6926–6786 BC` |
| Tizqar | 305 | `6836–6531 BC` | `6786–6481 BC` |
| Ilku | 900 | `6531–5631 BC` | `6481–5581 BC` |
| Iltasadum | 1200 | `5631–4431 BC` | `5581–4381 BC` |
| Enmebaragesi | 900 | `4431–3531 BC` | `4381–3481 BC` |
| Aga of Kish | 625 | `3531–2906 BC` | `3481–2856 BC` |

Matrix note:
The first three kings do not enact the visible parallel `+50` column. The hidden `50` grammar is held in Nangishlishma's dual `670/720` state and becomes visible downstream through En-tarah-ana.

## Audit Notes

Audit note:
Decimal precession display has been normalized. The source phrase `71.66 years/degree` or `71.6666` is preserved as source-history approximation only. The executable arithmetic state is `71⅔ = 215/3`.

Audit note:
Moses “80-year life” wording from the initial source has been corrected by state. The active local calculation is `1526 BC → 1446 BC = 80`, Moses' birth-to-Exodus span, not Moses' whole lifespan.

Audit note:
`10126 BC` is a downstream overshoot from the standard Balih start `10136 BC` under forward-time BC movement. The `10`-year displacement is explained locally by `2160 − 2150 = 10`.

Audit note:
`1875.6 BC` Joseph homage / nations-bowing display is retained as author-supplied. It is classified as local `10x` comparison unless File_03 or File_16 later registers it as a controlled node.

Audit note:
`29516 BC` is retained as the author-supplied Mirror-side counterpart display label for `AD 29515`. This is not the active File_19 local Pillar node and should not be treated as ordinary historical chronology.

Audit note:
`432 × 71⅔ = 30960` is the exact form of the source phrase `432 × 71.6666`. Use `71⅔ = 215/3` for executable arithmetic.

Audit note:
`1446 BC → AD 29515 = 30960` checks by ordinary civil cross-axis arithmetic: `1446 + 29515 − 1 = 30960`. The `29516 BC` rows are same-side BC calculations inside the Mirror-side counterpart display state, not civil cross-axis calculations.

Audit note:
`434886 BC` checks arithmetically in the Danielic / Pillar rows, but its provenance remains dependency-controlled. It is not the ordinary SKL Short Pre-Creation base `452886 BC` and not the SKL Long Pre-Creation base `459366 BC`.

Audit note:
`434886 BC → AD 27366 = 462250 = 215 × 2150` checks only under File_21's SKL rounded mod-10 Mirror display `BC + AD − 2`. Ordinary civil cross-axis arithmetic gives `462251`, so this row must remain explicitly state-labeled.

Audit note:
Use live repository spelling `Jachin` for `AD 29515`. The chart spelling `Jochin` is source-history only unless quoted from the chart.

Audit note:
The chart also displays `434886–434856 BC`. File_26 currently opens only `434886 BC` because the active route is from `2886 BC` by `432000`. Opening `434856 BC` would require a separate companion rail from `2856 BC`.

## Unresolved Issues

| Issue | Current handling |
|---|---|
| File_30 AD 33 target | Dependency-controlled. `File_26` preserves the target vector but does not locally verify File_30's `490/4900` count. |
| En-tarah-ana downstream `+50` coordinate derivation | Controlled by File_24 / File_21 / File_34. `File_26` does not rederive those coordinates. |
| Joseph homage / nations-bowing `1875.6 BC` display | Retained as author-supplied local `10x` comparison. Recommended for File_03 or File_16 indexing if it becomes a stable repository node. |
| `18756/18766 BC` 10x Nexus rail | Retained as bounded File_26 comparison. Recommended for File_21 / File_24 cross-check if future files use it as more than a File_26 state. |
| `434856 BC` companion rail | Not opened. Would require separate state-control from `2856 BC` by `432000`. |

## Revision and archive note

This public-clean Markdown source removes only archival amendment history, pass logs, pressure-test repetition, pointer-refresh records, and obsolete replacement-workflow notes.

Detailed revision history is preserved in the `Repository_Change_Archive`.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed.

Final pressure-test status is preserved in the file header. Detailed pressure-test records are archived rather than repeated in the public file body.

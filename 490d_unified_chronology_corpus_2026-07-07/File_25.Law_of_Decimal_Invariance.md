File: File_25
Title: The Law of Decimal Invariance
Status: Final; post-final pressure tested; File_26 Decimal Invariance cross-file citation update applied; 5366/536 restoration Decimal Invariance update applied and focused pressure tested; 910-cycle Mirror / File_29 cross-reference update applied and focused pressure tested; x1 910-gear corroboration update applied and focused pressure tested
Pressure-test status: Current; focused pressure test complete for the 5366/536 restoration update, 910-cycle Mirror / File_29 update, x1 910-gear corroboration update, and public-clean cleanup; prior post-final pressure-tested status retained.
Register Agreement status: Checked against State_Vocabulary_Register v1.17 and active companion controls; convergence / pointer update only; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries changed.
Publication-cleanup status: Public-clean Markdown source; archival amendment history routed to Repository_Change_Archive.
Primary domain: SKL; Cumulative; Comparative; Prophetic; Mirror; Theological
Traditions: MT; LXX; SKL; Danielic / biblical restoration field
Canonical source: Markdown
Primary anchors: 27366 BC; 20916 BC; 20886 BC; 18766/18756 BC; 1876.6/1875.6 BC; 14466 BC; 5586 BC; 5556 BC; 5496 BC; 5486 BC audit note; 5426 BC; 5366 BC; 536.6 BC; 4116 BC; 2736 BC; 2091 BC; 1446 BC; 606 BC; 586 BC audit note; 4646 BC; 4586 BC; 4516 BC; 445 BC; 445.6 BC; 4456 BC; 536 BC; 666 BC; 466 BC; AD 445; AD 536; AD 586 audit note; AD 606; AD 1446; AD 4456; AD 4516; AD 4586; AD 4646; AD 5366; AD 5426; AD 5496; AD 5556; 29516 BC; 2856 BC; AD 2856; AD 2736; 434886 BC; AD 27366; AD 29515
Related files: File_00; File_01; File_02; File_05; File_07; File_08; File_17; File_18; File_21; File_22; File_23; File_24; File_26; File_29; File_51a; File_54; Restart Capsule v11.15; 490d Repository Style Guide v2.5; Project Procedures v3.2
Major operators: decimal scaling `÷ 10`; 10x scale-state; same-side BC subtraction; BC-coordinate variant addition; civil cross-axis span `BC + AD − 1` where exact civil span is opened; biblical 10x Mirror-scale display; SKL / Rounded mod-10 display where that state is explicitly opened; source-labeled Mirror mod-10 display `BC + AD − 2` where explicitly opened; File_26 bounded 10x Nexus rail comparison; `18766 ÷ 10 = 1876.6`; `18756 ÷ 10 = 1875.6`; `5496 − 130 = 5366`; `5366 ÷ 10 = 536.6`; `5366 − 536 = 4830 = 10 × 483`; `4830 × 70/69 = 4900`; `5366 − 466 = 4900`; `5366 − 1446 = 3920 = 80 × 49`; `1446 − 466 = 980 = 2 × 490`; `5496 − 666 = 4830`; `5366 − 4456 = 910 = 70 × 13 = 7 × 130`; `536 − 445 = 91 = 7 × 13`; `5366 + 60 = 5426`; `5426 + 70 = 5496`; `5496 + 60 = 5556`; `5556 + 5366 − 2 = 10920 = 910 × 12 = 30 × 364`; `5586 + 5366 − 2 = 10950 = 30 × 365`; `4586 − 4456 = 130`; `5496 − 4586 = 910`; `4646 + 4456 − 2 = 9100`; `4586 + 4516 − 2 = 9100`; `4516 + 4586 − 2 = 9100`; `4456 + 4646 − 2 = 9100`; `1446 − 536 = 910`; `536 − 445 = 91`; `536 + 445 − 1 = 980 = 2 × 490`; `AD 536 → AD 1446 = 910`; `1446 − 606 = 840`; `606 − 536 = 70`; `AD 536 → AD 606 = 70`; `AD 606 → AD 1446 = 840`; `70 + 840 = 910`; `606 − 586 = 20` audit row; `29516 − 18766 = 10750`; `29516 − 14466 = 15050`; `1446 + 29515 − 1 = 30960`; `434886 + 29515 − 1 = 464400`; `434886 + 27366 − 2 = 462250`; `91`; `130`; `430`; `483`; `490`; `910`; `1290`; `2150`; `4300`; `4830`; `4900`; `9100`; `10920`; `10950`; SKL Long `24510`
Major modal states: explanatory reference-file state; decimal-shadow display state; finite-decimal display state; historical Abraham Call state; historical Exodus state; Artaxerxes / Nehemiah historical wall-decree state; Daniel 9 four-decree weave state; restoration-decree weave state; LXX Year-6 / 2Cainan-removed restoration coordinate state; Daniel 4830 restoration 10x branch state; 2Cainan-included / 666 fallen-man branch state; LXX 910-cycle Mirror corroboration state; LXX `60 + 70 + 60` Terah / 2Cainan variant-rail state; LXX `10920 / 10950` days-years Mirror display state; Artaxerxes 10x decree-rail Mirror display state; x1 Exodus-to-restoration 910-cycle corroboration state; x1 Babylonian `70 + 840` Kenan echo state; File_29 7/13 harmonic weave dependency state; LXX Shem death witness-state; MT Shem-death comparison state; SKL shifted Flood / upper Deep Time state; SKL `+30` / rail-30 Abrahamic coordinate state; cumulative MT + restored 2nd Cainan Exodus-shadow state; biblical 10x Mirror-scale display state; SKL / Rounded mod-10 display-state; SKL Long `24510` terminal state; BC/AD counterpart display-label state; File_26 18766/18756 Nexus rail comparison state; File_26 Pillar / 10x extension state; File_26 remote `434886 BC` circuit state; Rounded Scaffold non-retrojection state
Major claim-status labels: source-title / local axiom language; arithmetic fact; structural inference; providential synchronization; explanatory reference protocol; dependency-controlled; audit note
Revision basis: 490d Repository Style Guide v2.5; Restart Capsule v11.15; State Vocabulary Register v1.17; Project Procedures v3.2; prior finalized File_25 source; author-supplied Decimal Invariance update notes; File_26 and File_29 dependency updates; Repository_Change_Archive (non-controlling history).

Current refresh note: Public-clean trust-status equalization aligns active control pointers to 490d Repository Style Guide v2.5, Restart Capsule v11.15, State_Vocabulary_Register v1.17, and Project Procedures v3.2. Register Agreement status is recorded in the header; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed. Detailed pass logs, pressure-test records where applicable, pointer-refresh history, and replacement-workflow notes are preserved in Repository_Change_Archive.
# File_25 — The Law of Decimal Invariance

## Related Repository Files

| File | Function for `File_25` |
|---|---|
| `File_00` | Foundational source-map discipline, Mirror / civil / inverse firewalling, 10x scale-state vocabulary, and state-safe retrieval precedent. |
| `File_01` | Controls Abraham’s historical Call anchor at `2091 BC`; `2091.6 BC` in this file remains a decimal-shadow display unless a later pass opens another date-state. |
| `File_02` | Controls Flood / SKL terminology where Sumerian A / SKL Short, Sumerian B / SKL Long, `+30 Apparent Mode`, `+2 local rail`, and `+720 scale-state` appear. |
| `File_05` | Controls the primary historical Exodus anchor at `1446 BC`; `1446.6 BC` in this file remains a decimal-shadow display unless a later pass opens another date-state. |
| `File_07` | Controls Creation / Adam / Year-6 state language where MT Year-6 `4116 BC`, LXX Year-6 `5496 BC`, and 2Cainan-related Creation comparisons are opened by dependency. |
| `File_08` | Controls Kings / Exile field vocabulary where `586 BC` is retained as an adjacent Fall of Jerusalem date-state and is not silently substituted for the `606 BC → 536 BC = 70` captivity row. |
| `File_17` | Controls Danielic span-component vocabulary, especially `490`, `430`, `1290`, the four-decree Daniel 9 field, and `606/605 BC`, `587/586 BC`, and `537/536 BC` prophetic-span adjacency where opened by dependency. |
| `File_18` | Supplies table-source and raw-data dependency for MT / LXX / cumulative / SKL rows where exact source-table verification is required. |
| `File_21` | Controls the `27366 BC → 20916 BC → 14466 BC` bridge, the `12900 = 10 × 1290` display, the LXX Shem `2736 BC → 1446 BC = 1290` bridge, and the SKL rounded mod-10 display where SKL state is explicitly opened. |
| `File_22` | Controls cumulative harmonic language, actual / rounded firewalling, and Rounded Scaffold non-retrojection where clean decimal or 10x forms might otherwise be over-read. |
| `File_23` | Controls the Tebah / `3.5` source-state and the SKL / rounded mod-10 display background for `6 BC → AD 6 = 10` where SKL or Rounded state is active. |
| `File_24` | Immediate predecessor context for the SKL Long `24510` state, the `27366 BC → 2856 BC` terminal relation, and the First Dynasty of Kish `23`-kings arrangement. |
| `File_26` | Extends Decimal Invariance / 10x display vocabulary through the `18766/18756 BC` Nexus rail comparison, the `29516 BC` Pillar counterpart display, and the remote `434886 BC` circuit. `File_25` cites these as dependency-controlled extensions and does not rederive the full File_26 argument. |
| `File_29` | Controls the 7/13 harmonic weave, including `91 = 7 × 13`, `130 = 10 × 13`, `910 = 13 × 70 = 7 × 130`, the Exodus-to-Return `910` display, the Rule of Atonement claim-status restraint, and source-labeled Mirror display language where the 70 / 130 relationship is opened. File_18 supplies the same-name Kenan / Cainan support where 1Kenan and 2Cainan are compared. |
| `File_51a` | Rounded Scaffold precedent. `File_25` does not open the Rounded Scaffold unless a later pass explicitly says so. |
| `File_54` | Later-file style exemplar for claim-status restraint, Mirror role classification, and non-inflated comparative synthesis. |

Dependency note:
`File_25` is a short explanatory reference file. It may cite arithmetic displays from controlling files, but it does not locally replace those files, re-open their full derivations, or pressure test their tables.

File_26 cross-file citation note:
`File_26` is now cited as the bounded extension of Decimal Invariance into the `18766/18756 BC` Nexus rail comparison, the `AD 29515` / `29516 BC` Pillar field, and the remote `434886 BC` circuit. These rows are dependency-controlled File_26 displays inside `File_25`; they do not expand the local proof burden of `File_25` and do not replace the original File_25 examples.

External LXX source-control note:
By author direction, this file does not repeat an external LXX source-reconstruction note. LXX chronology is referenced only as an internal repository witness-state where the Shem row is needed for the local decimal-invariance explanation.

## 0. File-function

`File_25` defines the Decimal Invariance / 10x Mirror-scale phenomenon as a reference protocol for later files.

The file does not attempt to prove the full holographic-like nature of the chronology. Its function is to give enough controlled examples that later files can reference the phenomenon in passing without rebuilding the decimal-shadow, 10x Mirror-scale, and cycle-expansion mechanics each time.

The term `Law` is preserved as source-title / local axiom language. In this Final source, the term does not make every supporting example proof-bearing by itself. Individual calculations are classified as arithmetic facts; the repeated 10x pattern is classified as structural inference; theological meaning, where opened, is classified as providential synchronization.

The current bounded updates add Mirror-corroboration and x1-corroboration layers to the `5366 / 536` restoration example. The Mirror layer treats the LXX no-2Cainan coordinate as mechanistically linked to the Nehemiah `4456` coordinate by a `910`-year cycle. The x1 layer shows the same `910` and `91` gear operating from the Exodus through the restoration / Nehemiah field and its AD-side counterpart, cross-referenced to `File_29`'s 7/13 harmonic weave.

Publication-cleanup note:
This public-clean source preserves the Final, post-final pressure-tested, focused-pressure-tested, and current publication-clean status labels in the header. Detailed pass notes, pressure-test records, bounded update records, and replacement-workflow notes are archived rather than repeated in the public body.
### 0.1 Working state register

The following states are active in `File_25`. They are simultaneous, non-competing states unless a local section explicitly narrows the working state.

| State | Primary values / forms | Node-class / function | Handling in `File_25` |
|---|---:|---|---|
| File identity state | `File_25`; The Law of Decimal Invariance | explanatory reference file | Current source is Final; post-final pressure tested. |
| Explanatory reference-file state | whole file | later-reference protocol | The file explains the Decimal Invariance / 10x Mirror-scale phenomenon for later citation. It does not attempt to prove the full holographic-like nature of the chronology. |
| Decimal-shadow display state | `20916 ÷ 10 = 2091.6`; `14466 ÷ 10 = 1446.6`; `5366 ÷ 10 = 536.6`; `445.6 × 10 = 4456` | 10x / 1/10 visual echo | Decimal-shadow values are display values. They do not replace historical event anchors unless a later pass opens an exact date-state. |
| Finite-decimal display state | `445.6`; `2091.6`; `1446.6`; `536.6`; source-retained `445.(6)` | local decimal print-state | `445.6` remains the executable finite-decimal source display. The repeating form `445.(6)` is retained as source-history only and is not converted into a new operator. |
| Historical Abraham Call state | `2091 BC` | Call of Abraham | Controlled by `File_01`. Do not replace with `2091.6 BC`. |
| Historical Exodus state | `1446 BC` | primary Exodus anchor | Controlled by `File_05`. Do not replace with `1446.6 BC`. |
| Artaxerxes / Nehemiah historical wall-decree state | `445 BC` | decree to return and rebuild Jerusalem's walls | Controlled by `File_17` in the Daniel 9 decree field. Do not replace with `445.6`, `4456 BC`, `AD 445`, or `AD 4456`. |
| Daniel 9 four-decree weave state | `538 BC`; `519 BC`; `458 BC`; `445 BC`; average `490 BC` | composite decree field | The four decree labels remain distinct historical decree states. `490 BC` is an arithmetic center / average, not a replacement decree. |
| Restoration / Daniel revelation node | `536 BC`; Daniel 9–12 `538–536 BC` revelation field | return / revelation node | Mentioned as a bounded inner weave inside the Nehemiah `4900` field and as the 10x target of the `5366 BC` LXX 2Cainan-removed coordinate. It does not become the main wall-decree node. |
| LXX Year-6 / 2Cainan-removed restoration coordinate state | `5496 BC`; `5496 − 130 = 5366`; `5366 BC`; `5366 ÷ 10 = 536.6`; `5366 − 4116 = 1250` | LXX Creation Year-6 coordinate with 2Cainan `130` removed / MT Year-6 comparison | New bounded File_25 insertion. `5366 BC` is a Decimal Invariance coordinate and does not replace LXX Year-6 `5496 BC`, MT Year-6 `4116 BC`, or the historical `536 BC` restoration node. |
| Daniel 4830 restoration 10x branch state | `5366 BC → 536 BC = 4830 = 10 × 483`; `4830 × 70/69 = 4900`; `5366 BC → 466 BC = 4900`; `5366 BC → 1446 BC = 3920 = 80 × 49`; `1446 BC → 466 BC = 980 = 2 × 490` | LXX Year-6 to restoration / Exodus / post-Exodus extension | Active only in the bounded §4.3 example. The main local point is the `5366 BC → 536 BC` 10x imaging; the Exodus and `466 BC` rows are secondary arithmetic displays. |
| 2Cainan-included / 666 fallen-man branch state | `5366 + 130 = 5496`; `666 − 536 = 130`; `5496 BC → 666 BC = 4830 = 10 × 483` | 2Cainan / Watcher-associated branch; fallen-man symbolic target | Preserves the author-supplied symbolic branch. It does not replace the `536 BC` restoration node or make `666 BC` the main Daniel 9 endpoint. |
| LXX 910-cycle Mirror corroboration state | `5366 − 4456 = 910`; `910 = 70 × 13 = 7 × 130`; `536 − 445 = 91 = 7 × 13` | LXX no-2Cainan coordinate and Nehemiah 10x coordinate relation | Active only as Mirror corroboration for the bounded `5366 / 536` example. It cross-references `File_29` for the 70 / 130 and 7 / 13 harmonic meanings. |
| LXX `60 + 70 + 60` Terah / 2Cainan variant-rail state | `5366 → 5426 → 5496 → 5556`; increments `60 + 70 + 60`; `5366 + 130 = 5496`; `5496 + 60 = 5556` | LXX no-2Cainan, Terah-only, 2Cainan-restored, and Terah + 2Cainan rail sequence | Uses BC-coordinate variant addition, not ordinary same-side elapsed subtraction. The pattern preserves the interaction of `70` and `130` without replacing the main `5366 / 536` restoration display. |
| LXX `10920 / 10950` days-years Mirror display state | `5556/5496/5426/5366 BC → AD 5366/5426/5496/5556 = 10920` under mod-10 display; `5586 BC → AD 5366 = 10950` | source-labeled Mirror / mod-10 display; Adam apparent-age comparison | The `10920` display is `910 × 12 = 30 × 364`. The `10950` display is `30 × 365`. The added `30` apparent years of Adam convert the 364-year day-register into the 365-year solar-register. |
| Artaxerxes 10x decree-rail Mirror display state | `4646/4586/4516/4456 BC → AD 4456/4516/4586/4646 = 9100`; `9100 = 10 × 910 = 100 × 91` | 10x rail counterpart of Artaxerxes start, Ezra, implied middle, and Nehemiah decree field | The historical decree labels remain `464 BC` start-of-reign display by author direction, `458 BC` Ezra, and `445 BC` Nehemiah. The `10x` rail labels are display coordinates, not replacement historical dates. |
| x1 Exodus-to-restoration 910-cycle corroboration state | `1446 BC → 536 BC = 910`; `536 BC → 445 BC = 91`; `536 BC → AD 445 = 980 = 2 × 490`; `AD 445 → AD 536 = 91`; `AD 536 → AD 1446 = 910` | normal-scale counterpart of the 10x `910 / 9100` gear | Uses same-side BC subtraction, civil cross-axis `BC + AD − 1`, and same-side AD subtraction under their own states. This corroborates the 10x display without replacing it. |
| x1 Babylonian `70 + 840` Kenan echo state | `1446 BC → 606 BC = 840`; `606 BC → 536 BC = 70`; `AD 536 → AD 606 = 70`; `AD 606 → AD 1446 = 840`; `70 + 840 = 910` | Exodus-to-return decomposition and same-name Kenan / Cainan echo | The `606 BC → 536 BC = 70` row is the executable captivity row. The supplied `586 BC` / `AD 586` forms are retained as audit-controlled source-display conflicts unless a later author decision opens a distinct state. |
| File_29 7/13 harmonic weave dependency state | `91`; `130`; `910`; `7 × 13`; `10 × 13`; `13 × 70`; `7 × 130` | cross-file harmonic meaning and claim-status control | `File_29` supplies the theological / structural interpretation that `13` is bounded or reconciled by `7`. `File_25` uses it as cross-reference support, not as a replacement for local arithmetic. |
| LXX Shem death witness-state | `2736 BC`; `2736 BC → 1446 BC = 1290` | Shem death / Daniel 12 bridge | Retained as an internal repository witness-state. External LXX source-control discussion is omitted by author direction in this file. |
| MT Shem-death comparison state | `1956 BC` | MT no-`+60 Terah`, Gear 1 / Japheth-side Shem death companion | Accepted by author direction and supported by the File_24 Shem death Gear 1 / Gear 2 state. Locally used only for the `2736 − 1956 = 780` comparison. |
| SKL shifted Flood / upper Deep Time state | `27366 BC`; `27396 BC` where dependency opens rail-pair context | SKL Long / shifted Flood layer | Controlled by `File_21` and `File_24`. Do not collapse with ordinary MT / SP / LXX Flood dates. |
| SKL `+30` / rail-30 Abrahamic coordinate state | `20886 BC`; `20916 BC`; `20916 = 20886 + 30` by dependency | SKL rail coordinate / Abrahamic decimal-shadow coordinate | `20916 BC` is treated locally as a SKL `+30` / rail-30 Abrahamic coordinate. The source label “Post-Flood Start” is retained only as source-history / audit language unless a dependency supports it. |
| Cumulative MT + restored 2nd Cainan Exodus-shadow state | `14466 BC`; `14465 BC` by dependency where full-year pair is opened | cumulative Creation / Exodus-shadow coordinate | `14466 BC` is a cumulative / restored-2nd-Cainan display coordinate. It does not replace the historical Exodus at `1446 BC`. |
| Historical shadow bridge state | `2736 BC`; `2091 BC`; `1446 BC`; `645 + 645 = 1290` | LXX Shem / Abraham / Exodus shadow bridge | Same-side BC display state. It is related to the SKL bridge by 10x patterning but does not become the same node-class as the SKL anchors. |
| Biblical 10x Mirror-scale display state | `445 BC → AD 4456`; `5366 BC → 536 BC`; `536 BC → AD 445`; `1446 BC → AD 2856` | rounded mod-10 display intent for biblical examples | Active local File_25 display-state. It does not require every biblical row to be processed by the SKL / Rounded `BC + AD − 2` operator. |
| BC/AD counterpart display-label state | `4456 BC` / `AD 4456`; `2856 BC` / `AD 2856`; `2736 BC` / `AD 2736` | axis-specific counterpart labels | Similar numerals across the BC and AD axes are not the same node. The axis label is part of the state. |
| SKL / Rounded mod-10 display-state | `27366 BC → AD 2736 = 30100 = 7 × 4300`; `6 BC → AD 6 = 10` where SKL / Rounded state is active | SKL / Rounded cross-Mirror simplification | Reserved for SKL or Rounded contexts. Do not impose this operator on the Nehemiah / biblical rows unless a later section explicitly opens it. |
| Civil cross-axis background state | `BC + AD − 1` where exact civil elapsed counting is opened | ordinary civil cross-axis span | Background check only in `File_25` unless a row explicitly opens exact civil-span arithmetic. It is not the same as biblical 10x display intent and not the SKL / Rounded mod-10 display. |
| SKL Long `24510` terminal state | `27366 − 2856 = 24510`; `24510 = 57 × 430`; `24510 = 19 × 1290` | First Dynasty of Kish / SKL Long terminal anchor | Controlled by `File_24`. Used here to show why the `2856 BC` / AD `2856` counterpart relation matters. |
| File_26 `18766/18756 BC` Nexus rail comparison state | `18766 ÷ 10 = 1876.6`; `18756 ÷ 10 = 1875.6` | 10x Nexus rail comparison | Dependency-controlled by `File_26`. It brackets the `1876 BC` Nexus at the 10x scale and remains a display-state comparison, not a replacement historical anchor. |
| File_26 Pillar / 10x extension state | `29516 BC`; `AD 29515`; `10750`; `15050`; `30960` | Pillar counterpart and actual Pillar extension | Dependency-controlled by `File_26`, `File_19`, and `File_21`. It extends the `430`, `2150`, `8600`, and `10x` grammar without turning `29516 BC` into ordinary chronology. |
| File_26 remote `434886 BC` circuit state | `434886 BC`; `AD 27366`; `AD 29515`; `462250`; `464400` | remote Pre-Creation / Danielic circuit | Dependency-controlled by `File_26` and `File_21`. It preserves the operator distinction between civil cross-axis `BC + AD − 1` and SKL rounded mod-10 display `BC + AD − 2`. |
| Rounded Scaffold non-retrojection state | clean decimal / mod-10 / 10x values | non-retrojection guard | Clean values in this file do not become File_51a Rounded Scaffold values unless that state is explicitly opened. |
| Dependency-controlled data state | detailed MT / LXX Shem rows; detailed SKL / Berossus derivations | table-source or upstream derivation | Retain as dependency-controlled where the local file does not supply the full source row or full derivation. `1956 BC` is accepted locally as author-confirmed Gear 1 companion state, not as a full Shem-table reconstruction. |

Modal-state rule:
A shared numeral, date-label, Mirror label, decree label, SKL coordinate, decimal-shadow value, or cycle value does not create identity unless state, node-class, tradition, axis, and operator agree.

### 0.2 File map

| Section | Function | Active state / operator |
|---|---|---|
| §0 | File-function, state register, File map, node-class register, operator firewalls, Machine Guards, arithmetic check register, and claim-status register | explanatory reference-file state; modal-state, arithmetic, and argument controls |
| §1 | Defines Decimal Invariance | decimal-shadow display state; 10x scale-state |
| §2 | Records core decimal-shadow examples | Abraham; Exodus; Nehemiah; finite-decimal display state |
| §3 | Records the Shem / Flood / Abraham / Exodus bridge | SKL Deep Time; LXX Shem; historical shadow bridge; `6450`; `645`; `12900`; `1290` |
| §4 | Adds the Daniel 9 / Nehemiah 10x backbone, restoration-decree weave, `5366 BC → 536 BC` LXX restoration example, 910-cycle Mirror corroboration, and x1 910-gear corroboration | Daniel 9 four-decree weave; Nehemiah 10x backbone; LXX Year-6 / 2Cainan-removed restoration coordinate; Daniel 4830 restoration 10x branch; LXX 910-cycle Mirror corroboration; x1 Exodus-to-restoration 910-cycle; x1 Babylonian `70 + 840` Kenan echo; File_29 7/13 harmonic weave; biblical 10x Mirror-scale display |
| §5 | Adds the Exodus / SKL Long `24510` bridge | `430`; `4300`; `24510`; SKL Long terminal state; BC/AD counterpart display labels |
| §5A | Records File_26 Decimal Invariance extensions | `18766/18756 BC`; `29516 BC`; `434886 BC`; File_26 dependency state |
| §6 | Distinguishes local display states and operators | biblical 10x display; SKL / Rounded mod-10; civil-span background; Rounded Scaffold non-retrojection |
| §7 | Records dependency and source-control boundaries | cross-file dependency tracking |
| §8 | Preserves final audit and dependency notes | resolved author-decision items; non-blocking audit notes; dependency-controlled |
| Revision and archive note | Records publication-clean archive routing | archive-routing control |

Machine Guard [FILE MAP RESTRAINT]:
The File map is a retrieval aid only. Do not expand it into a prose table of contents or make it duplicate the heading hierarchy.

### 0.3 Node-class register

| Date or value | Node-class | Active state | Do not collapse with |
|---|---|---|---|
| `27366 BC` | SKL shifted Flood / upper Deep Time anchor | SKL shifted Flood / upper Deep Time state | `2736 BC` LXX Shem death; ordinary MT / SP / LXX Flood dates |
| `27396 BC` | SKL rail-pair companion where opened by dependency | SKL Long rail-pair context | `27366 BC` base local anchor unless rail-pair state is active |
| `20886 BC` | SKL base rail | Sumerian A / SKL Short by dependency | `20916 BC` `+30` / rail-30 coordinate |
| `20916 BC` | SKL `+30` / rail-30 Abrahamic decimal-shadow coordinate | SKL rail-30 Abrahamic coordinate state | generic “Post-Flood Start”; `2091 BC` historical Call; `2091.6 BC` decimal shadow |
| `14466 BC` | cumulative MT + restored 2nd Cainan Exodus-shadow coordinate | cumulative MT + restored 2nd Cainan state | `1446 BC` historical Exodus; `1446.6 BC` decimal shadow |
| `5496 BC` | LXX Year-6 coordinate with 2Cainan branch included | LXX Year-6 / 2Cainan-included branch | `5366 BC` removed-`130` coordinate; `5496 BC → 666 BC` target branch |
| `5366 BC` | LXX Year-6 coordinate after 2Cainan `130` removal | LXX Year-6 / 2Cainan-removed restoration coordinate state | `536.6 BC` decimal-shadow display; `536 BC` restoration node; `5496 BC` 2Cainan-included coordinate |
| `536.6 BC` | finite decimal-shadow display | decimal-shadow display state | `536 BC` restoration / Daniel revelation node |
| `4116 BC` | MT Year-6 / Adam-state comparison | MT Year-6 comparison by File_07 dependency | `5366 BC` LXX 2Cainan-removed coordinate; ordinary Creation endpoint states |
| `2736 BC` | LXX Shem death witness-state / integer-display shadow | LXX Shem death witness-state | exact `27366 ÷ 10` value unless an integer-display operator is defined; `AD 2736` SKL / Rounded exemplar target |
| `1956 BC` | MT no-`+60 Terah`, Gear 1 / Japheth-side Shem death companion | MT Shem-death comparison state | `1958 BC` Gear 2 endpoint; generic untagged Shem-death label |
| `2091 BC` | Call of Abraham | historical Abraham Call state | `2091.6 BC`; `20916 BC` |
| `2091.6 BC` | decimal-shadow display value | decimal-shadow display state | historical Abraham Call date |
| `1446 BC` | primary Exodus anchor | historical Exodus state | `1446.6 BC`; `14466 BC`; AD-side `AD 1446` display labels; x1 corroboration rows unless those states are explicitly opened |
| `606 BC` | Babylonian captivity start / exile-start display by dependency | x1 Babylonian `70 + 840` Kenan echo state | `586 BC` Fall of Jerusalem adjacent state; `536 BC` restoration / return endpoint |
| `586 BC` | Fall of Jerusalem adjacent state / audit-controlled source-display conflict in this x1 insertion | File_08 / File_17 dependency; audit note | executable `606 BC → 536 BC = 70` captivity row. Do not read `606 BC → 586 BC` as `70`. |
| `1446.6 BC` | decimal-shadow display value | decimal-shadow display state | historical Exodus date |
| `445 BC` | Artaxerxes / Nehemiah wall-decree label | historical wall-decree state | `445.6`; `4456 BC`; `AD 445`; `AD 4456` |
| `445.6` | finite-decimal display value | finite-decimal display state | repeating `445.(6)` unless later opened; `445 BC` decree date |
| `4456` | 10x numeric display value | Nehemiah 10x backbone state | automatic date-label without BC or AD axis |
| `4456 BC` | deep-side 10x coordinate label | restoration-decree weave state | `AD 4456` AD-side target |
| `AD 4456` | AD-side Nehemiah 10x target | biblical 10x Mirror-scale display state | `4456 BC` deep-side coordinate; `445 BC` decree |
| `536 BC` | return / Daniel revelation node | restoration / Daniel revelation node | Cyrus `538 BC`; four-decree average `490 BC`; `536.6 BC` decimal-shadow display; `666 BC` 2Cainan-included branch target |
| `666 BC` | 2Cainan-included symbolic branch target | 2Cainan-included / 666 fallen-man branch state | `536 BC` restoration node; ordinary historical event rows unless a dependency opens them |
| `5426 BC` | LXX Terah-only rail from `5366 BC` | LXX `60 + 70 + 60` Terah / 2Cainan variant-rail state | `5486 BC`; `5496 BC`; ordinary Creation endpoint unless local state opens it |
| `5556 BC` | LXX Terah + 2Cainan rail | LXX `60 + 70 + 60` Terah / 2Cainan variant-rail state | `5496 BC` 2Cainan-restored rail; `5586 BC` apparent-age overlay |
| `5586 BC` | LXX apparent-age overlay, `5556 + 30` | LXX `10920 / 10950` days-years Mirror display state | `5556 BC`; ordinary LXX Creation coordinate unless apparent-age state is active |
| `5486 BC` | audit-controlled source-display conflict in the 10920 Mirror sequence | audit note / possible source slip; known LXX rounded state elsewhere | executable `5496 BC` rail in this section unless later author decision opens `5486` as intentional |
| `4646 BC` | 10x Artaxerxes start-of-reign display rail | Artaxerxes 10x decree-rail Mirror display state | historical `464 BC`; `5556 BC` LXX rail unless `+910` comparison is active |
| `4586 BC` | 10x Ezra decree display rail | Artaxerxes 10x decree-rail Mirror display state | historical `458 BC`; `5496 BC` LXX rail unless `+910` comparison is active |
| `4516 BC` | implied 10x middle rail between Ezra and Nehemiah | Artaxerxes 10x decree-rail Mirror display state | historical decree label unless a later file opens one; `5426 BC` LXX rail unless `+910` comparison is active |
| `AD 4516`; `AD 4586`; `AD 4646` | AD-side counterpart display rails | Artaxerxes 10x decree-rail Mirror display state | BC-side rails with matching numerals; ordinary civil-span targets unless local state opens them |
| `AD 5366`; `AD 5426`; `AD 5496`; `AD 5556` | AD-side counterpart rails for the `10920` LXX Mirror display | LXX `10920 / 10950` days-years Mirror display state | BC-side rails with matching numerals; civil cross-axis spans unless mod-10 Mirror display is explicitly active |
| `466 BC` | 4900-year expanded target and post-Exodus `980` display | Daniel 4830 restoration 10x branch state | `445 BC` wall decree; `536 BC` restoration node |
| `AD 445` | AD-side restoration-weave display target | biblical 10x Mirror-scale display state | `445 BC` Nehemiah decree |
| `AD 536` | AD-side return-counterpart rail in the x1 sequence | x1 Exodus-to-restoration 910-cycle corroboration state | `536 BC` restoration node; `AD 586` audit-controlled source-display conflict |
| `AD 586` | audit-controlled source-display conflict in the x1 insertion | audit note / possible source slip | executable `AD 536 → AD 606 = 70` row. Do not read `AD 586 → AD 606` as `70`. |
| `AD 606` | AD-side captivity-start counterpart rail | x1 Babylonian `70 + 840` Kenan echo state | `606 BC` on the BC axis; `AD 586` audit-controlled source-display conflict |
| `AD 1446` | AD-side Exodus-counterpart terminal display in the x1 sequence | x1 Exodus-to-restoration 910-cycle corroboration state | `1446 BC` historical Exodus; mixed source form `AD 1446 BC` |
| `490 BC` | four-decree arithmetic center | Daniel 9 decree-average composite state | any single decree label |
| `2856 BC` | SKL Long terminal anchor | SKL Long `24510` terminal state | `AD 2856` Exodus Mirror-scale target |
| `AD 2856` | AD-side Exodus 10x target | biblical 10x Mirror-scale display state | `2856 BC` SKL Long terminal anchor |
| `AD 2736` | SKL / Rounded exemplar target | SKL / Rounded mod-10 display-state | `2736 BC` LXX Shem death witness-state |
| `24510` | SKL Long whole-year span | Sumerian B / SKL Long state by dependency | File_51a Rounded Scaffold value; `24500` rounded half-`49000` comparison |
| `24500` | half-`49000` comparison value | Danielic comparison / offset display | `24510` SKL textual whole-year total |

Node-class rule:
The axis marker is part of the node-class. For example, `2856 BC` and `AD 2856` share a numeral but not an axis, node-class, tradition, or operator.

### 0.4 Operator and display-state register

| Operator / display state | Active form | Scope in `File_25` | Pass 2 / Pass 3 handling |
|---|---|---|---|
| Decimal scaling | `÷ 10`; `× 10` | Core Decimal Invariance examples, including `5366 ÷ 10 = 536.6` | Pass 3 verifies the executable finite-decimal rows. The values remain display values, not replacement event dates. |
| Finite-decimal print state | `.6` in `2091.6`, `1446.6`, `445.6` | Source-display values | Pass 3 verifies the finite-decimal rows. Repeating-decimal `445.(6)` remains source-history only because it would not yield the displayed `4456`. |
| Same-side BC subtraction | higher BC date minus lower BC date | SKL bridge, historical shadow bridge, SKL Long terminal span | Pass 3 verifies the locally executable same-side spans. |
| Biblical 10x Mirror-scale display | `445 BC → AD 4456`; `5366 BC → 536 BC`; `536 BC → AD 445`; `1446 BC → AD 2856` | Local biblical examples | Pass 3 checks exact civil background where it is naturally available and preserves display-state rows where exact civil counting is not the active burden. The SKL / Rounded `BC + AD − 2` operator is not imposed on the biblical rows. |
| LXX `5366 / 536` restoration 10x branch | `5366 − 536 = 4830 = 10 × 483`; `4830 × 70/69 = 4900` | Bounded §4.3 example | Visible arithmetic checks; same-side BC subtraction governs spans. The local burden is the `5366 BC → 536 BC` 10x restoration display. |
| 2Cainan `130` branch shift | `5496 − 130 = 5366`; `5496 − 666 = 4830`; `666 − 536 = 130` | Bounded §4.3 symbolic branch | Preserves the author-supplied Watcher / fallen-man branch without replacing the `536 BC` restoration node. |
| LXX 910-cycle same-side relation | `5366 − 4456 = 910`; `910 = 70 × 13 = 7 × 130`; `536 − 445 = 91 = 7 × 13` | Bounded §4.4 Mirror-corroboration example | Checks by same-side BC subtraction and multiplication. `File_29` controls the 7/13 harmonic interpretation. |
| BC-coordinate variant addition | `5366 + 60 = 5426`; `5426 + 70 = 5496`; `5496 + 60 = 5556` | LXX Terah / 2Cainan variant rail | Addition moves the BC coordinate backward / higher. This is variant-coordinate construction, not elapsed same-side subtraction. |
| Source-labeled Mirror mod-10 display | `BC + AD − 2` where explicitly opened | LXX `10920` and Artaxerxes `9100` displays | Opened only inside §4.4 for the stated Mirror-corroboration rows. It does not retroactively rewrite the Nehemiah / Daniel 9 biblical rows in §§4.1–4.3. |
| LXX days-years Mirror display | `5556 + 5366 − 2 = 10920`; `5496 + 5426 − 2 = 10920`; `5426 + 5496 − 2 = 10920`; `5366 + 5556 − 2 = 10920`; `5586 + 5366 − 2 = 10950` | Bounded §4.4 Mirror display | Checks when `5496`, not source-slip `5486`, is used in the second rail. `10920 = 910 × 12 = 30 × 364`; `10950 = 30 × 365`. |
| Artaxerxes 10x rail Mirror display | `4646 + 4456 − 2 = 9100`; `4586 + 4516 − 2 = 9100`; `4516 + 4586 − 2 = 9100`; `4456 + 4646 − 2 = 9100` | 10x decree rail / counterpart display | Checks under the explicitly opened mod-10 Mirror display. Historical decree labels remain dependency-controlled. |
| x1 Exodus-to-restoration 910-cycle | `1446 − 536 = 910`; `536 − 445 = 91`; `536 + 445 − 1 = 980`; `AD 536 − AD 445 = 91`; `AD 1446 − AD 536 = 910` | normal-scale counterpart to the 10x `910 / 9100` gear | Uses same-side BC subtraction, civil cross-axis span, and same-side AD subtraction under their own operators. |
| x1 Babylonian `70 + 840` Kenan echo | `1446 − 606 = 840`; `606 − 536 = 70`; `AD 606 − AD 536 = 70`; `AD 1446 − AD 606 = 840`; `70 + 840 = 910` | captivity decomposition of the x1 `910` cycle | Executable with `536`, not `586`, as the 70-year endpoint. `586` remains an adjacent Fall of Jerusalem / audit-controlled source-display conflict in this section. |
| Civil cross-axis background | `BC + AD − 1` where exact civil span is opened | Background verification only | Pass 3 uses it only as a background check. It does not override local biblical 10x Mirror-scale display intent. |
| SKL / Rounded mod-10 display | `BC + AD − 2` only where SKL / Rounded state is explicitly active | SKL / Rounded exemplars, especially `6 BC → AD 6 = 10` and `27366 BC → AD 2736` | Pass 3 verifies the `27366 BC → AD 2736` row only under this SKL / Rounded display-state. It is not imposed on the Nehemiah / Daniel 9 biblical rows. |
| SKL `+30` / rail-30 state | `20886 → 20916` by dependency | `20916 BC` node-class control | Pass 3 verifies the local `20886 + 30 = 20916` relation only as a dependency-controlled rail display. Full SKL date-generation remains unopened. |
| SKL Long terminal state | `27366 → 2856`; `24510` | First Dynasty of Kish / SKL Long display | Pass 3 verifies the local `24510` factors and offset displays under the File_24 dependency. |
| Rounded Scaffold state | File_51a Rounded Scaffold | Not opened locally | Clean, mod-10, 10x, and harmonically regular values remain local display states unless this state is explicitly opened. |
| File_26 dependency-controlled extension | `18766/18756 BC`; `29516 BC`; `434886 BC`; `AD 27366`; `AD 29515` | File_26 citations only | Cited as a downstream Decimal Invariance extension. Local File_25 examples remain the original proof-bearing rows. |
| Claim-status classification | arithmetic fact / structural inference / providential synchronization | Completed for local Argument Pass | Pass 4 classifies proof burden without changing arithmetic, anchors, modal states, node-classes, sign conventions, operators, Mirror protocols, or theological claims. |

Sign-convention note:
Same-side BC spans use subtraction. Cross-axis exact civil spans, where opened, use `BC + AD − 1`. Biblical 10x Mirror-scale display is a local display-state and is not reduced to either exact civil counting or SKL / Rounded `BC + AD − 2` unless the local row explicitly opens that state.

### 0.5 Machine Guards

Machine Guard [DECIMAL SHADOW NOT REPLACEMENT DATE]:
Decimal-shadow values such as `2091.6 BC`, `1446.6 BC`, and `445.6 BC` are display values generated by decimal-invariance logic. They do not replace historical anchors such as `2091 BC`, `1446 BC`, or `445 BC` unless a later pass explicitly opens an exact date-state.

Machine Guard [FINITE DECIMAL / REPEATING DECIMAL FIREWALL]:
The working display `445.6` is a finite-decimal print-state because the source also gives the executable relation `445.6 × 10 = 4456`. The source form `445.(6)` remains audit-controlled. Do not silently convert it into a repeating-decimal operator or alter downstream labels without an Arithmetic or Author-Decision Pass.

Machine Guard [BIBLICAL 10X MIRROR-SCALE DISPLAY]:
In `File_25`, `mod-10` names the rounded 10x Mirror-scale intent of the display. Biblical examples such as `445 BC → AD 4456`, `5366 BC → 536 BC`, `536 BC → AD 445`, and `1446 BC → AD 2856` are not forced into the SKL / Rounded `BC + AD − 2` operator. Exact civil counting may remain available as a background check, but the local explanatory burden is the 10x display-state.

Machine Guard [5366 / 536 RESTORATION 10X EXAMPLE]:
`5366 BC → 536 BC = 4830 = 10 × 483` is a bounded Decimal Invariance example tied to the restoration / Daniel revelation node. `5366 BC` is the LXX Year-6 coordinate after the 2Cainan `130` is removed. It does not replace `5496 BC`, `4116 BC`, `536 BC`, or the Nehemiah wall-decree node.

Machine Guard [2CAINAN 130 / 666 BRANCH]:
The `5496 BC → 666 BC` branch is opened only when the 2Cainan `130` is included. It preserves the author-supplied Watcher / fallen-man symbolic reading and must not replace the `5366 BC → 536 BC` restoration example or become the main Daniel 9 endpoint.

Machine Guard [LXX 910-CYCLE MIRROR / FILE_29 CROSS-REFERENCE]:
The `5366 BC → 4456 BC = 910` row is a Mirror-corroboration layer for the `5366 / 536` restoration example. It invokes `File_29` for the 7/13 interpretation of `910 = 70 × 13 = 7 × 130`. It must not replace the local decimal-shadow row `5366 ÷ 10 = 536.6`, the historical `536 BC` restoration node, or the `445 BC` Nehemiah decree.

Machine Guard [5496 / 5486 SOURCE-SLIP AUDIT]:
The author supplied `5486` in one Mirror sequence. The immediately preceding `60 + 70 + 60` construction and the `10920` mod-10 checks require `5496`. This revision uses `5496` as the executable rail and preserves `5486` as an audit-controlled source-display conflict unless a later author decision opens `5486` intentionally.

Machine Guard [SOURCE-LABELED MOD-10 MIRROR DISPLAY]:
The `10920` and `9100` rows in §4.4 explicitly open source-labeled mod-10 Mirror display using `BC + AD − 2`. This does not convert all File_25 biblical 10x rows into SKL / Rounded operator rows and does not override ordinary civil cross-axis arithmetic where `BC + AD − 1` is explicitly opened.

Machine Guard [ARTAXERXES 10X RAILS NOT HISTORICAL DATES]:
`4646 BC`, `4586 BC`, `4516 BC`, and `4456 BC` are 10x display rails. They do not replace historical Artaxerxes start, Ezra, implied middle, or Nehemiah decree labels. Historical date states remain governed by their controlling files.

Machine Guard [X1 910-GEAR CORROBORATION]:
The x1 rows `1446 BC → 536 BC = 910`, `536 BC → 445 BC = 91`, `536 BC → AD 445 = 980`, `AD 445 → AD 536 = 91`, and `AD 536 → AD 1446 = 910` corroborate the 10x gear at normal scale. They do not replace the `5366 / 536`, `4456 / 445`, `10920`, or `9100` display states.

Machine Guard [606 / 586 CAPTIVITY AUDIT]:
The executable 70-year captivity row in this x1 insertion is `606 BC → 536 BC = 70`, with AD-side counterpart `AD 536 → AD 606 = 70`. The supplied source forms `606 BC + 70 = 586 BC` and `AD 586 + 70 = AD 606` do not check under ordinary arithmetic and remain audit-controlled unless a later author decision opens a distinct state. `586 BC` remains a Fall of Jerusalem adjacent state by dependency, not the executable 70-year endpoint from `606 BC`.

Machine Guard [SKL / ROUNDED DISPLAY RESERVATION]:
The `BC + AD − 2` simplification belongs only where the SKL / Rounded display state is explicitly active, such as the `6 BC → AD 6 = 10` type of example or the SKL / Rounded rows inherited from `File_21` / `File_23`. Do not impose it on the Nehemiah / Daniel 9 biblical examples.

Machine Guard [BC/AD COUNTERPART LABELS]:
BC-side and AD-side labels that share the same numeral are counterpart display labels, not identical nodes. `4456 BC` and `AD 4456`, `2856 BC` and `AD 2856`, and `2736 BC` and `AD 2736` must remain axis-tagged.

Machine Guard [RAIL-30 ABRAHAMIC COORDINATE]:
`20916 BC` is treated locally as a SKL `+30` / rail-30 Abrahamic decimal-shadow coordinate by dependency. Do not treat it as a generic “Post-Flood Start” unless a controlling dependency explicitly supports that node-class.

Machine Guard [ROUNDED SCAFFOLD NON-RETROJECTION]:
Clean decimal, mod-10, 10x, Mirror-like, SKL mod-10, or harmonically regular values in this file are not File_51a Rounded Scaffold values unless a local section explicitly opens the Rounded Scaffold state.

Machine Guard [FILE_25 FILE_26 DEPENDENCY-CONTROLLED EXTENSIONS]:
The File_26 rows cited in `File_25` are dependency-controlled extensions of Decimal Invariance. `18766/18756 BC`, `29516 BC`, `AD 29515`, `434886 BC`, and `AD 27366` must remain state-labeled under File_26 / File_21 controls. They do not replace the local File_25 examples, do not open the Rounded Scaffold, and do not mix civil cross-axis `BC + AD − 1` with SKL rounded mod-10 display `BC + AD − 2`.

### 0.6 Arithmetic check register

Arithmetic Pass scope:
Pass 3 checked visible arithmetic only under the active states and operators established in Pass 2. It did not source-check every upstream table row, mark the file Final, or pressure test the file. Pass 4 uses these checked rows only to clarify argument role and proof burden.

| Field | Calculation | Result | Arithmetic Pass status |
|---|---|---:|---|
| Abraham decimal shadow | `20916 ÷ 10` | `2091.6` | Checks as finite decimal-shadow display. |
| Exodus decimal shadow | `14466 ÷ 10` | `1446.6` | Checks as finite decimal-shadow display. |
| Nehemiah finite-decimal display | `445.6 × 10` | `4456` | Checks. `445.(6) × 10` would not yield `4456`; the repeating form remains audit-controlled. |
| SKL rail-30 relation | `20886 + 30` | `20916` | Checks as dependency-controlled rail display. |
| SKL Deep Time bridge A | `27366 − 20916` | `6450` | Checks as same-side BC subtraction. |
| SKL Deep Time bridge B | `20916 − 14466` | `6450` | Checks as same-side BC subtraction. |
| SKL Deep Time total | `6450 + 6450` | `12900` | Checks. |
| Danielic scale display | `10 × 1290` | `12900` | Checks. |
| Historical shadow bridge A | `2736 − 2091` | `645` | Checks as same-side BC subtraction. |
| Historical shadow bridge B | `2091 − 1446` | `645` | Checks as same-side BC subtraction. |
| Historical shadow total | `2736 − 1446` | `1290` | Checks as same-side BC subtraction. |
| `27366` exact decimal quotient | `27366 ÷ 10` | `2736.6` | Does not equal `2736`. Preserve `2736 BC` as LXX Shem witness-state / integer-display shadow unless a later operator is named. |
| MT / LXX Shem comparison | `2736 − 1956` | `780` | Checks. `1956 BC` is accepted locally as the MT no-`+60 Terah`, Gear 1 / Japheth-side Shem death companion by author direction and File_24 dependency. |
| Four-decree sum | `538 + 519 + 458 + 445` | `1960` | Checks. |
| Four-decree average | `1960 ÷ 4` | `490` | Checks. |
| Nehemiah 10x backbone, civil-background check | `445 + 4456 − 1` | `4900` | Checks where exact civil background is opened. |
| Deep-side restoration block | `4456 − 536` | `3920` | Checks as same-side BC subtraction. |
| Deep-side `490` block count | `8 × 490` | `3920` | Checks. |
| Restoration-side block, civil-background check | `536 + 445 − 1` | `980` | Checks where exact civil background is opened. |
| Restoration-side `490` block count | `2 × 490` | `980` | Checks. |
| Nehemiah segmented total | `3920 + 980` | `4900` | Checks. |
| LXX Year-6 2Cainan removal | `5496 − 130` | `5366` | Checks as author-supplied bounded Year-6 coordinate. |
| LXX / MT Year-6 comparison | `5366 − 4116` | `1250` | Checks as same-side BC subtraction by File_07 / File_18 dependency. |
| Restoration decimal shadow | `5366 ÷ 10` | `536.6` | Checks as finite decimal-shadow display. |
| Restoration 10x branch | `5366 − 536` | `4830` | Checks as same-side BC subtraction. |
| Daniel 9 `483` 10x scale | `10 × 483` | `4830` | Checks. |
| `4830` prophetic expansion | `4830 × 70/69` | `4900` | Checks exactly. |
| Expanded restoration target | `5366 − 466` | `4900` | Checks as same-side BC subtraction. |
| Moses age-80 Exodus span | `5366 − 1446` | `3920` | Checks as same-side BC subtraction. |
| Moses age-80 Jubilee count | `80 × 49` | `3920` | Checks. |
| Post-Exodus expanded span | `1446 − 466` | `980` | Checks as same-side BC subtraction. |
| Post-Exodus `490` count | `2 × 490` | `980` | Checks. |
| 2Cainan included coordinate shift | `5366 + 130` | `5496` | Checks as coordinate shift. |
| 2Cainan target shift | `666 − 536` | `130` | Checks as same-side BC comparison. |
| 2Cainan included branch | `5496 − 666` | `4830` | Checks as same-side BC subtraction. |
| LXX / Nehemiah 910 cycle | `5366 − 4456` | `910` | Checks as same-side BC subtraction. |
| `910` 7/13 factors | `70 × 13`; `7 × 130` | `910` | Checks. `File_29` controls the 7/13 interpretation. |
| Decimal counterpart `91` cycle | `536 − 445` | `91` | Checks as same-side BC subtraction. |
| `91` 7/13 factor | `7 × 13` | `91` | Checks by multiplication. |
| LXX Terah rail | `5366 + 60` | `5426` | Checks as BC-coordinate variant addition. |
| LXX 2Cainan-restored rail | `5426 + 70` | `5496` | Checks as BC-coordinate variant addition. |
| LXX Terah + 2Cainan rail | `5496 + 60` | `5556` | Checks as BC-coordinate variant addition. |
| LXX rail sequence | `60 + 70 + 60` | `190` | Checks; spans `5366` to `5556`. |
| LXX outer Mirror display A | `5556 + 5366 − 2` | `10920` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| LXX inner Mirror display A | `5496 + 5426 − 2` | `10920` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| LXX inner Mirror display B | `5426 + 5496 − 2` | `10920` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| LXX outer Mirror display B | `5366 + 5556 − 2` | `10920` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| `10920` cycle factors | `910 × 12`; `30 × 364` | `10920` | Checks. |
| Apparent-age Mirror display | `5586 + 5366 − 2` | `10950` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| `10950` solar-day factor | `30 × 365` | `10950` | Checks. |
| Source-slip audit row | `5486 + 5426 − 2` | `10910` | Does not equal `10920`; preserve `5486` as audit-controlled source-display conflict unless later author decision opens it. |
| Ezra-to-Nehemiah span | `458 − 445` | `13` | Checks as same-side BC subtraction. |
| 10x Ezra-to-Nehemiah rail | `4586 − 4456` | `130` | Checks as same-side BC subtraction. |
| LXX / Ezra 910 alignment | `5496 − 4586` | `910` | Checks as same-side BC subtraction. |
| LXX / Nehemiah 910 alignment | `5366 − 4456` | `910` | Checks as same-side BC subtraction. |
| Artaxerxes start / LXX terminal rail | `5556 − 4646` | `910` | Checks as same-side BC subtraction. |
| Artaxerxes rail Mirror display A | `4646 + 4456 − 2` | `9100` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| Artaxerxes rail Mirror display B | `4586 + 4516 − 2` | `9100` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| Artaxerxes rail Mirror display C | `4516 + 4586 − 2` | `9100` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| Artaxerxes rail Mirror display D | `4456 + 4646 − 2` | `9100` | Checks under explicitly opened source-labeled mod-10 Mirror display. |
| `9100` scale factors | `10 × 910`; `100 × 91` | `9100` | Checks. |
| x1 Exodus-to-return span | `1446 − 536` | `910` | Checks as same-side BC subtraction. |
| x1 return-to-Nehemiah span | `536 − 445` | `91` | Checks as same-side BC subtraction. |
| x1 restoration cross-axis span | `536 + 445 − 1` | `980` | Checks as civil cross-axis span; `980 = 490 + 490 = 2 × 490`. |
| x1 AD-side Nehemiah-to-return span | `536 − 445` | `91` | Checks as same-side AD subtraction from `AD 445` to `AD 536`. |
| x1 AD-side return-to-Exodus counterpart span | `1446 − 536` | `910` | Checks as same-side AD subtraction from `AD 536` to `AD 1446`. |
| x1 Exodus-to-606 span | `1446 − 606` | `840` | Checks as same-side BC subtraction. |
| x1 70-year captivity row | `606 − 536` | `70` | Checks as same-side BC subtraction. |
| x1 AD-side 70-year counterpart | `606 − 536` | `70` | Checks as same-side AD subtraction from `AD 536` to `AD 606`. |
| x1 AD-side 840-year counterpart | `1446 − 606` | `840` | Checks as same-side AD subtraction from `AD 606` to `AD 1446`. |
| x1 Kenan echo total | `70 + 840` | `910` | Checks; interpreted as echo of 1Kenan's `70 + 840 = 910` lifespan by File_18 / File_29 dependency. |
| Source `586` audit row | `606 − 586`; `586 − 536` | `20`; `50` | The supplied `606 BC + 70 = 586 BC` does not check. Preserve `586 BC` as audit-controlled Fall of Jerusalem adjacent state. |
| Source `AD 586` audit row | `606 − 586`; `656 − 586` | `20`; `70` | The supplied `AD 586 + 70 = AD 606` does not check; `AD 536 + 70 = AD 606` is the executable counterpart. |
| Exodus 10x display target | `10 × 430` | `4300` | Checks as target value. The displayed pair `1446 BC → AD 2856` is retained as biblical 10x Mirror-scale display, not as an exact civil-span row. |
| Exodus civil-background comparison if opened | `1446 + 2856 − 1` | `4301` | Background check only. Do not use this to overwrite the local display-state. |
| SKL Long terminal span | `27366 − 2856` | `24510` | Checks as same-side BC subtraction. |
| SKL Long `430` factor | `57 × 430` | `24510` | Checks. |
| SKL Long `1290` factor | `19 × 1290` | `24510` | Checks. |
| Half-`49000` comparison | `50 × 490` | `24500` | Checks. |
| `24510` offset | `24500 + 10` | `24510` | Checks. `24510` stands `10` above `24500`. |
| SKL / Rounded exemplar | `27366 + 2736 − 2` | `30100` | Checks only under the SKL / Rounded mod-10 display-state. |
| SKL / Rounded `4300` count | `7 × 4300` | `30100` | Checks. |
| File_26 Nexus rail A | `18766 ÷ 10` | `1876.6` | Checks as dependency-controlled 10x display. |
| File_26 Nexus rail B | `18756 ÷ 10` | `1875.6` | Checks as dependency-controlled 10x display. |
| File_26 Pillar counterpart span | `29516 − 18766` | `10750` | Checks as same-side BC subtraction under File_26 dependency. |
| File_26 Pillar civil span | `1446 + 29515 − 1` | `30960` | Checks as ordinary civil cross-axis arithmetic under File_26 / File_21 dependency. |
| File_26 remote civil span | `434886 + 29515 − 1` | `464400` | Checks as ordinary civil cross-axis arithmetic under File_26 / File_21 dependency. |
| File_26 remote SKL mod-10 span | `434886 + 27366 − 2` | `462250` | Checks only under SKL rounded mod-10 display-state. |

Arithmetic Pass rule:
A checked calculation does not collapse states. A display row that does not check under ordinary civil arithmetic is preserved with its local display-state rather than silently corrected.

### 0.7 Claim-status and argument-control register

Argument Pass scope:
Pass 4 classifies the evidential role of the already-checked material. It does not change arithmetic, anchors, modal states, node-classes, sign conventions, operators, Mirror protocols, theological claims, or cross-file dependencies.

| Claim-status | Function in `File_25` | Local examples | Handling |
|---|---|---|---|
| Source-retained / local axiom language | Preserves the title and source thesis without making every row independently proof-bearing | `The Law of Decimal Invariance`; “scale-invariant” | Retain as file-level naming language. The supporting evidence is classified separately. |
| Arithmetic fact | Direct calculation from stated constants under the active operator | `20916 ÷ 10 = 2091.6`; `5366 − 536 = 4830`; `5366 − 4456 = 910`; `5556 + 5366 − 2 = 10920`; `4646 + 4456 − 2 = 9100`; `4830 × 70/69 = 4900`; `27366 − 20916 = 6450`; `445 + 4456 − 1 = 4900`; `24510 = 57 × 430` | May carry local arithmetic burden where the state and operator are explicit. |
| Dependency-controlled arithmetic fact | Calculation or source row controlled by another file or table | `2091 BC`; `1446 BC`; `445 BC`; `27366 BC`; `2856 BC`; detailed MT / LXX Shem rows where not locally opened | Retain with dependency boundaries. Do not expand dependency-controlled rows into independent proof. |
| Structural inference | Pattern inferred from repeated, state-controlled arithmetic displays | Decimal Invariance; 10x Mirror-scale behavior; Nehemiah 10x backbone; `5366 BC → 536 BC` restoration branch; LXX 910-cycle Mirror corroboration; x1 Exodus-to-restoration 910-cycle corroboration; x1 Babylonian `70 + 840` Kenan echo; Artaxerxes 10x decree rails; restoration-decree weave; SKL Long `24510` cycle participation | Supports the file's reference function, but does not by itself prove full holographic-like architecture. |
| Providential synchronization | Theological interpretation of cross-system coherence beyond demonstrable authorial intent | The larger meaning of SKL, biblical, Danielic, and restoration-field convergence where stated, including the bounded `5366 / 536` restoration example | Preserve as interpretive / theological meaning. It must not replace arithmetic or dependency control. |
| Theological / symbolic note | Preserves symbolic meaning without making it carry the arithmetic burden | `666 BC` as fallen-man / Watcher-associated branch target when the 2Cainan `130` is included; 1Kenan's `70 + 840 = 910` lifespan as a same-name Kenan / Cainan echo | Retain as symbolic branch language. It does not replace the `536 BC` restoration node or the main 10x arithmetic row. |
| Mirror / display-state explanation | Explains why a counterpart label matters without turning it into exact elapsed-span proof | `5366 BC → 536 BC`; `5556/5496/5426/5366 BC → AD 5366/5426/5496/5556 = 10920`; `4646/4586/4516/4456 BC → AD 4456/4516/4586/4646 = 9100`; `1446 BC → AD 2856`; `27366 BC → AD 2736`; `4456 BC` / `AD 4456` axis-pairs | Classify by local display-state. Do not collapse with civil span, SKL / Rounded mod-10 display, Protocol 1, or Rounded Scaffold states. |
| Audit note | Value, source row, or operator whose exact status requires state labeling | `2736` as integer-display shadow rather than exact quotient; `445.(6)` as source-history; `5486` as source-display conflict in the 10920 sequence; `586` / `AD 586` in the x1 captivity insertion as source-display conflicts; mixed source label `AD 1446 BC`; “Post-Flood Start” as source-history | Preserve state classification. Do not silently convert source-history or display-state language into a new operator. |

Argument-control rule:
Arithmetic facts establish the visible display rows. Structural inference names the repeated Decimal Invariance pattern. Providential synchronization may explain why the pattern matters, but it does not carry the arithmetic burden. `File_25` is therefore an explanatory reference file, not a full proof file for the holographic-like nature of the chronology.

Machine Guard [LAW AS SOURCE-TITLE / LOCAL AXIOM LANGUAGE]:
The word `Law` in the title is retained as source-title and local axiom language. It does not convert audit-controlled rows, display-state rows, or dependency-controlled rows into settled proof. If a later file needs `Decimal Invariance` as a foundational axiom, that promotion should be handled by cross-file glossary or control-file update rather than silently inferred from this title.

## 1. Definition: Decimal Invariance

Source definition:
The Unified Chronology is scale-invariant. Major epochal boundaries in the Deep Time / SKL range appear as decimal reflections or 10x scale counterparts in the biblical historical timeline.

Claim-status note:
The definition is retained as source-title / local axiom language. The individual numeric rows are arithmetic facts where checked; the claim that these rows instantiate Decimal Invariance is structural inference; any theological meaning attached to the cross-system fit is providential synchronization.

Source-scope note:
The original definition identified the Deep Time range as `10000–20000 BC`. The expanded File_25 argument now also opens the wider SKL shifted Flood layer at `27366 BC`. Pass 1 preserves both by using the broader phrase `Deep Time / SKL range`.

Controlled thesis:
Decimal Invariance names the local File_25 phenomenon in which selected Deep Time, SKL, cumulative, and biblical restoration nodes appear in 1/10 or 10x relation to historical biblical anchors. This file records the display-state and selected examples for later cross-reference. It does not require the reader to treat every example as exact civil elapsed-time proof or as a Rounded Scaffold value.

Modal-state note:
Decimal Invariance is a local display-state protocol. It does not by itself identify a decimal-shadow coordinate with a historical event-node, a SKL rail, a cumulative Creation coordinate, a civil cross-axis span, or a Rounded Scaffold value.

## 2. Core Decimal Shadows

Claim-status note:
The operations in this table are arithmetic facts where the stated decimal calculation checks. The event-archetype labels are structural labels that organize the examples. The historical or biblical anchors remain dependency-controlled by their own files and are not replaced by the decimal-shadow values.

| Event archetype | Deep Time / display coordinate | Operation | Decimal-shadow / 10x value | Historical or biblical anchor | Handling |
|---|---:|---:|---:|---:|---|
| The Calling | `20916 BC` | `÷ 10` | `2091.6 BC` | `2091 BC` Call of Abraham | Decimal-shadow display; `2091 BC` remains the File_01 historical anchor. |
| The Exodus | `14466 BC` | `÷ 10` | `1446.6 BC` | `1446 BC` Exodus | Decimal-shadow display; `1446 BC` remains the File_05 historical anchor. |
| The Nehemiah wall decree | `445.6` | `× 10` | `4456` | `445 BC` Artaxerxes / Nehemiah wall decree | Daniel 9 10x backbone; `445 BC` remains the historical decree label. |
| Restoration / Daniel revelation node | `5366 BC` | `÷ 10` | `536.6 BC` | `536 BC` end of the 70-year captivity / Daniel 9–12 revelation field | Decimal-shadow / 10x display; `536 BC` remains the restoration node. |

Terminology note:
The original malformed source phrase that combined `AD` and `BC` on the same coordinate is normalized as `AD 4456` where the AD-side Mirror-scale target is meant. Where the argument discusses the deep-side subdivision of the same field, `4456 BC` remains a separate display label.

Cross-reference vector:
The `2091.6 BC` Abraham decimal-shadow value should be cross-referenced to `File_01`, which controls the `2091 BC` Abraham Call anchor. The decimal display does not replace the historical anchor.

Modal-state note:
The table contains distinct node-classes: historical biblical anchors, decimal-shadow display values, restoration coordinates, and 10x counterpart labels. The values may visually correspond, but identity is not assumed unless state, axis, and operator agree.

## 3. Shem / Flood / Abraham / Exodus Bridge

### 3.1 SKL Deep Time bridge

Active state: SKL shifted Flood / upper Deep Time state; SKL `+30` / rail-30 Abrahamic coordinate state; cumulative MT + restored 2nd Cainan Exodus-shadow state.

The expanded File_25 source opens the `27366 BC → 20916 BC → 14466 BC` bridge as the main Deep Time side of Decimal Invariance.

| From | To | Displayed span | Function |
|---|---:|---:|---|
| `27366 BC` | `20916 BC` | `6450` | SKL shifted Flood to Abrahamic decimal-shadow coordinate. |
| `20916 BC` | `14466 BC` | `6450` | Abrahamic decimal-shadow coordinate to cumulative Creation / Exodus-shadow coordinate. |
| `27366 BC` | `14466 BC` | `12900 = 10 × 1290` | Deep Time / SKL form of the Danielic `1290` structure. |

Source-retained calculation:

```markdown
27366 − 20916 = 6450
20916 − 14466 = 6450
6450 + 6450 = 12900 = 10 × 1290
```

Arithmetic Pass note:
All three displayed relations check under same-side BC subtraction and integer multiplication.

Claim-status note:
The `6450 / 6450 / 12900` rows are arithmetic facts under the SKL Deep Time state. Their use as the Deep Time side of Decimal Invariance is structural inference.

### 3.2 Historical shadow bridge

Active state: historical shadow bridge state; LXX Shem death witness-state.

| From | To | Displayed span | Function |
|---|---:|---:|---|
| `2736 BC` | `2091 BC` | `645` | LXX Shem death witness-state to Abraham Call. |
| `2091 BC` | `1446 BC` | `645` | Abraham Call to Exodus. |
| `2736 BC` | `1446 BC` | `1290` | LXX Shem death witness-state to Exodus. |

Source-retained calculation:

```markdown
2736 − 2091 = 645
2091 − 1446 = 645
2736 − 1446 = 1290
```

Arithmetic Pass note:
All three displayed relations check under same-side BC subtraction.

Claim-status note:
The `645 / 645 / 1290` rows are arithmetic facts under the historical shadow bridge state. Their visual and structural pairing with the SKL `6450 / 6450 / 12900` bridge is structural inference, not node identity.

Audit note:
The source says that `2736` is 1/10th of `27366 BC`, but exact division gives:

```markdown
27366 ÷ 10 = 2736.6
```

Pass 3 therefore preserved `2736 BC` as an LXX Shem death witness-state and integer-display shadow, not as the exact decimal quotient of `27366`.

### 3.3 MT comparison note

The author-supplied source also compares the LXX Shem death at `2736 BC` with an MT Shem death comparison at `1956 BC`.

Arithmetic Pass note:
The author-supplied comparison checks arithmetically as:

```markdown
2736 − 1956 = 780
```

Finalization note:
By author direction, `1956 BC` is accepted locally as the MT no-`+60 Terah`, Gear 1 / Japheth-side Shem death companion. `File_24` preserves the paired Shem death state `1956/1958 BC`, with `1958 BC` as the Gear 2 / Shem-axis endpoint for its Danielic capstone and `1956 BC` as the Gear 1 companion. `File_25` uses only the Gear 1 companion needed for the `780` comparison and does not reconstruct the full Shem-table logic.

## 4. Daniel 9, the Nehemiah 10x Backbone, the 5366 / 536 Restoration Example, and the 910-Cycle Mirror

Active state: Artaxerxes / Nehemiah historical wall-decree state; Daniel 9 four-decree weave state; restoration / Daniel revelation node; LXX Year-6 / 2Cainan-removed restoration coordinate state; LXX 910-cycle Mirror corroboration state; File_29 7/13 harmonic weave dependency state; biblical 10x Mirror-scale display state.

The Nehemiah wall-decree node gives the Daniel 9 field a compact 10x backbone. In decimal-shadow display:

```markdown
445.6 × 10 = 4456
445 BC → AD 4456 = 4900
4900 = 10 × 490
```

Arithmetic Pass note:
The finite-decimal and civil-background checks are:

```markdown
445.6 × 10 = 4456
445 + 4456 − 1 = 4900
4900 = 10 × 490
```

This file treats the Nehemiah row as a biblical 10x Mirror-scale display. The exact civil-background check happens to agree with the displayed `4900`, but the local explanatory burden remains the 10x display-state. It does not impose the SKL / Rounded `BC + AD − 2` operator on the Nehemiah example.

Claim-status note:
The finite-decimal row, civil-background check, and `10 × 490` equality are arithmetic facts. The term `Nehemiah 10x backbone` is structural inference describing how the Daniel 9 wall-decree node functions inside this explanatory file.

Node-class note:
`445 BC` is the historical Artaxerxes / Nehemiah decree label. `445.6` is a finite-decimal display value. `4456 BC` and `AD 4456` are axis-specific 10x counterpart labels. These labels are related in the local display-state but are not identical nodes.

### 4.1 Four-decree weave

Active state: Daniel 9 decree-average composite state.

`File_17` treats the Daniel 9 decree field as a four-decree composite rather than as only one isolated decree. The four dates remain historical decree labels; `490 BC` is the arithmetic average / center, not a replacement decree.

| Decree author | Date | Purpose | Reference |
|---|---:|---|---|
| Cyrus | `538 BC` | rebuild Temple | Ezra 1:1–4 |
| Darius I | `519 BC` | resume Temple work | Ezra 6:1–12 |
| Artaxerxes I | `458 BC` | restore Law / people, Ezra | Ezra 7:11–26 |
| Artaxerxes I | `445 BC` | rebuild walls, Nehemiah | Nehemiah 2:1–8 |

Source-retained calculation:

```markdown
538 + 519 + 458 + 445 = 1960
1960 ÷ 4 = 490 BC
```

Arithmetic Pass note:
The sum and average check.

Structural note:
The Nehemiah decree is the main 10x backbone in `File_25`, but it is not isolated from the other decree nodes. It sits inside the broader decree weave.

Claim-status note:
The four-decree sum and average are arithmetic facts. Treating the four decrees as an interwoven Daniel 9 field is structural inference by `File_17` dependency. `490 BC` is not a replacement decree date.

### 4.2 The `536 BC` return / Daniel revelation node

Active state: restoration-decree weave state; restoration / Daniel revelation node; biblical 10x Mirror-scale display state.

The `536 BC` return / Daniel revelation node is mentioned briefly to show that the Nehemiah `4900` field contains a smaller restoration-decree weave.

Source-retained display:

```markdown
4456 BC → 536 BC = 3920 = 8 × 490
536 BC → AD 445 = 980 = 2 × 490
3920 + 980 = 4900
```

Arithmetic Pass note:
The executable checks are:

```markdown
4456 − 536 = 3920
8 × 490 = 3920
536 + 445 − 1 = 980
2 × 490 = 980
3920 + 980 = 4900
```

The first row is same-side BC subtraction. The second cross-axis row checks under ordinary civil-background arithmetic where that background state is opened.

This file does not elaborate the full decree weave. It records enough of the pattern to show that the `4900 = 10 × 490` Nehemiah field contains an `8 × 490` deep-side block and a `2 × 490` restoration-side block.

Claim-status note:
The `3920`, `980`, and `4900` rows are arithmetic facts under their stated same-side or civil-background operators. The restoration-decree weave is structural inference and remains intentionally bounded.

Display-label note:
`536 BC` is the restoration / Daniel revelation node. `AD 445` is an AD-side display target inside the restoration-decree weave. It is not the same node as the `445 BC` wall decree.

The broader Daniel 9–12 revelation context remains dependency-controlled. The local purpose is explanatory: to show why later files can mention the Nehemiah 10x backbone and the `5366 BC → 536 BC` restoration example without re-explaining the whole decree field.

### 4.3 The `5366 BC → 536 BC` LXX restoration example

Active state: LXX Year-6 / 2Cainan-removed restoration coordinate state; Daniel 4830 restoration 10x branch state; 2Cainan-included / 666 fallen-man branch state; biblical 10x Mirror-scale display state.

The overlooked Decimal Invariance example belongs beside the Nehemiah `4456 / 445.6` material because it lands on the same `536 BC` restoration / Daniel revelation node already used inside the `4900` Nehemiah field.

The author-supplied LXX Year-6 coordinate is `5496 BC`. In the bounded local branch, removing the 2Cainan `130` yields `5366 BC`:

```markdown
5496 − 130 = 5366
5366 − 4116 = 1250
5366 ÷ 10 = 536.6
```

The `5366 BC` coordinate then images the `536 BC` restoration node at the Daniel 9 `483` scale:

```markdown
5366 BC → 536 BC = 4830
4830 = 10 × 483
4830 × 70/69 = 4900
```

Argument-control note:
Daniel 9 remains in its basic `483 + 7 = 490` form. In this display, the `483` component appears at 10x scale as `4830`, and the exact `70/69` expansion converts `4830` to `4900`.

Secondary Exodus / Moses-80 display:

```markdown
5366 − 1446 = 3920 = 80 × 49
5366 − 466 = 4900
1446 − 466 = 980 = 2 × 490
```

This secondary row shows the same coordinate touching the Exodus when Moses was `80` and then extending to `466 BC`, `980` years after the Exodus. It is subordinate to the main local point: `5366 BC → 536 BC = 4830 = 10 × 483`, with `4830 × 70/69 = 4900`.

2Cainan / Watcher branch:
When the 2Cainan `130` is included, the coordinate shifts from the `5366 / 536` restoration branch to the `5496 / 666` branch:

```markdown
5366 + 130 = 5496
666 − 536 = 130
5496 − 666 = 4830 = 10 × 483
```

Symbolic note:
The `666 BC` target is retained as the author-supplied fallen-man / Watcher-associated branch. This symbolic branch does not replace the `536 BC` restoration node. It shows how inclusion of the 2Cainan `130` shifts the same `4830` structure from `5366 BC → 536 BC` to `5496 BC → 666 BC`.

Claim-status note:
The visible rows are arithmetic facts under same-side BC subtraction, exact ratio expansion, and decimal scaling. The interpretation that the `5366 / 536` and `5496 / 666` branches participate in Decimal Invariance is structural inference. The Watcher / fallen-man reading of `666 BC` is a theological / symbolic note.

### 4.4 The 910-cycle Mirror corroboration

Active state: LXX 910-cycle Mirror corroboration state; LXX `60 + 70 + 60` Terah / 2Cainan variant-rail state; LXX `10920 / 10950` days-years Mirror display state; Artaxerxes 10x decree-rail Mirror display state; x1 Exodus-to-restoration 910-cycle corroboration state; x1 Babylonian `70 + 840` Kenan echo state; File_29 7/13 harmonic weave dependency state; source-labeled mod-10 Mirror display.

The `5366 / 536` restoration example also aligns with the Nehemiah `4456 / 445` field by a `910`-year cycle:

```markdown
5366 − 4456 = 910
910 = 70 × 13 = 7 × 130
536 − 445 = 91
91 = 7 × 13
```

This gives the LXX no-2Cainan coordinate a direct relation to the Nehemiah 10x coordinate. The large-side span is `910`; the decimal-side counterpart is `91`. `File_29` controls the meaning of the 7/13 harmonic, including `91 = 7 × 13`, `130 = 10 × 13`, and `910 = 13 × 70 = 7 × 130`.

Claim-status note:
The arithmetic rows are facts under same-side BC subtraction and multiplication. The claim that the LXX no-2Cainan branch is intended providentially to behave mechanistically along `910`-year cycles is structural inference and providential synchronization, cross-referenced to `File_29`.

#### 4.4.1 LXX `60 + 70 + 60` rail

The LXX rail can be stated as a controlled BC-coordinate variant-addition sequence:

```markdown
5366 + 60 = 5426
5426 + 70 = 5496
5496 + 60 = 5556
60 + 70 + 60 = 190
```

The same rail may be read from `5366 BC` as:

```markdown
5366 → 5426 → 5496 → 5556
```

The `5496 BC` coordinate restores the 2Cainan `130` relative to `5366 BC`:

```markdown
5366 + 130 = 5496
```

The final `5556 BC` coordinate adds the `+60 Terah` rail to the Cainan-included coordinate:

```markdown
5496 + 60 = 5556
```

Argument-control note:
This is not ordinary elapsed same-side subtraction. It is BC-coordinate variant addition. The pattern records a rail sequence in which `60` and `70` interact with the 2Cainan `130` state.

#### 4.4.2 The `10920` Mirror display

When the mod-10 Mirror display is explicitly opened for this subsection, the LXX rail forms a fourfold cross-pair:

```markdown
5556 BC → AD 5366 = 10920
5496 BC → AD 5426 = 10920
5426 BC → AD 5496 = 10920
5366 BC → AD 5556 = 10920
```

Executable mod-10 checks:

```markdown
5556 + 5366 − 2 = 10920
5496 + 5426 − 2 = 10920
5426 + 5496 − 2 = 10920
5366 + 5556 − 2 = 10920
```

The cycle factors are:

```markdown
10920 = 910 × 12
10920 = 30 × 364
```

This permits the source reading that the LXX no-2Cainan branch participates in a 364-day Enochian register at Mirror scale.

Audit note — `5486` source-display conflict:
The author supplied `5486` in one Mirror sequence. The immediately preceding `60 + 70 + 60` construction and the `10920` checks require `5496`. If `5486` is used in the second cross-pair, the row gives:

```markdown
5486 + 5426 − 2 = 10910
```

This revision therefore uses `5496` as the executable rail and retains `5486` as an audit-controlled source-display conflict unless a later author decision opens it intentionally.

#### 4.4.3 Apparent Adam `30` and the `10950` solar display

When the apparent `30` years of Adam are added to `5556 BC`, the rail moves to `5586 BC`:

```markdown
5556 + 30 = 5586
5586 + 5366 − 2 = 10950
10950 = 30 × 365
```

The source reading is that the apparent `30` years of Adam are represented fractionally across the Mirror as both:

```markdown
10920 = 30 × 364
10950 = 30 × 365
```

Claim-status note:
The arithmetic checks under the explicitly opened mod-10 Mirror display. The interpretation that the `30` apparent years bridge a 364-day and 365-day day-year register is structural inference and providential synchronization. It does not convert `5586 BC` into an ordinary LXX event date.

#### 4.4.4 Artaxerxes decree rails at 10x

The normal decree-field side gives the same pattern at 10x scale. The Ezra decree in `458 BC`, in the `7th year of Artaxerxes`, stands `13` years before the Nehemiah wall decree in `445 BC`, in the `20th year of Artaxerxes`:

```markdown
458 − 445 = 13
20 − 7 = 13
```

At 10x scale:

```markdown
4586 − 4456 = 130
```

This aligns with the LXX side:

```markdown
5496 − 4586 = 910
5366 − 4456 = 910
5556 − 4646 = 910
```

The author-supplied start-of-Artaxerxes display is `464 BC`, with `4646 BC` as its 10x rail. The implied middle rail is `4516 BC`, giving the same `60 + 70 + 60` pattern:

```markdown
4646 → 4586 → 4516 → 4456
60 + 70 + 60 = 190
```

The corresponding mod-10 Mirror display is:

```markdown
4646 BC → AD 4456 = 9100
4586 BC → AD 4516 = 9100
4516 BC → AD 4586 = 9100
4456 BC → AD 4646 = 9100
```

Executable checks:

```markdown
4646 + 4456 − 2 = 9100
4586 + 4516 − 2 = 9100
4516 + 4586 − 2 = 9100
4456 + 4646 − 2 = 9100
9100 = 10 × 910
9100 = 100 × 91
```

Argument-control note:
The historical decree labels remain `458 BC` and `445 BC`, with `464 BC` retained as the author-supplied start-of-Artaxerxes display. The `4646/4586/4516/4456 BC` values are 10x display rails. They do not replace the historical dates.

Cross-reference note:
`File_29` should be cross-referenced here because it supplies the larger 70 / 130 relation: `910 = 13 × 70 = 7 × 130`, with `91 = 7 × 13` as the decimal counterpart. `File_25` uses the 910-cycle Mirror rows as corroboration for Decimal Invariance; it does not re-prove the full 7/13 harmonic weave.

#### 4.4.5 x1 Exodus / Return / Kenan echo corroboration

Active state: x1 Exodus-to-restoration 910-cycle corroboration state; x1 Babylonian `70 + 840` Kenan echo state; File_29 7/13 harmonic weave dependency state; same-side BC subtraction; civil cross-axis span `BC + AD − 1`; same-side AD subtraction.

The same gear appears at x1, not only at 10x:

```markdown
1446 BC → 536 BC = 910
536 BC → 445 BC = 91
```

Executable checks:

```markdown
1446 − 536 = 910
536 − 445 = 91
910 = 70 × 13 = 7 × 130
91 = 7 × 13
```

The cross-axis middle then closes around the restoration / Nehemiah field:

```markdown
536 BC → AD 445 = 980 = 490 + 490 = 2 × 490
AD 445 → AD 536 = 91 = 7 × 13
AD 536 → AD 1446 = 910 = 70 × 13 = 7 × 130
```

Executable checks:

```markdown
536 + 445 − 1 = 980
536 − 445 = 91
1446 − 536 = 910
```

The same x1 cycle also decomposes through the Babylonian captivity row:

```markdown
1446 BC → 606 BC = 840
606 BC → 536 BC = 70
840 + 70 = 910
```

AD-side counterpart:

```markdown
AD 536 → AD 606 = 70
AD 606 → AD 1446 = 840
70 + 840 = 910
```

Typological / structural note:
The `70 + 840 = 910` display echoes 1Kenan's lifespan structure: Kenan begets at `70` and lives another `840`, totaling `910`. Because 1Kenan and 2Cainan share the same Hebrew name by dependency, the normal-scale Exodus / captivity / return row supports the same 7/13 and 70/130 thread that the 10x rows display at larger scale.

Audit note — `586` / `AD 586` source-display conflict:
The author supplied the forms `606 BC + 70 ... = 586 BC` and `AD 586 + 70 = AD 606`. Under ordinary arithmetic these do not check:

```markdown
606 − 586 = 20
586 − 536 = 50
606 − 536 = 70
AD 606 − AD 586 = 20
AD 606 − AD 536 = 70
```

This revision therefore uses `606 BC → 536 BC = 70` and `AD 536 → AD 606 = 70` as the executable captivity rows. `586 BC` remains an adjacent Fall of Jerusalem date-state by `File_08` / `File_17` dependency and may still be meaningful in other files, but it is not the 70-year endpoint from `606 BC` in this local x1 insertion.

Audit note — mixed-axis source label:
The source phrase `AD 1446 BC` is normalized to `AD 1446`. The mixed `AD` / `BC` label is retained only as source-slip evidence and is not executable node-class language.

Claim-status note:
The visible rows are arithmetic facts under their stated operators. The claim that the x1 rows corroborate a mechanical-like `10x` gear is structural inference and providential synchronization. The Kenan / Cainan same-name connection is dependency-controlled and functions here as a typological / structural note, not as an independent proof row inside `File_25`.

## 5. SKL Long `24510` and the Exodus Mirror-Scale Display

Active state: biblical 10x Mirror-scale display state; SKL Long `24510` terminal state; BC/AD counterpart display-label state.

The Exodus node also participates in the local 10x Mirror-scale pattern:

```markdown
1446 BC → AD 2856 [biblical 10x Mirror-scale display]
4300 = 10 × 430
```

Arithmetic Pass note:
The target value checks:

```markdown
4300 = 10 × 430
```

If exact civil elapsed counting is opened, the background civil comparison is:

```markdown
1446 + 2856 − 1 = 4301
```

Pass 3 therefore preserves `1446 BC → AD 2856` as a local biblical 10x Mirror-scale display rather than recasting it as an exact civil-span row or forcing the SKL / Rounded `BC + AD − 2` operator onto the example.

The AD-side `2856` display is significant because `2856 BC` is also the SKL Long terminal anchor tied to the First Dynasty of Kish `23`-kings span.

Node-class note:
`AD 2856` is the AD-side Exodus 10x display target. `2856 BC` is the SKL Long terminal anchor. They are counterpart display labels, not one node.

SKL Long terminal display:

```markdown
27366 − 2856 = 24510
24510 = 57 × 430
24510 = 19 × 1290
24500 = 50 × 490
24510 = 24500 + 10
```

Arithmetic Pass note:
All displayed `24510` relations check under same-side BC subtraction, integer factorization, and offset arithmetic.

Terminology note:
Pass 3 verifies the neutral offset language `24510 = 24500 + 10`. The source phrase that treated `24510` as short of `24500` is no longer used as executable wording, because the arithmetic relationship is `10` above `24500`.

Structural note:
The SKL Long `24510` span allows the First Dynasty of Kish `23`-kings total to participate in both the `430` cycle and the `1290` cycle while remaining visibly adjacent to the half-`49000` Danielic field.

Claim-status note:
The `24510` factor rows and the `24500 + 10` offset are arithmetic facts. The statement that the SKL Long total participates in the `430`, `1290`, and half-`49000` Danielic fields is structural inference. The broader theological significance of this cross-system coherence is providential synchronization where that interpretive state is opened.


## 5A. File_26 Decimal Invariance extension

Active state: File_26 `18766/18756 BC` Nexus rail comparison state; File_26 Pillar / 10x extension state; File_26 remote `434886 BC` circuit state.

`File_26` extends the Decimal Invariance pattern beyond the local examples already established in this file. `File_25` cites these rows as dependency-controlled File_26 material. It does not rederive the full Nangishlishma, Balih, Pillar, or remote Pre-Creation argument.

The Nexus rail comparison is:

```markdown
18766 ÷ 10 = 1876.6
18756 ÷ 10 = 1875.6
18766 − 18756 = 10
```

The Pillar / 10x extension is:

```markdown
29516 − 27366 = 2150
29516 − 20916 = 8600
29516 − 18766 = 10750
29516 − 14466 = 15050
1446 + 29515 − 1 = 30960
```

The remote circuit is:

```markdown
434886 − 1446 = 433440 = 1290 × 336
434886 + 29515 − 1 = 464400 = 1290 × 360
434886 + 27366 − 2 = 462250 = 215 × 2150
```

Operator note:
The `AD 29515` rows use ordinary civil cross-axis arithmetic where an actual AD Pillar state is active. The `AD 27366` row uses the SKL rounded mod-10 display operator `BC + AD − 2`. The two operators are not interchangeable.

Claim-status note:
The displayed arithmetic facts check under the stated operators. The claim that they extend the Decimal Invariance pattern is structural inference by File_26 dependency. The theological reading remains providential synchronization where that interpretive state is opened.

## 6. Modal-State and Operator Firewalls

`File_25` distinguishes related but non-identical states. Pass 3 verifies visible arithmetic under these states without collapsing the states.

| State / operator | Function in `File_25` | Handling |
|---|---|---|
| Decimal-shadow display | `20916 ÷ 10 = 2091.6`; `14466 ÷ 10 = 1446.6`; `445.6 × 10 = 4456` | Core local thesis. Do not convert decimal shadows into replacement event dates. |
| Finite-decimal display | `.6` display values | Preserve as source display. `445.(6)` remains audit-controlled and does not silently replace `445.6`. |
| Historical anchor state | `2091 BC`; `1446 BC`; `445 BC` | Historical / biblical labels remain controlled by their own files. Decimal-shadow values do not replace them. |
| Same-side BC span | SKL bridge; historical shadow bridge; SKL Long terminal display | Same-side BC arithmetic uses subtraction. Pass 3 verifies the locally executable same-side spans. |
| Biblical 10x Mirror-scale display | `445 BC → AD 4456`; `5366 BC → 536 BC`; `536 BC → AD 445`; `1446 BC → AD 2856` | Local mod-10 display intent for biblical examples. Pass 3 checks civil-background arithmetic where it naturally applies, but does not force `BC + AD − 2` or overwrite display-state rows. |
| LXX `5366 / 536` restoration branch | `5496 − 130 = 5366`; `5366 − 536 = 4830 = 10 × 483`; `4830 × 70/69 = 4900` | Bounded Decimal Invariance example near the Nehemiah field. Same-side BC arithmetic governs the span; `536.6` is decimal-shadow display, not a replacement date. |
| LXX 910-cycle Mirror corroboration | `5366 − 4456 = 910`; `910 = 70 × 13 = 7 × 130`; `536 − 445 = 91 = 7 × 13` | Cross-referenced to `File_29`. It supports the mechanistic 70 / 130 reading without replacing the `5366 / 536` decimal-shadow row. |
| x1 Exodus-to-restoration 910-cycle corroboration | `1446 BC → 536 BC = 910`; `536 BC → 445 BC = 91`; `536 BC → AD 445 = 980`; `AD 445 → AD 536 = 91`; `AD 536 → AD 1446 = 910` | Normal-scale corroboration of the 10x gear. Same-side BC, civil cross-axis, and same-side AD operators remain distinct. |
| x1 Babylonian `70 + 840` Kenan echo | `1446 BC → 606 BC = 840`; `606 BC → 536 BC = 70`; `AD 536 → AD 606 = 70`; `AD 606 → AD 1446 = 840` | Supports the same `70 + 840 = 910` thread. `586 BC` / `AD 586` are audit-controlled source-display conflicts in this local row. |
| 2Cainan-included `666` branch | `5496 − 666 = 4830`; `666 − 536 = 130` | Symbolic branch preserving the Watcher / fallen-man interpretation. It does not replace the `536 BC` restoration node. |
| LXX `10920 / 10950` mod-10 Mirror display | `5556/5496/5426/5366 BC → AD 5366/5426/5496/5556 = 10920`; `5586 BC → AD 5366 = 10950` | Active only where §4.4 explicitly opens source-labeled mod-10 Mirror display. `5486` is retained as an audit-controlled source-display conflict, not executable in the `10920` rail. |
| Artaxerxes 10x decree rail | `4646/4586/4516/4456 BC → AD 4456/4516/4586/4646 = 9100` | Active only as 10x display rail. Historical decree labels remain dependency-controlled. |
| Civil cross-axis span | `BC + AD − 1` where exact civil elapsed counting is opened | Background verification state only. It checks the Nehemiah and restoration-side rows but gives `4301` for `1446 BC → AD 2856`, confirming that row is display-state rather than exact civil-span arithmetic. |
| SKL / Rounded mod-10 display | `6 BC → AD 6 = 10`; `27366 BC → AD 2736 = 30100 = 7 × 4300` where SKL / Rounded state is active | Reserved for SKL or Rounded contexts; not the default operator for the Nehemiah / Daniel 9 biblical rows. |
| File_26 extension states | `18766/18756 BC`; `29516 BC`; `AD 29515`; `434886 BC`; `AD 27366` | Dependency-controlled extensions. Preserve File_26 operator labels and do not turn them into local File_25 proof rows. |
| BC/AD counterpart display labels | `4456 BC` / `AD 4456`; `2856 BC` / `AD 2856`; `2736 BC` / `AD 2736` | Shared numerals do not create node identity. Axis labels must remain visible. |
| Rounded Scaffold state | File_51a Rounded Scaffold | Not opened in `File_25`. Clean values remain local display states. |

SKL / Rounded exemplar retained from the source:

```markdown
27366 BC → AD 2736 = 30100 = 7 × 4300
```

Arithmetic Pass note:
This row checks only under the SKL / Rounded mod-10 display-state:

```markdown
27366 + 2736 − 2 = 30100
7 × 4300 = 30100
```

If ordinary civil cross-axis arithmetic were opened, the background comparison would be:

```markdown
27366 + 2736 − 1 = 30101
```

Pass 3 handling:
This row remains classified as a SKL / Rounded exemplar of cross-Mirror mod-10 display behavior. It does not require the Nehemiah / Daniel 9 biblical examples to be rewritten under the same formal SKL / Rounded operator.

Operator note:
The biblical 10x Mirror-scale display is a local `File_25` display-state. It preserves the author’s mod-10 intent while avoiding unnecessary exact-operator burden in this explanatory file. If later files need exact elapsed civil counting, they must explicitly open the civil cross-axis state.

Argument-control note:
This operator firewall is not an argument against exact civil arithmetic. It prevents exact civil arithmetic, SKL / Rounded mod-10 display, and biblical 10x display intent from carrying each other’s proof burden.

## 7. Dependency and Source-Control Boundaries

Dependency note — Abraham:
The Call of Abraham remains `2091 BC` by `File_01` dependency. `2091.6 BC` is a decimal-shadow display.

Dependency note — Exodus:
The Exodus remains `1446 BC` by `File_05` dependency. `1446.6 BC` is a decimal-shadow display.

Dependency note — Daniel 9:
The four-decree field, the Artaxerxes / Nehemiah decree, and the `490 BC` average are controlled by `File_17`.

Dependency note — LXX / MT Year-6 and the `5366 / 536` restoration example:
The `5496 BC`, `5366 BC`, and `4116 BC` Year-6 comparison is dependency-controlled by `File_07` / `File_18` where exact Creation-table rows are opened. `File_25` uses the author-supplied row only as a bounded Decimal Invariance example.

Dependency note — SKL bridge:
The `27366 BC → 20916 BC → 14466 BC` bridge and the LXX Shem `2736 BC → 1446 BC = 1290` bridge are controlled by `File_21` and table-source dependencies in `File_18`.

Dependency note — SKL Long `24510`:
The `27366 − 2856 = 24510` display, the Sumerian B / SKL Long terminal anchor, and the First Dynasty of Kish `23`-kings context are controlled by `File_24` and deeper SKL / Berossus dependencies.

Dependency note — File_26 Decimal Invariance extension:
The `18766/18756 BC` Nexus rail comparison, the `29516 BC` Pillar counterpart display, the actual `AD 29515` Pillar checks, and the remote `434886 BC` circuit are controlled by `File_26` with File_21 / File_19 dependencies where Pillar or SKL rounded mod-10 display is active. `File_25` cites these rows as downstream Decimal Invariance extension material.

Dependency note — File_29 7/13 harmonic weave:
`File_29` controls the 7/13 harmonic weave and the 70 / 130 relationship used in the `910` cycle. `File_25` cites `File_29` only to classify the meaning of `91`, `130`, and `910`; it does not import the full File_29 proof-of-design diagnostic or probability-language burden.

Dependency note — Exile / Fall of Jerusalem adjacency:
`606 BC`, `586 BC`, `536 BC`, and the related exilic / restoration labels remain dependency-controlled by the Kings / prophetic-span files where full historical and schematic treatment is opened. In this local x1 insertion, the executable 70-year captivity row is `606 BC → 536 BC = 70`; `586 BC` is retained as an adjacent Fall of Jerusalem state and audit-controlled source-display conflict, not silently forced into the 70-year row.

Dependency note — same-name Kenan / Cainan echo:
The claim that 1Kenan and 2Cainan share the same Hebrew name, and that 1Kenan's `70 + 840 = 910` lifespan reinforces the 70 / 130 thread, is dependency-controlled by the Cainan / Kenan sector files. `File_25` uses it only as x1 corroboration for Decimal Invariance and does not open a separate Cainan-sector proof.

Dependency note — Rounded Scaffold:
The Rounded Scaffold is not opened in `File_25`. Clean 10x and mod-10 values remain local display states unless a later pass explicitly opens the File_51a Rounded Scaffold state.

Claim-status boundary note:
Dependencies supply source rows and larger derivations. `File_25` supplies a controlled reference explanation of Decimal Invariance. Where this file cites a dependency-controlled row, the local argument should not be read as independently re-proving the dependency.

## 8. Final Audit and Dependency Notes

Finalization handling:
The author directed that straightforward author-decision items be resolved by best judgment rather than held open as a separate Author-Decision Pass. The Finalization Pass therefore closes the controlled revision sequence by classifying the remaining items as source-history, display-state, dependency-controlled, or resolved author-direction items.

Final note — decimal values versus historical year anchors:
`2091.6 BC`, `1446.6 BC`, and `445.6` check as decimal-shadow / display values. `2091 BC`, `1446 BC`, and `445 BC` remain the historical anchors unless a later file opens an exact decimal-year date-state.

Final note — `445.(6) BC` versus `445.6`:
The executable source display is finite-decimal `445.6`, because:

```markdown
445.6 × 10 = 4456
```

The source-history form `445.(6)` is retained only as non-executable source shorthand in this file. It is not treated as a repeating-decimal operator because that would produce `4456.(6)`, not `4456`.

Final note — `2736` as integer-display shadow of `27366`:
The source visually aligns `2736` with `27366 BC`, but exact arithmetic gives:

```markdown
27366 ÷ 10 = 2736.6
```

The Final source therefore classifies `2736 BC` as the LXX Shem death witness-state and integer-display shadow of `27366 BC`, not as the exact decimal quotient. This resolves the item for local File_25 purposes without inventing a new truncation, rounding, or decimal-seasonal operator.

Final note — `1956 BC` MT Shem comparison:
The comparison checks arithmetically:

```markdown
2736 − 1956 = 780
```

By author direction, `1956 BC` is accepted locally as the MT no-`+60 Terah`, Gear 1 / Japheth-side Shem death companion. `File_24` preserves the `1956/1958 BC` Shem death Gear 1 / Gear 2 pair, with `1958 BC` selected for its Danielic capstone and `1956 BC` retained as the Gear 1 companion. `File_25` uses only the `1956 BC` companion needed for the MT / LXX Shem comparison.

Final note — `20916 BC` node-class label:
The source table labels `20916 BC` as “Post-Flood Start.” The Final source does not promote that label. The active local node-class is SKL `+30` / rail-30 Abrahamic decimal-shadow coordinate:

```markdown
20886 + 30 = 20916
```

“Post-Flood Start” remains source-history language only.

Final note — `5366 / 536` restoration example:
The added restoration example preserves the author-supplied branch logic:

```markdown
5496 − 130 = 5366
5366 − 536 = 4830 = 10 × 483
4830 × 70/69 = 4900
```

`5366 BC` remains a bounded LXX Year-6 / 2Cainan-removed coordinate. `536 BC` remains the restoration / Daniel revelation node. `536.6 BC` is a decimal-shadow display, not a replacement date.

Final note — 2Cainan / `666` branch:
Including the 2Cainan `130` shifts the branch from `5366 / 536` to `5496 / 666`:

```markdown
5366 + 130 = 5496
666 − 536 = 130
5496 − 666 = 4830
```

The `666 BC` target is retained as a symbolic fallen-man / Watcher-associated branch. It is not a replacement for the `536 BC` restoration node.

Final note — 910-cycle Mirror / File_29 cross-reference:
The added Mirror corroboration preserves the author-supplied 70 / 130 logic:

```markdown
5366 − 4456 = 910
910 = 70 × 13 = 7 × 130
536 − 445 = 91 = 7 × 13
```

`File_29` controls the 7/13 meaning of these rows. `File_25` uses them only as Decimal Invariance / Mirror corroboration.

Final note — `5496` versus `5486` in the `10920` rail:
The author supplied `5486` in one sequence. The immediately preceding `60 + 70 + 60` rail and the `10920` mod-10 Mirror checks require `5496`:

```markdown
5496 + 5426 − 2 = 10920
5486 + 5426 − 2 = 10910
```

This revision uses `5496` as the executable coordinate and preserves `5486` as an audit-controlled source-display conflict unless a later author decision opens an intentional `5486` state.

Final note — `10920 / 10950` days-years Mirror display:
The executable display checks as:

```markdown
10920 = 910 × 12 = 30 × 364
5586 + 5366 − 2 = 10950 = 30 × 365
```

The source reading that Adam's apparent `30` years move the LXX Mirror display from the 364-day register to the 365-day register is retained as structural inference and providential synchronization.

Final note — Artaxerxes 10x decree rails:
The `4646/4586/4516/4456 BC` rails are retained as 10x display coordinates. They do not replace the historical Artaxerxes start display, Ezra decree, implied middle rail, or Nehemiah wall decree labels.

Final note — x1 910-gear corroboration:
The normal-scale corroboration checks as:

```markdown
1446 − 536 = 910
536 − 445 = 91
536 + 445 − 1 = 980 = 2 × 490
AD 536 → AD 1446 = 910
1446 − 606 = 840
606 − 536 = 70
70 + 840 = 910
```

The executable captivity row is `606 BC → 536 BC = 70`. The supplied `586` / `AD 586` forms remain audit-controlled because they yield `20`, not `70`, in the local row. The mixed label `AD 1446 BC` is normalized to `AD 1446`.

Final note — BC/AD counterpart labels:
`4456 BC` and `AD 4456`, `2856 BC` and `AD 2856`, and `2736 BC` and `AD 2736` are counterpart display labels, not identical nodes. Axis labels remain part of the node-class.

Final note — `24510` relation to `24500`:
The executable relationship is:

```markdown
24500 = 50 × 490
24510 = 24500 + 10
```

The Final source therefore says that `24510` stands `10` above `24500`. It does not retain the earlier source wording that treated `24510` as short of `24500`.

Final note — biblical 10x Mirror-scale display versus exact operators:
The biblical examples preserve mod-10 display intent without requiring the SKL / Rounded `BC + AD − 2` operator. The Nehemiah `445 BC → AD 4456 = 4900` row and the restoration-side `536 BC → AD 445 = 980` row also check under ordinary civil background. The Exodus `1446 BC → AD 2856` row remains biblical 10x Mirror-scale display because exact civil counting gives:

```markdown
1446 + 2856 − 1 = 4301
```

Final note — claim-status of `Law`:
`Law` is retained as source-title / local axiom language. The repeated 10x pattern is structural inference. The larger theological meaning is providential synchronization where opened. This local classification does not require a control-file promotion of Decimal Invariance to a repository-wide foundational axiom.

Final unresolved issues:
No author-decision blockers remain after Finalization. Remaining items are dependency-boundary, source-history, display-state, or future cross-file update notes. The post-final pressure test found no publication blocker. The bounded x1 insertion retains one non-blocking audit item: the supplied `606 BC + 70 = 586 BC` / `AD 586 + 70 = AD 606` forms do not check under ordinary arithmetic and are replaced in executable rows by `606 BC → 536 BC = 70` and `AD 536 → AD 606 = 70`.

## Revision and archive note

This public-clean Markdown source removes only archival amendment history, pass logs, pressure-test repetition, pointer-refresh records, and obsolete replacement-workflow notes.

Detailed revision history is preserved in the `Repository_Change_Archive`.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed.

Final pressure-test status is preserved in the file header. Detailed pressure-test records are archived rather than repeated in the public file body.

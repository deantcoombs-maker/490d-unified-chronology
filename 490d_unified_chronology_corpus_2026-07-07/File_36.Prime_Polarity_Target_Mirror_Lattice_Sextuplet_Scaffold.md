File: File_36
Title: The Prime-Polarity / Target-Mirror Lattice, Rare Clusters, and the Sextuplet Prophetic Scaffold
Legacy source title: retained in §4 source-history note
Status: Final; post-final pressure tested; P1 local Mirror author addendum applied and pressure tested; sampled post-equalization pressure test complete June 28 2026
Pressure-test status: Current; sampled post-equalization pressure test complete for the File_33 / File_36 paired prime-polarity audit; publication-clean verification complete; post-final pressure test and P1 local Mirror refinement pressure test retained; no publication blockers.
Register Agreement status: Checked against State_Vocabulary_Register v1.17 and active companion controls; convergence / pointer update only; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries changed.
Publication-cleanup status: Public-clean Markdown source; archival amendment history routed to Repository_Change_Archive.
Primary domain: Prime; Statistical; Calendar; Prophetic; Theological; SKL / Berossus by dependency
Traditions: Prime-grid; MT / Exodus by dependency; BJ / Jubilees by comparison; SKL / Berossus by File_34 dependency; Danielic / prophetic; Priestly / calendrical
Canonical source: Markdown
Primary anchors: 5; 7; 11; 23; 25; 30; 46; 6 BC; 1 BC; AD 1; 12 BC; AD 12; 19 BC; 20 BC; 30 BC; AD 25; AD 27; AD 28; AD 30; AD 35; 36 BC; 3 BC; 1399 BC; 1400 BC; 1380; 1426; 1472; P3 101; P6 1481; P7 1871; P16 16061; P19 19421; P25 43781; 1476 BC; 1506 BC; AD 1465; AD 1495; 1866 BC; 1896 BC; AD 1855; AD 1885; 1876 BC; AD 1875; 1886 BC; 1846 BC; 16056 BC; 16086 BC; AD 16045; AD 16075; 19416 BC; 19446 BC; AD 19405; AD 19435; 43776 BC; 43806 BC; AD 43765; AD 43795; 3360; 43680; 63210; 1290; 490; 49; 1446 BC; 1406 BC; 486 BC
Related files: File_00; File_05; File_06; File_07; File_08; File_12; File_13; File_16; File_17; File_18; File_21; File_24; File_32; File_33; File_34; File_35; File_51a; File_54; 490d Repository Style Guide v2.5; Restart Capsule v11.15; State_Vocabulary_Register v1.17; Project Procedures v3.2
Major operators: positive-polarity output `Q − 5 / Q + 25`; negative-polarity / signed A-space output `Q − 16 / Q + 14`; bounded P1 local covenant numeric-year Mirror display; P1 / P2 Christological six-year boundary; P1 age-12 and age-30 civil-span displays; Herodian Temple `46`-year field; target-Mirror where actual chronology opens Mirror; civil cross-axis span `BC + AD − 1`; same-side BC span; Modulo 30; Modulo 210; Key of 23 `25/23`; Danielic `483`, `490`, `1290`; Priestly `336`; `3360`; `43680 = 130 × 336 = 120 × 364`; `63210 = 1290 × 49 = 129 × 490 = 430 × 147`; actual Kings / SKL / Berossus `+50` where dependency-controlled
Major modal states: File identity state; Final post-pressure source state; post-final pressure-tested state; P1 local Mirror author addendum state; Prime-Polarity / Target-Mirror Reconciliation state; P1 Exceptional Origin state; P1 local covenant numeric-year Mirror display state; P2 foundational sextuplet state; P1 / P2 Christological six-year boundary state; P3 mod210 sextuplet state; positive-polarity output state; negative-polarity / signed A-space rendering state; target-Mirror state; civil cross-axis measurement state; retired source mechanism state; actual `+50` dependency state; midpoint / centricity diagnostic state; rare-cluster structural inference state; SKL / Berossus File_34 dependency state; theological note state
Major claim-status labels: arithmetic fact; exact-ratio arithmetic fact; structural inference; theological note; providential synchronization; reception-history note; exploratory note; source-history label; dependency-controlled; audit note
Revision basis: 490d Repository Style Guide v2.5; Restart Capsule v11.15; State_Vocabulary_Register v1.17; Project Procedures v3.2; File_32 Final with Prime-Polarity Correction; File_33 Final with Prime-Polarity Correction; File_35 Final with P1–P100 polarity-date table; File_34 Final for SKL / Berossus dependency control; File_00 foundation / archive-routing precedent; prior finalized File_36 source; Repository_Change_Archive (non-controlling history).
Current refresh note: Public-clean trust-status equalization aligns active control pointers to 490d Repository Style Guide v2.5, Restart Capsule v11.15, State_Vocabulary_Register v1.17, and Project Procedures v3.2. Register Agreement status is recorded in the header; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed. Detailed pass logs, pressure-test records where applicable, pointer-refresh history, and replacement-workflow notes are preserved in Repository_Change_Archive.

# File_36 — The Prime-Polarity / Target-Mirror Lattice, Rare Clusters, and the Sextuplet Prophetic Scaffold

## Related Repository Files

| File | Function for `File_36` |
|---|---|
| `File_00` | Foundational source-map discipline, state-safe retrieval, Mirror / civil / inverse firewalling, Machine Guards, claim-status restraint, and Prime–Civil / Polarity Namespace cross-file controls. |
| `File_05` | Primary `1446 BC` Exodus target, Sojourn / Exodus firewalling, and Exodus-to-Christ expansion where Exodus anchors are opened. |
| `File_06` | Conquest `1406 BC`, land-rest / `1400 BC` boundary, Joshua-Jesus typological state, and Creation-week / Conquest-week comparison where the six-year boundary is opened. |
| `File_07` | Creation-week microstructure, Year-6 / Adam-state comparison, Creation / Incarnation analogy, and `6+1` creation-week terminal vocabulary where opened by dependency. |
| `File_08` | Kings of Judah dual-ledger `+50`, Temple / Exodus schematic route, and actual historical Kings ledger where monarchy `+50` material is opened. |
| `File_12` | Calendrical physics for Priestly `336`, Prophetic `360`, Enochian `364`, solar / Julian `365 / 365.25`, and claim-status handling for calendar states. |
| `File_13` | Christological date-state controls for `6 BC`, `1 BC`, AD `1`, AD `25`, AD `30`, AD `35`, dual Christ anchors, Second-Adam / Jan-1 covenant numeric-year Mirror, age-12 / age-30 displays, ministry language, and NT fulfillment labels. |
| `File_16` | `1876 BC` Nexus, `430 / 400` Sojourn / Affliction distinction, Protocol 1 `AD 1875` target-Mirror, Ramesside integration, and `1876 BC` shared-year-label discipline. |
| `File_17` | Prophetic constants and span-component grammar for `483`, `490`, `1290`, `360`, `430`, `400`, and `25/23`. |
| `File_18` | Chronological data tables, Kings / Jubilee `+50` dependency context, and raw chronology support where tables are cited. |
| `File_21` | SKL Unified Protocol dependency where SKL scale-state or SKL rail language is referenced. |
| `File_24` | Kish / SKL 23-kings arrangement and SKL terminal-pair context where background SKL material is noted. |
| `File_32` | Primary Prime Harmonic controls: positive polarity, negative-polarity / signed A-space, P1 exception, P2 rail-window, P2 negative bridge, prime-space cross-polarity, and Coordinate Mirror as secondary agreement. |
| `File_33` | Ordinal Lock controls, Prime–Civil namespace alignment, P2 positive rail-window `6 BC / 36 BC`, P2 negative bridge `6 BC / AD 25`, and the Herodian Temple / John 2 `46`-year field by dependency. |
| `File_34` | SKL / Berossus `+50`, `361`, `486 BC`, `2370`, and `30 + 20 + 30` dependency control. |
| `File_35` | Prime-Polarity Correction, P1–P100 polarity-date table, and operator-boundary controls. |
| `File_51a` | Rounded Scaffold / A-space / Protocol 1 precedent and non-retrojection control where rounded or Mirror vocabulary is opened. |
| `File_54` | Later-file claim-status restraint and P2 `6 BC / 36 BC` versus `6 BC / AD 25` non-collapse precedent by dependency. |

## 0. File-function

`File_36` registers the rare-prime cluster architecture downstream of `File_32`, `File_33`, and `File_35`. Its active final function is to show how P6, P7, P16, P19, and P25 operate as rare-cluster scaffold nodes that target actual chronological states through corrected positive and negative prime-polarity outputs.

The inherited source mechanism corrected during this revision is documented once in §4. It does not carry proof burden elsewhere in the file.

Argument-control rule:
Arithmetic facts and dependency-controlled chronology carry the base layer. Rare-cluster patterns may support structural inference. Theological readings explain meaning but do not replace arithmetic, source control, or modal-state discipline.

Modal-state rule:
A shared number, date-label, cluster member, prime start, `Q` value, Mirror label, or `+50` does not create identity unless state, node-class, operator, and dependency agree.

## 0.1 Finalization and post-final pressure-test scope

This source is Final and has received the post-final pressure test.

The pressure test:

- confirms `File_36` remains Final;
- preserves the author-approved Prime-Polarity / Target-Mirror correction;
- keeps the retired inherited mechanism as one brief audit-controlled main-body note;
- verifies retained executable arithmetic after the P1 local Mirror refinement;
- preserves the Pass 4 claim-status calibration;
- pressure-tests the P1 / P2 Christological six-year boundary and the P1 local covenant numeric-year Mirror refinement;
- does not otherwise reopen arithmetic, modal-state logic, title choice, or dependency routing.

## 0.2 Working state register

| State | Primary value or form | Node-class / function | Handling in `File_36` |
|---|---:|---|---|
| Final source state | `File_36`; Final; post-final pressure tested | file identity | Current file status. |
| Prime-Polarity / Target-Mirror Reconciliation state | author-approved control | controlling correction | Governs all active sections. |
| Positive-polarity output state | `Q − 5 / Q + 25` | primary Prime–Civil scaffold | Retained as active operator. |
| Negative-polarity / signed A-space state | `Q − 16 / Q + 14` | native opposite-polarity Prime–Civil output | Retained as active operator. |
| Target-Mirror state | actual chronology opens Mirror | chronological target-state | Mirror belongs to actual chronology, not default prime output. |
| Civil cross-axis measurement state | `BC + AD − 1` | span operator | Measures span after BC and AD coordinates exist. |
| Retired source mechanism state | old inherited operator | source-history / audit note | Documented once in §4; non-executable. |
| Actual `+50` state | Kings / SKL / Berossus / Jubilee / target-span | dependency-controlled chronology | Retained where controlled by `File_08`, `File_18`, `File_34`, or related files. |
| P1 Exceptional Origin state | `P1 = 5` | origin boundary | Not an ordinary mod30 rail member; completed through P2 and Key of 23. |
| P2 foundational sextuplet state | `{7, 11, 13, 17, 19, 23}` | initial sextuplet | Supplies mod30 foundation. |
| P1 local covenant numeric-year Mirror display state | P1 row displayed as `1 BC`; AD `30`; AD `12`; `19 BC` | bounded Christological target-Mirror display | Added by author refinement and pressure tested; controlled by `File_13`; not a default prime-output Mirror. |
| P1 / P2 Christological six-year boundary state | `6 BC → AD 1 = 6`; `1406 BC → 1400 BC = 6`; `1406 BC → 6 BC = 1400`; `1400 BC → AD 1 = 1400`; `1400/1399 BC → 20/19 BC = 1380` | theological / chronological target-state | Added by author addendum and pressure tested; dependency-controlled by `File_13`, `File_06`, `File_07`, and `File_33` for the Temple field. |
| Key-of-23 completion state | `23 → 25` | theological / chronological completion | Completes `{5, 7, 11, 13, 17, 19, 23, 25}` as a signed-polarity `30` field. |
| P3 mod210 state | sextuplet start `97`; active `Q = 101` | first full mod210 sextuplet | Establishes ordinary mod210 continuation after P2. |
| Rare-cluster state | quintuplets and sextuplets | structural inference | Preserved, with source proof-language calibrated. |
| Sextuplet start state | `7`; `97`; `16057`; `19417`; `43777`; `1091257` | cluster start values | Separate from active `Q` values. |
| Active `Q` state | P3 `101`; P6 `1481`; P7 `1871`; P16 `16061`; P19 `19421`; P25 `43781` | polarity-output generator | Used for corrected polarity rows. |
| P6 / P7 quintuplet relation | `3360` | Priestly decade relation | Preserved with node-class distinction. |
| P16 / P19 sextuplet relation | `3360` | Priestly decade relation | Preserved. |
| P19 target-span state | `50 × 360 = 18000` | prophetic target-span | Retained; not a prime-internal `50`. |
| P25 / P19 bridge state | `63210 = 1290 × 49` | prime-native opposite-polarity bridge measured by civil cross-axis span | Retained and reclassified. |
| SKL / Berossus dependency state | `361`; `486 BC`; `720 × 361`; `30 + 20 + 30`; `+50` | File_34 dependency | Preserved only by dependency routing unless File_34 derivation is locally opened. |

## 0.3 Node-class register

| Label | Meaning | Example |
|---|---|---|
| Sextuplet start | First term of a prime sextuplet | P16 start `16057`; P19 start `19417`; P25 start `43777` |
| Active `Q` | Prime-quad value used by the corrected polarity row | P16 `Q = 16061`; P19 `Q = 19421`; P25 `Q = 43781` |
| Positive `B⁺` output | `Q − 5` rendered as BC-side coordinate | P25 `43781 − 5 = 43776 BC` |
| Positive `M⁺` output | `Q + 25` rendered as BC-side coordinate | P25 `43781 + 25 = 43806 BC` |
| Negative-polarity output | `Q − 16 / Q + 14` rendered as AD-side coordinate | P25 AD `43765 / 43795` |
| Target-Mirror counterpart | Mirror of an actual chronological target-state | `1876 BC → AD 1875` |
| Civil cross-axis bridge | `BC + AD − 1` between existing coordinates | `19446 + 43765 − 1 = 63210` |
| Midpoint / centricity diagnostic | `Q + 5` observed as field center after target-Mirror display | P7 center `1876 BC`; P16 center `16066 BC` |
| Audit-only inherited output | A date produced by the retired inherited mechanism | Retain only where actual chronology independently supplies the target. |

## 0.4 Machine Guards

### Machine Guard [Prime-Polarity / Target-Mirror Restraint]

Do not use Coordinate Mirror as a default prime-output operator.

Positive-polarity and negative-polarity / signed A-space outputs are the active Prime–Civil scaffold.

When Mirror appears, assign it to the actual chronological target-state unless a local section explicitly opens a bounded explanatory display.

Do not use retired inherited operators as proof-bearing prime-generation logic.

Do not delete actual `+50` systems in Kings, SKL, Berossus, Jubilees, or prophetic target-spans where dependency-controlled.

### Machine Guard [Node-Class Separation]

Do not collapse sextuplet start values, active `Q` values, positive outputs, negative-polarity outputs, target-Mirror counterparts, midpoint diagnostics, and audit-only inherited outputs.

### Machine Guard [SKL / Berossus Dependency]

Do not locally rederive SKL / Berossus `361`, `486 BC`, `720 × 361`, `2370`, `30 + 20 + 30`, or Kings / Berossus `+50` derivation. Route these through `File_34` unless `File_36` explicitly opens the full File_34 derivation.

## 0.5 Claim-status control register

| Claim-status label | Use in `File_36` |
|---|---|
| Arithmetic fact | Verified calculations, exact differences, products, factors, civil spans, rail spans, and same-side spans. |
| Exact-ratio arithmetic fact | Factorizations or identities such as `63210 = 1290 × 49 = 129 × 490 = 430 × 147`. |
| Structural inference | Rare-cluster architecture, pillar pairing, polarity-row alignment, and lattice significance inferred from verified arithmetic. |
| Theological note | Sabbath, covenant, Exodus, Christological, Curse-Resolution, eighth-from-seventh, and new-creation meanings. |
| Providential synchronization | Claim that the verified arithmetic and multi-file target alignments display divine design or providential arrangement. |
| Source-history label | Legacy source vocabulary retained only to explain correction history. |
| Dependency-controlled | SKL / Berossus, Kings `+50`, `361`, `486 BC`, `2370`, `30 + 20 + 30`, and other claims controlled by another file. |
| Audit note | Retired, uncertain, source-history, or non-executable material preserved for traceability. |

## 1. Controlling correction — Prime-Polarity / Target-Mirror Reconciliation

### 1.1 Governing law

```markdown
The primes form the scaffold.
The chronology supplies the targets.
The actual targets already carry Mirror where Mirror is active.
Therefore, a separate Mirror of the prime outputs is usually redundant and should be avoided.
```

This law does not remove the negative-polarity side from the prime scaffold. The negative-polarity / signed A-space side is native to prime-space cross-polarity. What is removed is the additional source claim that a Foundation Mirror supplies a separate executable prime-generation layer.

### 1.2 Active operators

| Category | Operator / state | Handling |
|---|---|---|
| Positive-polarity output | `Q − 5 / Q + 25` | Primary Prime–Civil scaffold. |
| Negative-polarity output | `Q − 16 / Q + 14` | Native signed A-space rendering. |
| Target-Mirror | Actual chronological target-state Mirror | Use only where the target chronology opens Mirror. |
| Civil cross-axis span | `BC + AD − 1` | Measures spans after BC and AD coordinates exist. |
| Actual `+50` systems | Kings / SKL / Berossus / Jubilees / prophetic target-spans | Preserve where dependency-controlled. |

## 2. P1 / P2 / P3 foundation

### 2.1 P1 as Exceptional Origin

`P1 = 5` is not an ordinary mod30 prime-quad rail member. It is the Exceptional Origin.

The source row is retained as an exceptional row:

| P# | Q | Source positive / negative display | Handling |
|---:|---:|---|---|
| P1 | `5` | AD `1`; `30 BC`; `12 BC`; AD `19` | Exceptional-origin row; not normalized into ordinary mod30 rail logic. |

The apparent orphan-status of `5` is resolved when P1 and P2 are read together:

```markdown
P1 supplies: 5
P2 supplies: {7, 11, 13, 17, 19, 23}
Key of 23 supplies: 23 → 25
```

Together:

```markdown
{5, 7, 11, 13, 17, 19, 23, 25}
```

This does not make `25` a prime. It makes `25` the Key-of-23 chronological completion of the seventh prime-limit value `23`.

Claim-status: arithmetic fact for the listed gaps and signed-polarity spans; structural inference for the P1/P2 foundation completion; theological note for the eighth-from-seventh / new-creation reading.

### 2.2 P2 as mod30 foundation

The foundational sextuplet is:

```markdown
{7, 11, 13, 17, 19, 23}
```

Its gaps are:

```markdown
4, 2, 4, 2, 4
```

The seven consecutive primes from `5` through `23` are:

```markdown
{5, 7, 11, 13, 17, 19, 23}
```

with gaps:

```markdown
2, 4, 2, 4, 2, 4
```

The Key-of-23 completion gives:

```markdown
{5, 7, 11, 13, 17, 19, 23, 25}
```

with gaps:

```markdown
2, 4, 2, 4, 2, 4, 2
```

Across signed polarity:

```markdown
5 − (−25) = 30
7 − (−23) = 30
11 − (−19) = 30
13 − (−17) = 30
17 − (−13) = 30
19 − (−11) = 30
23 − (−7) = 30
25 − (−5) = 30
```

P2 also supplies the central Christological foundation under the corrected namespace:

```markdown
P2 positive rail-window: 6 BC / 36 BC
P2 negative-polarity / signed A-space bridge: 6 BC / AD 25
```

These are related but not one continuous arithmetic operator.

### 2.2A P1 / P2 Christological foundation and bounded P1 local Mirror display

Active state: P1 / P2 Christological foundation state; bounded P1 local covenant numeric-year Mirror display state.  
Claim-status: arithmetic fact for the civil-span calculations; dependency-controlled for Christological anchors, the Second-Adam / Jan-1 covenant numeric-year Mirror, and the Herodian Temple field; reception-history note for the traditional December 25 / January 1 circumcision hinge; theological note for the Second Adam / new-creation reading; exploratory note for the possible historical midpoint near `3 BC`.

Machine Guard [P1 LOCAL MIRROR EXCEPTION]:
The P1 display below is a bounded Christological target-state exception. It does not reopen Coordinate Mirror as a default prime-output operator. It uses File_13's local covenant numeric-year Mirror vocabulary because P1 is the Exceptional Origin and because the P1 row is here being read as a Christological target-state matrix.

The P1 / P2 foundation display is:

| P# | Q | Positive / `B⁺` slot | Positive / `M⁺` slot | Negative / `N⁻₁` slot | Negative / `N⁻₂` slot |
|---:|---:|---:|---:|---:|---:|
| P1 | `5` | AD `1` | `30 BC` | `12 BC` | AD `19` |
| P2 | `11` | `6 BC` | `36 BC` | `6 BC` | AD `25` |

Pressure-test wording guard:
For P2 and later ordinary rows, the slot labels correspond to the corrected formulas `Q − 5`, `Q + 25`, `Q − 16`, and `Q + 14`. P1 is the Exceptional Origin row and is not an ordinary formula row. Its slots are retained as the inherited P1 Christological display: AD `1`, `30 BC`, `12 BC`, and AD `19`.

#### 2.2A.1 P1 local covenant numeric-year Mirror display

Active state: P1 local covenant numeric-year Mirror display state.

For ordinary prime-pillar rows, the governing rule remains that the primes form the scaffold and the actual chronology supplies the targets. P1 is the exception row because it is the Exceptional Origin. In this bounded Christological state, the P1 row may be displayed through the local covenant numeric-year Mirror because the arrangement itself revolves around the AD `1` / `1 BC` covenant hinge.

P1 may therefore be displayed as follows:

| P1 source coordinate | Bounded local Mirror display | Christological target-state |
|---:|---:|---|
| AD `1` | `1 BC` / AD `1` hinge | conception / birth / circumcision covenant hinge by reception-history state |
| `30 BC` | AD `30` | about-age-30 ministry display |
| `12 BC` | AD `12` | age-12 temple episode display |
| AD `19` | `19 BC` | Herodian Temple / body-temple field |

This is not ordinary Protocol 1. It is the local covenant numeric-year Mirror display controlled by File_13 and used here because P1 itself is functioning as the mirror-facing initial state of the Christological target chronology.

#### 2.2A.2 P1 age-12 and age-30 displays

Active state: P1 local covenant numeric-year Mirror display state; civil cross-axis measurement state.

Using `1 BC` as the Christological hinge:

```markdown
1 BC → AD 12:
1 + 12 − 1 = 12

1 BC → AD 30:
1 + 30 − 1 = 30
```

Thus P1 displays the two explicit age fields given for Christ in the biblical narrative: age `12` and about age `30`. These are Christological target-state displays, not independent prime-Mirror generators.

The source coordinates may also be read as the inverse-facing scaffold:

```markdown
12 BC → AD 1:
12 + 1 − 1 = 12

30 BC → AD 1:
30 + 1 − 1 = 30
```

Both displays are retained because the P1 row is itself being treated as mirror-facing in this bounded Christological foundation state.

#### 2.2A.3 P1 AD `19` / `19 BC` and the Herodian Temple field

Active state: P1 local covenant numeric-year Mirror display state; Herodian Temple dependency state by File_33.

Under the bounded P1 display:

```markdown
AD 19 → 19 BC
```

The `19 BC` coordinate functions as the body-temple field adjacent to the Herodian Temple start window. File_33 controls the historically elastic Herodian Temple start field as `21/20/19 BC`, with `20 BC` functioning as the approximate center / average and John 2's forty-six-year marker pointing to the AD `26/27/28` ministry field by source-control.

The P1 arithmetic checks are:

```markdown
19 BC → 1 BC:
19 − 1 = 18

19 BC → AD 12:
19 + 12 − 1 = 30
```

Thus `19 BC` stands `18` years before the `1 BC` hinge and `30` years before the AD `12` temple episode display.

The Herodian Temple field checks are:

```markdown
20 BC → AD 27:
20 + 27 − 1 = 46

19 BC → AD 28:
19 + 28 − 1 = 46
```

Claim-status: arithmetic fact for the spans; historical source-control field by File_33 for `21/20/19 BC` and AD `26/27/28`; theological note for the body-temple interpretation.

#### 2.2A.4 Tabernacle / Temple `46`-year harmonic

Active state: Herodian Temple dependency state by File_33; Tabernacle / Exodus target-state by File_05 and File_17; Christological target-state by File_13.

The `20/19 BC` Temple field also aligns with the Mosaic Tabernacle year-label field by multiples of `46`:

```markdown
1446 BC → 20 BC:
1446 − 20 = 1426
1426 = 31 × 46

1445 BC → 19 BC:
1445 − 19 = 1426
1426 = 31 × 46
```

The same field reaches the John 2 ministry display as:

```markdown
1446 BC → AD 27:
1446 + 27 − 1 = 1472
1472 = 32 × 46

1445 BC → AD 28:
1445 + 28 − 1 = 1472
1472 = 32 × 46
```

This preserves the Mosaic Tabernacle / Herodian Temple / Christ-body comparison as a structural and theological display. The `1446/1445 BC` tabernacle year-label is dependency-controlled and is not re-derived locally.

#### 2.2A.5 Conquest land-rest and the `1380 = 30 × 46` bridge

Active state: Conquest / land-rest target-state by File_06; Herodian Temple dependency state by File_33.

The Conquest land-rest field also aligns with the Herodian Temple field:

```markdown
1400 BC → 20 BC:
1400 − 20 = 1380
1380 = 30 × 46

1399 BC → 19 BC:
1399 − 19 = 1380
1380 = 30 × 46
```

This keeps the six-year Conquest / land-rest boundary in view:

```markdown
1406 BC → 1400 BC = 6
```

and preserves the broader P1 / P2 Christological six-year boundary:

```markdown
6 BC → AD 1:
6 + 1 − 1 = 6
```

The two endpoint-pair spans remain:

```markdown
1406 BC → 6 BC = 1400
1400 BC → AD 1 = 1400
```

Thus the Conquest / New-Eden six-year boundary and the Christological six-year boundary form an ordered comparison.

#### 2.2A.6 P2 thirty-year and forty-year displays

Active state: P2 positive rail-window state; P2 negative-polarity / signed A-space bridge state.

P2 repeats the about-30 ministry span from the `6 BC` Christological birth node:

```markdown
6 BC → AD 25:
6 + 25 − 1 = 30
```

P2 also preserves the positive rail-window:

```markdown
6 BC / 36 BC
```

Where the actual Christological target chronology opens Mirror, `36 BC` may correspond to AD `35` as a target-Mirror display:

```markdown
6 BC → AD 35:
6 + 35 − 1 = 40
```

This supplies the full forty-year generation display from the `6 BC` Christological node. The AD `30` and AD `33` ministry / Passion displays remain Christological cut-short or refinement states controlled by File_13 and related Passion-date dependencies; they do not replace the full AD `35` generation display.

The 40-year Wilderness / Christological generation pairing may be displayed as:

```markdown
1446 BC → 6 BC = 1440
1406 BC → AD 35 = 1440
1440 = 4 × 360
```

This connects the Exodus-to-Conquest wilderness field with the Christological generation field without changing the prime-polarity operators.

The actual historical birth date is not adjudicated here. The midpoint of the `6 BC → AD 1` boundary falls near `3 BC`, which may be retained as an exploratory note only.

Theological note:
In this bounded reading, P2 presents Christ at the beginning of the creation field through the `6 BC` node, while P1 presents the AD `1` / `1 BC` hinge as the covenantal and mirror-facing new-creation boundary. This is compatible with the Second Adam reading and with the phrase “the beginning of the creation of God” in Revelation 3:14, but it remains theological interpretation rather than arithmetic proof.

### 2.3 P3 as mod210 sextuplet anchor

OEIS A022008 defines prime sextuplet starts for the form:

```markdown
(p, p + 4, p + 6, p + 10, p + 12, p + 16)
```

and begins:

```markdown
7; 97; 16057; 19417; 43777; 1091257
```

Reference: <https://oeis.org/A022008>

P3 uses the sextuplet:

```markdown
{97, 101, 103, 107, 109, 113}
```

with active core `Q = 101`.

Prime–Civil row:

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P3 | `101` | `96 BC` | `126 BC` | AD `85` | AD `115` |

The positive and negative rails each preserve a `30`-year span:

```markdown
126 − 96 = 30
115 − 85 = 30
```

P3 is the first full ordinary mod210 expression of the sextuplet scaffold after the P2 foundation.

## 3. General corrected Prime–Civil field

For an active prime-quad value `Q`:

```markdown
Positive polarity:
Q − 5
Q + 25

Negative-polarity / signed A-space:
AD (Q − 16)
AD (Q + 14)
```

If the actual chronological target-state opens Mirror, then the AD-side target counterparts may be displayed on the BC plane as:

```markdown
AD (Q − 16) → (Q − 15) BC
AD (Q + 14) → (Q + 15) BC
```

Flattened target-Mirror display:

```markdown
Q − 15
Q − 5
Q + 15
Q + 25
```

Spacing:

```markdown
10 + 20 + 10 = 40
```

Therefore:

```markdown
Q + 5
```

may appear as a midpoint / centricity diagnostic, but it is not an executable prime-output operator.

## 4. Brief source-history note

Legacy source title: **The Prime Double Lock, Rare Clusters, and the Sextuplet Prophetic Lattice**.

Audit note:
The inherited source derived an inner confirmation protocol from a P1/P2 Foundation Mirror and executed `Q − 25 / Q + 5` as an active prime-generation layer, producing a `20 + 10 + 20 = 50` spread. Current author-approved control retires that mechanism from executable status. The arithmetic may remain source-history where needed for traceability, but it carries no active proof burden in this Final file.

## 5. P6 test — Exodus / Mosaic field

### 5.1 Corrected P6 row

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P6 | `1481` | `1476 BC` | `1506 BC` | AD `1465` | AD `1495` |

### 5.2 Target-Mirror field

Where actual chronology opens target-Mirror:

```markdown
AD 1465 → 1466 BC
AD 1495 → 1496 BC
```

Flattened field:

```markdown
1466 BC
1476 BC
1496 BC
1506 BC
```

Spacing:

```markdown
1466 → 1476 = 10
1476 → 1496 = 20
1496 → 1506 = 10

Total = 40
Center = 1486 BC
```

### 5.3 P6 target checks

```markdown
1476 + 1465 − 1 = 2940 = 6 × 490
1446 + 1495 − 1 = 2940 = 6 × 490
```

Inner `3 × 490` structure:

```markdown
1476 − 6 = 1470 = 3 × 490
6 + 1465 − 1 = 1470 = 3 × 490

1446 + 25 − 1 = 1470 = 3 × 490
1495 − 25 = 1470 = 3 × 490
```

Therefore:

```markdown
1476 BC → 6 BC → AD 1465
1446 BC → AD 25 → AD 1495
```

Each complete route is:

```markdown
3 × 490 + 3 × 490 = 6 × 490 = 2940
```

### 5.4 P6 and actual `+50`

Actual Kings / Berossus `+50` remains dependency-controlled and valid:

```markdown
1496 BC → 1016 BC → 536 BC
480 + 480 = 960

1446 BC → 966 BC → 486 BC
480 + 480 = 960
```

The `+50` belongs to the actual Kings / SKL / Berossus chronology, not to an active prime-internal spread.

Claim-status: arithmetic fact for the polarity row and spans; dependency-controlled for Kings / Berossus `+50`; structural inference for the `40`-field alignment.

## 6. P7 test — Abrahamic / Nexus field

### 6.1 Corrected P7 row

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P7 | `1871` | `1866 BC` | `1896 BC` | AD `1855` | AD `1885` |

### 6.2 Target-Mirror field

Where actual chronology opens target-Mirror:

```markdown
AD 1855 → 1856 BC
AD 1885 → 1886 BC
```

Flattened field:

```markdown
1856 BC
1866 BC
1886 BC
1896 BC
```

Spacing:

```markdown
1856 → 1866 = 10
1866 → 1886 = 20
1886 → 1896 = 10

Total = 40
Center = 1876 BC
```

### 6.3 P7 constant-volume checks

```markdown
1896 + 1855 − 1 = 3750
1876 + 1875 − 1 = 3750
1866 + 1885 − 1 = 3750
```

Factor:

```markdown
3750 = 30 × 5 × 5 × 5
```

### 6.4 P7 target-state handling

| Date | Corrected classification |
|---:|---|
| `1866 BC` | P7 positive-polarity scaffold output. |
| `1896 BC` | P7 positive-polarity scaffold output. |
| AD `1855` | P7 negative-polarity / signed A-space output. |
| AD `1885` | P7 negative-polarity / signed A-space output. |
| `1876 BC` | Actual chronological Nexus target; midpoint diagnostic in the flattened field. |
| AD `1875` | Mirror of the actual `1876 BC` Nexus where that target-state opens Mirror. |
| `1886 BC` | Actual Isaac-death node; AD `1885` may correspond to its target-Mirror counterpart. |
| `1846 BC` | Actual Affliction-start state; retained by chronology, not by active prime generation. |

Chronology checks:

```markdown
1876 − 1446 = 430
1846 − 1446 = 400
1876 − 1846 = 30
```

Claim-status: arithmetic fact for the corrected P7 row and target spans; structural inference for the Nexus-centered alignment; theological note for the Abrahamic / covenantal interpretation.

## 7. P6 / P7 Quintuplet Pillars

### 7.1 Rare quintuplet classification

P6 and P7 remain rare quintuplet pillars.

| Cluster | Type | Complete set | Span |
|---|---|---:|---:|
| P7 Abrahamic quintuplet | `p − 4` form | `{1867, 1871, 1873, 1877, 1879}` | `1879 − 1867 = 12` |
| P6 Mosaic quintuplet | `p + 12` form | `{1481, 1483, 1487, 1489, 1493}` | `1493 − 1481 = 12` |

Theological note:
The number `12` frames both the promise of the tribes and the Exodus / Sinai organization of the tribes.

### 7.2 P6 / P7 `3360` relation

The P6 and P7 quintuplets remain bound by `3360`.

| P7 component | P6 component | Sum |
|---:|---:|---:|
| `1867` | `1493` | `3360` |
| `1871` | `1489` | `3360` |
| `1873` | `1487` | `3360` |
| `1877` | `1483` | `3360` |
| `1879` | `1481` | `3360` |

Factorizations:

```markdown
3360 = 10 × 336
3360 = 7 × 480
3360 = 70 × 48
3360 = 7 × 40 × 12
```

Claim-status: arithmetic fact for the sums and factorizations; structural inference for the Priestly / tribal interpretation.

## 8. Outer Quintuplet Lattice

The P6 / P7 relation is not isolated. The source also places Q2 and Q4 as outer quintuplet pillars governed by the same `3360` constant.

| Label | Type | Complete set | Position |
|---|---|---:|---|
| Q2 | `p − 4` form | `{97, 101, 103, 107, 109}` | Outer lower |
| Q4 | `p − 4` form | `{3457, 3461, 3463, 3467, 3469}` | Outer upper |

Start and end checks:

```markdown
3457 − 97 = 3360
3469 − 109 = 3360
```

State label:
This is a same-side prime-space / rare-cluster relation, not a Coordinate Mirror relation.

## 9. P7 to P6 iniquity bridge and actual `+50` firewall

### 9.1 Ezekiel `390 + 40 = 430` structure

Active state: actual chronology / prophetic bridge.

```markdown
1876 − 1486 = 390
1486 − 1446 = 40
1876 − 1446 = 430
390 + 40 = 430
```

The `390` component is an Ezekiel curse-number bridge; the `40` component corresponds to Moses in Midian and the Exodus / Wilderness pattern by dependency.

### 9.2 P7-to-P6 projection

The P7 field projects toward the Mosaic field by `390` years:

```markdown
1896 − 390 = 1506 BC
1876 − 390 = 1486 BC
1866 − 390 = 1476 BC
1846 − 390 = 1456 BC
```

Revised state label:
This is a prophetic / chronological bridge, not a prime-output operator. `1876 BC` and `1846 BC` are actual target-states supplied by chronology.

### 9.3 Actual `+50` termination boundary

Actual chronology retains the `+50` display:

```markdown
1446 BC + 50 = 1496 BC
1406 BC + 50 = 1456 BC
```

This is a Kings / Conquest / schematic target-state display, not an active prime-internal spread.

### 9.4 `390 → 340` and `170` half-cycle

```markdown
390 − 50 = 340
340 = 20 × 17
170 = 340 ÷ 2
```

Claim-status: arithmetic fact for the reduction and factorization; dependency-controlled structural inference where File_35’s 17-chain subsystem is opened.

## 10. Sextuplet Prophetic Lattice

### 10.1 External sequence control

OEIS A022008 defines prime sextuplet starts for the form:

```markdown
(p, p + 4, p + 6, p + 10, p + 12, p + 16)
```

The source registry is retained with no-comma formatting:

| # | Start value | Label | Complete set | Function |
|---:|---:|---|---:|---|
| `1` | `7` | Foundation | `{7, 11, 13, 17, 19, 23}` | Christological / P2 foundation |
| `2` | `97` | Q2 | `{97, 101, 103, 107, 109, 113}` | Outer pillar / P3 mod210 anchor |
| `3` | `16057` | P16 | `{16057, 16061, 16063, 16067, 16069, 16073}` | Wilderness / `40` |
| `4` | `19417` | P19 | `{19417, 19421, 19423, 19427, 19429, 19433}` | Kingdom / target-span `+50` |
| `5` | `43777` | P25 | `{43777, 43781, 43783, 43787, 43789, 43793}` | Messiah / `120` |
| `6` | `1091257` | Deep Time | `{1091257, 1091261, 1091263, 1091267, 1091269, 1091273}` | cosmic scale |

Node-class note:
The start value is not always the active `Q` used in the corrected polarity row. P16 start `16057` uses active `Q = 16061`; P19 start `19417` uses active `Q = 19421`; P25 start `43777` uses active `Q = 43781`.

OEIS A200504 lists initial primes in prime sextuplets preceding maximal gaps and begins:

```markdown
7; 97; 19417; 43777
```

Reference: <https://oeis.org/A200504>

OEIS A233426 lists primes in sextuplets at the end of maximal gaps and begins:

```markdown
97; 16057; 43777; 1091257; 6005887
```

Reference: <https://oeis.org/A233426>

### 10.2 Priestly `3360` link

```markdown
19417 − 16057 = 3360
3360 = 10 × 336
```

State label:
This is a same-side sextuplet-start relation from P16 to P19.

### 10.3 Q2 to P19 Danielic `69`-weeks relation

```markdown
19417 − 97 = 19320
19320 = 40 × 483
483 = 69 × 7
```

Expansion display:

```markdown
40 × 483 = 19320
40 × 490 = 19600
```

Claim-status: arithmetic fact for the checked products; structural inference for the Danielic meaning.

### 10.4 Q2 to P25 Grand Unification gap

```markdown
43777 − 97 = 43680
43680 = 130 × 336
43680 = 120 × 364
```

State label:
This is a same-side sextuplet-start gap between Q2 and P25. It supports the Grand Unification identity but does not replace the P25 active `Q = 43781` polarity row.

### 10.5 P19 / P25 bridge

The source retained a boundary sum:

```markdown
19417 + 43793 = 63210
63210 = 49 × 1290
```

Revised controlling classification:
The main P19 / P25 bridge is a **prime-native opposite-polarity bridge measured by civil cross-axis span**.

| Bridge | Calculation | Result |
|---|---:|---:|
| P19 `M⁺` to P25 `N⁻₁` | `19446 + 43765 − 1` | `63210` |
| P19 `B⁺` to P25 `N⁻₂` | `19416 + 43795 − 1` | `63210` |
| P25 `M⁺` to P19 `N⁻₁` | `43806 + 19405 − 1` | `63210` |
| P25 `B⁺` to P19 `N⁻₂` | `43776 + 19435 − 1` | `63210` |

Factorization:

```markdown
63210 = 1290 × 49
63210 = 129 × 490
63210 = 430 × 147
```

This is not a Mirror bridge and not an external chronological overlay. The positive and negative coordinates are native Prime–Civil outputs; the civil cross-axis formula measures the span once the coordinates exist.

Claim-status: arithmetic fact for the four `63210` cross-axis spans and factorization; structural inference for the Danielic opposite-polarity bridge.

### 10.6 Deep Time links

Source-retained calculations:

```markdown
1091257 − 16057 = 1075200 = 3200 × 336
1091257 − 43777 = 1047480 = 812 × 1290
```

Claim-status: arithmetic fact for the products; structural inference for the large-scale lattice relationship.

## 11. P16 test — Wilderness / `40` field

### 11.1 Corrected P16 row

P16 belongs to the A022008 sextuplet start `16057`, with active `Q = 16061`.

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P16 | `16061` | `16056 BC` | `16086 BC` | AD `16045` | AD `16075` |

### 11.2 Target-Mirror field

Where actual chronology opens target-Mirror:

```markdown
AD 16045 → 16046 BC
AD 16075 → 16076 BC
```

Flattened field:

```markdown
16046 BC
16056 BC
16076 BC
16086 BC
```

Spacing:

```markdown
16046 → 16056 = 10
16056 → 16076 = 20
16076 → 16086 = 10

Total = 40
Center = 16066 BC
```

`16066 BC` is a midpoint / centricity diagnostic only.

### 11.3 Retained positive-polarity spectrum

```markdown
16086 − 14610 = 1476 BC
16056 − 14610 = 1446 BC

14610 = 40 × 365.25
```

### 11.4 Corrected target-Mirror checks

```markdown
16046 − 14600 = 1446 BC
14600 = 40 × 365

16046 − 14560 = 1486 BC
14560 = 40 × 364

16076 − 14600 = 1476 BC
14600 = 40 × 365
```

Additional checked target relations:

```markdown
16075 + 1406 − 1 = 17480
17480 = 23 × 760
17480 = 38 × 460

16076 − 4116 = 11960
11960 = 299 × 40

16076 − 5496 = 10580
10580 = 23 × 460
10580 = 23 × 23 × 20

16076 − 14006 = 2070
2070 = 23 × 90
```

Audit-only note:
`16036 BC` is not executable in this file unless an independent chronological target-state is supplied.

Claim-status: arithmetic fact for the corrected P16 row and target checks; structural inference for the Wilderness / `40` field.

## 12. P19 test — Prophetic Jubilee target-span

### 12.1 Corrected P19 row

P19 belongs to the A022008 sextuplet start `19417`, with active `Q = 19421`.

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P19 | `19421` | `19416 BC` | `19446 BC` | AD `19405` | AD `19435` |

### 12.2 Target-Mirror field

Where actual chronology opens target-Mirror:

```markdown
AD 19405 → 19406 BC
AD 19435 → 19436 BC
```

Flattened field:

```markdown
19406 BC
19416 BC
19436 BC
19446 BC
```

Spacing:

```markdown
19406 → 19416 = 10
19416 → 19436 = 20
19436 → 19446 = 10

Total = 40
Center = 19426 BC
```

### 12.3 P19 Wilderness target lock

The corrected P19 field maps by `50 × 360 = 18000` to the Exodus / Wilderness / Conquest field:

| P19 field point | Span | Target |
|---:|---:|---:|
| `19446 BC` | `18000 = 50 × 360` | `1446 BC` |
| `19436 BC` | `18000 = 50 × 360` | `1436 BC` |
| `19416 BC` | `18000 = 50 × 360` | `1416 BC` |
| `19406 BC` | `18000 = 50 × 360` | `1406 BC` |

Arithmetic:

```markdown
19446 − 18000 = 1446
19436 − 18000 = 1436
19416 − 18000 = 1416
19406 − 18000 = 1406
```

Thus P19 retains the `50 × 360` prophetic target-span while landing on a `40`-year field:

```markdown
1446 BC
1436 BC
1416 BC
1406 BC
```

The `50` belongs to the prophetic target-span; the corrected prime field itself is `10 + 20 + 10 = 40`.

Claim-status: arithmetic fact for the corrected P19 row and target-span; structural inference for the prophetic Jubilee / Wilderness field.

## 13. P25 test — Messiah / `120` field

### 13.1 Corrected P25 row

P25 belongs to the A022008 sextuplet start `43777`, with active `Q = 43781`.

| P# | Q | Positive `Q − 5` | Positive `Q + 25` | Negative `Q − 16` | Negative `Q + 14` |
|---:|---:|---:|---:|---:|---:|
| P25 | `43781` | `43776 BC` | `43806 BC` | AD `43765` | AD `43795` |

### 13.2 Target-Mirror field

Where actual chronology opens target-Mirror:

```markdown
AD 43765 → 43766 BC
AD 43795 → 43796 BC
```

Flattened field:

```markdown
43766 BC
43776 BC
43796 BC
43806 BC
```

Spacing:

```markdown
43766 → 43776 = 10
43776 → 43796 = 20
43796 → 43806 = 10

Total = 40
Center = 43786 BC
```

`43786 BC` is midpoint / centricity only.

### 13.3 P25 / P19 same-polarity gap

The same-side point-to-point gap from P19 to P25 is:

```markdown
43781 − 19421 = 24360
43776 − 19416 = 24360
43806 − 19446 = 24360
43765 − 19405 = 24360
43795 − 19435 = 24360
```

This is valid, but it is not the main Danielic bridge.

### 13.4 P25 / P19 opposite-polarity bridge

The primary Danielic bridge is the P19 / P25 prime-native opposite-polarity bridge measured by civil cross-axis span:

```markdown
19446 + 43765 − 1 = 63210
19416 + 43795 − 1 = 63210
43806 + 19405 − 1 = 63210
43776 + 19435 − 1 = 63210
```

Factorization:

```markdown
63210 = 1290 × 49
63210 = 129 × 490
63210 = 430 × 147
```

Claim-status: exact-ratio arithmetic fact for the four spans and factorization; structural inference for the Daniel 9 / Daniel 12 / Ezekiel 4 synthesis.

### 13.5 P25 / P16 row translation

P25 also translates the P16 row by a same-side polarity-preserving span:

```markdown
43781 − 16061 = 27720 = 77 × 360
43776 − 16056 = 27720 = 77 × 360
43806 − 16086 = 27720 = 77 × 360
43765 − 16045 = 27720 = 77 × 360
43795 − 16075 = 27720 = 77 × 360
```

This confirms that the corrected positive / negative polarity rows preserve the lattice without the retired source mechanism.

### 13.6 P25 retained spectrum

The P25 positive `M⁺` anchor remains:

```markdown
M⁺ = 43806 BC
```

The P25 spectrum is retained where dependency-controlled:

```markdown
43806 − 40320 = 3486 BC
40320 = 120 × 336

43806 − 43200 = 606 BC
43200 = 120 × 360

43806 − 43320 = 486 BC
43320 = 120 × 361

43806 − 43680 = 126 BC
43680 = 120 × 364

43806 − 43800 = 6 BC
43800 = 120 × 365

43806 BC → AD 25 = 43830
43830 = 120 × 365.25
```

The `486 BC`, `361`, SKL, Berossus, and Metonic rows remain File_34-controlled dependency material, not local proof burden inside File_36.

Claim-status: arithmetic fact for the spectrum calculations; dependency-controlled for `361`, `486 BC`, SKL, and Berossus interpretation.

## 14. Grand Unification equation

### 14.1 Identity

```markdown
43680 = 120 × 364
43680 = 130 × 336
```

Component checks:

```markdown
120 × 364 = 43680
130 × 336 = 43680
```

State labels:

| Form | State |
|---:|---|
| `120 × 364` | `120`-cycle Enochian / calendrical form |
| `130 × 336` | Priestly / patriarchal form |

### 14.2 Geometric structural inference

The identity links the P25 / Q2 gap to the same `43680` value used by the `120`-cycle and the `130 × 336` Priestly form.

Claim-status: exact-ratio arithmetic fact for the identity; structural inference for its unifying significance.

## 15. SKL / Berossus / Metonic dependency-control section

### 15.1 Retained dependency material

The following material is retained only under File_34 dependency control:

```markdown
361
486 BC
120 × 361 = 43320
720 × 361 = 259920
2370
30 + 20 + 30
Kings / Berossus +50
```

### 15.2 Revised handling

`File_36` may cite SKL / Berossus material as dependency-controlled support, but it does not locally rederive the SKL / Berossus system.

Actual SKL / Berossus `+50` remains valid where controlled by File_34 and related Kings / restoration files. Retiring the inherited prime mechanism does not amend those actual `+50` systems.

Claim-status: dependency-controlled.

## 16. Exodus target-Mirror Jubilee

### 16.1 Corrected civil-span display

The corrected civil cross-axis display is:

```markdown
1446 + 1495 − 1 = 2940
2940 = 6 × 490
```

This replaces any compressed display that silently treats `1446 BC` as `1445` without stating the transformation.

### 16.2 Bidirectional tuning fork

```markdown
1446 BC → AD 1495 = 2940 = 6 × 490
1476 BC → AD 1465 = 2940 = 6 × 490
```

Claim-status: arithmetic fact for the civil-span display; structural inference for the Exodus / Joshua tuning-fork relation.

## 17. System-wide summary

### 17.1 Retained architecture

The file retains:

```markdown
P1 Exceptional Origin
P1 / P2 Christological six-year boundary
P1 local covenant numeric-year Mirror display
P2 foundational sextuplet
P3 mod210 sextuplet anchor
P6 / P7 quintuplet pillars
P16 / P19 / P25 sextuplet lattice
positive-polarity outputs
negative-polarity / signed A-space outputs
target-Mirror only where chronology opens Mirror
P19 `50 × 360` target-span
P19 / P25 `63210 = 1290 × 49`
P25 `M⁺ = 43806 BC` spectrum
actual Kings / SKL / Berossus `+50` systems by dependency
```

### 17.2 Corrected pillar table

| Pillar | Q | Positive outputs | Negative-polarity outputs | Flattened target-Mirror field where active | Center diagnostic | Result |
|---:|---:|---:|---:|---:|---:|---|
| P6 | `1481` | `1476 BC / 1506 BC` | AD `1465 / 1495` | `1466 / 1476 / 1496 / 1506 BC` | `1486 BC` | Confirms `40` field and `6 × 490` bridge |
| P7 | `1871` | `1866 BC / 1896 BC` | AD `1855 / 1885` | `1856 / 1866 / 1886 / 1896 BC` | `1876 BC` | Confirms Nexus-centered Abrahamic field |
| P16 | `16061` | `16056 BC / 16086 BC` | AD `16045 / 16075` | `16046 / 16056 / 16076 / 16086 BC` | `16066 BC` | Confirms Wilderness / Exodus calendar field |
| P19 | `19421` | `19416 BC / 19446 BC` | AD `19405 / 19435` | `19406 / 19416 / 19436 / 19446 BC` | `19426 BC` | Confirms `50 × 360` target-span to `1446–1406 BC` |
| P25 | `43781` | `43776 BC / 43806 BC` | AD `43765 / 43795` | `43766 / 43776 / 43796 / 43806 BC` | `43786 BC` | Confirms P19/P25 `1290 × 49` opposite-polarity bridge |

### 17.3 Retained / retired summary

| Category | Status |
|---|---|
| Rare-cluster / sextuplet architecture | Retained. |
| Positive-polarity outputs | Retained. |
| Negative-polarity / signed A-space outputs | Retained. |
| Target-Mirror | Retained only where actual chronology opens Mirror. |
| Civil cross-axis measurement | Retained with `BC + AD − 1`. |
| Actual Kings / SKL / Berossus / Jubilee `+50` | Retained where dependency-controlled. |
| Inherited inner confirmation mechanism | Retired from executable prime-generation; documented once in §4. |
| Lunar rows | Removed from executable P16 and P25 spectrum displays. |

## 18. Audit notes

### Audit note 1: P2 / AD `25` namespace

`6 BC / AD 25` is the P2 negative-polarity / signed A-space bridge. `6 BC / 36 BC` is the P2 positive rail-window. These are related but not one continuous arithmetic operator.

### Audit note 2: P6 active `Q` versus P6 quintuplet member `1489`

P6 active polarity row uses `Q = 1481`. The P6 quintuplet relation with P7 includes member `1489`. These are different node-classes and should not be collapsed.

### Audit note 3: P16 old four-date set

`16086 BC` and `16056 BC` remain positive-polarity outputs. `16066 BC` may function only as a midpoint diagnostic. `16036 BC` is audit-only unless independently supplied by actual chronology.

### Audit note 4: P19 / P25 `63210`

The source boundary sum `19417 + 43793 = 63210` is arithmetically valid. The active bridge is the corrected prime-native opposite-polarity civil cross-axis span yielding the same `63210` across four pairings.

### Audit note 5: SKL / Berossus `+50`

Actual SKL / Berossus / Kings `+50` remains valid by dependency. It is not amended by retiring the inherited prime-internal `50` explanation.

### Audit note 6: Civil Mirror Jubilee display

Use the civil cross-axis form:

```markdown
1446 + 1495 − 1 = 2940
```

Do not rely on an unstated transformed `1445 + 1495 = 2940` display in the main argument.

### Audit note 7: Claim-weight restraint

Theological and providential claims are retained as theological note or providential synchronization. They do not replace arithmetic fact, dependency control, or active operator state.

### Audit note 8: P1 / P2 Christological foundation and bounded P1 local Mirror display

The P1 / P2 Christological addendum is retained as a bounded author addition checked in the post-final pressure test. It does not reopen the retired inherited source mechanism and does not alter the corrected positive / negative polarity operators.

P1 may be displayed through the local covenant numeric-year Mirror because P1 is the Exceptional Origin and because the P1 row is being used as a Christological target-state matrix. This is not a default Mirror of prime outputs.

The exact arithmetic retained is:

```markdown
6 + 1 − 1 = 6
1406 − 1400 = 6
1406 − 6 = 1400
1400 + 1 − 1 = 1400
1 + 12 − 1 = 12
1 + 30 − 1 = 30
12 + 1 − 1 = 12
30 + 1 − 1 = 30
19 − 1 = 18
19 + 12 − 1 = 30
20 + 27 − 1 = 46
19 + 28 − 1 = 46
1446 − 20 = 1426 = 31 × 46
1445 − 19 = 1426 = 31 × 46
1446 + 27 − 1 = 1472 = 32 × 46
1445 + 28 − 1 = 1472 = 32 × 46
1400 − 20 = 1380 = 30 × 46
1399 − 19 = 1380 = 30 × 46
6 + 25 − 1 = 30
6 + 35 − 1 = 40
1446 − 6 = 1440
1406 + 35 − 1 = 1440
```

The AD `1` / `1 BC` hinge, the age-12 and age-30 displays, the AD `35` generation completion, the `20/19 BC` Herodian Temple field, and the `3 BC` midpoint are Christological target-state, reception-history, dependency-controlled, or exploratory theological displays. They are not new prime-generation operators.

## 19. Revision and archive note

This public-clean Markdown source removes only archival amendment history, the completed post-final pressure-test report, the completed revision log, and the historical cross-file recommendation ledger.

Detailed revision history is preserved in the `Repository_Change_Archive`.

Final pressure-test status is preserved in the file header. Detailed pressure-test records are archived rather than repeated in the public file body.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed.

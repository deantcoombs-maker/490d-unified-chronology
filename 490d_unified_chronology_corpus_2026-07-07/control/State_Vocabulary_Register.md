File: State_Vocabulary_Register_v1.20
Title: 490d State Vocabulary Register v1.20 — Ezekiel Endpoint / Intercalation-Cycle Convention Entry
Status: v1.20 — Ezekiel Endpoint / Intercalation-Cycle Convention Entry registered as Part D §D.19 (July 6 2026; author-directed bounded control-file update from the deferred-item-6 resolution discussion: File_17 §13A.8 open item closed by author derivation — the `1290 + 1290` endpoint is the fall of the temple, intercalated form; File_17 v5.3 §13A extension registered — siege-frame derivation state, SP year-mirror display state, leap-month scheme-instance state, the `2580 = 430 + 2150 = 430 × 6` sibling decomposition with the Lamech cross-scale twin display; File_12 §7A registered — 360-day intercalation cycle state `6 × (6 × 360 + 30) + (4 × 360 + 30) = 14610 = 40 × 365.25`, counted-or-not option principle as the 360-calendar counterpart of the epagomenal exclusion, `1260 / 1290` same-half-week corollary with placement rule, year-scale instantiation display with the AD 25n exact-closure note; calibration-pair display — 364-Enochian → tropical, intercalated-360 → Julian; guards registered — Julian-mean versus File_09 Julian Jan-1 non-collapse, `2730` dual-reading no-collapse, placement-declaration rule; claim-splits fixed — the scheme as author-declared system / structural inference, the intended-option reading as historical-critical inference, the SP year-mirror as structural inference / providential synchronization; no pre-existing definition, Machine Guard, arithmetic value, anchor, modal state, node-class, sign convention, operator, slash-pair, range, envelope, Mirror protocol, claim-status label, theological claim, or dependency boundary altered); v1.19 — Regular / Cumulative Page-Publication Convention Entry registered as Part D §D.18 (July 6 2026; author-directed bounded control-file update from the Regular / Cumulative chronology-page publication discussion record and the sixteen-file bounded corpus pass — Files 01, 03, 07, 13, 14, 16, 17, 18, 20, 22, 44, 46, 51a, 51b, 53, 56; Long/Short terminology deprecation; SOR MT-sub-variant protocol states; LXX original-Lamech / transform-pair / dual-locality / 777 overlay-row / 430-alignment states with two author-ruled File_18 guard supersessions and the File_20 reclassification supersession recorded per non-silent-repair; Ezekiel date-string state; deep-time overlay application / twenty-of-years / staged-Flood / double-expansion licensing / Adam-field states; 12558 dual-placement / ×10 display / backbone field-harmonic / increment-corollary states; Adam-field Mirror lock 23/24 × 920 with Shem ±2 lock-key and providential claim-split; cumulative apparent-span / five-arm 10080 lattice / Revelation-11 ×10 template / inferred secondary-node class / Joshua register-conversion / center-jewel states; composite-head Joshua lock; conception-anchor rail / rod-termini classification / temple AD-image; consecutive-birth interval application with File_51b §14 controlling; Levi / Judah post-Jacob span; Benjamin 1906 birth-label; four-roads shared-label display; unified Enoch accounting / SP 364-day compression / relocated display block; SOR −5 mod-5 absorption and 4116 shared-label note; the non-propagation and non-collapse guard set registered; explicit-versus-inferred node principle recorded as pending; no pre-existing definition, Machine Guard, arithmetic value, anchor, modal state, node-class, sign convention, operator, slash-pair, range, envelope, Mirror protocol, claim-status label, theological claim, or dependency boundary altered); v1.18 — File_56 Convention Entry registered as Part D §D.17 at File_56 author-invoked Finalization (July 2 2026; author-directed bounded control-file update from the Creation-decade discussion record; SP Enochian Day-4 phase-anchor state (cumulative-only), Creation decade envelope Days 1–10 with DecNorm 10 / DecSpread 10.5, Day-10 terminus node-class, implied Day-1 label states 13406 BC / 13866 BC, Aaron-member Adam selection, decade segment-marking states 3+7 / 7+3, 299-multiple Exodus lock state, 1495 dual-expansion state, 605 phase-invariant gap state, 12000 Creation–Conquest transposition state, Priestly Day-1 / Solar Day-4 origin states, 12600 dual-convergence boundary state, Mirror-test landing state (13406n / 13407t BC → AD 1406t / 1407n; span 14812 = 23 × 23 × 28; × 25/23 = 16100 = 7 × 2300), Mirror-test principle, and the Tier-1 / Tier-2 / Tier-3 firewall registered; fourteen File_56 Machine Guards registered; author-directed File_07 §1 axiom amendment and File_18 §6C.1 phase-anchor amendment recorded as routed cross-file amendments pending pressure test; no pre-existing arithmetic, anchor value, modal state, node-class, sign convention, operator, slash-pair, range, envelope, Mirror protocol, Machine Guard content, claim-status label, theological claim, or dependency boundary altered); v1.17 — File_50a Compact Convention Entry registered as Part D §D.16 after File_50 public-clean control follow-up (June 27 2026; author-directed bounded control-file update; File_50a Naturalized Master Table state, naturalization `P_nat = P − 11`, Base-90 grid `V = round(P_nat / 90) × 90`, Civil clutch `Civil = V + 6`, Diff / three-state gear-selector `D = P_nat − V ∈ {0,+30,−30}`, P2 naturalization-zero, P1 bypass, table-source preservation, scale-reading restraint, and File_50c routing boundary registered; eight File_50a Machine Guards registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.16 — File_44 Compact Convention Entry registered as Part D §D.15 after Files_41–45 public-clean cleanup (June 27 2026; author-directed bounded public-clean cross-file update; File_44 scaling-law protocol split state, P2 split controls, MirrorVolume / Seal700 provenance, Creation-head 10x dependency-controlled triad, Flood Gear `1050` ladder, display-only macro motif, mod-50 gearbox display state, LXX Maximum Year-6 / Adam-state display, `5256 BC` shared date-label, MT / SP / LXX witness-state commentary, and File_45 proof-burden boundary registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.15 — File_51b Convention Entry registered as Part D §D.14 after File_51b finalization and post-final Pressure Test (June 27 2026; author-directed bounded cross-file audit; formal Cross-Polarity Mirror mechanics, Protocol 1 / Protocol 2 target-generation, civil span target discipline, `8280` cross-modal centricity, LXX `5556 BC` actual `+60 Terah` / Year-6 `11111` self-mirror state, `11111 / 11110 / 11100 / 11109` translation-engine states, `11106 BC` singularity, `11109` fractional-boundary route-independence, File_51a §23.7 mod-5 dependency, biological / Levitical calendar-scaling restraint, and File_52a / File_52b inverse-bridge firewall registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.14 — File_51a public-clean handoff and File_55 standard SKL Creation rail-contraction registration update June 27 2026 (author-directed bounded control-file update; File_51a / File_51b scaffold-mechanics versus Mirror-mechanics handoff guard and §§16.7–16.8 rounded Conquest-week block-display convention registered; File_55 June 22 standard SKL Creation `10 × 25920` state, `mod720 → mod480`, selected `mod2160 → mod1440`, `±720 → ±480`, `+30 → +20`, nested Key-of-23 boundary, `430 / 432` dual-precessional state, `3706 / 3226 / 2746 BC` adjacent-state robustness bracket, and added Machine Guards registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.13 — File_52a / File_52b inverse-number architecture and technical supplement registered June 26 2026 (author-directed bounded cross-file update; Final post-final pressure-tested File_52a and File_52b state vocabulary, dependency routing, removed Mirror-row boundary, and twenty-five Machine Guards registered as Part D §D.12; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.12 — File_55 post-final SKL / Berossus amendment and File_34 hybrid Alulim clarification registered June 21 2026 (author-directed bounded control-file update; File_34 hybrid `34566 + 241200 = 275766` origin and File_55 downstream `275766 → 184326 → 1446 BC` routing registered; File_55 ordered `±720 / mod2160` rail, Creation–Flood–terminal sample, Alulim / Adam and Alorus / Adam states, Ubara-Tutu exception, and nine additional Machine Guards added, bringing File_55 to twenty-seven Machine Guards; File_34 glossary and §32U controls expanded; no pre-existing arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.11 — File_55 Convention Entry registered as Part D §D.11 at File_55 finalization after full pressure testing, File_23 comparison, source-working-record comparison, and bounded post-revision cross-file audit (June 20 2026; author-directed bounded control-file update; Moses-node thirds-primer vocabulary, operator formulas, paired cumulative Cainan-displacement controls, SKL / Berossus macro-thirding states, the methodological / null-test boundary, the File_23 `460` / schematic-`480` firewall, and eighteen Machine Guards registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.10 — File_50c Convention Entry registered as Part D §D.10 after File_50c finalization and post-final Pressure Test (June 18 2026; author-directed bounded cross-file update; File_50c Bipartite Grid Landing vocabulary, executable exact-strike inventory, P1 bypass boundary, source-parenthetical admission state, coordinate-family target-identity state, hit-cardinality vocabulary, corrected `72 / 28 / 16` scan state, eight Machine Guards, and nearest-720 half-step tie convention note registered; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered); v1.9 — File_53 Convention Entry registered as Part D §D.9 after File_53 finalization and post-final Pressure Test (June 18 2026; author-directed bounded control-file update; File_53 state vocabulary and eight Machine Guard entries registered; File_53 post-final pressure-test count correction recorded; no arithmetic, anchor value, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary altered; no Style Guide or Restart Capsule update required; Repository_Change_Archive update pending by author direction); v1.8 — File_48 Convention Entry registered as Part D §D.8 at File_48 finalization (June 12 2026; author-directed bounded control-file update; File_48 wobble usage registered as convergence with §31.138, not novelty; companion pointers unchanged this pass); v1.7 — File_42 Convention Entry registered as Part D §D.7 at File_42 finalization, and the §D.6 `Q − 5` reconciliation hold closed as convergence with active routing unchanged (June 12 2026; author-directed bounded control-file update); v1.6 — File_40 Convention Entry registered as Part D §D.6 at File_40 finalization (June 11 2026; author-directed bounded control-file update; companion pointers refreshed: Restart Capsule v11.7; Project_Procedures v3.2); v1.5 — File_39 Convention Entry registered as Part D §D.5 at File_39 finalization (June 11 2026; author-directed bounded control-file update); v1.4 — File_38 Convention Entry registered as Part D §D.4 at File_38 finalization (June 11 2026; author-directed bounded control-file update); v1.3 — Restart Capsule Appendix T Samaria addenda absorbed as §31.125a–§31.125i and §32T items 11–19 with Machine Guard; Restart Capsule Appendix AB residuals absorbed as §32I.1, §32J.1, §32M.1, §32N.3, and §32O.1; anchor-tier convention note registered as Part D §D.3 (June 10 2026); v1.2 — File_47 registration appended as Part D §D.2 (June 10 2026); v1.1 — File_46 registration appended as Part D (June 10 2026); v1.0 created June 10 2026; content migrated verbatim from 490d Repository Style Guide v2.4 (§31.38–§31.167; §32A–§32U; File_36 / File_41 / File_45 glossary additions and Machine Guards); migration is relocation only — no definition, Machine Guard, arithmetic value, anchor, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary was altered
Primary domain: Methodological
Canonical source: Markdown
Revision basis: 490d Repository Style Guide v2.5; Project Procedures v3.3; State_Vocabulary_Register v1.19 as prior current edition; Restart Capsule v11.18; the July 2026 page-publication discussion record and sixteen bounded file amendments; File_56 Final (Creation Decade Protocol; author-invoked Finalization July 2 2026); File_50a_50b_Public_Clean_v1_1_Control_Registered.md; File_50c_Public_Clean_v1_1_Control_Registered.md; File_44 Public Clean v1.0; File_44 Final post-final pressure tested; File_43 Final Harmonized; File_45 Public Clean v1.0 by downstream dependency; File_51b Final post-final pressure tested; File_51a Public Clean v1.1; File_52a Final post-final pressure tested; File_52b Final post-final pressure tested; File_54 Public Clean v1.0; author-directed File_50a compact Register update June 27 2026
Related files: 490d Repository Style Guide v2.5; Project_Procedures v3.3; Restart Capsule v11.17; File_56 Final; File_50a_50b Public Clean v1.1; File_50c Public Clean v1.1; File_50d_50e Public Clean v1.1; File_44 Public Clean v1.0; File_44 Final post-final pressure tested; File_43 Final Harmonized; File_45 Public Clean v1.0; File_51a Public Clean v1.1; File_51b Final post-final pressure tested; File_52a Final post-final pressure tested; File_52b Final post-final pressure tested; File_54 Public Clean v1.0; File_55 Public Clean v1.1; File_34 Final with File_55 hybrid Alulim clarification; Repository_Change_Archive v1.3 (pointer not refreshed in this bounded pass; later Archive updates remain pending by author direction)

# 490d State Vocabulary Register v1.20

## 0. File-function

This Register is the single active home for per-file modal-state vocabulary, per-file convention updates, and per-file Machine Guards that were previously accreted into the 490d Repository Style Guide. The Style Guide v2.5 retains generic, repository-wide rules and the generic glossary entries (former §31.1–§31.37); this Register holds everything file-specific.

Numbering from the Style Guide v2.4 source is preserved exactly so that any existing cross-reference of the form `Style Guide §31.x` or `Style Guide §32x` resolves here without renumbering. A reference to `Style Guide v2.4 §31.139` is now read `State Vocabulary Register §31.139`.

Control status: the entries in Parts A, B, and C are active controls, equal in authority to their former placement in the Style Guide. The Repository_Change_Archive holds historical records only and is non-controlling.

When new files are finalized, their state vocabulary, convention updates, and Machine Guards register here. The Style Guide and Project Procedures receive at most a one-line pointer.

## 0.1 File map

| Part | Function | Source migrated from |
|---|---|---|
| Part A | Per-file modal-state glossary entries §31.38–§31.167 | Style Guide v2.4 §31 |
| Part B | Per-file convention updates and Machine Guards §32A–§32U | Style Guide v2.4 §32A–§32U |
| Part C | File_36, File_41, File_45 glossary additions and Machine Guards | Style Guide v2.4 update-record subsections |
| Part D | Post-migration file registrations | Post-final file-specific entries |

Machine Guard [REGISTER MIGRATION FIDELITY]:
All entries below are verbatim relocations from Style Guide v2.4. Do not treat relocation as revision. If an entry appears to conflict with a repository file, resolve it exactly as it would have been resolved when the entry lived in the Style Guide.

---

# Part A. Per-File Modal-State Glossary (migrated Style Guide §31.38–§31.167)

### 31.38 Three-Gear Protocol state

A File_11 state in which Gear 1 / Japheth, Gear 2 / Shem, and Gear 3 / Ham function as simultaneous Gear settings under Shem Superposition. Gear 1 is `+0`, Gear 2 is `+2`, and Gear 3 is `+4` from Gear 1 / `+2` from Gear 2. These settings are not corrections of one another. The active Gear, figure, operator, and dependency must be stated before applying a Gear shift.

### 31.39 Flood-Gear rail state

The File_11 / File_00 rail field containing `2456 BC`, `2458 BC`, and `2460 BC`. These are distinct Gear rail states and must not be collapsed into one Flood date or one manuscript chronology. In File_02 terminology, `2458 BC` remains the MT Standard-Normal / Gear-2 / Shem-axis Flood state unless a local file explicitly opens another rail.

### 31.40 Filling Protocol state

A File_11 state mapping cumulative Kenan / Cainan coordinates to Creation-week Days 4 / 6 / 7 and to Gear 3 / Gear 2 / Gear 1. Its default scope is the local cumulative Kenan / Cainan field. Do not generalize the Filling Protocol to unrelated Creation-week, Flood, or cumulative fields unless a controlling dependency explicitly broadens the scope.

### 31.41 Source-history biographical shorthand

A legacy or source-history biographical label retained for traceability without becoming an active confirmed chronological state. Example: `Noah 504?` in File_11 is source-history shorthand only. It must not be promoted to a confirmed biographical state unless a controlling dependency defines that state.

### 31.42 Priestly 336-day Ledger state

A foundational Priestly service-cycle architecture generated by `24 × 7 = 168`, `168 + 168 = 336`, and `48 × 7 = 336`. The execution-variant invariant `24 × 14 = 336` may be noted where relevant. This state is not a failed solar year and not a replacement for the 360-day Prophetic Year or the 364-day Enochian Year.

### 31.43 24-course roster source-control state

A source-control state grounded in the 24 priestly courses of 1 Chronicles 24:1–19, with associated weekly service, Sabbath transition, New Testament continuity, Josephus, Mishnah, and Qumran Mishmarot witnesses. It supports the Priestly 336-day Ledger architecture but does not by itself determine every historical execution detail.

### 31.44 Seven-day service-period state

The Priestly service-period state in which a course serves for seven days. The standard source-control anchor is 1 Chronicles 9:25. It supports `24 × 7 = 168` when the 24 courses are treated as a half-year service ledger.

### 31.45 Sabbath transition-boundary state

The weekly transition boundary between outgoing and incoming priestly divisions, source-controlled by 2 Kings 11:5–7 and 2 Chronicles 23:4–8. It supports the Sabbath-to-Sabbath service-cycle architecture without requiring every local file to reconstruct temple-roster administration.

### 31.46 Priestly-Enochian bridge corollary

A corollary state in which four festival or epagomenal weeks bridge the 336-day Priestly Ledger to the 364-day Enochian Year: `4 × 7 = 28`; `336 + 28 = 364`. It does not erase the 336-day baseline and should not be treated as one mandatory historical mechanism unless a local file supplies source-control.

### 31.47 Priestly-Enochian half-week straddle

A File_12 dependency state in which the Priestly weekly vector and the Enochian solar Creation-week vector produce a structural `3.5`-day liturgical split. Under the day-year principle, this may project as `3.5 × 360 = 1260` and compare with `3.5 × 364 = 1274`. Do not generalize this state to every `3.5` value unless the active file opens that operator.

### 31.48 Source-control bibliography vector

A compact source-control list attached to a state or operator. For the Priestly 336-day Ledger, the vector includes 1 Chronicles 24:1–19, 1 Chronicles 9:25, 2 Kings 11:5–7, 2 Chronicles 23:4–8, Luke 1:5, Josephus Antiquities 7.365–366, Mishnah Taanit 4:2, Mishnah Sukkah 5:7–8, Qumran Mishmarot texts such as 4Q320–4Q330, and secondary scholarship such as Talmon, VanderKam, Elior, Ben-Dov, and Ratzon / Johnson. The vector supports source-control; it does not replace local arithmetic or modal-state discipline.

### 31.49 Christological joint-file state

A file-state in which a repository file gathers Christ-centered articulation points from multiple domains rather than proving one isolated chronology. `File_13` is the current example. It touches genealogy, Creation-week, Flood-week, Conquest, Incarnation, Passion, Temple, LXX, and Mirror states. A Christological joint file may index dependency-controlled results from other files, but it must not silently convert them into local proofs.

### 31.50 Second-Adam / Jan-1 covenant numeric-year Mirror

A File_13 local covenant Mirror label tied to the Jan 1, AD 1 naming / circumcision hinge. It may display readable labels such as `5321 BC ↔ AD 5321` and `2801 BC ↔ AD 2801`. This is not ordinary elapsed civil-span arithmetic and not automatically Protocol 1. Executable cross-axis spans still use `BC + AD − 1` unless a local Mirror protocol is explicitly opened.

### 31.51 File_13 Passion-date pair state

A Christological Passion-date pair state. `AD 33n` is the preferred Friday Nisan `14/15` Passion state. `AD 30n` remains a possible Friday Nisan `14/15` Passion state. `Nisan 14/15` remains a Gospel-calendar slash-pair. The non-Passion display members are handled separately under the AD 33n / AD 30n / AD 33t / AD 34 display-state firewall.

### 31.52 File_13 day-year purification display

A birth-of-Christ day-year state in which Dec 25, 1 BC is Day -6, the inclusive eighth-day circumcision reaches Jan 1, AD 1, and the traditional 40th-day purification reaches symbolic AD 33 by 39 full days. AD 34 may be retained only as a completed-period / exclusive-end purification display state. AD 34 is not a Passion-date state.

### 31.53 Matthew exile-bearing Abraham head bracket

A File_13 / File_43 Matthean head-envelope state generated by the overlapped exile-bearing generation. The head bracket is `2876 BC`, with phase-expanded display `2877t/2876n BC → 2870t/2869n BC` where exact Tishri/Nisan phase work is active. It is not Abraham's literal birth and must remain distinct from the ordinary Matthean Abraham anchor `2806 BC`.

### 31.54 Cumulative MT Shem / Noah Mirror state

A File_13 cumulative-MT Mirror coordinate-completion state for `5436 BC ↔ AD 5436`. In this state, `5436 BC` is the cumulative MT birth of Shem / death of Noah field, 600 years before the `4836–4829 BC` Shem-death / Flood / Arphaxad-birth field. The `536 BC/AD` pivot display is a dual-side 4900-year display, not the direct civil midpoint of `5436 BC → AD 5436`.

### 31.55 AD 33n / AD 30n / AD 33t / AD 34 display-state firewall

A master node-class firewall for File_13-related Christological date labels. `AD 33n` is the preferred Friday Nisan `14/15` Passion state. `AD 30n` is a possible Friday Nisan `14/15` Passion state. `AD 33t` is the Tishri-side completion member and must not be treated as a Tishri / Tabernacles crucifixion. AD 34 is a completed-period purification display state or, if separately opened as AD 34/35, an Acts expansion field. Do not collapse these labels into one Passion date and do not treat non-Passion display members as crucifixion dates.

### 31.56 File_13 LXX actual / rounded firewall

A state-control firewall distinguishing File_13's LXX actual Creation endpoint and Year-6 states from File_54's rounded comparison states. In `File_13`, `5494 BC` is the LXX actual Creation endpoint, `5496 BC` is the LXX Year-6 state, and `5526 BC` is the LXX Year-6 +30 Apparent Age state. These do not include `+60 Terah` and must not be replaced by File_54's rounded `5486 BC`, rounded +30 `5516 BC`, or Year-6 +60 `5556 BC` states.

### 31.57 Phase-resolved paired Mirror coordinate-completion

A Mirror coordinate-completion state for paired Tishri/Nisan dates. The controlling display rule is `Yt/(Y−1)n BC → AD (Y−1)t/Y n` when exact `.5` phase-pair work is opened. This is distinct from a single-date numeric-year Mirror node and distinct from ordinary civil cross-axis span-counting.

### 31.57a File_43 Luke / Matthew Genealogical Bridge state

A File_43 state in which Luke and Matthew are harmonized through a locked 70-year carrier while preserving mandatory split states. It does not collapse LukeSpan with LukeIntervals, does not collapse MattSpan_BirthHinge with MattSpan_Carrier, and does not chain the P2 rails-window with the A-space bridge.

### 31.57b LukeSpan / LukeIntervals split state

A File_43 split between `LukeSpan = 5320 = 76 × 70`, the names-span from Adam to Christ as names, and `LukeIntervals = 5250 = 75 × 70`, the interval-count between the 76 names. Use the label required by the local calculation.

### 31.57c MattSpan_BirthHinge / MattSpan_Carrier split state

A File_43 split between `MattSpan_BirthHinge = 2800 = 40 × 70`, the Abraham to `6 BC` bracket, and `MattSpan_Carrier = 2870 = 41 × 70`, the Abraham to AD 65 carrier bracket. Bare `Matthew span` should not be used where the distinction matters.

### 31.57d Genealogical MirrorVolume state

A File_43 span-provenance volume state defined as `8050 = LukeIntervals 5250 + MattSpan_BirthHinge 2800`. It is not formed with MattSpan_Carrier and is not a formal Mirror protocol unless a local section explicitly opens such a protocol.

### 31.57e Seal700 state

A File_43 expansion seal generated by the ordinary Key-of-23 whole-year expansion: `8050 × 25/23 = 8750`, and `8750 − 8050 = 700 = 10 × 70`. It is not an independent genealogy span.

### 31.57f Display-table-only genealogy ledger

A File_43 display state in which the Luke / Matthew 70-year genealogy ledger is a mechanized ledger for visualization and comparison. It must not overwrite LukeSpan, LukeIntervals, MattSpan_BirthHinge, MattSpan_Carrier, MirrorVolume, expanded volume, or Seal700.

### 31.57g Figurative God bookend label state

A File_43 / File_54 display-label state in which `God` refers figuratively to Luke's `Adam, son of God` endpoint and to Christ the Son of God as a theological bookend. It must not be read as divine origination, a literal birth chronology for God, or a Matthean textual datum.

### 31.57h Double-generational knot compositional state

A File_43 compositional state in which an end-cap or middle generation may be overlapped for literary or typological framing. It is not an arithmetic input and must not replace the locked span labels.

### 31.57i Job-only restoration warrant state

A File_43 source-control state narrowing the double-generational-knot warrant to Job as a typological / compositional restoration analogy. Job's restored `140` years may be read typologically as `2 × 70`, with a doubled-life or four-generation restoration horizon, but this does not create a local chronology proof about Job. Unclear secondary warrants should be removed or marked audit-controlled unless locally sourced.

### 31.57j Outer Luke rail / overlapped middle-generation state

A File_43 / File_54 dependency-controlled state in which the outer Luke rail `5356 BC → AD 5355` gives `10710 = 153 × 70`, and adding the overlapped middle Christ-generation gives `10780 = 154 × 70 = 22 × 490`. This explains the `153 + 1` display without altering File_43's local span locks.

### 31.57k Alpha-Omega 22-fold bracket state

A File_43 / File_54 typological and reception-history state in which `22 × 490 = 22 × 7 × 70` frames the start, central Christ-generation, and terminal rail as an Alpha-Omega / beginning-middle-end display. This is not a local arithmetic driver and does not prove direct Lukan, Johannine, or BJ dependence.

### 31.57l Revelation Alpha-Omega reception-history note

A claim-status note for using Revelation 1:8, Revelation 21:6, and Revelation 22:13 as Alpha-Omega / Beginning-End / First-Last framing. It supplies theological / typological / reception-history context, not independent chronological derivation.

### 31.57m BJ / Jubilees 22-fold Creation-genealogy dependency state

A dependency-controlled state in which File_53's BJ / Jubilees 22-fold Creation-genealogy witness is cited through File_54 to support the symbolic fit of a 22-fold bracket. It does not import the full File_53 proof burden into a local file unless explicitly opened.

### 31.57n File_43 P2 File_32 routing state

A File_43 final dependency-routing state in which active P2 rails-window and A-space bridge handling routes through bounded File_32 terminology: `36 BC ↔ 6 BC` belongs to the P2 rails-window, and `6 BC ↔ AD 25` belongs to the P2 signed A-space bridge. Do not revive the removed active File_42 dependency label for File_43.

Machine Guard [FILE_43 SPAN SPLIT]:
Do not use `LukeSpan` where File_43 requires `LukeIntervals`. Do not use `MattSpan_Carrier` where File_43 requires `MattSpan_BirthHinge`. Bare `Matthew span` is insufficient where the distinction matters.

Machine Guard [FILE_43 P2 NON-STRADDLING]:
Do not chain `36 BC ↔ 6 BC ↔ AD 25` as one continuous operator. The `36 BC ↔ 6 BC` relation belongs to the P2 rails-window, and `6 BC ↔ AD 25` belongs to the P2 signed A-space bridge.

Machine Guard [FILE_43 ALPHA-OMEGA NON-DRIVER]:
The Alpha-Omega `22 × 490` note is theological / typological / reception-history framing. It is not a new local arithmetic operator, not a new Mirror protocol, not a substitute for File_43's genealogy locks, and not proof of direct textual dependence unless a later source-critical dependency explicitly opens that claim.

### 31.58 Biological-fractal state

A File_14 state in which biological, gestational, purification, and numerical structures are read as coordinated micro-chronology controls. The principal values are `270`, `275`, `276`, and `280`, but each value retains its own node-class and operator.

### 31.59 Variant-gap gestation state

A File_14 state in which the manuscript variant gap functions as a gestational structure. `275` is the variant gap; `270` and `280` arise by diagonal measurement; `276` arises only when the full Creation-year boundary extension is included.

### 31.60 Sequential variant-gap anatomy

The decomposed File_14 display `60 + 155 + 60 = 275`. It names the sequential anatomy of the variant field and must not be collapsed into the compressed boundary display without restating the active state.

### 31.61 Compressed boundary display

The File_14 display `60 + 215 = 275`. It is a compressed boundary form used in Triple-1656 contexts. It preserves the same `275` gap but does not replace the sequential anatomy `60 + 155 + 60 = 275`.

### 31.62 `276` full-year boundary-extension state

A File_14 boundary-envelope state generated by adding one full Creation year to the `275` variant gap. The controlling Year-6 example is `4176n BC → 3901n BC = 275`; `3901n BC → 3900n BC = 1`; therefore `4176n BC → 3900n BC = 276`. This preserves `275` as the variant gap and permits the exact conversion `276 × 70/69 = 280`.

### 31.63 Triple-1656 Spine

A File_14 structural state in which LXX Maximum, MT Minimum, Flood, and Fall-of-Jerusalem date-states align through repeated `1656` spans. This state must preserve its active endpoint and boundary labels, especially `5555 BC`, `5554 BC`, `3899 BC`, `3898 BC`, `2243 BC`, `2242 BC`, `587 BC`, and `586 BC`.

### 31.64 Typological mirroring

A reciprocal or reflected typological structure that uses mirror-like language without opening formal repository Mirror target-generation. In File_14, the `40 + 40` purification material uses typological mirroring / reciprocal-structure language, not File_00 foundational Mirror protocol.

### 31.65 LXX Maximum node-label state

A File_14 state separating the LXX Maximum Creation labels by node-class: `5561 BC` as Creation Week opening / Year-1 display; `5560 BC` as Year-1-end display; `5556 BC` as Year-6 / Adam-state display; `5555 BC` as Year-7-start boundary; and `5554 BC` as endpoint / Year-7-end display. Do not collapse these labels into one LXX Maximum date.

### 31.66 `587/586 BC` adjacent Fall of Jerusalem date states

A File_14 date-pair state in which `587 BC` is an allowed Fall of Jerusalem date and the local exact Triple-1656 terminus under Year-7-boundary reckoning, while `586 BC` remains the primary repository Fall of Jerusalem / schematic endpoint state. The label `587 BC` must not be treated as merely a non-historical boundary short of Jerusalem's fall.

### 31.67 Precessional-day component-display convention

A File_14 display convention for precessional tables in which an added year value is first stated arithmetically and then expressed in standard 72-year precessional-day units. Examples: `144 = 72 × 2 = 2 precessional days`; `432 = 72 × 6 = 6 precessional days`; `72 = 72 × 1 = 1 precessional day`. This is a display convention, not a Residue Protocol and not ordinary elapsed-day arithmetic.


### 31.68 Authorized generational-unit state

File_15's state in which Abrahamic `100`, Jacobean `70`, and Mosaic `40` function as authorized generational units. These values must not be treated as generic interchangeable component values unless the File_15 state is active.

### 31.69 Call-to-death generational-span register

File_15's authorization rationale in which Abraham `75 → 175 = 100`, Jacob `77 → 147 = 70`, and Moses `80 → 120 = 40` define the three authorized generational units. The exact spans are dependency-controlled arithmetic facts; the unifying rationale is structural inference.

### 31.70 Exact normalized generational triad

The exact File_15 ratio state `100:70:40 = 10:7:4`. This form replaces proof-bearing decimal approximation in generational-unit comparisons.

### 31.71 Abrahamic-base generational fractions

The File_15 fractional display `10/10`, `7/10`, and `4/10 = 2/5`, using the Abrahamic `100` as base. Preserve the exact fractions and do not replace them with approximate decimals unless preserving source-history display.

### 31.72 Generational LCM state

The File_15 convergence state `LCM(40, 70, 100) = 1400`. The value belongs to the authorized generational-unit triad and must not be treated as an ordinary span-family value without naming the active state.

### 31.73 Ordered endpoint-pair display

A bracket display in which the first member of one side pairs with the first member of the other side, and the second member pairs with the second member. File_15's `1406–1399 BC → 6 BC–AD 2` is the controlling example: `1406 BC → 6 BC` and `1399 BC → AD 2` are active. The display does not imply that every internal cross-pair has the same value.

### 31.74 File_15 restored cumulative Cainan dependency state

The File_15 state preserving `14466 BC → 6 BC = 14460` as a same-side arithmetic fact while leaving the restored cumulative Cainan mechanism dependency-controlled. Do not collapse this state with regular restored 2nd Cainan `+130`.

---

### 31.75 Raw data source state

A file-state in which the file supplies base chronological rows, tables, and lookup values for other rule-source files. `File_18` is the current example. A raw data source may preserve dependency-controlled operators without locally re-proving every operator it tabulates.

### 31.76 Minimum baseline table state

A table-state in which displayed rows give the minimum baseline values and variant values are derived only by named operators such as `+60 Terah`, `+215 Sojourn`, `+275`, restored regular 2nd Cainan `+130`, or restored cumulative 2nd Cainan `+460`. Do not treat a derived variant as a correction of the baseline.

### 31.77 Source-table state

A state in which the values in a table are governed by the active source tradition and table method. A source-table state may differ from another table in the same file because the method, witness tradition, or correction rule differs. Do not harmonize source-table values unless the local file explicitly opens that harmonization.

### 31.78 Shared year-label state

A year label occupied by multiple node-classes or traditions. Identity requires state, node-class, tradition, and operator agreement. `1876 BC` is the main repository-wide example, but other labels may function similarly.

### 31.79 Envelope member-label

A displayed member inside a range, week, or envelope. The member-label may be upper, lower, terminal, interior, Tishri, Nisan, or phase-resolved. It must not be treated as a correction of the whole envelope unless the local file states that it replaces the envelope.

### 31.80 Phase-resolved envelope member

An envelope member whose identity depends on an explicit Nisan / Tishri phase-state. Example: File_18 resolves the MT cumulative Peleg member `3495t` as the Tishri terminal member of the parallel Peleg span `3502t–3495t BC`, paired with the Nisan span `3501n–3494n BC`.

### 31.81 Website-to-minimum adjustment state

A source-history conversion state in which older website tables are translated back to repository minimum baselines by subtracting active website variants such as `+215 Sojourn`, `+60 Terah`, or `+275`. Preserve this as a publication-source adjustment, not as a new chronology.

### 31.82 SP Shem Superposition

The Samaritan Pentateuch Shem state in which Shem legitimately occupies both Noah-bound and Arphaxad-bound toggle positions. File_18 expresses this through the `599+ / 600` Noah-year issue and the `2990/2991 BC` / `2390/2391 BC` Shem pair. This state may ripple backward toward Adam but must not propagate forward past the Arphaxad-bound post-Flood row unless a local file explicitly opens that effect.

### 31.83 Abraham / Terah four-position field

The File_18 variant field in which Abraham has two birth states, `1951/2166 BC`, while Terah has four birth/death states because the `+60 Terah` state doubles the `±215 Sojourn` field. The four Terah states are `2021–1816 BC`, `2081–1876 BC`, `2236–2031 BC`, and `2296–2091 BC`. Terah's begetting display is `70/130 state-dependent`. This field remains four-position through 2nd Cainan if restored 2nd Cainan is inserted; the eight-position field begins at Arphaxad / Flood and propagates back to Adam.

### 31.84 2nd Cainan variant-boundary state

The File_18 state in which restored 2nd Cainan is inserted between Arphaxad and Shelah without making 2nd Cainan himself an eight-position row. 2nd Cainan retains four possible dates, like Terah: births `2611/2551 BC` and `2396/2336 BC`; deaths `2151/2091 BC` and `1936/1876 BC`. The eight-position field begins with Arphaxad / Flood and propagates back to Adam only when restored 2nd Cainan is explicitly opened. The resulting generational variant-count ladder is `12–8–4–2–0`: Adam through Arphaxad / Flood have eight possible dates; 2nd Cainan through Terah have four; Abraham through Joseph / Levi have two; Kohath and Amram have zero direct regular pivot dates; Aaron / Moses onward remain fixed / unaffected.

### 31.84a Pentateuchal Funnel / all-even structural harmony state

The File_18 structural state in which the restored-2nd-Cainan variant-count ladder maps onto the four primary Pentateuchal narrative arcs. When the full restored-Cainan regular chronology field is explicitly opened, Creation → Flood corresponds to `12` generations and `8` possible date states; Flood → Abraham corresponds to `8` generations and `4` possible date states; Abraham → entering Egypt corresponds to `4` generations and `2` possible date states; Egypt → Conquest / death of Moses corresponds to `2` floating generations and `0` direct regular pivot dates.

Claim-status note:
The generation counts and variant counts are arithmetic / table-structure facts once the active restored-Cainan state is opened. The mapping to the Pentateuchal narrative arcs is structural inference and theological / providential synchronization. It is not a new operator and must not alter table rows.


### 31.84b Cainan macro-vector B6 state

A File_16 / File_18 dependency state in which the MT Cumulative + 2nd Cainan table-source values supply macro-vector coordinates for the `1876 BC` Nexus. `File_18` supplies Cainan `4858–4851 BC`, Shelah / convergence envelope `4398–4391 BC`, selected `4396 BC`, regular Cainan `2551–2091 BC` and `2336–1876 BC`, and lower-bound Entry `1661 BC`. `File_16` controls the Category B / B6 classification through `2300`, `2520`, `2730`, and `1260 → 1290 → 1260`. This state does not add Category A event-node witnesses and does not replace the regular `+130` / cumulative `+460` Cainan distinction.

### 31.84c Audit-controlled `2307` / solar companion state

A companion audit state associated with the Cainan macro-vector. The same-side checks `4858 − 2551 = 2307` and `4398 − 2091 = 2307` may be retained as exploratory sanctuary / Hanukkah-style material. The solar comparison remains audit-controlled because `4398 − 1661 = 2737`, while `7.5 × 365 = 2737.5` before a phase-resolved calendar dependency is opened. Do not promote either companion into an active operator unless a later sanctuary / Hanukkah / phase-resolved solar file explicitly does so.

### 31.85 File_18 LXX corrected source-table state

The active File_18 LXX source-table state in which Lamech uses begetting age `182` and lifespan `753`, and Nahor uses lifespan `208`. Smith remains an external LXX reconstruction witness; Smith's `777` Lamech lifespan amendment is not adopted as File_18's active LXX table value.

### 31.86 MT cumulative Peleg phase-resolved Mirror application

The File_18 application of File_09 phase-resolved Mirror rules to MT cumulative Peleg. The Nisan span is `3501n–3494n BC`; the parallel Tishri span is `3502t–3495t BC`. The `3495t` member is a Tishri terminal member, not an error in the envelope.

### 31.87 SKL Pillars +30 Apparent Mode target

A dependency-controlled SKL display-state in which Pillars of Time rows become executable only when the active `+30 Apparent Mode` rail is named. Do not treat the Pillars row as ordinary whole-year arithmetic outside its rail state.

### 31.88 Cumulative Flood / SKL Long Day-2 target

A dependency-controlled display-state in which cumulative Flood and SKL Long Day-2 comparisons require the SKL Long target. Do not mix the SKL Short target into this row.

### 31.89 Tammuz → Abraham dependency-controlled span

A File_34-controlled SKL / Berossus span. File_18 may preserve `360 × 360 = 129600` as a table datum, but dependent files must not locally rederive it unless the File_34 operator field is opened.

### 31.90 Total SKL span +720 rail

A display-state in which the total SKL span `453600 = 1260 × 360` checks only when the SKL Short Pre-Creation `+720` rail is active. Do not use this value as a generic SKL total without naming the active rail.

### 31.91 Source-retained design claim

A design or proof statement retained from source because it preserves the author's theological or structural interpretation. Such claims must be classified by claim-status and may function as structural inference, theological note, providential synchronization, or dependency-controlled claim rather than replacing arithmetic facts.

### 31.91 Dependency-controlled SKL textual datum

An SKL / Berossus datum preserved in a local file because the table requires it, while full derivation remains controlled by `File_34` or another SKL dependency. The local file may cite the datum but must not silently fill in missing derivation.

### 31.92 Author-resolved Etana SKL row

The File_18 SKL row where Etana displays `1500` years and `11696–10136 BC`, while ordinary same-side subtraction gives `1560`. File_21 resolves the row by preserving `1500` as the source-retained reign value, preserving `11696–10136 BC` as the visible variant range, and making the proof-bearing harmonic the opening-anchor relation: `11696 − 1406 = 10290 = 210 × 49`. Do not develop the row around `49 × 40`, and treat `116936 BC` as a typo for `11696 BC` if that malformed label appears in legacy material.

### 31.93 Rounded Scaffold non-retrojection state

A state-control rule derived from `File_22` finalization: clean, mod-10, harmonically regular, Mirror, SKL mod-10, 10x-scaled, or apparently rounded values must not be identified with the `File_51a` Rounded Scaffold unless the local section explicitly opens the Rounded Scaffold state.

### 31.94 Patriarchal Quartet scalar state

A File_22 cumulative harmonic state in which Abraham `175`, Isaac `180`, Jacob `147`, and the minor note `130` function as scalar values for the Cumulative MT harmonic argument. Preserve the scalar state without turning it into an ordinary biographical proof unless the local file opens that use.

### 31.95 Cumulative-Regular Decimal Bridge state

A File_22 state connecting actual cumulative chronology and actual regular chronology through the `9890/989` relation. It is a Cumulative MT / Regular MT bridge and not the File_51a Rounded Scaffold unless a local section explicitly opens the rounded counterpart.

### 31.96 `9890/989` Decimal Bridge

The File_22 bridge in which the actual Creation-level macro-gap `9890` corresponds by 10x scale to the actual Abraham-to-Exodus micro-gap `989`. The `9890` value may display as endpoint / lower-bound pairs or as internal Year-6 / Adam-state pairs when the active node-class is stated.

### 31.97 Actual `9890` versus rounded `9900` firewall

The `9890` cumulative-to-regular MT Creation bridge belongs to File_22 actual chronology. The `9900` counterpart belongs to File_51a rounded chronology. These are comparable but non-identical states. Do not correct `9890` to `9900`, and do not read `9900` backward into File_22 unless Rounded Scaffold is explicitly opened.

### 31.98 Cumulative Shem birth-week state

A File_22 state generated by applying the `8575` MT cumulative lifespan total from Adam through Noah to the cumulative Creation-week register. In this state, `5436n BC` is the Nisan head / upper member of the cumulative Shem birth-week, while `5431n BC` is an internal Year-6 coordinate in the same birth-week.

### 31.99 Endpoint / lower-bound versus Year-6 / Adam-state pair

A Creation-week node-class distinction used in File_22. `14004/4114` is the Nisan endpoint / lower-bound form of the `9890 = 23 × 430` bridge; `14006/4116` is the internal Year-6 / Adam-state form. Both may be valid under their active node-class.

### 31.100 Levitical Aaron/Moses `3.5`-phase display

A File_22 phase-sensitive display in which the ordinary Aaron/Moses `3`-year branch and the Tishri/Nisan phase-resolved `3.5`-year branch remain simultaneous. Decimal-looking values such as `1446.5 BC` must be treated as phase-state displays, not ordinary decimal chronology.

### 31.101 Jared / 2nd Cainan `460` ladder

A File_22 Watcher / Cainan state connecting Jared and restored 2nd Cainan through `460`, `2300`, `4600`, and related scalar forms. Preserve whether a row is cumulative, regular, lifespan, birth-transfer, or dependency-controlled.

### 31.102 Birth-transfer composite `6900` state

A File_22 composite harmonic state in which `4600 + 2300 = 6900 = 15 × 460` joins Jared and regular restored 2nd Cainan's birth through birth-transfer structure. It is not a direct same-side span to the death of 2nd Cainan at `2091 BC`.

### 31.103 File_22 Residue Protocol state

The File_22 residue cluster in which values such as `0.575`, `1.75`, `7.5`, `35`, and `42` operate only under Residue Protocol, physical residue, conceptual residual-display, or residual day-year lift. Do not feed these display constants back into ordinary whole-year Key-of-23 date arithmetic.

### 31.104 `12558` harmonic root state

A File_22 harmonic-root cluster involving `12558`, `12740`, `12600`, `13650`, and related expansion displays. Treat it as a File_22 state unless another file explicitly reopens the derivation.

### 31.105 Unified 490 harmonic state

A File_22 state connecting regular and cumulative harmonic displays through `490`, including values such as `2940 = 6 × 490`, `12740 = 26 × 490`, and `10750 = 25 × 430` where the active file opens them.

### 31.106 Decimal Invariance

A File_25 explanatory reference-file state in which selected Deep Time, SKL, cumulative, biblical historical, and restoration-field nodes are displayed in `1/10` or `10x` relation. Decimal Invariance is a structural inference from state-controlled arithmetic displays, not by itself a replacement of any historical anchor, SKL rail, cumulative coordinate, civil cross-axis span, or Rounded Scaffold value.

### 31.107 Decimal-shadow display

A display-state in which a larger coordinate is divided by `10`, or a smaller coordinate is multiplied by `10`, to show visual or structural resonance. Examples from File_25 include `20916 ÷ 10 = 2091.6`, `14466 ÷ 10 = 1446.6`, and `445.6 × 10 = 4456`. Decimal-shadow values do not replace historical anchors unless a local file explicitly opens an exact date-state.

### 31.108 Finite-decimal display state

A local print-state for finite decimal values such as `445.6`, `2091.6`, and `1446.6`. In File_25, `445.6` is executable because `445.6 × 10 = 4456`; the source-retained `445.(6)` form remains source-history residue unless a later file opens a repeating-decimal operator.

### 31.109 Biblical 10x Mirror-scale display

A File_25 display-state in which biblical chronology is read through rounded `10x` Mirror-scale intent. It is not identical to exact civil cross-axis counting and not identical to the SKL / Rounded `BC + AD − 2` display. Some biblical rows may also check under civil background, while others remain display-state only.

### 31.110 Mod-10 display intent

The local File_25 term for rounded `10x` display intention. It may describe biblical 10x Mirror-scale rows without forcing those rows into the SKL / Rounded `BC + AD − 2` operator. Where the SKL / Rounded operator is active, that separate state must be named.

### 31.111 Nehemiah 10x backbone

The Daniel 9 / Artaxerxes wall-decree display in which `445.6 × 10 = 4456` and the Nehemiah decree field is shown as `445 BC → AD 4456 = 4900 = 10 × 490` where the File_25 biblical 10x Mirror-scale display is active. The historical wall-decree anchor remains `445 BC` by dependency.

### 31.112 Restoration-decree weave

A bounded File_25 Daniel 9 restoration-field display in which the `536 BC` return / Daniel revelation node participates inside the Nehemiah `4900` field. File_25 records `4456 BC → 536 BC = 3920 = 8 × 490` and `536 BC → AD 445 = 980 = 2 × 490` as a concise weave, while leaving the full Daniel 9 decree pattern for later files.

### 31.113 SKL Long `24510` terminal state

A Sumerian B / SKL Long terminal relation controlled by File_21 and File_24. File_25 cites `27366 − 2856 = 24510`, with `24510 = 57 × 430 = 19 × 1290` and `24510 = 24500 + 10`. This state supports the Exodus / AD 2856 10x display without opening the Rounded Scaffold.

### 31.114 BC/AD counterpart display-label state

A state in which BC-side and AD-side labels share the same numeral but remain axis-specific counterpart display labels rather than identical nodes. File_25 examples include `4456 BC` / `AD 4456`, `2856 BC` / `AD 2856`, and `2736 BC` / `AD 2736`.

### 31.115 Source-title / local axiom language

A claim-status control for titles or source thesis labels such as File_25's `Law`. The wording may be retained as source-title or local axiom language without promoting the claim to a repository-wide foundational axiom or making audit-controlled rows proof-bearing.

### 31.116 Decimal Invariance structural inference

The claim-status of the repeated Decimal Invariance pattern in File_25. Checked arithmetic rows remain arithmetic facts; the statement that those rows instantiate Decimal Invariance is structural inference; larger theological meaning is providential synchronization where opened.

### 31.117 Providential synchronization for 10x Mirror-scale display

The theological interpretation that state-controlled `10x` Mirror-scale alignments may participate in a larger providential mathematical grammar. This claim-status does not replace arithmetic verification, historical anchor control, or dependency boundaries.


### 31.118 Samaria Node schematic envelope state

A File_00 schematic envelope state for the collapse of the Northern Kingdom of Israel. The actual Fall of Samaria remains late `722 BC ±1`, after a siege beginning about `725 BC`. The schematic envelope is `726–721 BC`. The `726 BC` member may represent the idealized start of the three-year siege, the idealized fall of Samaria, or the conceptual collapse-point of the Northern Kingdom. The `721 BC` member is the terminal / latest-fall label of the same node. Do not force the actual fall date to equal either endpoint.

### 31.119 Actual Fall of Samaria state

The historical state in which Samaria falls in late `722 BC ±1`, after a siege beginning about `725 BC`. This state remains distinct from the schematic Samaria Node and does not carry the File_00 structural arithmetic by itself.

### 31.120 Israel literal-reign schematic ledger

A File_00 schematic ledger in which the stated reigns of Israel's kings are counted at the year level without harmonizing co-regencies or overlaps. The ledger is conceptually back-projected onto the `967/966 BC` Temple node and lands in the `726/725 BC` Samaria onset field. It does not correct Jeroboam's actual accession, the actual beginning of the Northern Kingdom, or the actual late-`722 BC` fall of Samaria.

### 31.121 Samaria / Jerusalem twin-city judgment vector state

A File_00 / File_16 dependency state in which the `1876 BC` Nexus generates two sister-city judgment horizons: `1876 BC → 726 BC = 1150` for Samaria and `1876 BC → 586 BC = 1290` for Jerusalem. The interval between the schematic Samaria onset and Jerusalem is `726 BC → 586 BC = 140 = 2 × 70`.

### 31.122 Samaria Node / Immanuel 720 overlay state

A File_00 overlay state using File_19 and File_13 dependencies. The governing chain is `1446 BC → 726 BC = 720`, `726 BC → 6 BC = 720`, and `721 BC → 1 BC = 720`. The same `720` corresponds to `360 + 360` and to the Dial of Ahaz precessional state `10 × 72 = 720`. It does not change the File_13 dual Christ-anchor controls.

### 31.123 Samaria Node three-tradition Creation overlay state

A File_00 comparative overlay in which the LXX, SP, and MT Creation rails align with the Samaria / Temple field: LXX `5556 BC → 726 BC = 4830`; `4830 × 70/69 = 4900`; SP `4416 BC → 966 BC = 3450`; MT `4176 BC → 726 BC = 3450`; `3450 × 70/69 = 3500`. The `240`-year SP / MT Creation differential is reproduced as `966 BC → 726 BC = 240`.

### 31.124 Abrahamic 240-cycle overlay state

A File_00 Samaria Node overlay using Abraham's birth at `2166 BC`, the Temple node at `966 BC`, and the Samaria onset label `726 BC`: `2166 BC → 966 BC = 1200 = 5 × 240`; `966 BC → 726 BC = 240`; `2166 BC → 726 BC = 1440 = 6 × 240 = 4 × 360`.

### 31.125 Samaria Node sub-year marker exploratory note

A non-load-bearing File_00 exploratory note in which short Northern reigns may mark schematic years inside the Israel literal-reign schematic ledger. The Zimri / Omri field is displayed as `22 + 2 + 24 + 2 = 50`, placing `966 − 50 = 916 BC`; `1406 − 916 = 490`. The Zechariah / Shallum / Menahem field is displayed as `916 − 150 = 766 BC`; `2166 − 766 = 1400`; `2066 − 766 = 1300`. This note is exploratory and does not correct actual regnal chronology.

### 31.125a Divided Kingdom actual / schematic route-triad state

A File_00 Samaria Node state naming the active route before applying Kings schematics: actual division `930 BC`; Southern Kingdom / Kings of Judah schematic route `980 BC`; Northern Kingdom / Kings of Israel schematic route `966 BC`. Supporting displays are `980 − 930 = 50` and `966 − 930 = 36`. Name the active route-state; do not blend routes.

### 31.125b United / Divided Kingdom `360/365` overlay state

A File_00 schematic Kingdom sweep through Saul, Temple, Samaria, and Jerusalem: `1086 BC → 966 BC = 120 = 3 × 40`; `1446 BC → 1086 BC → 726 BC = 360 + 360`; `1086 BC → 721 BC = 365`; `1086 BC → 586 BC = 500`. Schematic overlay; not harmonized regnal chronology.

### 31.125c Samaria / Jerusalem Pillar metric state

A Pillar-facing precessional corroboration of the sister-city field: `726 BC → AD 29515 = 30240 = 420 × 72` and `586 BC → AD 29515 = 30100 = 420 × 71⅔`, using civil cross-axis span `BC + AD − 1`. It corroborates but does not replace the File_16-dependent twin-city judgment vector.

### 31.125d Abrahamic-to-exile trunk pattern state

A File_00 trunk display showing `726 BC` as intrinsic to the Abrahamic-to-exile trunk independent of the Pillar metric: trunk nodes `2166 / 1446 / 726 / 586 / 446 BC`; branch nodes `1086 / 1016 / 966 BC`.

### 31.125e Samaria Node `276 → 280` flattening state

The state explaining how the actual siege / fall field flattens into the schematic `726–721 BC` node: `722/721 BC → 446/445 BC = 276`; `276 × 70/69 = 280`; `726/725 BC → 446/445 BC = 280`. Related File_13 context preserves `275 + 1 = 276` and `276 × 70/69 = 280` as full-year boundary-extension arithmetic.

### 31.125f Exile-return Mirror disclosure state

The AD-side Mirror disclosure of the Babylon / return and Samaria counterpart field: `606/605 BC → 536 BC = 70/69 → 70`; `536 BC → AD 725 = 1260`; `539 BC → AD 722 = 1260`; `536 BC → AD 445 = 980 = 2 × 490`, using civil cross-axis span `BC + AD − 1`.

### 31.125g Elijah Northern Kingdom ministry overlay state

A bounded Elijah state inside the Northern Kingdom schematic route: source-control `875–848 BC`; actual-history mod-5 `876–846 BC`; stylized Northern route `906–876 BC`; optional Ahab field `905/904 BC`. Supporting displays are `2166 BC → 906 BC = 1260`, `2166 BC → 876 BC = 1290`, `1086 BC → 906 BC → 726 BC = 180 + 180`, and `1087/1086 BC → 905/904 BC → 723/722 BC = 182 + 182 = 364`.

### 31.125h Elijah Northern-route rationale state

The rationale state binding the Elijah overlay to the Northern Kingdom / Kings of Israel route `966 BC` rather than the Southern `980 BC` `+50` route, because Elijah is a Northern Kingdom prophet, especially in relation to Ahab and Jezebel.

### 31.125i Samaria temporal-conversion convergence state

The state coordinating the forward `360/364/365` displays with the backward `276 → 280` expansion around Samaria: `1086 BC → 446 BC = 640 = 40 × 4 × 4`; `1086 BC → 722 BC = 364`; `722 BC → 446 BC = 276`; `1086 BC → 726 BC = 360`; `726 BC → 446 BC = 280`; `364 + 276 = 360 + 280 = 640`.

### 31.126 Prime Quadruple scaffold state

A File_32 state in which a Prime Quadruple is `{Q,Q+2,Q+6,Q+8}` and all four values are prime. `Q` is the prime start value. This state must not be treated as a date until the local prime-to-polarity operator is applied.

### 31.127 Prime start value state

The OEIS A007530 start value `Q` of a Prime Quadruple. It is a prime-sequence value, not a civil date and not a rails-space coordinate by itself.

### 31.128 Positive-polarity date-output / rails-space `B⁺/M⁺` state

The ordinary File_32 positive-polarity prime-date output. `B⁺ = Q − 5`; `M⁺ = Q + 25`; `M⁺ − B⁺ = 30`. Where the Prime–Civil Interface is opened, these are primary positive-polarity date coordinates. They are not Coordinate Mirror / Protocol 1 and they are not generated by the negative-polarity operator.

### 31.129 `M` rail / apparent rail `+30` state

The File_32 same-side apparent rail formerly compressed in older source tables as `Mirror (+30)`. It means `M⁺ = B⁺ + 30 = Q + 25`. It is not the same as Coordinate Mirror / Protocol 1, raw prime-space cross-polarity, civil cross-axis span-counting, Protocol 2, SKL rounded mod-10 Mirror display, or inverse-number reversal.

### 31.130 Raw prime-space cross-polarity state

A signed prime-lattice operation in which the quad reverses across polarity: `{Q,Q+2,Q+6,Q+8} ↔ {−(Q+8),−(Q+6),−(Q+2),−Q}`. Raw cross-polarity is a lattice operation and not a civil date by itself. It must not be collapsed with positive `B⁺/M⁺`, negative-polarity / signed A-space date output, Coordinate Mirror, or an invented `+60` rail.

### 31.131 Negative-polarity / signed A-space date-output state

The corrected File_32 / File_33 / File_35 state in which negative polarity may generate primary date coordinates when signed A-space rendering is explicitly opened. The ordinary operator is `N⁻₁ = Q − 16` and `N⁻₂ = Q + 14`, with P1 handled as an exception where needed. For P2, this gives the negative-polarity / signed A-space bridge `6 BC / AD 25`; for P23, it gives AD `31705 / 31735`; for P34, it gives AD `81025 / 81055`.

### 31.132 Coordinate Mirror / Protocol 1 secondary-agreement state

A civil counterpart display such as `n BC → AD (n − 1)` or `AD n → (n + 1) BC`. In the corrected prime-polarity hierarchy, Coordinate Mirror is normally secondary agreement after a positive or negative polarity date coordinate already exists. It is not raw prime-space cross-polarity and does not replace polarity generation unless a local file explicitly gives Mirror proof burden.

### 31.133 P2 positive / negative polarity baseline state

The corrected P2 baseline keeps two primary polarity outputs distinct:

```markdown
P2 positive rail-window: 6 BC / 36 BC
P2 negative-polarity / signed A-space bridge: 6 BC / AD 25
```

The P2 rails-window and the P2 negative-polarity / signed A-space bridge are related by the prime-grid grammar, but they are not one continuous arithmetic operator.

### 31.134 Modulo 210 corridor state

A prime-space corridor state for ordinary Prime Quadruple centers. After the P1 exception, ordinary quad centers fall in `15`, `105`, or `195 mod 210`. Cross-polarity sends `15 ↔ 195` and fixes `105`.

### 31.135 No-`+60` ordinary prime-rail guard

A File_32 Machine Guard. The ordinary positive prime rail is only `B⁺/M⁺`, separated by `30`. A `+60` value may appear only under a separately named operator, dependency-controlled later state, or macro-Mirror state. Do not add a default `+60` rail to repair P31 or other bisection language.

### 31.136 P23 trunk / polarity-square state

The File_32 / File_33 P23 trunk state in which P23 is the central Prime Harmonic anchor. Keep P23 start `31721`, positive-polarity dates `31716 BC / 31746 BC`, negative-polarity dates AD `31705 / 31735`, secondary Coordinate Mirror displays, and raw prime-space cross-polarity distinct. The P23 polarity-square lock preserves `31740 = 23 × 23 × 60`.

### 31.137 P23 `k+8` end-value state

The File_37-dependent state in which the terminal member of P23 is used: `31729 = 31721 + 8`. This state is distinct from the P23 start value `31721` and is required for File_37 sums such as `31729 + 157271 = 189000` and `31729 + 295871 = 327600`.

### 31.138 Formation Wobble statistical state

The File_32 pairwise `k`-difference enrichment scan. The recomputed Direction Summary value for P23 is `+50.669% ≈ +51%` over n = `10` to `30`, with peak `+153.333%` at n = `10`. Treat the Wobble as a freshly recomputed statistical result under the reconstructed File_32 metric, not as a formal probability model for providential design by itself.

### 31.139 File_33 Ordinal Lock / Ordinal Authentication state

A File_33 state in which a prime-grid target is tested by both Value Lock and Ordinal Lock. The Value Lock uses the Prime Quadruple value or rail-derived anchor. The Ordinal Lock uses the ordinal `n` of the Prime Quadruple through patterns such as `n² × Unit` or `n × Constant`. This state is final in File_33, but it remains non-retrojected into File_32 as File_32's ordinary local base operator.

File_33 also preserves the Ordinal Squared pattern as a strong observed tendency, not as a declared universal axiom beyond the tested range.

### 31.140 P23-P46-P69-P92 dependency-controlled state

A File_37-controlled prime-grid extension involving the P23-P46-P69-P92 tetrad, P23 `k+8`, `189000`, P23-P46 forward cross-rail span `125580`, P69 Shem-birth / lifespan encoding, P92 `1380` capstone, and `327600`. File_32 preserves this as dependency-controlled and does not locally rederive the full tetrad. Removed loop language, the retired prime-gap derivation, and retired global variance claims are not active.

### 31.140A Key-of-23 Foundational Prime Set

The File_37 label for `{5, 7, 11, 13, 17, 19, 23}` when the set is used as P1 `5` plus the Exceptional Sextuplet. The set becomes mod-30 complete when the Key of 23 expands `23 → 25`, yielding `5 + 25 = 30`, `7 + 23 = 30`, `11 + 19 = 30`, and `13 + 17 = 30`. Do not use the narrower six-member label for the seven-member File_37 set; reserve Exceptional Sextuplet for `{7, 11, 13, 17, 19, 23}`.

### 31.140B Retired File_37 prime-gap boundary

A File_37 final-control boundary. File_37 does not derive global `±2` from a prime-internal gap model, and it does not use a retired prime-width foundation as executable proof. Actual `+50` states remain valid only in their own chronology systems where locally opened, especially SKL / Berossus and Kings of Judah.

### 31.140C P92 tetrad capstone state

The File_37 P92 state in which P92 is the `4 × 23` member of the tetrad and functions as the `1380` capstone: `420876 − 5496 = 415380 = 301 × 1380`, `420876 − 4116 = 416760 = 302 × 1380`, and `420876 − 2736 = 418140 = 303 × 1380`.

### 31.140D Prime-grid qualitative probability diagnostic

A claim-status restraint for prime-grid convergence language. Convergence may be described as structurally significant or providential synchronization, but it is not formal statistical proof unless a formal probability model is supplied.

### 31.141 P26 adjacent-prime symbolic / precise validation state

The File_32 / File_33 P26 state in which `51341 − 43781 = 7560 = 6 × 1260 = 21 × 360`. In the Beast / Curse display, symbolic AD `666` remains a symbolic target while the precise civil `52000 = 130 × 400` landing is AD `665`. This is the controlling example for distinguishing a symbolic AD target from a precise civil AD target.

### 31.142 P26–P50 precessional extension state

An appendix-only File_32 extension state. It may use precessional constants such as `72` and `71⅔`, and later File_33 ordinal architecture may clarify rows, but the extension does not retroactively change the P1–P25 main-body Prime Harmonic proof burden.

### 31.143 Coordinate / span distinction

A table-control rule: a coordinate such as `67206 BC` is not the same thing as a span such as `67200 = 336 × 200`. Do not write coordinate values as if they were equal to span products.

### 31.144 Arithmetic correction by later-file confirmation

A bounded correction state in which a source arithmetic value is corrected only after the local operator and a later dependency confirm the correction. Example: File_32 corrects P24 active `M` rail from source-history `34876 BC` to `34866 BC` under `34836 + 30 = 34866`, with later File_33 confirmation.

### 31.145 Prime-grid providential synchronization

A claim-status label for repeated checked prime-grid alignments whose meaning exceeds strictly demonstrable arithmetic or source-critical intent. It may be used alongside arithmetic fact or structural inference, but it does not replace either.

### 31.146 Prime-grid reception-history note

A non-proof-bearing historical-context label for external or reception-history associations attached to prime-grid targets. Such notes require source-control before they become proof-bearing historical claims.

### 31.147 Source-retained theorem-status label

A claim-status control for inherited labels such as `VERIFIED`, `VALIDATED`, `[ESTABLISHED]`, `[LOCKED]`, `theorem`, `proof`, or `prediction`. Preserve the source label where needed, but do not promote it to current proof status unless the local arithmetic, statistical, and dependency controls support that specific claim.

### 31.148 Rounded / mod-10 display-state firewall

A firewall distinguishing ordinary civil `BC + AD − 1`, File_32 Coordinate Mirror, File_25 biblical 10x display, File_21 / File_23 SKL rounded mod-10 display, and Rounded Scaffold Protocol 1. Use `BC + AD − 2` only where the local rounded / mod-10 display state is explicitly opened.

### 31.149 P2 rails-window Coordinate Mirror state

The Coordinate Mirror of the P2 rails-window: P2 `B = 6`, `M = 36`; coordinate counterparts are `6 BC → AD 5` and `36 BC → AD 35`. This state is distinct from the P2 A-space bridge `6 BC → AD 25`.

### 31.150 SKL Enmenduranan / Enoch-figure `46000` state

A File_32 / File_24 dependency state in which the SKL Enmenduranan / Enoch-figure pair `45996/45966 BC` lands on the Coordinate Mirror of the P2 `36/6 BC` rail-window: `45996 BC → AD 5 = 46000` and `45966 BC → AD 35 = 46000`. It is not a direct P46 civil-span claim.

### 31.151 Full P31 bisection audit state

The File_32 audit state preserving the source statement `72000 + 72000 = 144000` without promoting it under ordinary rails. The checked ordinary-rail facts are `P31 B − P4 M = 72216 − 216 = 72000` and `P45 M − P4 B = 144186 − 186 = 144000`. The full bisection requires a separately named second-side operator.


### 31.152 File_34 post-final citation-control state

A routing state used when dependent files cite `File_34 Final; post-final pressure tested` for full SKL / Berossus derivation. It replaces older File_34 source-section residue with the current File_34 map while preserving legacy labels as source history where needed.

### 31.153 Berossus computational-anchor state

A File_34 state in which `486 BC` functions as the Berossus computational / figurative anchor for backward projection and Pillar arithmetic. It remains distinct from `536 BC` as the biblical historical restoration anchor and from `539t/538n BC` as the fall-of-Babylon / Persian-transition field.

### 31.154 Fall-of-Babylon / restoration / Berossus-anchor three-state field

A File_34 state-control field separating `539t/538n BC`, `536 BC`, and `486 BC`. The `539t/538n BC` field belongs to the fall of Babylon and Persian transition; `536 BC` belongs to the biblical restoration / return field; `486 BC` belongs to the Berossus computational-anchor state. Do not collapse the source shorthand `539/536 BC` into a single node-class.

### 31.155 Precessional Envelope state

The File_34 SKL / Berossus unification state using `72`, `6480`, `12960`, `13680`, `25920`, and `720`. The Ideal pathway uses the upper-anchor `12960` gap. The Metonic pathway uses the lower-anchor `13680 = 12960 + 720` gap. These pathways remain simultaneous and non-collapsed.

### 31.156 Cosmic Pixel state

The File_34 scale-state in which `720 = 2 × 360` expresses the scaled Shem anomaly. It is related to the `+2 local rail`, but it is not interchangeable with the local two-year rail unless the active file explicitly shifts from local date scale to SKL / Berossus scale-state.

### 31.157 SKL base / rail firewall

A File_34 firewall distinguishing SKL physical base anchors from rail anchors. The base pair is `2906 BC` / `2856 BC`. The `+30 Apparent Mode` rail pair is `2936 BC` / `2886 BC`. The `+2 local rail` produces paired displays such as `2938/2936 BC`, `2908/2906 BC`, `2888/2886 BC`, and `2858/2856 BC`. Do not treat these as one range or as corrections of one another.

### 31.158 SKL-to-Berossus `2370` corridor state

A File_34 Appendix B.5 corridor in which SKL rail-pair anchors are projected forward in same-side BC arithmetic by `2370` years: `2938/2936 BC − 2370 = 568/566 BC`; `2908/2906 BC − 2370 = 538/536 BC`; `2888/2886 BC − 2370 = 518/516 BC`; `2858/2856 BC − 2370 = 488/486 BC`. These are paired slash-displays. Pair each left member with the corresponding left member and each right member with the corresponding right member.

### 31.159 `30 + 20 + 30` restoration corridor

A File_34 Appendix B.5 corridor in which `566 BC → 536 BC → 516 BC → 486 BC` forms `30 + 20 + 30`. The `536/516 BC` pair is the central `20`-year hub and is not a failed `30`-year span. This mirrors the SKL-side corridor `2936 BC → 2906 BC → 2886 BC → 2856 BC`.

### 31.160 Paired slash-display arithmetic

A state-safe arithmetic rule for slash-pairs that function as paired endpoints. Paired slash-displays must be checked componentwise. Do not treat them as ranges, averages, or free endpoint pools unless a local file explicitly opens such an operator.

### 31.161 Nisan / Tishri phase-chain arithmetic

A calendar-state rule for File_34 Appendix B and related phase chains. Nisan remains the unsuffixed default where the file does not open Tishri. A Tishri straddle may be present, but it remains a phase-state and must not silently replace the Nisan-default calculation.

### 31.162 Appendix B phase-chain dependency state

A dependency-control state for compressed File_34 Appendix B material, including Pillar spans, `720 + 180`, `272160`, Alulim pixel checks, and Nisan / Tishri slash-pairs. Dependent files should route this machinery to File_34 unless they explicitly reopen the active operators.

### 31.163 Pillar / AD target state

A File_34 civil cross-axis state for Pillar targets such as `AD 29395`, `AD 29465`, and `AD 29515`. Use `BC + AD − 1` when computing BC-to-AD spans. Do not silently convert Pillar spans into Coordinate Mirror, Prime Mirror, inverse-number, or rounded / mod-10 display states.

### 31.164 Sanctuary Fractal / Key-of-23 expansion state

A File_34 state in which `27600 = 12 × 2300` and `27600 × 25/23 = 30000` are read inside the Sanctuary Fractal and Pillar framework. The arithmetic fact is separate from any structural inference or providential synchronization claim.

### 31.165 Authorial forward-time plus display

A source-display convention in which `+` language may describe movement forward through time even though same-side BC arithmetic is performed by subtraction. Preserve source display language but verify executable spans under the correct BC operator.

### 31.166 Expansion difference / non-Residue state

A File_34 distinction for differences such as `2400` that arise from Key-of-23 expansion or structural span comparison. Do not classify such differences as formal Residue Protocol unless the local file explicitly opens Residue Protocol state.

### 31.167 State-conditioned SKL `+1260` display

A File_34 state for `SKL anchor + 1260` displays such as `2886/2856 BC → 4146/4116 BC`. The active anchor pair must be named before the display is used as proof-bearing arithmetic.

### 31.167a Hybrid Berossus / SKL Alulim Creation-overlay state

A File_34 comparative state in which the listed SKL eight-kings antediluvian reign total `241200` is overlaid on the Berossus Flood anchor `34566 BC`, yielding `275766 BC`. The coordinate is hybrid and does not replace the Berossus ten-kings span `432000`, Berossus Alorus / Adam `466566 BC`, native SKL Creation coordinates, or the Berossus Flood anchor. The source-retained `+720` Alulim pixel check is a separate relation.

### 31.167b File_55 downstream thirds-marker clarification state

A dependency state in which File_55 applies its Moses-node primer to the File_34-controlled hybrid coordinate: `275766 → 184326 → 1446 BC`, followed by `184326 − 2886 = 181440 = 7 × 25920`. File_34 controls the origin and node-class of `275766 BC`; File_55 controls the thirding operator and marker interpretation. `184326 BC` is not promoted to a File_34 primary anchor.


---

# Part B. Per-File Convention Updates and Machine Guards (migrated Style Guide §32A–§32U)

## 32A. File_00-Derived Convention Update

The following repository-wide conventions are adopted from the finalized `File_00` revision.

1. `n` and `t` are calendar-state display labels: `n = Nisan-state label`; `t = Tishri-state label`.
2. A suffixed date must preserve its suffix unless a controlled revision explicitly converts it into an exact phase-resolved computational state.
3. Compressed slash-pairs should be expanded into full display form where AI parsing risk is present. Example: `13997/6 BC` should display as `13997/13996 BC` unless source-history requires the shorthand.
4. Slash-pairs paired with AD Mirror targets require node-class tagging before arithmetic. Do not treat the pair as an executable civil-span endpoint unless the file states that function.
5. Civil cross-axis spans still use `BC + AD − 1`; no alternate missing-year operator is introduced for slash-pairs, Mirror display states, or A-space display labels.
6. The Key-of-23 Residue Protocol is separate from ordinary whole-year conversion by `25/23`, `70/69`, and `300/299`.
7. Physical residue uses `N / 40 days` only when the Residue Protocol is active.
8. Conceptual residual-display is display logic, not physical drift.
9. Residual day-year lift may explain displays such as `6.9 days → 7.0 days` and `172.5 physical residue-days → 175 residual-display days`, but those display constants must not be fed back into civil-date arithmetic.
10. The `294/293` ratio is an Enochian side ratio, not a Key-of-23 operator.

## 32B. File_02-Derived Flood / SKL Convention Update

The following conventions are adopted from the pressure-tested `File_02` revision and its post-pressure SKL textual-datum, Enochian 1260 / 1274, and File_34 citation-control patches.

1. Use `MT Minimum-Regular` for the forward-most regular MT comparison state.
2. Use `MT Standard-Normal` for the normative operating MT state under the full 430 years in Egypt and without `+60 Terah`.
3. Do not use bare `MT Regular` when the minimum and standard-normal states could be confused.
4. Treat `4836–4829 BC` and `5296–5289 BC` as cumulative Flood Week envelopes.
5. Treat `AD 65–72` as the judgment-week envelope when paired with the base rounded cumulative Flood Week.
6. AD 70 may be identified as the internal event-target of the judgment-week envelope, but it should not replace the envelope endpoints.
7. Use `Sumerian A / SKL Short` for the 17980-year state anchored to `2906 BC`, beginning at `20886 BC`.
8. Use `Sumerian B / SKL Long` for the 24510-year state anchored to `2856 BC`, beginning at `27366 BC`.
9. Label SKL rows by rail: `Physical`, `+30 Apparent Mode`, `+2 local rail`, or `+30 +2`.
10. Treat `+720 scale-state` as a declared SKL/Berossus cosmic-scale state, not as an automatic replacement for the local `+2` rail.
11. Change SKL table headers from `Textual total` to `Whole-year total` when the appended `3 months and 3.5 days` datum is preserved but not locally computed.
12. Preserve the appended SKL textual datum `3 months and 3.5 days` for both SKL Short and SKL Long unless a source-specific file proves a narrower state.
13. Keep whole-year arithmetic executable locally: `20886 − 2906 = 17980` and `27366 − 2856 = 24510`.
14. Keep the appended month/day function dependency-controlled by `File_34` unless the file explicitly opens that operator.
15. When using the Sumerian-Christological bridge, distinguish the Pre-Creation rail from the Alulim / Adam rail.
16. The `1260 × 360` bridge may be stated as `453606 − 6 = 453600 = 1260 × 360` when the Pre-Creation +720 rail is active.
17. The Solar / Enochian split may be stated as `262806 − 6 = 262800 = 365 × 720` and `262086 − 6 = 262080 = 364 × 720` when Sumerian A / SKL Short / SKL List 1 is active.
18. The Enochian 1260 / 1274 Pre-Creation outer-bound pair may be stated as `453606 − 6 = 453600 = 1260 × 360` and `458646 − 6 = 458640 = 1274 × 360 = 1260 × 364` when the Sumerian A / SKL Short Pre-Creation backward `+720` rail and the Sumerian B / SKL Long Pre-Creation forward `−720` rail are active.
19. Cite or dependency-tag `File_34` for SKL/Berossus derivation, the `3 months and 3.5 days` function, and the `+2` / `+720` toggle mechanics.
20. Cite or dependency-tag File_50c where the Bipartite `±720` dual-bracket or Bipartite Grid Landing operator is doing mechanical work.

Machine Guard [FILE_02 FLOOD / SKL UPDATE]:
Do not collapse the `File_02` Flood/SKL terminology into older generic labels. Preserve `MT Minimum-Regular`, `MT Standard-Normal`, cumulative Flood Week, judgment-week envelope, Sumerian A / SKL Short, Sumerian B / SKL Long, `+30 Apparent Mode`, `+2 local rail`, and `+720 scale-state` whenever those distinctions are active.

---

## 32C. File Map and Rounded Scaffold Convention Update

The following conventions are adopted after the `File_03` Joseph revision and the authorial clarification on Rounded Scaffold handling.

1. Add a File map near the top of a repository file when it improves AI retrieval.
2. Keep the File map terse, functional, and state-aware.
3. Do not let a File map become a decorative table of contents or a prose summary.
4. Before `File_51a`, references to Rounded Scaffold dates are clarity-only forward references unless the local file explicitly opens the Rounded Scaffold as an active methodological state.
5. A Rounded Scaffold value must not overwrite literal MT, SP, LXX, BJ, SOR, SKL, Berossus, standard, actual, or file-local chronological states.

Machine Guard [FILE MAP RESTRAINT]:
A File map exists for AI retrieval and structural navigation. It must remain terse, functional, and state-aware. Do not let it become a prose-heavy table of contents or duplicate the heading hierarchy.

Machine Guard [ROUNDED SCAFFOLD FORWARD REFERENCE]:
Before `File_51a` or another rounded-focused file, use Rounded Scaffold dates only to prevent state confusion. Do not treat them as literal chronology or as replacements for the active tradition.

---
## 32D. Machine-Ingestion Convention Update

The following conventions are adopted after the machine-ingestion / state-safe retrieval refinement.

1. Canonical Markdown remains the source layer.
2. RAG, Knowledge Graph, JSON, JSONL, embedding metadata, and prompt/response training pairs are derived extraction layers.
3. Do not reshape repository files into synthetic Q/A format merely to prepare for future fine-tuning.
4. Standardized metadata should remain compact: required fields stay fixed; optional fields appear only where useful.
5. File maps should support retrieval and state recognition, not prose summary.
6. Machine Guards should remain sparse. Add them only where a future AI system or editor could plausibly collapse a critical distinction.
7. Dense sections should identify active state, operator, node-class, tradition, claim-status, or dependency where needed for state-safe retrieval.
8. A later extraction process may convert headers, File maps, state registers, Machine Guards, claim-status notes, dependency notes, and arithmetic tables into structured AI records.
9. Derived AI records must preserve arithmetic, anchors, modal states, node-classes, sign conventions, operators, Mirror protocols, inverse-number status, claim-status labels, and dependency boundaries.
10. Already-final files do not require reopening merely because this convention was added. They may receive a future micro-patch only if their header, File map, or state-safe retrieval controls are clearly deficient.

Machine Guard [STATE-SAFE RETRIEVAL]:
The purpose of AI-forward formatting is not merely to increase retrieval probability. It is to prevent retrieved material from being applied under the wrong state, operator, tradition, node-class, claim-status, or dependency boundary. Retrieval convenience must not override source precision.

---


## 32E. File_11 Three-Gear Cross-File Convention Update

The following conventions are adopted after the File_11 Second Pressure Test and cross-file update package.

1. `Three-Gear Protocol state` names the simultaneous Gear 1 / Japheth, Gear 2 / Shem, and Gear 3 / Ham settings under Shem Superposition.
2. `Flood-Gear rail state` names the distinct rail labels `2456 BC`, `2458 BC`, and `2460 BC`. These labels are not interchangeable with one another and are not generic Flood dates.
3. Source-history shorthand such as `Noah 504?` must remain source-history unless a controlling dependency defines it as a confirmed biographical state.
4. `Filling Protocol state` is restricted to the local cumulative Kenan / Cainan field unless a controlling dependency broadens its scope.
5. File_11 Residue Protocol material must preserve the distinction between ordinary Key-of-23 whole-year conversion, physical residue, conceptual residual-display, and residual day-year lift.

Machine Guard [THREE-GEAR STATE SAFETY]:
Gear 1 / Japheth, Gear 2 / Shem, and Gear 3 / Ham are simultaneous Gear states under Shem Superposition. Do not collapse them into one Flood date, one Creation anchor, or one manuscript tradition. State the active Gear, figure, operator, and dependency before applying a Gear shift.

Machine Guard [NOAH 504 SOURCE-HISTORY]:
`Noah 504?` and similar source-history shorthand must not be promoted to an active confirmed biographical state unless a controlling dependency defines the state. In File_11, the active control is Gear 3 / Ham / `+4` rail-state.

Machine Guard [FILLING PROTOCOL SCOPE]:
The Filling Protocol maps Gear states to Days 4 / 6 / 7 only within the local cumulative Kenan / Cainan field unless a controlling dependency explicitly broadens the mapping. Do not use the Filling Protocol as a general Creation-week or Flood operator.

---

## 32F. File_12 Priestly 336-Day Ledger Cross-File Convention Update

The following conventions are adopted after the pressure-tested `File_12` Priestly 336-day Ledger and source-control update package.

1. `Priestly 336-day Ledger state` names the foundational Priestly service-cycle baseline generated by `24 × 7 = 168`, `168 + 168 = 336`, and `48 × 7 = 336`.
2. The two-week execution variant `24 × 14 = 336` preserves the same conceptual total and may be cited as a corollary / execution-variant invariant.
3. The festival-week bridge `4 × 7 = 28`; `336 + 28 = 364` is a Priestly-Enochian bridge corollary, not a replacement for the 336-day baseline.
4. The Priestly, Prophetic, and Enochian registers must remain distinct: `336`, `360`, and `364` are parallel calendar states unless a local file states an explicit conversion or bridge operator.
5. The Priestly-Enochian half-week straddle may project as `3.5 × 360 = 1260` and compare with `3.5 × 364 = 1274`, but this remains a File_12 dependency unless the active file opens it.
6. Source-control vectors may compactly list the ancient and modern sources that support a state. They do not turn every source-control witness into a local arithmetic operator.

Machine Guard [PRIESTLY 336-DAY LEDGER STATE]:
The 336-day Priestly Ledger is foundational architecture, not a failed solar year. Do not demote it to peripheral context, and do not collapse it into the 360-day Prophetic Year, the 364-day Enochian Year, or the Solar Year state.

Machine Guard [PRIESTLY / ENOCHIAN BRIDGE COROLLARY]:
`336 + 28 = 364` is a bridge corollary. It may connect the Priestly ledger to the Enochian calendar, but it does not erase the Priestly baseline and does not require one exact historical festival-week mechanism unless a local file supplies source-control.

Machine Guard [PRIESTLY SOURCE-CONTROL SCOPE]:
The 24-course roster, seven-day service period, Sabbath transition boundary, NT continuity, Josephus witness, rabbinic Mishmarot witness, and Qumran Mishmarot witness support the Priestly architecture. Granular festival-week execution and Qumran-specific phase details remain source-control bounded unless locally made proof-bearing.

---

## 32G. File_13 Christological Matrix Cross-File Convention Update

The following conventions are adopted after the Final and pressure-tested `File_13` Christological Matrix source.

1. `Christological joint-file state` names File_13's cross-domain function: genealogy, Creation-week, Flood-week, Conquest, Incarnation, Passion, Temple, LXX, and Mirror states touch in one Christological articulation file.
2. `Second-Adam / Jan-1 covenant numeric-year Mirror` names the local covenant Mirror label tied to Christ's naming and circumcision on Jan 1, AD 1. It preserves `5321 BC ↔ AD 5321` and `2801 BC ↔ AD 2801` as readable covenant labels while keeping executable spans under the civil operator `BC + AD − 1` unless a named Mirror protocol is opened.
3. `File_13 Passion-date pair state` names the state in which `AD 33n` is the preferred Friday Nisan `14/15` Passion state while `AD 30n` remains a possible Friday Nisan `14/15` Passion state.
4. `File_13 day-year purification display` distinguishes AD 33 inclusive purification / Passion-center display from AD 34 completed-period purification display. AD 34 must not be used as a Passion-date replacement.
5. `Matthew exile-bearing Abraham head bracket` names the Matthean `2876 BC` head-envelope generated by the overlapped exile-bearing generation. It remains distinct from the ordinary `2806 BC` Abraham anchor and must not be treated as Abraham's literal birth.
6. `Cumulative MT Shem / Noah Mirror state` names the `5436 BC ↔ AD 5436` coordinate-completion state. The `536 BC/AD` pivot display is a dual-side 4900-year display, not ordinary civil midpoint arithmetic.
7. `AD 33n / AD 30n / AD 33t / AD 34 display-state firewall` may be used as a compact master node-class label where File_13 controls are relevant to later typological or Christological comparisons.
8. `Dual Christ anchor` preserves `6 BC` and `1n BC → AD 1n` as coordinated states rather than corrections of one another.
9. `LXX actual Creation endpoint state` preserves File_13's `5494 BC`, `5496 BC`, and `5526 BC` actual LXX states separately from File_54 rounded states.
10. Phase-resolved paired Mirror coordinate-completion must preserve File_09's suffix-inversion rule where exact Tishri/Nisan paired Mirror work is opened.

Machine Guard [FILE_13 CHRISTOLOGICAL STATE SAFETY]:
File_13 supplies Christological state vocabulary for later files. Later files may cite these state labels for dependency control, but they must not re-prove File_13's Christological Matrix locally unless the local file explicitly opens that argument.

Machine Guard [FILE_13 PASSION-DATE / NON-PASSION-DISPLAY SAFETY]:
`AD 33n`, `AD 30n`, `AD 33t`, and AD 34 are separate File_13 states. `AD 33n` is the preferred Friday Nisan `14/15` Passion state. `AD 30n` is a possible Friday Nisan `14/15` Passion state. Do not collapse `AD 30n` into `AD 33n`. Do not use `AD 33t` as a Tishri / Tabernacles crucifixion or second crucifixion date. Do not convert AD 34 purification-display language into a Passion date.

Machine Guard [FILE_13 LXX ACTUAL / ROUNDED FIREWALL]:
`5494 BC`, `5496 BC`, and `5526 BC` are File_13 LXX actual states. They must not be replaced by File_54 rounded LXX states unless the Rounded Scaffold or Year-6 +60 comparison state is explicitly active.

## 32H. File_14 Biological Fractals Cross-File Convention Update

The following conventions are adopted after the Final and post-final pressure-tested `File_14` Biological and Numerical Fractals source.

1. `Biological-fractal state` names File_14's micro-chronology control layer for gestation, purification, variant-gap, and numerical-fractal structures.
2. `Variant-gap gestation state` preserves `275` as the variant gap while permitting diagonal `270` and `280` readings and the full-year `276` boundary-extension state.
3. `Sequential variant-gap anatomy` names `60 + 155 + 60 = 275`; `compressed boundary display` names `60 + 215 = 275`. Both are valid display states and neither corrects the other.
4. `276 full-year boundary-extension state` is generated by including the full Creation year: `4176n BC → 3901n BC = 275`; `3901n BC → 3900n BC = 1`; `4176n BC → 3900n BC = 276`; `276 × 70/69 = 280`.
5. `Triple-1656 Spine` preserves repeated `1656` spans across LXX Maximum, MT Minimum, Flood, and Fall-of-Jerusalem date-states. It must not erase the ordinary `586 BC` repository schematic state.
6. `587/586 BC adjacent Fall of Jerusalem date states` preserves `587 BC` as an allowed Fall of Jerusalem date and local exact Triple-1656 terminus, while `586 BC` remains the primary repository Fall of Jerusalem / schematic endpoint state.
7. `Typological mirroring` distinguishes File_14's `40 + 40` reciprocal purification language from formal repository Mirror target-generation.
8. `LXX Maximum node-label state` separates `5561 BC`, `5560 BC`, `5556 BC`, `5555 BC`, and `5554 BC` by node-class.
9. Later files may cite File_14 for gestation, purification, biological-fractal, variant-gap, Triple-1656, and Sarah-Eve typology controls where opened. Do not use File_14 to replace File_13's Christological Matrix or File_16's `1876 BC` Nexus controls.

Machine Guard [FILE_14 BIOLOGICAL-FRACTAL VALUES]:
`270`, `275`, `276`, and `280` are related but distinct File_14 values. `270` is viability / `9 × 30`; `280` is full-term / `7 × 40`; `275` is the variant gap / average; `276` is the full-year boundary-extension state. Do not use one value to correct another without naming the active state.

Machine Guard [FILE_14 276 BOUNDARY-EXTENSION]:
`276` is not a replacement for the `275` variant gap. It is generated by including one full Creation year in the Year-6 boundary envelope, as in `4176n BC → 3900n BC = 276`. The conversion `276 × 70/69 = 280` is exact under the stated operator.

Machine Guard [FILE_14 587 / 586 FALL-OF-JERUSALEM STATES]:
`587 BC` is an allowed Fall of Jerusalem date and the local exact Triple-1656 terminus. `586 BC` remains the primary repository Fall of Jerusalem / schematic endpoint state. Do not word `587 BC` as merely a boundary short of Jerusalem's fall, and do not use `586 BC` to erase the exact Triple-1656 `587 BC` state.

Machine Guard [FILE_14 TYPOLOGICAL MIRRORING]:
File_14's `40 + 40` purification material uses typological mirroring / reciprocal-structure language. It does not open File_00 foundational Mirror protocol, Protocol 1, Protocol 2, inverse-number logic, or cross-axis target generation.

---

## 32I. File_15 Generational Axiom Cross-File Convention Update

The following conventions are adopted after the Final and post-final pressure-tested `File_15` Generational Axiom source.

1. `Authorized generational-unit state` names the Abrahamic `100`, Jacobean `70`, and Mosaic `40` units when File_15's generational axiom is active.
2. `Call-to-death generational-span register` names the authorization logic: Abraham `75 → 175 = 100`, Jacob `77 → 147 = 70`, and Moses `80 → 120 = 40`.
3. `Exact normalized generational triad` names `100:70:40 = 10:7:4`.
4. `Abrahamic-base generational fractions` names `10/10`, `7/10`, and `4/10 = 2/5`.
5. `Generational LCM state` names `LCM(40, 70, 100) = 1400` as the synchronization point of the three authorized units.
6. `Ordered endpoint-pair display` names displays such as `1406–1399 BC → 6 BC–AD 2`, where `1406 BC → 6 BC` and `1399 BC → AD 2` are the active pairs.
7. `File_15 restored cumulative Cainan dependency state` preserves `14466 BC → 6 BC = 14460` as checked same-side arithmetic while leaving the Cainan mechanism dependency-controlled.

Machine Guard [FILE_15 GENERATIONAL-UNIT STATE]:
The Abrahamic `100`, Jacobean `70`, and Mosaic `40` are authorized generational units only when the File_15 state is active. They may interact with other component families, but they must not be used to correct or overwrite those families.

Machine Guard [FILE_15 EXACT-RATIO DISPLAY]:
Use exact ratio displays for the generational triad: `100:70:40 = 10:7:4`, with Abrahamic-base fractions `10/10`, `7/10`, and `4/10 = 2/5`. Decimal approximation may be retained only as source-history display.

Machine Guard [FILE_15 ORDERED ENDPOINT-PAIR DISPLAY]:
The File_15 bracket `1406–1399 BC → 6 BC–AD 2` is an ordered endpoint-pair display. Use same-side BC subtraction for `1406 BC → 6 BC`. Use civil cross-axis span `BC + AD − 1` for `1399 BC → AD 2`. Do not infer that every internal cross-pair equals `1400` unless a local file explicitly opens that stronger claim.

---
### 32I.1 Capsule absorption (June 10 2026)

| File_15 state | Register handling |
|---|---|
| Cumulative MT 14000 state | `14006 BC → 6 BC = 14000` belongs to the cumulative MT File_15 Christological lock. |

(Absorbed from Restart Capsule v11.3 Appendix AB.1; wording carried verbatim.)

## 32J. File_18 Chronological Data Tables Cross-File Convention Update

The following conventions are adopted after the Final and post-final pressure-tested `File_18` Chronological Data Tables source. `File_18` is a raw data source / number-source file. It supplies table rows and dependency-controlled lookup values; it does not locally re-prove every operator it tabulates.

1. `Raw data source state` names `File_18`'s function as the repository number-source paired with rule-source files.
2. `Minimum baseline table state` names File_18's baseline method: variants derive from the baseline by stated operators.
3. `Source-table state` prevents LXX regular, LXX cumulative, SP cumulative, and SKL rows from being harmonized merely because they share a tradition label.
4. `Shared year-label state` protects labels such as `1876 BC` from node-class collapse.
5. `SP Shem Superposition` preserves the `599+ / 600` Noah-year ambiguity and Shem's `2990/2991 BC` pair.
6. `Abraham / Terah four-position field` preserves Abraham `1951/2166 BC` and Terah `2021/2081/2236/2296 BC` birth states.
7. `2nd Cainan variant-boundary state` preserves the rule that 2nd Cainan itself has four possible dates, while Arphaxad / Flood through Adam become eight-position only if restored 2nd Cainan is explicitly opened.
7A. `Pentateuchal Funnel / all-even structural harmony state` preserves the structural importance of the `12–8–4–2–0` ladder as a mapping of variant mechanics onto Creation → Flood, Flood → Abraham, Abraham → entering Egypt, and Egypt → Conquest / death of Moses.
8. `File_18 LXX corrected source-table state` preserves Lamech `182 / 753` and Nahor `208` in the active File_18 LXX tables.
9. `MT cumulative Peleg phase-resolved Mirror application` preserves `3495t` as a Tishri terminal member inside the Peleg envelope.
10. `SKL display-state rail dependency` protects Pillars, cumulative Flood / SKL Day-2, Tammuz → Abraham, and total SKL span rows from rail collapse.
11. `Author-resolved Etana SKL row` preserves the Etana row without silently correcting either the source-retained reign value or the visible date range. Its proof-bearing harmonic is `11696 − 1406 = 10290 = 210 × 49`; `49 × 40` is not the controlling relation.
12. `Website-to-minimum adjustment state` names legacy 490d.com table-setting conversions back to File_18 minimum baseline rows. Treat this as source-history conversion control, not as a new chronology or independent operator.
13. `Source-retained design claim` names inherited design language preserved with claim-status control. It may be retained where it explains the file's theological interpretation, but it must not replace arithmetic, source-state, or dependency proof.
14. `Providential multi-tradition synchronization` names a cross-tradition claim-status category in which MT, SP, LXX, BJ / Jubilees, SKL, Berossus, or related traditions participate in a larger providential mathematical grammar without proving direct scribal intent.
15. `Dependency-controlled SKL textual datum` names SKL / Berossus row material retained because `File_34` supplies, or should supply, the full derivation.
16. `Hezekiah prophetic complex state` names the applied File_19 system in which the `701 BC` / `686 BC` Hezekiah field, the Dial of Ahaz, the `185000`-year count-to-chronology state, the `720` precessional structure, the `430` / `490` / `215` interlocks, and the Christological targets function together. Treat it as an applied prophetic-demonstration state, not as a replacement for Exodus, Conquest, Kings, Christological, or Mirror control files.
17. `Dial of Ahaz precessional state` names the File_19 state in which `10` degrees / steps correspond to `720` years by `10 × 72 = 720` while preserving the alternate precessional-day display `71⅔ = 215/3` where the active file opens it. Do not use the alternate display to correct the standard `72`-year precessional day.
18. `AD 29515 actual Pillar state` names the active civil deep-time Pillar node for the File_19 Hezekiah complex. The File_19 arithmetic is `AD 184315 − AD 29515 = 154800 = 430 × 360`. Legacy BC-side mirror coordinate conventions are not active unless a controlling Mirror file explicitly opens them.
19. `Bidirectional prophetic span / sundial mirror` names localized chronological symmetries that mimic the backward / forward physics of the Dial of Ahaz. This is not Protocol 1 Mirror target generation, not Protocol 2 phase Mirror, not civil cross-axis span-counting, not 10x Mirror scaling, and not inverse-number reversal unless a local file explicitly opens one of those operators.
20. `Immanuel 65/49 crossing state` names the File_17-hosted monarchy-to-return / Isaiah midpoint field transferred out of File_19. Its compact display is `930 BC → 735/734 BC → 539 BC`, with `195 + 1 + 195 = 391 = 17 × 23`; it also preserves the `65` and `49` crossing family where the active dependency opens it.
21. `Temple 430.5 month-state bridge` names the File_19 / File_17 monarchy-ledger relation `36 + 394.5 = 430.5`, together with the close month-aware schematic bridge from `1016 BC` second month toward Tammuz/Av `586 BC`. It is a month-state bridge and must not be treated as an ordinary exact integer `430` unless the local section states that approximation.
22. `Literal post-exilic 430 coreference state` names the File_19 foundation-to-foundation row `967 BC` second month → `537 BC` second month = `430`. It supplements the ordinary display `966 BC → 536 BC = 430` and does not replace the schematic `1016 BC → 586 BC` Kings route.
23. `49/50 Kings dual-ledger awareness state` names the relation in which `1016 BC → 967 BC = 49` while the ordinary `1016 BC → 966 BC = 50` display remains the Kings dual-ledger `+50` offset. The two displays preserve adjacent-state awareness; neither corrects the other.

Machine Guard [FILE_19 HEZEKIAH COMPLEX]:
When File_19 language is cited outside File_19, name the active state: Hezekiah prophetic complex state, Dial of Ahaz precessional state, AD 29515 actual Pillar state, Bidirectional Prophetic Span / Sundial Mirror, Immanuel 65/49 crossing state, Temple 430.5 month-state bridge, literal post-exilic 430 coreference state, or 49/50 Kings dual-ledger awareness state. Do not collapse those states into one generic Mirror, Kings, Temple, or Christological operator.

Machine Guard [FILE_19 PILLAR STATE]:
`AD 29515` is the actual civil Pillar node for the File_19 Hezekiah complex. Do not restore a BC-side Pillar coordinate into File_19 or treat a legacy BC-side convention as ordinary elapsed civil-span arithmetic unless a controlling Mirror file explicitly opens that state.

Machine Guard [FILE_19 TEMPLE 430 COREFERENCE]:
`967 BC` second month → `537 BC` second month = `430` is a literal post-exilic foundation-to-foundation coreference. It supplements, but does not replace, `966 BC → 536 BC = 430` or the schematic `1016 BC → 586 BC` route. Preserve the adjacent `967/966 BC` and `537/536 BC` displays as state-pairs.

Machine Guard [FILE_18 DATA TABLE SOURCE]:
`File_18` is a raw chronological data source. Later files may use its rows as lookup data, but must not force the table file to carry full local proof of every operator. When a File_18 row depends on another file, retain the dependency label.

Machine Guard [FILE_18 BASELINE / VARIANT FIELD]:
File_18 tables present minimum baseline dates unless a local table states another active state. Derived dates require named operators: `+60 Terah`, `+215 Sojourn`, `+275`, regular restored Cainan `+130`, cumulative restored Cainan `+460`, or an explicitly opened SKL rail.

Machine Guard [CAINAN MACRO-VECTOR B6]:
The Cainan macro-vector is a Category B / B6 dependency state controlled by `File_16` and sourced from `File_18` table values. Do not add new Category A event-node witnesses, do not change the `7 + 6 = 13` core inventory, and do not use the macro-vector to overwrite regular `+130` or cumulative `+460` Cainan handling.

Machine Guard [CAINAN 2307 / SOLAR AUDIT]:
The `2307` inclusive companion and solar `7.5 × 365` comparison remain audit-controlled unless a later controlling file opens the inclusive sanctuary / Hanukkah or phase-resolved solar operator explicitly.

Machine Guard [FILE_18 SHEM STATES]:
Ordinary regular-localized Shem `±2`, cumulative-propagated Shem `±2`, and SP Shem Superposition are distinct states. SP Shem Superposition uses the Noah-year `599+ / 600` ambiguity and may ripple backward toward Adam, but it must not propagate forward past the Arphaxad-bound row unless locally opened.

Machine Guard [FILE_18 TERAH FOUR-POSITION FIELD]:
Abraham has two birth states, `1951/2166 BC`. Terah has four birth/death states: `2021–1816 BC`, `2081–1876 BC`, `2236–2031 BC`, and `2296–2091 BC`. The `70/130` begetting-age display is state-dependent. Do not reduce Terah to Abraham's two-date field.

Machine Guard [FILE_18 2ND CAINAN VARIANT BOUNDARY]:
When restored 2nd Cainan is inserted, do not assign eight possible dates to 2nd Cainan itself. 2nd Cainan remains four-position: births `2611/2551 BC` and `2396/2336 BC`; deaths `2151/2091 BC` and `1936/1876 BC`. The eight-position field begins at Arphaxad / Flood and propagates back to Adam only when restored 2nd Cainan is explicitly opened. Use the `12–8–4–2–0` ladder when the full generation-count state is active.

Machine Guard [FILE_18 PENTATEUCHAL FUNNEL]:
When the Pentateuchal Funnel state is cited, state both layers: the generation blocks are `12–8–4–2`, and the corresponding variant-count fields are `8–4–2–0`. This is a structural harmony generated by File_18's variant-boundary mechanics, not merely a boundary label and not a new date operator. Preserve the four narrative arcs: Creation → Flood, Flood → Abraham, Abraham → entering Egypt, and Egypt → Conquest / death of Moses.

Machine Guard [FILE_18 LXX CORRECTED SOURCE-TABLE]:
The active File_18 LXX tables use Lamech `182 / 753` and Nahor `208`. Smith remains an external LXX reconstruction witness, but Smith's `777` Lamech lifespan amendment is not the active File_18 table value.

Machine Guard [FILE_18 PELEG MIRROR APPLICATION]:
The MT cumulative Peleg row preserves the Nisan span `3501n–3494n BC` and the parallel Tishri span `3502t–3495t BC`. The `3495t` value is a phase-resolved terminal member and checks inside the `4900 = 70 × 70 = 100` Jubilees Mirror application. It is not an upper-member correction.

Machine Guard [FILE_18 SKL DISPLAY-STATE RAILS]:
SKL Pillars, cumulative Flood / SKL Day-2, Tammuz → Abraham, and total SKL span rows are active-rail or dependency-controlled displays. Name the active rail before using them as executable arithmetic outside `File_18`.

Machine Guard [FILE_18 / FILE_21 ETANA SKL ROW]:
The Etana row displays source-retained `1500` years and `11696–10136 BC`; same-side subtraction of the visible range gives `1560`. File_21 resolves the proof-bearing use through the opening-anchor relation `11696 − 1406 = 10290 = 210 × 49`. Do not silently change the terminal date or reign value, and do not develop the row around unsupported `49 × 40` language.

---
### 32J.1 1876 Cainan macro-vector capsule absorption (June 10 2026)

| State | Register handling |
|---|---|
| Cainan / Famine envelope-to-envelope projection | `4398–4391 BC → 1878–1871 BC` by `2520`, with selected `4396 BC → 1876 BC`. The envelopes remain distinct. |
| Regular-Cainan `460` symmetry | `2551 − 2091 = 460`; `2336 − 1876 = 460`. This is same-side lifespan symmetry, not a replacement for regular `+130` or cumulative `+460`. |
| Triple-target Cainan matrix | `2300` targets `2091 BC`; `2520` targets `1876 BC`; `2730` targets `1661 BC`. Each target keeps its node-class. |
| Daniel 12 sandwich | `4396 BC → 3136 BC → 1846 BC → 586 BC` by `1260 → 1290 → 1260`. `1846 BC` remains Affliction / Bondage origin; `1876 BC` remains Entry / Call / Super-Anchor where that state is active. |

Machine Guard [CAINAN MACRO-VECTOR VOCABULARY CONTROL]:
These rows are vocabulary control. `File_16` controls Nexus proof and Category B classification. `File_18` controls table-source values. Do not convert this register entry into a new operator register or a new event-node inventory.

(Absorbed from Restart Capsule v11.3 Appendix AB.2; row wording carried verbatim; the Machine Guard carries the former [APPENDIX AB CAINAN MACRO-VECTOR] content.)

## 32M. File_20 Book of Jubilees Fourth-Witness Cross-File Convention Update

This bounded convention update incorporates the Final, post-final pressure-tested `File_20` Book of Jubilees fourth-witness controls into repository-wide state vocabulary.

`File_20` treats BJ / Jubilees as a non-canonical, derivative, comparative witness. BJ / Jubilees may function as fourth witness, holographic lens, typological key, shared-tradition evidence, or providential synchronization, but it must not be treated as canonical Scripture or as an independent replacement for MT, SP, or LXX.

### 32M.1 File_20 state labels

| Label | Meaning | Handling |
|---|---|---|
| BJ / Jubilees fourth-witness state | Comparative witness beside MT, SP, and LXX | Non-canonical and derivative; useful for state-controlled comparison. |
| BJ / Jubilees non-canonical derivative witness state | BJ works from MT / SP / LXX materials rather than replacing them | Preserve witness value while avoiding canonical-status confusion. |
| SP-to-BJ exclusive offset state | SP Creation `4200 BC` to BJ Creation `3856 BC` gives `344` | Same-side BC subtraction; repository exclusive mathematical baseline. |
| SP inclusive-theological offset state | SP Creation `4199 BC` to BJ Creation `3856 BC` gives `343 = 7 × 49` | Inclusive / theological state; favored when the BJ seven-Jubilee scheme is active. |
| SP Creation dual-anchor state | `4200 BC`; `4199 BC` | Exclusive and inclusive SP Creation anchors; not corrections of one another. |
| BJ Noah 600th-year tolerance label | `3149 BC ± 1` | Tolerance label for terminus reckoning of Noah's 600th year; distinct from Ideal `3150 BC` and Actual `3148 BC`. |
| BJ Abraham call-to-Conquest 500-year display state | `1906 BC → 1406 BC = 500`; decomposed as `1906 BC → 1806 BC = 100` and `1806 BC → 1406 BC = 400` | Same-side BC display in the BJ / Jubilees side-road register. |
| BJ / Jubilees 364-day source-control state | Jubilees 6:30; 6:32; 6:38; immediate context 6:23–38 | Source-control basis for File_20 calendar claims; do not rest the claim on “scholarly consensus” language. |
| File_20 Cross-Mirror civil-span control | `5555 BC + 7000 = AD 1446`; §12.8.3 uses civil-span arithmetic where cross-axis rows are active | Treat as civil-span / Cross-Mirror display material. Use `BC + AD − 1` for BC/AD checks. |
| Former File_20 §14 deletion state | Key-of-23 SP realignment deleted from the primary argument | If revived, classify as appendix-only or dependency-controlled unless a stronger local operator is supplied. |

### 32M.2 Machine Guards

Machine Guard [FILE_20 BJ FOURTH-WITNESS CONTROL]:
BJ / Jubilees is a non-canonical derivative comparative witness in `File_20`. It may corroborate, illuminate, or synchronize with MT, SP, and LXX, but it does not replace the canonical textual traditions or become an independent primary chronology.

Machine Guard [FILE_20 BJ OFFSET STATES]:
Preserve both offset states. `4200 BC − 3856 BC = 344` is the exclusive mathematical baseline. `4199 BC − 3856 BC = 343 = 7 × 49` is the inclusive-theological BJ / Jubilees offset. Do not collapse either state into the other.

Machine Guard [FILE_20 NOAH TOLERANCE LABEL]:
`3149 BC ± 1` is a tolerance label for the terminus of Noah's 600th year. It must not replace the Ideal Noah label `3150 BC` or the Actual Noah label `3148 BC`, and the convenience titles formerly attached to those tracks are not source controls.

Machine Guard [FILE_20 BJ 500-YEAR DISPLAY]:
The BJ / Jubilees `500` display runs from Abraham's call at `1906 BC` to the primary Conquest at `1406 BC`. It decomposes as `100 + 400` through Abraham's death at `1806 BC`. Do not treat the `500` as an undefined branch span or as a correction to the MT highway state.

Machine Guard [FILE_20 JUBILEES 6 SOURCE CONTROL]:
When `File_20` invokes the BJ / Jubilees `364`-day calendar, the controlling source cluster is Jubilees 6:30, 6:32, and 6:38, with Jubilees 6:23–38 as immediate context and Jubilees 6:37 as adjacent warning context. Do not use “scholarly consensus” as the proof-bearing source-control label for this claim.

Machine Guard [FILE_20 CROSS-MIRROR CIVIL-SPAN CONTROL]:
`File_20` §12.8.3 Cross-Mirror rows use civil-span mathematics where BC/AD coordinates are active. The forward calculation `5555 BC + 7000 = AD 1446` is exact under `AD = span − BC + 1`; cross-axis checks use `BC + AD − 1`. Do not treat this as inverse-number logic or a new Mirror protocol unless a later file explicitly opens that protocol.

Machine Guard [FILE_20 FORMER §14 DELETION]:
The former File_20 Key-of-23 SP realignment section is deleted from the primary argument. Future use of that material requires appendix-only, exploratory, or dependency-controlled classification unless a stronger local operator is supplied.

---


### 32M.1 Capsule absorption (June 10 2026)

| State | Register handling |
|---|---|
| BJ Flood cluster | `2549 BC` is Flood entry; `2548 BC` belongs to the post-Flood / ark-exit / mountains-appear field where locally opened. Do not collapse the cluster into one generic Flood date. |

(Absorbed from Restart Capsule v11.3 Appendix AB.3; wording carried verbatim.)

## 32N. File_21 SKL Unified Protocol Cross-File Convention Update

This bounded convention update incorporates the Final, post-final pressure-tested `File_21` SKL Unified Protocol controls into repository-wide state vocabulary.

`File_21` is an extension-module application file. It applies the SKL `360:1` scalar and four-state algorithm while preserving dependency boundaries for full SKL / Berossus derivation (`File_34`) and File_50-series ±720 / bipartite mechanics.

### 32N.1 File_21 state labels

| File_21 state | Style Guide handling |
|---|---|
| SKL Unified Protocol state | Use for `360:1`, SKL Short / Long, four-state date generation, deep-time matrix, and projective SKL outputs. |
| Four-state SKL date-generation matrix | Preserve `Date = B + (Switch₁ × S) + (Switch₂ × V) ± 30`, with `S = 6480`, `V = 720`, and `±30` applied last. |
| SKL `7200` maximum state | `S + V = 6480 + 720 = 7200`; do not collapse with `+2 local rail` or `+720 scale-state`. |
| Multi-unit `6480` state | State whether `6480` is years, days, a precessional quarter, textual-shift value, SKL rail, or residual-display value. |
| Matryoshka scale-state | Require explicit `×360` or `×360²` operator. Treat arithmetic products as scale-state facts and design language as structural inference / providential synchronization. |
| SKL rounded mod-10 Mirror display-state | Use `BC + AD − 2` only where `File_21` explicitly opens SKL Mirror display. Ordinary civil spans remain `BC + AD − 1`. |
| File_21 Residue Protocol `6480.25` state | Preserve exact `11270 × 0.575 = 6480.25`; rounded `6480` is textual-shift / Residue Protocol display only. |
| Projective Boundary / Temple-Time Pillar coordinate state | Jachin `AD 29515`, Boaz `AD 29465`, and shared inside-skin coordinate `AD 29395` are Projective Boundary / Temple-Time coordinates, not ordinary historical anchors. Source-context link: https://www.1260d.com/temple-time.pdf |
| Jachin / Jochin spelling control | `Jachin` is live repository spelling; `Jochin` is inherited SKL chart / earlier File_21 source-history spelling only. |
| File_21 Etana local-harmonic state | Preserve `1500` as source-retained reign value and `11696–10136 BC` as variant-awareness display while treating the proof-bearing harmonic as `11696 − 1406 = 10290 = 210 × 49`. |
| File_21 claim-status source-label guard | Preserve source labels such as `[ESTABLISHED]` as source-history / source-status labels; classify each local claim by arithmetic fact, structural inference, theological note, providential synchronization, Projective Boundary, dependency-controlled, exploratory, appendix-only, or audit note where needed. |
| `434886 BC` dependency-controlled state | Treat `434886 BC` as a Danielic / adjusted Pre-Creation anchor by dependency unless a later file opens the full provenance. |

### 32N.2 Machine Guards

Machine Guard [FILE_21 SKL UNIFIED PROTOCOL]:
`File_21` controls the SKL Unified Protocol as an application layer: `360:1` scalar, four-state algorithm, SKL Short / Long state handling, Projective Boundary outputs, Matryoshka scale-state, and related File_21-specific display states. It does not replace `File_34` for full SKL / Berossus derivation or the File_50-series for ±720 / bipartite mechanics.

Machine Guard [FILE_21 SKL MIRROR DISPLAY]:
The File_21 SKL rounded mod-10 Mirror display uses `BC + AD − 2` only when SKL is explicitly viewed “in the Mirror.” Ordinary civil cross-axis arithmetic remains `BC + AD − 1`. Do not generalize this display into a new civil-span operator or into File_51 Protocol 1 / Protocol 2 unless the local file explicitly opens that protocol.

Machine Guard [FILE_21 PILLAR / TEMPLE-TIME]:
Jachin `AD 29515`, Boaz `AD 29465`, and inside-skin coordinate `AD 29395` are Projective Boundary / Temple-Time Pillar coordinate states. Do not treat them as ordinary historical anchors, do not derive them solely from SKL, and do not restore `Jochin` as live spelling except in source-history quotation.

Machine Guard [FILE_21 RESIDUE 6480.25]:
Theorem 35.7's exact product is `11270 × 0.575 = 6480.25`. The rounded `6480` is a Residue Protocol / textual-shift display only and must not be used as the exact product.

Machine Guard [FILE_21 MATRYOSHKA SCALE]:
Matryoshka values require their active scale operator, such as `×360` or `×360²`. Do not process recursive SKL scale-state coordinates as ordinary chronology.


### 32N.3 Capsule absorption (June 10 2026)

| State | Register handling |
|---|---|
| SKL Short / Long shift control | When comparing `24510` and `17980`, the controlled shift explanation is `24510 − 17980 = 6530`; the 50-year anchor gap produces `6530 − 50 = 6480`; the date-state shift checks as `27366 − 20886 = 6480`. |
| SKL `+2` rail normalization inside rounded Mirror display | For `+2` local rail dates such as `2908`, `2858`, or `2888`, normalize back to the 6-ending parent (`2906`, `2856`, `2886`) before applying the rounded mod-10 Mirror display unless a section explicitly opens the whole `+2` stream. |
| `6480.25` Key-of-23 residue state | `6480.25 = 25921 ÷ 4 = 80.5²`; `87.5² − 80.5² = 1176 = 3.5 × 336`. This preserves the SKL fractional residue "three months and 3.5 days" as a residue theorem. |

Facet note: the `6480.25` Key-of-23 residue state (square-identity facet) and the §32N.1 File_21 Residue Protocol `6480.25` state (exact-product facet, `11270 × 0.575 = 6480.25` versus rounded `6480`, with Machine Guard [FILE_21 RESIDUE 6480.25]) are two facets of one value. They do not conflict; cite the facet the local argument opens.

(Absorbed from Restart Capsule v11.3 Appendix AB.4; row wording carried verbatim.)

## 32O. File_22 Cumulative MT Harmonics / Rounded Scaffold Non-Retrojection Update

This bounded convention update incorporates the Final, post-final pressure-tested `File_22` Cumulative MT Harmonics controls into repository-wide state vocabulary.

`File_22` controls the Patriarchal Quartet scalar state, the `9890/989` actual Decimal Bridge, the cumulative Shem birth-week state, the Levitical Aaron/Moses phase display, the Jared / 2nd Cainan `460` ladder, the birth-transfer composite `6900`, and the local Residue Protocol cluster. It also supplies the repository-wide Rounded Scaffold non-retrojection guard.

### 32O.1 File_22 state labels

| File_22 state | Style Guide handling |
|---|---|
| File_22 final source state | `File_22` is Final and post-final pressure tested. Its controls may be used as repository vocabulary unless a local file narrows the state. |
| Patriarchal Quartet scalar state | Abraham `175`, Isaac `180`, Jacob `147`, and minor-note `130` are cumulative harmonic scalars. Preserve the scalar-state function. |
| Cumulative-Regular Decimal Bridge state | `9890/989` links actual cumulative and actual regular chronology. It is not the Rounded Scaffold by default. |
| Actual `9890` bridge | `9890 = 23 × 430` is an actual File_22 bridge value. It may be displayed through endpoint / lower-bound or Year-6 / Adam-state pairs when node-class is stated. |
| Rounded `9900` counterpart | `9900` belongs to File_51a rounded chronology. It is a separate rounded counterpart, not a correction of File_22's actual `9890`. |
| Cumulative Shem birth-week state | `5436n BC` and `5431n BC` are positions inside the same cumulative Shem birth-week, not rounded variants or contradictions. |
| Levitical Aaron/Moses `3.5`-phase display | Preserve the ordinary `3`-year branch and the phase-resolved `3.5` branch. Decimal-looking labels remain phase-state displays. |
| Jared / 2nd Cainan `460` ladder | Preserve the cumulative / regular / birth-transfer / lifespan distinctions before using `460`, `2300`, `4600`, or `6900`. |
| Birth-transfer composite `6900` | `6900 = 4600 + 2300 = 15 × 460`; it is a Jared-to-regular-restored-Cainan-birth composite, not a direct death-boundary span to `2091 BC`. |
| File_22 Residue Protocol state | Values such as `0.575`, `1.75`, `7.5`, `35`, and `42` operate only under Residue Protocol where explicitly opened. |
| File_30 dependency boundary | File_22 may cite the AD 29–33 ministry-window propagation as a forward dependency, but does not locally reconstruct the File_30 argument. |

### 32O.2 Machine Guards

Machine Guard [ROUNDED SCAFFOLD NON-RETROJECTION]:
Do not identify clean, mod-10, harmonically regular, Mirror, SKL mod-10, 10x-scaled, or apparently rounded values with the File_51a Rounded Scaffold unless the local section explicitly opens that state. File_22's `9890` Cumulative-Regular Decimal Bridge is actual chronology. The `9900` rounded counterpart belongs to the later Rounded Scaffold state.

Machine Guard [FILE_22 DECIMAL BRIDGE]:
Preserve `9890` and `989` as the actual File_22 Decimal Bridge unless the local file explicitly opens a rounded counterpart. Do not correct `9890` to `9900`, and do not read File_51a rounded chronology backward into File_22.

Machine Guard [FILE_22 SHEM BIRTH-WEEK]:
Treat `5436n BC` and `5431n BC` as distinct coordinates inside the cumulative Shem birth-week. Do not explain the distinction by Rounded Scaffold retrojection.

Machine Guard [FILE_22 RESIDUE PROTOCOL]:
Do not feed File_22 residue constants such as `0.575`, `1.75`, `7.5`, `35`, or `42` back into ordinary date arithmetic unless the local Residue Protocol operator explicitly opens that use.

---

### 32O.1 Capsule absorption (June 10 2026)

| State | Register handling |
|---|---|
| Smith LXX source-control boundary (File_22 scope) | Smith is an external LXX Genesis source-reconstruction witness where LXX chronology is materially active; he is not proof-bearing for File_22 harmonic interpretation, Mirror logic, SKL / Berossus integration, Residue Protocol, or theological reading. |

(Absorbed from Restart Capsule v11.3 Appendix AB.5; wording carried verbatim. This is the File_22-scoped boundary; the parallel File_33 boundary is §32R.1 and the File_18 witness states are §31.85 / §32J.)

## 32P. File_25 Decimal Invariance Cross-File Convention Update

This bounded convention update incorporates the Final, post-final pressure-tested `File_25` Decimal Invariance controls into repository-wide state vocabulary.

`File_25` is an explanatory reference file. It defines the Decimal Invariance / 10x Mirror-scale phenomenon for later citation without attempting to prove the full holographic-like nature of the chronology. Its control function is vocabulary, state-safety, and operator firewalling.

### 32P.1 File_25 state labels

| File_25 state | Style Guide handling |
|---|---|
| File_25 final source state | `File_25` is Final and post-final pressure tested. Its controls may be used as repository vocabulary unless a local file narrows the state. |
| Decimal Invariance | Structural inference from repeated `1/10` and `10x` displays across SKL, cumulative, biblical, and restoration-field nodes. Not a replacement for historical anchors or exact civil-span proof. |
| Decimal-shadow display | Values such as `2091.6`, `1446.6`, and `445.6` are display values unless a local exact date-state is opened. |
| Finite-decimal display state | `445.6` is executable in `445.6 × 10 = 4456`; `445.(6)` remains source-history residue unless a repeating-decimal operator is opened. |
| Historical-anchor firewall | `2091 BC`, `1446 BC`, and `445 BC` remain historical anchors by dependency; decimal-shadow values do not replace them. |
| Biblical 10x Mirror-scale display | Local File_25 display-state for biblical `10x` Mirror-scale intent. It is not identical to exact civil cross-axis counting and not identical to SKL / Rounded `BC + AD − 2`. |
| Mod-10 display intent | File_25's rounded display intent. It may be used without forcing biblical examples into the SKL / Rounded operator. |
| Nehemiah 10x backbone | `445.6 × 10 = 4456`; `445 BC → AD 4456 = 4900 = 10 × 490` in the local biblical 10x display state. |
| Restoration-decree weave | Bounded Daniel 9 / return field: `4456 BC → 536 BC = 3920 = 8 × 490`; `536 BC → AD 445 = 980 = 2 × 490`; full decree-weave proof remains outside File_25. |
| SKL Long `24510` terminal state | `27366 − 2856 = 24510`; `24510 = 57 × 430 = 19 × 1290`; `24510 = 24500 + 10`. Controlled by File_21 / File_24 dependency. |
| BC/AD counterpart display-label state | Shared numeral does not imply identical node: `4456 BC` / `AD 4456`, `2856 BC` / `AD 2856`, and `2736 BC` / `AD 2736` remain axis-tagged. |
| LXX Shem integer-display shadow | `2736 BC` may function as LXX Shem death witness-state / integer-display shadow of `27366 BC`; exact division gives `27366 ÷ 10 = 2736.6` unless a later operator is named. |
| Law as source-title / local axiom language | Preserve `Law` as File_25 title language. Do not promote Decimal Invariance to repository-wide foundational axiom unless a later control-file update explicitly does so. |

### 32P.2 Machine Guards

Machine Guard [FILE_25 DECIMAL-SHADOW DISPLAY]:
Decimal-shadow values such as `2091.6 BC`, `1446.6 BC`, and `445.6` are display values. They do not replace historical anchors such as `2091 BC`, `1446 BC`, or `445 BC` unless a local file explicitly opens a new exact date-state.

Machine Guard [FILE_25 BIBLICAL 10X MIRROR-SCALE DISPLAY]:
In File_25, `mod-10` names rounded `10x` Mirror-scale display intent. Biblical examples such as `445 BC → AD 4456`, `536 BC → AD 445`, and `1446 BC → AD 2856` are not automatically processed by the SKL / Rounded `BC + AD − 2` operator. Where exact civil cross-axis counting is opened, use the civil operator separately.

Machine Guard [FILE_25 SKL / ROUNDED DISPLAY RESERVATION]:
The `BC + AD − 2` simplification belongs only where the SKL / Rounded display state is explicitly active, such as the `6 BC → AD 6 = 10` type of example or SKL / Rounded rows inherited from File_21 / File_23. Do not impose it on File_25's Nehemiah / Daniel 9 biblical examples.

Machine Guard [FILE_25 BC/AD COUNTERPART LABELS]:
BC-side and AD-side labels that share the same numeral are counterpart display labels, not identical nodes. Preserve the axis tags.

Machine Guard [FILE_25 LAW AS SOURCE-TITLE]:
The word `Law` in File_25 is source-title / local axiom language. It does not convert audit-controlled rows, display-state rows, or dependency-controlled rows into settled proof, and it does not promote Decimal Invariance to repository-wide foundational axiom status without a later explicit control-file update.

---



## 32Q. File_32 Prime Harmonic / Prime–Civil Polarity Namespace Cross-File Convention Update

This bounded convention update incorporates the current `File_32`, `File_33`, and `File_35` prime-polarity corrections into repository-wide state vocabulary.

`File_32` is the foundation file for Prime Quadruple scaffold logic, Modulo 30 / Modulo 210 prime-lattice grammar, Formation Wobble statistics, P23 trunk language, the P1–P100 polarity-date table, and the corrected Prime–Civil / Polarity Namespace. Later prime files may extend File_32 through ordinal, File_36 Prime-Polarity / Target-Mirror lattice, Half-Step, tetrad, Naturalized Grid, or File_50-series machinery, but those later systems must not be retrojected into File_32's ordinary local operators.

Active hierarchy:

```markdown
1. Positive-polarity date output is primary where opened.
2. Negative-polarity / signed A-space date output is primary where opened.
3. Coordinate Mirror / Protocol 1 is secondary agreement unless a local file explicitly gives Mirror proof burden.
```

This is the polarity-before-Mirror hierarchy.

### 32Q.1 File_32 / File_33 / File_35 state labels

| State | Style Guide handling |
|---|---|
| Prime Quadruple scaffold state | `Quad(Q) = {Q,Q+2,Q+6,Q+8}`. `Q` is a prime start value, not a date until projected. |
| Positive-polarity date-output state | `B⁺ = Q − 5`; `M⁺ = Q + 25`; `M⁺ − B⁺ = 30`. Primary positive Prime–Civil coordinate output where opened. |
| `M` rail / apparent rail `+30` state | Replacement for ambiguous inherited `Mirror (+30)` labels where the ordinary same-side positive rail is active. |
| Raw prime-space cross-polarity state | Signed reversed quad: `{Q,Q+2,Q+6,Q+8} ↔ {−(Q+8),−(Q+6),−(Q+2),−Q}`. Lattice state only; not a civil date by itself. |
| Negative-polarity / signed A-space date-output state | `N⁻₁ = Q − 16`; `N⁻₂ = Q + 14`. Primary negative-polarity coordinate output where explicitly opened. |
| P2 positive rail-window state | `6 BC / 36 BC`. This is the P2 positive-polarity output. |
| P2 negative-polarity / signed A-space bridge state | `6 BC / AD 25`. This is not the positive P2 `M` rail. |
| Coordinate Mirror / Protocol 1 secondary-agreement state | Civil counterpart display such as `36 BC → AD 35`, `6 BC → AD 5`, or `AD 81025 → 81026 BC`; secondary to polarity output unless proof burden is explicitly opened. |
| Rounded / mod-10 display-state firewall | `BC + AD − 2` belongs only to explicitly opened rounded / mod-10 states, not ordinary File_32 polarity output. |
| Modulo 210 corridor state | Prime-quad center corridors `15`, `105`, and `195 mod 210`; cross-polarity sends `15 ↔ 195` and fixes `105`. |
| No-`+60` ordinary prime-rail guard | Do not add a default `+60` rail. Any `+60` value requires a separately named state. |
| P1–P100 polarity-date table state | Reference table generated from `Q − 5`, `Q + 25`, `Q − 16`, and `Q + 14`; Mirror columns excluded. |
| P23 trunk / polarity-square state | Preserve P23 start `31721`, positive dates `31716 BC / 31746 BC`, negative dates AD `31705 / 31735`, and `31740 = 23 × 23 × 60`. |
| Key of 23 Pillars state | P23 and P46 are named Key of 23 Pillars because they activate the `23`, `69`, and `299` forms of the Key of 23 through `25/23`, `70/69`, and `300/299` branches. |
| P23 Creation-window `300/299` state | `35880 = 120 × 299 = 520 × 69 = 1560 × 23`; `35880 × 300/299 = 36000`; targets the `4176–4136 BC/AD` creation-window field by dependency. |
| P23-P46 direct-gap `299 → 300` state | Raw `P46 Q − P23 Q = 125550`; widened cross-rail span `125580 = 420 × 299`; `125580 × 300/299 = 126000`. |
| P46 Exodus / Conquest target-window state | `158700 = 23 × 6900 = 69 × 2300 = 23 × 23 × 300`; targets the Exodus / Conquest field through polarity-window logic. |
| P46 Creation-window `300/299` state | `161460 = 540 × 299`; `161460 × 300/299 = 162000`; creation-side labels remain dependency-controlled. |
| P23-P46 bridge state | `189000 = 525 × 360`; `525 = 483 × 25/23`; `483 = 23 × 21 = 69 × 7`. |
| Formation Wobble statistical state | Freshly recomputed File_32 metric; statistical support, not formal probability model by itself. |
| P31 one-sided `72000` rail lock | `P31 B − P4 M = 72000` checks; full bisection remains audit-controlled. |

### 32Q.2 Machine Guards

Machine Guard [FILE_32 POLARITY BEFORE MIRROR]:
Positive-polarity and negative-polarity prime-date outputs are the primary Prime–Civil Interface states where opened. Coordinate Mirror / Protocol 1 is a secondary counterpart display applied after a civil coordinate already exists unless the local file explicitly gives Mirror proof burden.

Machine Guard [FILE_32 POSITIVE / NEGATIVE NON-COLLAPSE]:
Do not collapse positive-polarity `B⁺/M⁺` with negative-polarity / signed A-space output. For P2, positive output is `6 BC / 36 BC`; negative output is `6 BC / AD 25`. For P23, positive output is `31716 BC / 31746 BC`; negative output is AD `31705 / 31735`.

Machine Guard [FILE_32 RAW CROSS-POLARITY]:
Raw prime-space cross-polarity is a signed lattice operation. It is not Coordinate Mirror and not a civil date by itself. When signed A-space rendering is explicitly opened, negative polarity may generate civil date outputs through `Q − 16` and `Q + 14`.

Machine Guard [FILE_32 RAILS / NO +60]:
The ordinary positive File_32 prime rail is `B⁺ = Q − 5`, `M⁺ = Q + 25`, with `M⁺ − B⁺ = 30`. Do not introduce a `+60` ordinary prime rail to repair P31 or other bisection language.

Machine Guard [FILE_32 COORDINATE MIRROR SECONDARY AGREEMENT]:
Coordinate Mirror may corroborate both positive and negative polarity coordinates. It must not replace polarity generation, and it must not deny counterpart status to a negative-polarity date once that date has been validly opened.

Machine Guard [FILE_32 / FILE_33 KEY OF 23 PILLARS]:
P23 and P46 may be named Key of 23 Pillars. Keep `25/23`, `70/69`, and `300/299` branches state-labeled. Do not collapse same-polarity point-to-point gaps, widened cross-rail spans, opposite-polarity bridges, or secondary Mirror displays.

Machine Guard [FILE_32 LATER-SYSTEM NON-RETROJECTION]:
File_33 Ordinal Authentication, File_35 Half-Step, File_36 Prime-Polarity / Target-Mirror lattice, File_37 tetrad mechanics, and File_50-series Naturalized Grid / Bipartite Grid Landing may clarify or extend File_32, but they are not File_32's default local operators unless the section explicitly opens that dependency state. File_36's inherited Double Lock is source-history only and must not be executed as a default prime operator.

---

## 32S. File_31 Rod of Enoch Cross-File Convention Update

The following conventions are adopted after the Final and post-final pressure-tested `File_31` Rod of Enoch source.

1. `Rod of Enoch state` names File_31's bounded geometric-theology file function, joining Enoch's `365`, the Enochian `364`, Methuselah `299 → 300`, Lamech `182`, Pi approximations, SKL Enoch anchors, and AD 33 solar synchronization as a local file thesis.
2. `Pi master-formula state` names the File_31 formulas that approximate Pi using state-controlled variables such as `w = 7`, `y = 365`, `g = 70`, `s = 1/86400`, `P = 1/25920`, and `p = 1/25800`. Precision should be stated by absolute error and decimal digits of absolute accuracy rather than by unsupported fixed-decimal-place language.
3. `Pi convergent generator state` names the File_31 generator using the Milü ratio, the Enochian `294`, and the Archimedean `22/7` correction. Standard continued-fraction convergent rank should be counted 1-based from `3/1` when that rank language is used.
4. `SKL Enoch anchor slash-pair state` preserves `66968/66966 BC` and `45968/45966 BC` as slash-pair display states whose provenance remains dependency-controlled unless `File_21`, `File_34`, or another active source-control file supplies the derivation.
5. `Selected-rail display state` names File_31 rows where one member of a slash-pair is used for a checked span while the full slash-pair remains preserved.
6. `AD 33 solar synchronization state` names the local File_31 display in which `294 × 364 = 107016` and `107016 ÷ 293 = 365.2423208`. The exact AD 33 tropical-year target and related time-scale / `ΔT` claims remain audit-controlled and non-load-bearing unless a fuller astronomical model is supplied.
7. File_31 must not override File_13 for Passion-date control. `AD 33n` remains the preferred Friday Nisan `14/15` Passion state. `AD 30n` remains a possible Friday Nisan `14/15` Passion state. `AD 33t` is a Tishri-side completion member only. AD 34 is a completed-period purification display only.
8. `Exploratory scientific analogy / providential synchronization state` may be used for File_31's Pi / solar-year / AD 33 material when the arithmetic checks but the external scientific model remains unproven.
9. `Claim-weight restraint state` names the File_31 rule that teleological or proof-inflated language should be removed or bounded; the file should state mathematical alignments directly and let readers evaluate implications.

Machine Guard [FILE_31 ROD OF ENOCH STATE SAFETY]:
File_31 is Final and post-final pressure tested, but it is not a control file for all Pi, SKL, or Passion-date claims. Later files may cite File_31 for Rod of Enoch / Pi-geometric material only with active state, operator, claim-status, and dependency boundaries stated.

Machine Guard [FILE_31 / FILE_13 PASSION BOUNDARY]:
File_31 may use AD 33 as a local solar-synchronization display. It must not be used to reassign Passion-date states. Passion-date candidates are controlled by File_13: `AD 33n` preferred Friday Nisan `14/15`; `AD 30n` possible Friday Nisan `14/15`; `AD 33t` and AD 34 are non-Passion display states.

Machine Guard [FILE_31 SCIENTIFIC-ANALOGY RESTRAINT]:
Do not convert File_31's Pi / tropical-year / AD 33 alignment into physics proof unless a local file supplies a full source-controlled astronomical model. Preserve the alignment as arithmetic fact plus exploratory scientific analogy / providential synchronization where appropriate.


## 32T. Samaria Node Cross-File Convention Update

This bounded convention update incorporates the File_00 Samaria Node cross-file control into repository-wide state vocabulary.

1. `Samaria Node schematic envelope state` names the `726–721 BC` conceptual envelope for the collapse of the Northern Kingdom of Israel.
2. `Actual Fall of Samaria state` preserves late `722 BC ±1`, after a siege beginning about `725 BC`, as the actual historical state.
3. `726 BC` is the onset / start-of-fall label. It may represent the idealized start of the three-year siege, the idealized fall of Samaria, or the conceptual collapse-point of the Northern Kingdom.
4. `721 BC` is the terminal / latest-fall label of the same Samaria Node. It should not be treated as a replacement for the actual late-`722 BC` fall date unless a local historical-source state explicitly opens that discussion.
5. `Israel literal-reign schematic ledger` names the year-level ledger that back-projects the stated reigns of Israel's kings onto the `967/966 BC` Temple node and lands in the `726/725 BC` Samaria onset field.
6. `Samaria / Jerusalem twin-city judgment vector state` names the File_16-dependent relation `1876 BC → 726 BC = 1150`, `1876 BC → 586 BC = 1290`, and `726 BC → 586 BC = 140 = 2 × 70`.
7. `Samaria Node / Immanuel 720 overlay state` names the File_19 / File_13-dependent relation `1446 BC → 726 BC = 720`, `726 BC → 6 BC = 720`, and `721 BC → 1 BC = 720`.
8. `Samaria Node three-tradition Creation overlay state` names the LXX / SP / MT overlay where LXX `5556 BC → 726 BC = 4830`, SP `4416 BC → 966 BC = 3450`, and MT `4176 BC → 726 BC = 3450`.
9. `Abrahamic 240-cycle overlay state` names `2166 BC → 966 BC → 726 BC = 1200 + 240 = 1440`.
10. Samaria Node material is structural inference / probable scribal design / providential synchronization depending local wording. It is not a reconstruction of actual Northern Kingdom regnal chronology.

11. `Divided Kingdom actual / schematic route triad` names actual `930 BC`, Southern schematic `980 BC`, and Northern schematic `966 BC`, with `980 − 930 = 50` and `966 − 930 = 36`.
12. `United / Divided Kingdom 360/365 overlay state` names `1086 → 966 = 120`, `1446 → 1086 → 726 = 360 + 360`, `1086 → 721 = 365`, and `1086 → 586 = 500`.
13. `Samaria / Jerusalem Pillar metric` names `726 BC → AD 29515 = 30240 = 420 × 72` and `586 BC → AD 29515 = 30100 = 420 × 71⅔`.
14. `Abrahamic-to-exile trunk pattern` names trunk nodes `2166 / 1446 / 726 / 586 / 446 BC` and branch nodes `1086 / 1016 / 966 BC`.
15. `Samaria Node 276 → 280 flattening state` names `722/721 → 446/445 = 276`, `276 × 70/69 = 280`, and `726/725 → 446/445 = 280`.
16. `Exile-return Mirror disclosure state` names `606/605 → 536 = 70/69 → 70`, `536 BC → AD 725 = 1260`, `539 BC → AD 722 = 1260`, and `536 BC → AD 445 = 980`.
17. `Elijah Northern Kingdom ministry overlay state` names source-control `875–848 BC`, actual-history mod-5 `876–846 BC`, stylized Northern route `906–876 BC`, and optional Ahab field `905/904 BC`.
18. `Elijah Northern-route rationale state` binds the Elijah overlay to the Northern `966 BC` route rather than the Southern `980 BC` `+50` route.
19. `Samaria temporal-conversion convergence state` names `1086 → 446 = 640`, `1086 → 722 = 364`, `722 → 446 = 276`, `1086 → 726 = 360`, `726 → 446 = 280`, and `364 + 276 = 360 + 280 = 640`. The Israel literal-reign schematic ledger's year-level display total is `241.5` before sub-year residue (`967/966 BC → 726/725 BC`).

(Items 11–19 absorbed June 10 2026 from Restart Capsule v11.3 Appendix T; the capsule appendix is the prior carrier and the named File_00 updates remain the controlling precedent.)

Machine Guard [SAMARIA NODE ELIJAH / TEMPORAL-CONVERSION CONTROL]:
The Samaria Node is a File_00 schematic envelope and dependency-control state. Do not use it to correct actual Northern Kingdom chronology, Jeroboam's actual accession, Ahab's actual accession, Elijah's actual historical ministry, or the actual late-`722 BC` fall of Samaria. Do not treat it as a File_51a Rounded Scaffold state. When the Elijah overlay is active, use the Northern Kingdom / Kings of Israel `966 BC` route rather than the Southern Kingdom / Kings of Judah `980 BC` `+50` route because Elijah belongs to the Northern Kingdom field of Ahab and Jezebel.

Machine Guard [SAMARIA NODE ACTUAL / SCHEMATIC FIREWALL]:
The actual Fall of Samaria remains late `722 BC ±1`, after a siege beginning about `725 BC`. The schematic Samaria Node is `726–721 BC`. Do not force the actual fall date to equal either schematic endpoint. `726 BC` is the onset / start-of-fall label; `721 BC` is the terminal / latest-fall label. Do not use the Samaria Node to correct Jeroboam's actual accession, the actual beginning of the Northern Kingdom, or the actual fall date of Samaria.

Machine Guard [SAMARIA NODE LEDGER SCOPE]:
The Israel literal-reign schematic ledger is conceptual / scribal. It may use the `967/966 BC` Temple node as a schematic head and the `726/725 BC` field as a schematic landing field. It must not be treated as harmonized regnal chronology.

Machine Guard [SAMARIA NODE SUB-YEAR MARKERS]:
The Zimri / Omri `916 BC` and Zechariah / Shallum / Menahem `766 BC` sub-year marker observations are exploratory and non-load-bearing. They may be retained as schematic short-reign diagnostics, but they must not correct actual Northern Kingdom regnal chronology, the Israel year-level ledger, the Samaria Node endpoints, or the actual late-`722 BC` Fall of Samaria.

Machine Guard [SAMARIA NODE ROUNDED-SCAFFOLD FIREWALL]:
The term `Samaria Node` is generic node-language, not a File_51a Rounded Scaffold state. Do not infer Rounded Scaffold logic merely because the node uses schematic or idealized dates.

## 32R. File_33 Ordinal Lock / Ordinal Authentication Cross-File Convention Update

This bounded convention update incorporates the Final, pressure-tested `File_33` Ordinal Lock controls into repository-wide state vocabulary.

`File_33` is the repository control for Ordinal Authentication in the Prime Grid. It tests whether a proposed prime-grid target is authenticated not only by the value of a Prime Quadruple but also by the ordinal position `n` of that quadruple. Its local operators are Value Lock, Ordinal Lock, `n² × Unit`, and `n × Constant`. These later operators may clarify File_32 extensions, but they are not retrojected into File_32 as File_32's ordinary local operators.

### 32R.1 File_33 state labels

| File_33 state | Style Guide handling |
|---|---|
| File_33 final source state | `File_33` Final; pressure tested. Controls Ordinal Authentication vocabulary for later prime-grid files. |
| Ordinal Authentication state | Value Lock and Ordinal Lock. Typical forms are `n² × Unit` and `n × Constant`. Use as File_33 local argument grammar, not as File_32's default operator. |
| Ordinal Squared tendency state | A strong observed tendency across tested primes, not a declared universal axiom. Preserve this epistemic restraint. |
| Source `Mirror Date` / `M` rail alias | In inherited File_33 prime tables, source `Mirror Date` means File_32 `M` rail / apparent rail where ordinary `B/M` is active. It is not Coordinate Mirror / Protocol 1 unless the local section explicitly opens that namespace. |
| Unsupported local `+10` mirror-display examples | Deleted from active File_33 proof. Do not revive a generic `+10 Year Mirror Jump`; File_32 supplies no such ordinary operator. |
| AD 1406 visible Conquest-week / numeric-year display state | `AD 1406` may be retained only where a File_51a / File_09 visible Conquest-week or numeric-year display state is explicitly opened. Strict Coordinate Mirror / Protocol 1 remains `1406 BC → AD 1405`. |
| P26 symbolic / precise AD target state | Symbolic AD `666` and precise civil AD `665` remain distinct. The precise cross-axis check is `51336 + 665 − 1 = 52000`. |
| P47 Herodian Temple start field | File_33 treats the Herodian Temple start as a `21/20/19 BC` field, with `20 BC` as center / average. John 2's forty-six-year marker points to AD `26/27/28`; `20 BC → AD 27 = 46` is the center display. |
| P50 decimal-shadow state | `187656 ÷ 100 = 1876.56`. Do not restate this as strict `100 × 1876`. |
| File_50a Naturalized Master Table dependency | File_50a remains the downstream computed-output dependency if revised. Do not infer Naturalized Master Table values locally in File_33. |
| File_33 pressure-test correction state | Visible pressure-test score is `19/20 [PASSED]`. The inherited `20/20` claim is corrected as source-history error. |
| LXX source-reconstruction witness in File_33 | Where LXX Genesis chronology is materially active, Smith may be cited as an external LXX Genesis 5 / 11 reconstruction witness only. Smith is not the source of File_33's prime-grid interpretation, Mirror logic, SKL / Berossus integration, providential synthesis, or theological reading. |

### 32R.2 File_33 Machine Guards

Machine Guard [FILE_33 ORDINAL NON-RETROJECTION]:
File_33 Ordinal Authentication is a later prime-grid control. Do not use `n² × Unit`, `n × Constant`, Value Lock, or Ordinal Lock as ordinary File_32 operators unless the local file explicitly opens File_33 dependency.

Machine Guard [FILE_33 MIRROR-DATE ALIAS]:
When inherited File_33 tables say `Mirror Date` and the operation is ordinary `B/M`, read the term as File_32 `M` rail / apparent rail. Do not convert it into Coordinate Mirror / Protocol 1, rounded / mod-10 display, or prime-space cross-polarity without an explicit local state.

Machine Guard [FILE_33 NO +10 MIRROR JUMP]:
Unsupported local `+10` mirror-display examples were removed from active File_33 proof. Do not introduce a generic `+10 Year Mirror Jump` in future prime-grid files unless a later final file defines a named operator.

Machine Guard [FILE_33 AD 1406 DISPLAY]:
For `1406 BC`, strict Coordinate Mirror / Protocol 1 is `AD 1405`. `AD 1406` is only a visible Conquest-week / numeric-year display state where File_51a / File_09 precedent is explicitly opened.

Machine Guard [FILE_33 SYMBOLIC / PRECISE AD TARGET]:
Preserve symbolic AD targets and precise civil AD targets as distinct states. P26 is the precedent: symbolic AD `666`; precise civil AD `665`; `51336 + 665 − 1 = 52000`.

Machine Guard [FILE_33 DEPENDENCY BOUNDARY]:
File_35 Half-Step, File_37 tetrad mechanics, File_50a Naturalized Master Table output, and any unavailable `FILE_33_PRESSURE_TEST.md` methodology remain dependency-controlled. Do not use missing dependencies to carry proof burden.

## 32U. File_34 SKL / Berossus Post-Final Vocabulary Update

This bounded convention update incorporates the Final, post-final pressure-tested `File_34` SKL / Berossus controls into repository-wide state vocabulary.

`File_34` is the controlling file for the full SKL / Berossus derivation, the Berossus computational anchor, the Precessional Envelope, the Sanctuary Fractal, the Appendix B Pillar / phase-chain machinery, the SKL-to-Berossus `2370` corridor, the `30 + 20 + 30` restoration corridor, and the hybrid Alulim / Berossus-aligned origin `34566 + 241200 = 275766`. Dependent files may cite or index those states, but they should not re-prove the full File_34 system unless the local file explicitly opens that derivation.

### 32U.1 File_34 state labels

| File_34 state | Style Guide handling |
|---|---|
| File_34 final source state | `File_34` Final; post-final pressure tested. Controls full SKL / Berossus derivation and File_34 post-final vocabulary. |
| File_34 post-final citation-control state | Cross-file routing state used when dependent files cite the Final File_34 map after post-final pressure testing. Do not use older File_34 section labels as current controls unless preserved as source-history residue. |
| Berossus computational-anchor state | `486 BC` is the figurative / computational backward-projection base. It does not replace `536 BC` as the biblical restoration anchor or `539t/538n BC` as the fall-of-Babylon / Persian-transition phase field. |
| Fall / restoration / computational firewall | `539t/538n BC`, `536 BC`, and `486 BC` are different node-classes. Preserve the active node-class before computing spans or naming historical meaning. |
| SKL base / rail firewall | `2906/2856 BC` are SKL physical base anchors; `2936/2886 BC` are `+30 Apparent Mode` rails; `2938/2936`, `2908/2906`, `2888/2886`, and `2858/2856 BC` are rail-pair states when the `+2` local rail is opened. |
| SKL whole-year / fractional-datum firewall | `17980` and `24510` are whole-year totals. The appended `3 months and 3.5 days` is a textual fractional-datum state and must not be collapsed into whole-year table arithmetic. |
| `+2 local rail` versus `+720 scale-state` | The local `+2` rail and the scaled `720 = 2 × 360` Cosmic Pixel are related but non-interchangeable. Declare which state is active. |
| Precessional Envelope state | File_34 control state for `6480`, `12960`, `13680`, `25920`, and `720`, including Ideal and Metonic pathway distinctions. |
| Ideal pathway state | Upper-anchor pathway using the `12960` half-year relation. Keep separate from the lower-anchor Metonic pathway. |
| Metonic pathway state | Lower-anchor pathway using `13680 = 12960 + 720`. Keep separate from the Ideal pathway and from generic Great-Year arithmetic. |
| Cosmic Pixel state | `720 = 2 × 360` at cosmic scale. It is not a floating adjustment and not the local `+2` rail. |
| Sanctuary Fractal state | File_34 state for `28086 BC → 486 BC = 27600 = 12 × 2300`, Key-of-23 expansion to `30000`, and related Pillar / Virtual Origin displays. |
| Pillar / AD target state | AD Pillar spans use civil cross-axis arithmetic `BC + AD − 1`. They are not automatically Coordinate Mirror, Prime Mirror, inverse-number reversal, or rounded / mod-10 display. |
| SKL-to-Berossus `2370` corridor state | Forward-time same-side BC projection from SKL rail pairs to the restoration / Berossus field: `2938/2936 − 2370 = 568/566 BC`, `2908/2906 − 2370 = 538/536 BC`, `2888/2886 − 2370 = 518/516 BC`, and `2858/2856 − 2370 = 488/486 BC`. |
| `30 + 20 + 30` restoration corridor | `566 → 536 → 516 → 486 BC` is the restoration / Berossus corridor. `536/516 BC` is the central `20`-year hub-pair, not a failed `30`-year span. |
| Paired slash-display arithmetic | Slash-pairs in the File_34 corridor and Appendix B Jared displays are paired endpoint displays. Check them pairwise; do not average, range-collapse, or cross-pair endpoints. |
| Nisan / Tishri phase-chain arithmetic | Nisan remains the default unless a Tishri suffix is opened. `n` and `t` suffixes mark phase states; optional Tishri straddles do not silently alter Nisan-default arithmetic. |
| Appendix B phase-chain dependency state | Compressed Appendix B scalar, Pillar, Nisan / Tishri, and slash-pair logic routes to File_34 unless a local file explicitly reopens the machinery with active operators. |
| Expansion difference / non-Residue state | File_34 `2400` is an expansion difference under Key-of-23 expansion unless a section explicitly opens formal Residue Protocol. |
| Non-active Files_31–33 source-residue state | In the File_34 post-final process, Files_31–33 are not controlling exemplars unless a later author direction explicitly reopens that dependency. Do not use prime-grid or formal Mirror material to reinterpret File_34 local SKL / Berossus states. |
| Hybrid Berossus / SKL Alulim Creation overlay | `34566 + 241200 = 275766`. Hybrid comparative coordinate; preserve Berossus `432000 / 466566 BC`, native SKL Creation, and the Berossus Flood anchor as distinct states. |
| File_55 downstream thirds-marker clarification | File_55 controls `275766 → 184326 → 1446 BC` and `181440 = 7 × 25920` to `2886 BC`. File_34 supplies the origin only; do not generalize the File_55 primer inside File_34. |
| State-conditioned SKL `+1260` display | Active anchor-pair display, for example `2886/2856 BC → 4146/4116 BC`. Name the active anchor pair before using the display as proof-bearing arithmetic. |

### 32U.2 File_34 Machine Guards

Machine Guard [FILE_34 SKL / BEROSSUS DEPENDENCY]:
Use `File_34` as the controlling file for full SKL / Berossus derivation, Precessional Envelope, Sanctuary Fractal, Pillar / AD target state, Appendix B phase-chain machinery, and the `2370` / `30 + 20 + 30` corridor. Index files may cite File_34 but should not locally re-prove the full system unless the section explicitly opens that derivation.

Machine Guard [FILE_34 486 / 536 / 539 FIREWALL]:
Do not collapse `486 BC`, `536 BC`, and `539t/538n BC`. `486 BC` is Berossus computational-anchor state; `536 BC` is biblical restoration-anchor state; `539t/538n BC` is fall-of-Babylon / Persian-transition phase field.

Machine Guard [FILE_34 `+2` / `+720` FIREWALL]:
Do not treat the local SKL `+2` rail as the same operator as the `+720` Cosmic Pixel. The `+720` value is the scaled Shem state `2 × 360`; the `+2` value is a local rail.

Machine Guard [FILE_34 `2370` CORRIDOR]:
When projecting the SKL rail quartet into the Berossus / restoration field, use forward-time same-side BC subtraction. Preserve paired slash-displays: `2938/2936 − 2370 = 568/566 BC`, `2908/2906 − 2370 = 538/536 BC`, `2888/2886 − 2370 = 518/516 BC`, and `2858/2856 − 2370 = 488/486 BC`.

Machine Guard [FILE_34 `30 + 20 + 30` HUB]:
Do not correct `536/516 BC` to a `30`-year span. It is the central `20`-year hub of the corridor `566 → 536 → 516 → 486 BC = 30 + 20 + 30`.

Machine Guard [FILE_34 PHASE-CHAIN / SLASH-PAIR]:
Check Nisan / Tishri chains and slash-pairs under their active state. A slash-pair is not a range, average, or free endpoint pool unless a local file explicitly opens that operator.

Machine Guard [FILE_34 PILLAR CIVIL SPAN]:
For BC-to-AD Pillar spans, use civil cross-axis arithmetic `BC + AD − 1`. Do not convert Pillar spans into formal Prime Mirror, Coordinate Mirror, inverse-number, or rounded / mod-10 displays without an explicit local state.

Machine Guard [FILE_34 NON-RESIDUE]:
Do not treat File_34 `2400` expansion difference or other clean deltas as formal Residue Protocol unless the local section explicitly opens Residue Protocol state.

Machine Guard [FILE_34 HYBRID ALULIM ORIGIN]:
`275766 BC` equals the Berossus Flood anchor `34566 BC` plus the listed SKL eight-kings total `241200`. This origin is distinct from the source-retained `+720` Alulim pixel check and does not replace either parent chronology.

Machine Guard [FILE_34 / FILE_55 THIRDING BOUNDARY]:
File_34 controls the hybrid origin `34566 + 241200 = 275766`. File_55 controls `275766 → 184326 → 1446 BC` and `184326 − 2886 = 181440 = 7 × 25920`. Do not promote `184326 BC` to a File_34 primary anchor or use the File_55 primer as a general File_34 operator.


---

# Part C. File_36 / File_41 / File_45 Glossary Additions and Machine Guards (migrated from Style Guide v2.4 update-record subsections)

### File_36 glossary additions

| Term | Definition / control |
|---|---|
| Prime-Polarity / Target-Mirror state | File_36 active state after finalization. Positive and negative prime-polarity outputs supply the scaffold; actual chronology supplies target dates; Mirror belongs to actual chronological target-states where opened. |
| target-Mirror | Mirror assigned to an actual chronological target-state, not to the prime-output mechanism by default. |
| midpoint / centricity diagnostic | A non-generative center such as `Q + 5` that may appear when a corrected positive / negative polarity field is displayed through target-Mirror. It is not an executable prime-output operator. |
| retired Double Lock source-history state | File_36 inherited `Q − 25 / Q + 5` mechanism, preserved only as a brief audit-controlled source-history note. It must not carry proof burden or be retrojected into File_32, File_33, or File_35. |
| P1 local Mirror Christological target-state exception | File_36-local Christological target-state display for P1. It does not reopen default Mirror of prime outputs and remains dependency-controlled by Christological files where broader Christ-date logic is active. |
| actual `+50` firewall | Actual Kings / SKL / Berossus / Jubilee `+50` systems remain valid where dependency-controlled; only the old prime-internal spread is retired. |

Machine Guard [FILE_36 PRIME-POLARITY / TARGET-MIRROR RESTRAINT]:
Do not use Coordinate Mirror as a default prime-output operator in File_36-dependent material. Positive-polarity and negative-polarity / signed A-space outputs are the active Prime–Civil scaffold. When Mirror appears, assign it to the actual chronological target-state unless a local section explicitly opens a bounded explanatory display.

Machine Guard [FILE_36 RETIRED DOUBLE LOCK]:
Do not execute `Q − 25 / Q + 5` as a prime-generation operator. Retain the inherited Double Lock language only as source-history / audit-controlled explanation unless a later file explicitly reopens a distinct dependency-controlled state.

Machine Guard [FILE_36 NODE-CLASS SEPARATION]:
Do not collapse sextuplet start values, active `Q` values, positive `B⁺`, positive `M⁺`, negative-polarity AD outputs, target-Mirror counterparts, midpoint diagnostics, and audit-only inherited outputs.


### File_41 glossary additions

| Term | Definition / control |
|---|---|
| Jachin inside-skin state | A unique File_41 Pillar boundary state generated by `AD 29515 − 120 = AD 29395`. The `−120` offset is not a general-purpose inside-skin generator and must not be exported to other Pillar or node systems unless a local rule explicitly opens it. |
| Restoration Mirror Anchor state | The AD-side restoration counterpart `AD 535`, corresponding to the `536 BC` restoration anchor under Protocol 1-style Mirror logic. It remains distinct from the `536 BC` historical restoration anchor and from ordinary same-side AD subtraction. |
| File_41 Jachin / Restoration lock state | The same-side AD span `AD 29515 − AD 535 = 28980 = 23 × 1260`, where `AD 29515` is the Jachin actual Pillar state and `AD 535` is the Restoration Mirror Anchor. The arithmetic is proof-bearing; the restoration-to-limit reading is theological note and providential synchronization. |
| File_41 Mosaic 460 link state | The same-side BC span `1986 BC → 1526 BC = 460`, where `1986 BC` is the Jacob-period hinge and `1526 BC` is the Moses birth anchor. This state participates in the Watcher / sanctuary / deliverance `460` family without collapsing those nodes into one chronology. |
| File_41 double 120-year template | The paired same-side BC spans `2106–1986 BC = 120` and `1526–1406 BC = 120`, bridged by `1986–1526 BC = 460`. |
| File_41 Temple-completion endpoint state | The dependency-controlled endpoint `946 BC` used in File_41's restoration row, where `1406 BC → 946 BC = 460`. This state belongs to the Conquest-to-Palace / Temple-completion display and must not replace the historical Temple foundation anchor `966 BC` or the Kings dual-ledger controls. |
| File_41 Flood-to-inside-skin arithmetic-control state | The civil cross-axis check `2456 BC → AD 29395 = 31850`, calculated as `2456 + 29395 − 1 = 31850 = 49 × 650`. This makes explicit the Flood-side arithmetic implied by File_41's War-of-the-Seeds visualization and inside-skin synthesis. It does not re-anchor the Flood chronology and does not generalize the Jachin inside-skin operator. |
| File_41 AD 115 target restraint | `AD 115` is only the local Prophetic Expansion target generated by `AD 535 − 420`. It is not a separate source-label state and should not be exported as independent terminology. |
| File_41 argument-control state | A claim-status calibration state in which File_41's verified arithmetic relations carry the base proof layer, while Jachin / Restoration coherence is classified as structural inference, theological synthesis is classified as theological note, and the larger design reading is classified as providential synchronization. This state does not assert a formal probability model or direct scribal intent. |

Machine Guard [FILE_41 JACHIN INSIDE-SKIN NON-EXPORT]:
Do not generalize the File_41 inside-skin subtraction. `AD 29515 − 120 = AD 29395` is a localized Jachin boundary operation, not a dynamic rule for other Pillar, anchor, or node systems.

Machine Guard [FILE_41 RESTORATION MIRROR ANCHOR]:
`AD 535` is the Restoration Mirror Anchor by dependency on the `536 BC` restoration anchor and Protocol 1-style counterpart logic. It does not erase or replace the historical `536 BC` restoration anchor.

Machine Guard [FILE_41 AD 115 TARGET RESTRAINT]:
`AD 115` is only the local Prophetic Expansion target generated by `AD 535 − 420`. It is not a separate source-label state and should not be exported as independent terminology.

Machine Guard [FILE_41 TEMPLE / FLOOD ENDPOINT DEPENDENCY]:
`946 BC` and `2456 BC` function in File_41 as dependency-controlled arithmetic inputs. `946 BC` belongs to the Conquest-to-Palace / Temple-completion row, and `2456 BC` belongs to the Flood-side cross-axis check. Neither redefines the controlling Kings, Temple, or Flood chronology.



### File_45 glossary additions

| Term | Definition / control |
|---|---|
| File_45 exact-sweep statistical proof state | A File_45 state in which a fixed target-frame is tested against a fixed node point cloud across the full `49000`-phase A-space cycle. The result is an exact phase-space frequency, not a Monte Carlo estimate and not a metaphysical-certainty claim. |
| A-space phase-sweep state | The coordinate state in which civil dates are first converted to A-space and then shifted by `k ∈ {0, …, 48999}`. Phase shifts must not be applied directly to BC / AD labels. |
| Fixed node-manifest state | The closed File_45 node-row set used for the exact sweep. No hidden nodes may be added, and targets may not be inserted into the node manifest. |
| Point-cloud expansion state | The deterministic expansion of each fixed node row into Boundary, `+30`, and `+40` generated points. Generated points are not additional node rows. |
| Weighted-node state | The File_45 scoring state in which ordinary generated points have weight `1`, while the `Jared_Abraham_Row` generated points have weight `2`. |
| Target-frame state | The rigid File_45 target set being tested. `T_5` and `T_6` are target frames, not node manifests and not cycle families. |
| Target-node coincidence state | A scoring state in which a target may coincide with a node-generated point at distance `0`. This coincidence is permitted, but it does not mean the target was added as a node. |
| Distance-0 perfect-hit rule | The rule that `d = 0` satisfies `d ≡ 0 mod c` for every cycle `c` in the active cycle family. |
| Verified score output state | The File_45 arithmetic state in which the observed scores were recomputed and verified: `{14006 BC, 1446 BC, 1406 BC}` scores `38`, `T_5` scores `66`, and `T_6` scores `83`. |
| Corrected exact phase-space frequency state | The File_45 corrected rarity state under the inclusive phase-domain definition: `{14006 BC, 1446 BC, 1406 BC}` has `N≥ = 28`, rarity `1 in 1750`; `T_5` has `N≥ = 9`, rarity `1 in 5444.4`; `T_6` has `N≥ = 3`, rarity `1 in 16333.3`. |
| Inclusive exact phase sweep state | The File_45 statistical-method state in which `N≥` is counted over all `k ∈ {0, …, 48999}`, including `k = 0`. |
| Daniel 9 pattern-template state | The File_45 interpretive state in which Danielic segmentation supplies a pattern-template for the x10 structure. It does not open a `434`-year lock and does not claim that Daniel 9 literally refers to Jared or 2nd Cainan. |
| Non-lock `+460` motif state | The File_45 commentary state in which the `+460` Cainan motif is acknowledged as structurally suggestive but excluded from the target set, node set, scoring algorithm, and phase sweep. |
| File_43 / File_44 proof-burden separation state | The File_45 dependency-control state in which File_43 supplies the genealogy-node lattice and File_44 supplies scaling-law controls. File_45 must not collapse those proof burdens or import File_44's dependency-controlled Creation-head triad as a File_45 proof input. |

Machine Guard [FILE_45 NODE / TARGET NON-COLLAPSE]:
Targets may coincide with node-generated points, including distance `0` hits, but targets must not be added to the fixed node manifest. Preserve node row, generated point, target frame, cycle family, score, and rarity as distinct states.

Machine Guard [FILE_45 A-SPACE PHASE SWEEP]:
All `+k` phase shifts in File_45 occur in A-space after civil-date conversion. Do not shift BC / AD labels directly.

Machine Guard [FILE_45 INCLUSIVE PHASE DOMAIN]:
When citing File_45 rarity values, use the inclusive domain `k ∈ {0, …, 48999}`. The corrected values are `N≥ = 28`, `N≥ = 9`, and `N≥ = 3` for the three published rows.

Machine Guard [FILE_45 DANIEL 9 RESTRAINT]:
Daniel 9 language in File_45 is a pattern-template state, not a new calendar lock, not a `434`-year lock, and not a literal referent claim about Jared or 2nd Cainan.

Machine Guard [FILE_45 FILE_43 / FILE_44 PROOF-BURDEN]:
Use File_43 for the genealogy-node lattice and File_44 for scaling-law controls. Do not use File_44 to supply File_45 nodes or targets, and do not use File_45's statistical results as proof-bearing material inside File_43 or File_44 unless a later file explicitly opens that dependency.

---

# Part D. Post-migration registrations

## D.1 File_46 Convention Entry (registered at Finalization, June 10 2026)

Source file: `File_46 Final — Harmonic Expansion Protocols`. Controlling precedents: `File_43` (lattice locks, display ledger, figurative God bookend), `File_44` / `File_45` (wave-set, Daniel-9 template), `File_22` (`14006 BC`).

| State | Definition | Guard |
|---|---|---|
| Expansion-operator state | Prophetic Ratio `70/69` and Priestly Ratio `25/23` as exact rationals acting on the `4830` base span (`4836 BC → 6 BC`) and its double `9660 = 2 × 4830`; Key-of-23 Prophetic / Priestly Expansion states | Do not decimalize; do not collapse the two ratios |
| File_46 lattice-placement claim frame | Span equations are arithmetic facts under their stated operator; identification of projected endpoints with `File_43` ledger boundaries is structural inference; source "LOCK" is a legacy placement label | Not the `File_33` Value Lock or Ordinal Lock state |
| Doubled base span | `9660 = 2 × 4830`, input to the dual expansion (`× 70/69 → 9800 → 4206 BC`; `× 25/23 → 10500 → 3506 BC` from `14006 BC`) | §3 of File_46 only |
| 11th-Week composition | Luke's step ahead of the Enoch pivot is `490 = 420` (conversion residue `5256 − 4836`) `+ 70` (head generation `5326 − 5256`); week counts `4900 = 10 × 490` and `5390 = 11 × 490` | `5326 BC` is an arithmetic lattice endpoint; the `God` label remains the figurative Luke-derived bookend label |

Machine Guards registered: [CIVIL SPAN EXECUTION] (enforce `BC + AD − 1` across the epoch boundary; no algebraic subtraction); [DISPLAY-LEDGER SPAN USE] (ledger boundaries used as endpoints restate node-class; `MattSpan` / `LukeSpan` / `LukeIntervals` locks not overwritten); [STATIC `+460/+30` MOTIF] (`460 + 30 = 490` is a static motif, never a generator).

Removal note: the source draft's prime-lattice section (P8–P12, including the "Enochian Unit" label) was removed by author direction June 10 2026; the prime-quadruple grid remains controlled by `File_32` / `File_33` / `File_35`.
## D.2 File_47 Convention Entry (registered at Finalization, June 10 2026)

Source file: `File_47 Final — Patriarchal Pairing System: Six Birth-Pairs from Noah to Jacob`. Controlling precedents: `File_00` (Key of 23; structural variators), `File_02` (Shem ±2 regular-localized state), `File_16` (`1876 BC` convergence; `+215` position slash-pairs), `File_18` (chronological data tables; cumulative architecture), `File_51a` (cumulative controls), `File_07` (restored 2nd Cainan `+130` / `460`).

| State | Definition | Guard |
|---|---|---|
| Pair node-class register P0–P6 | The 12-node Noah→Jacob chain as six birth-pairs (`pair1` = earlier-born; `pair2` = later-born) with P0 = Noah anchor node-class supplying the closing bracket; decadal pairing lock (birth mod 10 equal within each pair) | Pair-IDs are node-classes, not date states; P0 is an anchor, not a pair |
| Column A / Column B gap-states | Column A = within-pair birth gaps; Column B = between-pair birth gaps including the P0 bracket term (`500`; `501` SP S1; `502` S2 502-bracket reading); six gaps per column; same-side BC subtraction | The P0 bracket is Column B by definition even as the final step of an A-walk |
| File_47 mode-flag notation | `S0`/`S2` (MT) = Shem ±2 lower / upper members; `S0`/`S1` (SP) = local inclusive +1 Noah/Shem toggle; `T0`/`T60` = `+60 Terah` OFF/ON; `J0`/`J215` = `+215 Sojourn` OFF/ON | File_47-local shorthand bound by File_47 §0.3; `S0` is overloaded across traditions — always read with tradition scope; do not export bare flags |
| S2 dual-reading sub-states | Co-shift reading (bracket `500`; Column B `730`) and 502-bracket reading (bracket `502`; Column B `732`) | Neither sub-state corrects the other; state the active sub-state before citing a Column B total |
| File_47 SP S1 local inclusive toggle | +1 at the Noah/Shem pivot expressing the SP's inclusive reckoning of Noah's "600th year" (author clarification, June 10 2026) | Shares its mechanism description with the SP Normal / Toggle ON family but remains a distinct label; formal identification requires a dedicated cross-file check before merging |
| Column-walk operator (A-walk / B-walk) | Summing one gap column from a fixed start year (typically Jacob `2006 BC`), closing with the P0 bracket term | Walk coordinates that do not coincide with literal nodes are ENC encoded coordinates; never replace literal rows |
| Compression operator (pair-skip) | Discarding one gap column and treating the retained series as a condensed coordinate generator | Same ENC discipline as the Column-walk |
| ENC encoded-coordinate node-class | `ENC_*` coordinates such as encoded Cainan `2366 BC`; the B-walk terminal `2736 BC` is identified by the author (June 10 2026) as the LXX Gear 1 Noah death (implied LXX Gear 1 Noah birth `3686 BC`) | ENC coordinates are not literal anchors; identifications with literal nodes restate state, node-class, tradition, and operator |
| File_47 `+215` position state | All File_47 MT tables run the `+215` position with ±2: Noah `3058/3056 BC`; Shem `2558/2556 BC`; Flood `2458/2456 BC` (`File_16` §4.3.3) | Distinct from the `File_02` Minimum-Regular position (Noah `2843/2841 BC`); the positions do not contradict |
| Cumulative-route Levi coordinate | `1936 BC` terminus of the File_47 §12.2 cumulative lock = the cumulative-route Levi coordinate (route through Aaron, born `1529 BC`; Levi band `1936/1933 BC`; lifespan `137`), author identification June 10 2026 | Full cumulative derivation controlled by `File_18` / `File_51a` |

Machine Guards registered: [FILE_47 MODE-FLAG MAPPING] (flags are bound local shorthand; tradition scope mandatory); [FILE_47 POSITION STATE] (`+215` position; no conflict with `File_02`); [FILE_47 PAIRING INVARIANTS] (decadal clustering; Column A symmetry `160/160`; statistical closure property as restated June 10 2026 — B-walk `3/6` landings, baseline ≈ `0.115`, binomial P ≈ `0.023`, `4/6` with the LXX landing at P ≈ `0.0025`; central crossroads `2166/2167 BC`; calendar and Key-23 column locks; Cainan `460`-chain `2366 − 1446 = 920`).

Anchor-carrier note: File_47's Joseph and Levi material adds derivation routes, not new anchors. Joseph `1915 BC` / `1805 BC` (dual-route via `1876 BC`) is controlled by `File_03 ENTITY_JOSEPH`; Levi death `1782 BC` and the `77 + 70 + 77 = 224` arc by `File_04`; Levi birth `1919 BC` is carried by `File_51a` and `File_20`.

Removal note: by author direction June 10 2026, the source's §4.7 (Jared 460-signature line) and the former §10.2 / §10.5 / §10.6 / §10.7 walk tables were removed at Finalization; their ENC terminal coordinates (`2326`; `2826`; `2526`; `2626`; `3126`; `3086`) match no Gear 1 Noah/Shem date in MT, SP, or LXX, and the former `2526 BC` value is identified by the author as the SP `−215` 2nd Cainan birth, not a Flood coordinate.

Candidate convention note (non-controlling until adopted): the author intends possible repository-wide adoption of the File_47 compact mode-flag notation; the File_47 §0.3 mapping table is its definition. Until a dedicated control-file action adopts it, the flags remain File_47-local.


## D.3 Anchor-Tier Convention Note (registered June 10 2026)

Style Guide v2.5 applies the date label `anchor` functionally — "only when the date functions as an anchoring node" — and distinguishes `anchor`, `endpoint`, `boundary`, `block-member`, and `mirror target` as separate roles. Two tiers follow:

1. Repository endpoint anchors: terminal nodes from which chronological chains hang (for example Creation, Flood, Exodus, birth of Christ). These populate the Restart Capsule's Primary anchors inventory where capsule-governed.
2. Derived pegged dates / local anchoring nodes: dates fixed by deduction from a higher node, which then anchor further dates within their carrier files but are not repository endpoint anchors.

Registered instance (File_47 context): `1919 BC` (Levi birth, deduced as three years after Jacob's marriage in `1922 BC`) anchors `1782 BC` (Levi death at `137`) and the `1922–1915 BC` pregnancy / purification window; `1915 BC` (Joseph birth, after `1929 BC + 7 + 7`) anchors `1805 BC` (Joseph death at `110`) and the Serug diagonal lock `2065 − 1805 = 260`; cumulative-route Levi is `1936 BC` (band `1936/1933 BC`). These five are derived pegged dates carried by File_03 (Joseph), File_04 (Levi death), File_51a / File_20 (Levi birth), and File_18 / File_51a (cumulative `1936 BC`). They are correctly absent from the Restart Capsule Primary anchors inventory; File_47 declares no new anchors.

Machine Guard [ANCHOR TIER]:
Do not promote a derived pegged date to the Restart Capsule Primary anchors inventory merely because it functions as a local anchoring node, and do not strip the `anchor` label from a derived pegged date merely because it is not an endpoint. Name the tier when ambiguity matters.

## D.4 File_38 Convention Entry (registered at Finalization, June 11 2026)

Source file: `File_38 Final — The Sextuplet Engine`. Controlling precedents: `File_36` (prime namespace: sextuplet start / active `Q` / polarity outputs / target-Mirror restraint), `File_37` (retired prime-gap boundary, §31.140–§31.140D), `File_34` (SKL / Berossus derivation), `File_16` / `File_18` (`+50` Jubilee Offset / Kings data), `File_08` (Kings dual ledger), `File_00` (`+60 Terah`, §3.4.1), `File_51a` (SOR / −215 variant field).

| State | Definition | Guard |
|---|---|---|
| Sextuplet true-center / active-`Q` separation | True center = start + 8 (`15`; `105`; `16065`; `19425`; `43785`); active `Q` = start + 4 (`101`; `16061`; `19421`; `43781`). The File_38 §1 lattice runs on true centers; the §§3, 4, 6, 7 projections run on active `Q` and its offsets | Machine Guard [SEXTUPLET TRUE CENTER / ACTIVE Q SEPARATION] registered below; both usages valid in their own sections; do not collapse |
| ±30 Zero-Center Shift operator | `Q ± 30` anchors replacing the `+5 / −25` polarity base to center on the field; generates the `+60 Terah` gap (`+30 − (−30) = 60`); File_38 §4 | Not an ordinary File_32 prime rail; creates no default `+60` rail (§31.135 analog) |
| `90 + 30 + 90` Mod 210 lattice state | True-center lattice `90 + 30 + 90 = 210`; Sojourn vector `5 + 210 = 215`; Affliction vector `−25 + 210 = 185`; `215 − 185 = 30` | Signed placement is a polarity / A-space display; no integer year 0 (File_38 Machine Guard [ZERO POINT]) |
| Historical Triad / `1661 BC` state | `1876 / 1661 / 1446 BC`; `1661 BC` carries two verified identities: sojourn midpoint of `1876/1446`, and Sojourn-215 variant Entry (`1446 + 215 = 1661`) | Identical year-label to the capsule's Baseline Entry / minimum state (215-year Egypt span); never substitute `1876` for baseline `1661`; inventoried in the capsule header as of v11.5 |
| 2520 gerah state | Mene Mene Tekel Parsin weight sum `1000 + 1000 + 20 + 500 = 2520`; shekel = 20 gerahs (Exod 30:13); 50-shekel mina = 1000 gerahs; Parsin (half-mina) = 500 | Do not substitute the Babylonian 60-shekel mina (`1200` gerahs); the `2520` result depends on the 50-shekel system the source uses |
| Asymmetric polarity-crossover research-note state | Unopened bounded research note, File_38 §5.1: the `+50` variance can also be construed at the prime level through an asymmetrical crossover of polarities at the Mirror; deliberately omitted from use | Machine Guard [ASYMMETRIC POLARITY-CROSSOVER NOTE] registered below; never load-bearing |

Machine Guards registered:

Machine Guard [SEXTUPLET TRUE CENTER / ACTIVE Q SEPARATION]:
Do not conflate sextuplet true centers (start + 8) with active `Q` values (start + 4). The File_38 `90 + 30 + 90` lattice runs on true centers; the File_38 projections run on active `Q` and its offsets. Collapsing them corrupts the arithmetic of one or the other.

Machine Guard [ASYMMETRIC POLARITY-CROSSOVER NOTE]:
Do not treat the asymmetric polarity-crossover construction as an active operator, derivation, dependency, or proof for the `+50` variance or for any other value in any file. It is unopened research-note material only (File_38 §5.1). No calculation may cite it, and it must not be expanded, rederived, or made load-bearing without explicit author direction.

Pointer note: File_38 Final §0.3 remains the controlling precedent and carries the full guard set, including [ZERO POINT], [+50 KINGDOM TERMINATION], [RETIRED WIDTH-50 FOUNDATION] (whose repository-wide boundary is already §31.140B), and [SKL / BEROSSUS DEPENDENCY].

Correction note (registered): the File_38 Final §2.3 dependency note describes `1661 BC` as "a new anchor introduced by File_38." Registration audit found `1661 BC` already capsule-governed as the Baseline Entry / minimum state (215-year Egypt span); File_38 independently rederives that established state. The §2.3 wording is superseded by this entry; a one-line post-final citation-control touch to File_38 is recommended, subject to author approval.

## D.5 File_39 Convention Entry (registered at Finalization, June 11 2026)

Source file: `File_39 Final — The Resonant Lattice and the Creation Core`. Controlling precedents: `File_38` (sextuplet lattice; true-center / active-`Q` separation; ±215 Sojourn vector boundary), `File_36` (active-`Q` namespace; OEIS A200504 citation control; P19 / P25 pillar indices), `File_32` (210-grid grammar; Key of 23; Modulo 210 corridor boundary discipline), `File_00` (Zipper namespace separation, §3.23), `File_51a` (−215 variant field boundary), `File_34` (Precessional Envelope: `25920`, `6480`, `720`), `File_05` (primary `1446 BC` Exodus anchor).

| State | Definition | Guard |
|---|---|---|
| Sextuplet Zipper operator state | `Z = (S_a + S_b + 16)/210` over consecutive OEIS A200504 record-gap S-primes; `Z × 210` is the mirrored wingspan from the first prime of the opening sextuplet to the last prime of the closing sextuplet; Day 1 (`7 → 97`) yields no integer because `7 ≢ 97 (mod 210)`; author-confirmed June 11 2026 | Machine Guard [SEXTUPLET ZIPPER DEFINITION] registered below; never the File_00 §3.23 Zipper |
| File_39 S-prime sense | An A200504 record-gap sextuplet start — a strict subsequence of sextuplet starts | Do not conflate with A022008 or the File_38 lattice nodes |
| Creation Core / Wild Phase partition state | Core = stitches 1–6 (Days 2–7); terminal S-prime `82984537` (Limit of Manufacture); Wild Phase = stitches 7–22 | Boundary reading is structural inference; quantitative support is File_39 §5 |
| Two-channel resonance metric state | `R_m = mean(dist_m)/((m − 1)/2)`; Zipper channel primary (agents `7`, `23`); grid-reduced gap channel `g = ΔS/210` control (`5`, `13`, `19`, `29`); uniform expectation `(m + 1)/(2m)` | Statistical support, not a formal probability model (§31.138 restraint analog) |
| Active-`Q` convergence (File_39) | The v13.5 "Q-prime (quintuplet start)" = active `Q = S + 4`; Center `C = S + 8` is the true center | §D.4 Machine Guard [SEXTUPLET TRUE CENTER / ACTIVE Q SEPARATION] applies |
| Wing-mode `215/5` state | `+215` outer wing = `210 + 5` (grid plus inner core); `215 − 5 = 210` | Machine Guard [WING OFFSET 215] registered below |
| Sabbath anchor inclusive-boundary state | `A = S_cap − (210 + 1) = 82984326` | Machine Guard [SABBATH ANCHOR +1] registered below |
| Precessional descent state | `A − B = 184326` (`B = 82800000`, 23-bearing block); `A − Σ = 40326` (`Σ = 3200 × 25920 = 82944000`); `40326 − 6 × 6480 = 1446 → 1446 BC` (File_05 convergence); the `(210 + 1)` adjustment aligns the cap to the precessional landing (without it `S_cap − Σ = 40537`); `40326` reads on the BC axis as the `40326 BC` base derived from the Day 7 terminal S-prime (authorial clarification June 11 2026) | All values in years; multi-unit `6480` state declared; File_34 Precessional Envelope by dependency; `40326 BC` is a Tier-2 derived base per §D.3 |
| `4830` lattice convergence | `23 × 210 = 4830`; `4830 × 70/69 = 4900`; converges with the established civil-axis `4830` states (LXX `5556 BC → 726 BC`; `4836 BC → 6 BC` base span) | Convergence, not a new state; cite the facet the local argument opens |
| A200504 dual citation | OEIS A200504 is citation-controlled in both File_36 and File_39 | Cite the file whose construction is opened |

Machine Guards registered:

Machine Guard [SEXTUPLET ZIPPER DEFINITION]:
`Z = (S_a + S_b + 16)/210` is the authoritative definition. `Z` is a function of the endpoint sum, not of the interval gap `ΔS` alone; do not model, fit, or regress `Z` from `ΔS`. Every `Z` value in File_39 regenerates from this definition plus the OEIS A200504 terms in File_39 §2.1.

Machine Guard [FILE_00 ZIPPER NAMESPACE]:
The File_00 §3.23 Zipper is the Forward Tishri Shift calendar-state locking operator. It is unrelated to the File_39 operator. Within File_39 the qualified label Sextuplet Zipper is always used; do not route File_00 Zipper queries to File_39 or File_39 queries to File_00 §3.23.

Machine Guard [WING OFFSET 215]:
The File_39 `+215` outer wing is `210 + 5` (grid plus inner core), local to File_39's wing geometry. Do not conflate it with the ±215 Sojourn vector of File_38 or the −215 variant field of File_51a, which are chronological-variant operators on the civil axis.

Machine Guard [SABBATH ANCHOR +1]:
The `(210 + 1)` subtraction is the Sextuplet Modulus (`210`) plus the Boundary Inclusivity Toggle (`+1`). The `+1` is an authorized structural offset representing the inclusive / exclusive node boundary. Do not flag the `+1` as an arithmetic error; it is a locked state condition for the Sabbath anchor.

Decision record (registered): the v13.5 Day 5 theme label `palindrome lock` was removed by author direction June 11 2026 as a likely AI-generation artifact; the v13.5 §7.2 title qualifier `3 + 7` and block label `cosmic` were removed as non-derivable; §§7.2–7.3 were provisionally removed during finalization and retained with authorial clarifiers upon author review; the FILE_40 and File_18 dependencies of the v13.5 header were removed (FILE_40 is absent from the repository and is superseded by the explicit operator plus the OEIS source; no File_18 rows are opened).

Correction record (registered): six cells of the v13.5 §4 distance table and the v13.5 factorization `232001 = 23 × 10087` (incomplete; `10087 = 7 × 11 × 131`) were corrected with Audit notes; the v13.5 §5 statistics reproduce exactly as a two-channel computation whose design was unstated.

Pointer note: File_39 Final §0 (D0–D5 definitions and the Terminology record) remains the controlling precedent and carries the full guard set and audit notes.

## D.6 File_40 Convention Entry (registered at Finalization, June 11 2026)

Source file: `File_40 Final — The Dimensional Creation (Days 1–3) and the Sextuplet Zipper Mirror Rendering`. Controlling precedents: `File_38` (Sojourn vector `Q ± 215`; P25 sojourn-vector lower output `43566 BC`), `File_36` (active-`Q` namespace; printed P3 / P19 / P25 civil rows; `63210 = 1290 × 49` bridge), `File_39` (Sextuplet Zipper `Z = (S_a + S_b + 16)/210`; Day-stitch grammar; `1470 = 7 × 210 = 3 × 490`), `File_41` (AD 535 Restoration Mirror Anchor; Machine Guard [FILE_41 AD 115 TARGET]), `File_34` (Berossus `34566 BC`; `3606 BC` field control), `File_21` (Alulim / Adam rail `268566 BC`; SKL Flood `20886 BC`), `File_32` (P3 Base Date `96 BC`; P28 `63000` state; P26 symbolic-`666` precedent).

| State | Definition | Guard |
|---|---|---|
| Day-node mapping state | File_40 Days are closing nodes (Day 1 = S97/P3 Width; Day 2 = P19 Height; Day 3 = P25 Depth); File_39 Days are stitches; Height/Depth assignments agree; the grammars are never collapsed | Author-confirmed June 11 2026 |
| Day 3 S-Date state | `43566 BC = 43781 − 215` — the File_38 P25 sojourn-vector lower output carried under the File_40-local display label "S-Date" | Machine Guard [FILE_40 S-DATE SCOPE] registered below |
| Day 1 rails-separation state | Both P3 cross-axis pairs close on one modulus: `96 + 115 − 1 = 210`; `126 + 85 − 1 = 210`; general closure `2Q + 8 = 210` holds only at `Q = 101` | §5.1 resolution, author-confirmed at Finalization; precedent File_38 §7.4 |
| Day 2 jubilee-span state | `20886 − 19416 = 1470 = 3 × 490 = 7 × 210` on printed coordinates; the source's "Priestly expansion" mechanism remains underived — open audit flag accepted at Finalization | Do not present the `1470` span as the Priestly-expansion mechanism |
| 210² Restoration lock state | `43566 + 535 − 1 = 44100 = 210 × 210` | File_41 dependency |
| Babel Mirror (+720) state | `43566 − 3606 = 39960 = 666 × 60`; `3606 BC` is File_18-displayed, File_34-controlled; `666` is a factor display, not an AD target | P26 symbolic/precise distinction not opened |
| Prophetic Seed Expansion state | `43566 − 96 = 43470 = 69 × 630`; `43470 × 70/69 = 44100`; BC-only projection `96 + 44100 = 44196 BC` (local derived value) | `44196 BC` is a Tier-2 derived value per §D.3 |
| Sextuplet Zipper Mirror-rendering state | The File_39 Day 3 stitch (`Z = 301`; wingspan `63210`) rendered with the closing wing-tip (`−43793`) on the negative rail: `\|19417\| + \|−43793\| = 63210`; Q-shift `63210 − 210 = 63000 = 300 × 210 = 175 × 360 = 50 × 1260` | Display label "Mirror Zipper" retired by author direction June 11 2026; Guards [SEXTUPLET ZIPPER DEFINITION] and [FILE_00 ZIPPER NAMESPACE] apply |
| 299 → 300 secondary-variant state | `62790 × 300/299 = 63000`; `62790 = 299 × 210`; preserved source variant, not required for the primary lock | Variant, never primary |
| +10 Mirror Artifact state | `19446 + 43565 − 1 = 63010 = 63000 + 10`; deep-time closures `34566 + 19445 − 1 = 54010` and `268566 + 19445 − 1 = 288010` carry the same signature on `× 360` bases (`150 × 360`; `800 × 360`) | Machine Guard [FILE_40 +10 ARTIFACT VALUES] registered below |

Machine Guards registered:

Machine Guard [FILE_40 S-DATE SCOPE]:
The `−215` rendering that generates the Day 3 S-Date is the File_38 Sojourn-vector operator (`Q − 215`) applied to the P25 active `Q`; within File_40 it is opened only for the Dimensional Creation constellations. Do not apply the `−215` rendering dynamically to other prime constellations. Standard prime-to-civil routing uses the `Q − 5` base rail (File_36; File_32). Do not conflate this Sojourn rendering with the File_39 wing-mode `+215` or the File_51a −215 variant field.

Machine Guard [FILE_40 +10 ARTIFACT VALUES]:
`43565` and `AD 19445` are localized boundary artifacts (each the canonical coordinate less one) used only to demonstrate the +10 mirror zippering. Do not overwrite the canonical `43566 BC` Day 3 S-Date or the File_36 P19 civil rails (`19416 BC` / `19446 BC`) with these artifact values in global routing tables. The File_36 printed AD rails (`AD 19435`; `AD 43795`) are distinct values; no collision exists.

Arrival record (registered): the §D.5 decision record "FILE_40 is absent from the repository" was true when registered; File_40 entered the repository as File_40 Final on June 11 2026. The removed FILE_40 dependency of the File_39 header remains removed; File_39's Wild Phase rows regenerate from its own operator definition and OEIS source.

Decision record (registered): the v3.8 label "sextuplet start" for `43781` corrected to active `Q` (start `43777`); the v3.8 "(S-Date Scope)" guard superseded by [FILE_40 S-DATE SCOPE] (the v3.8 attribution of the `Q − 5` base rail to FILE_42 is held for reconciliation when File_42 is mounted); the display label "Mirror Zipper" retired; the v3.8 "decimal resonance" note (`4419.6 BC`) deleted as non-supporting — all by author direction June 11 2026. Four §5.1 resolutions author-confirmed at Finalization: Day 1 rails-separation derivation; Day 2 `1470` jubilee span; `AD 115` routing through the printed File_36 P3 row; `62790 = 299 × 210`.

Pointer note: File_40 Final §0 (working state register) and §§2–5 remain the controlling precedent and carry the full guard set, resolution records, and the open Day 2 mechanism audit note.

## D.7 File_42 Convention Entry (registered at Finalization, June 12 2026)

Source file: `File_42 Final — Prime Lattice Foundations (Geometry-First Derivation of the Early Prime-Quad Anchors)`; marked Final June 12 2026; post-final Pressure Test passed June 12 2026 (batteries A–H; findings F1 / F2 applied). Repository role: geometry-first source-history support — the active Prime–Civil / Polarity Namespace routes through `File_32` / `File_33` / `File_35` / `File_36` (Machine Guard [FILE_42 SOURCE-HISTORY NON-REACTIVATION], `File_43`; `File_44` dependency table); nothing in File_42 replaces those active controls. Controlling precedents: `File_32` / `File_33` / `File_35` / `File_36` / `File_37` (active prime namespace; printed polarity rows; Key-of-23 Foundational Prime Set), `File_43` / `File_44` (source-history and label guards), `File_13` (Johannine Temple 46/50; ministry / Passion display-state controls, §31.55), `File_05` (`1446 BC → AD 27 = 1472`), `File_00` (Covenant Crossover Zero Point T₀; 8-day Birth → Naming gap), `File_09` (Julian Jan-1 Mirror-axis phase model; suffix-inversion rule; quarter-day law), `File_51a` (§16.10 1650 / 1656 Dual Backbone), `File_03` (Joshua / Conquest state), `File_38` (90 + 30 + 90 true-center lattice — boundary only), `File_20` (Cross-Mirror AD 1446 state — boundary only), Restart Capsule §1.18 (Prime Lattice Interface).

| State | Definition | Guard |
|---|---|---|
| Prime-space / rails-space / A-space separation state | Three locked spaces: prime-space signed quad starts; rails-space `B := Q − 5`, `M := Q + 25`, `M − B = 30`; A-space civil integers, no year 0 (`A ≤ 0 → (1 − A) BC`; `A ≥ 1 → A AD`); no value crosses spaces without the stated mapping rule; the File_32 Prime–Civil Interface (§31.128) is the registered active mapping | Machine Guard [FILE_42 SPACE SEPARATION] (File_42 §0.3) |
| Operator A / Operator B label scope | Operator A = P2 rails-window (`B, M`); Operator B = P2 Mod-30 A-space bridge; equals the registered P2 split (§31.133); the labels are File_42 / File_43-local P2-split labels only — never reused for ratio expansions such as `300/299` or `70/69` | File_44 guard; Machine Guard [FILE_42 P2 NON-STRADDLING] (same boundary as [FILE_43 P2 NON-STRADDLING]) |
| Early-cluster base engine | `A := 1 − B = 6 − Q`; `Civil = (Q − 5) BC` — the base rail rendered BC-side, the File_32 positive-polarity reading `B⁺ = Q − 5` (§31.128); P2 → `6 BC`; P3 → `96 BC` | Machine Guard [FILE_42 EARLY-CLUSTER BASE ENGINE] registered below |
| Positive-base bridge scope state | `A(base) + 30 = 36 − Q`, licensed only where a prime-space 30-closure exists: natively at P2 (`2Q + 8 = 30` only at `Q = 11`; output AD 25, coinciding with `N⁻₂ = Q + 14`) and by Key-of-23 restoration at P1 (`5 − (−25) = 30`; output AD 31) | Machine Guard [POSITIVE-BASE BRIDGE SCOPE] registered below |
| AD 31 geometry-output state | AD 31 is a prime-geometry outcome converted to a civil date (`A(AD 1) = +1`; `+1 + 30 = +31`), met by the Johannine `+50` display from the `20 BC` field center; not a ministry / Passion node; hinge-member pairing: the BC hinge member gives AD 30 (`0 + 30`; the established about-age-30 display), the AD member gives AD 31 — at the midnight datum the forward window boundary is one instant carrying both year-labels (end of AD 30 / start of AD 31) | Machine Guard [AD 31 GEOMETRY OUTPUT] registered below; `AD 31` is a new control-file token (novelty-verification record below) |
| AD 26t / AD 27n phase-resolved ministry-start members | Pair standing `3.5` / `3` years before an AD 30n crucifixion (`26t → 30n = 3.5`; `27n → 30n = 3`), in parallel to AD 29t / AD 30n standing `3.5` / `3` years before the optionally preferred AD 33n crucifixion (`29t → 33n = 3.5`; `30n → 33n = 3`); members of the File_13 / File_31 ministry / Passion display field (§31.55) | New tokens (novelty-verification record below); author-confirmed June 11 2026 |
| P1 midnight-datum phase state | P1 attaches to T₀ = midnight, December 31, 1 BC → January 1, AD 1 (File_00 Covenant Crossover Zero Point); P2 onward attaches to Nisan; `6n BC` primary / `7t BC` secondary for the P2 birth anchor, pair mean at the December 31, 7 BC / January 1, 6 BC boundary (File_09 Julian Jan-1 Mirror-axis phase model); P1 phase shift Nisan → midnight datum = 3 months = 90 days | File_00; File_09; author direction June 12 2026 |
| Midnight-datum boundary rule | At midnight phase a year-label denotes its opening boundary; Mirror activation negates the offset from T₀; boundary labels invert numeral order across the Mirror; coordinate relation: opening-boundary offset = `A − 1`; boundary offsets are instants — A-space cell coordinates remain the computational engine | File_42 §5A.5 (pressure-test finding F1) |
| 6 BC reading-duality state | Birth-reading registered and controlling (Capsule §1.18; File_36); conception-reading is a File_42 typological layer (`6n BC + 270` days lands at the close of 6 BC, 5 years before the tradition-anchored chain at `1n BC`) | Machine Guard [6 BC READING DUALITY] registered below |
| Mirror conception–birth inversion state | Reflection through T₀ negates offsets and swaps t/n phases (File_09 suffix-inversion rule): `7t/6n BC ↔ AD 6t/7n`; every directed arc reverses, so the two readings of the 6 BC duality are exchanged across the Mirror; John 1:15, 30 carried as typological / theological note | File_42 §5C.5 |
| Tri-scale 90 + 30 + 90 state | `90 + 30 + 90 = 210` at unit scale (S1 / S2 true centers `±15` / `±105`), year scale (`96 BC → 6 BC → AD 25 → AD 115`; closure `96 + 115 − 1 = 210`; central 30 = the P2 bridge `6 + 25 − 1 = 30`), and day scale (leap-extended straddle `180 + 30 = 210` days; bare half-year 180; Julian check Jan 1 → Apr 1 = 90 days) | File_38 / File_40 / File_09 dependencies; [SEXTUPLET TRUE CENTER / ACTIVE Q SEPARATION] respected |
| Sextuplet-onset ±2 window state | S1 onset `7 = 5 + 2`; boundaries `3/2 BC ↔ AD 2/3` about T₀, numeral-inverted; Matthew 2:16 Herod reading carried as typological note; the historical layer is state-separated — the registered `6 BC` birth anchor is controlling; adjacent capsule state: the §7.4 Herod-window micro-scale `720` prophetic-day display (distinct mechanism; not merged) | File_42 §5B |
| Nativity day-scale rendering state | Quarter-day law `0.25 d = 6 h ↔ 0.25 y`: `7t BC` → December 25, 6 PM; `6n BC` → December 26, 6 AM (1 BC); member-spans `6.25` / `5.75`, mean `6`; December 25, 1 BC is reception-history, not a birth-date claim | File_42 §5C.1; File_09 quarter law |
| Polarity-gap ladder state | `2Q + 8` (positive base to `N⁻₂` mirror, cross-axis): 30 (P2, `Q = 11`); 210 (P3, `Q = 101`); 390 (P4, `Q = 191`); 1650 (P5, `Q = 821`); increments `2 × ΔQ` (`+180`; `+180`; `+1260 = 2 × 630`); `390 = 13 × 30` embolismic year | File_42 §5D |
| 1650-spine intersection state | P5 gap `1650` ∩ File_51a §16.10 Creation-Week module: `806 + 1650 = 2456`; `816 / 846 / 806 / 836 + 3300 = 4116 / 4146 / 4106 / 4136`; `806 / 836 + 13200 = 14006 / 14036`; `14006 − 4106 = 9900 = 6 × 1650`; `806 / 836` are midnight-inverted counterparts of the P5 AD values (AD 805 / AD 835; BC/AD counterpart display-label state) | File_51a §16.10 dependency; reciprocal registration File_51a §16.10.6 (cross-file update June 12 2026) |
| 2070 seed-span state | `6n BC → T₀` = `5 × 360 + 270 = 5.75 × 360 = 2070` days; `× 70/69 = 2100` (+30, the intercalary month); `× 25/23 = 2250` (+180; start pushed to `7t BC`); `30 + 180 = 210` composes the ladder step | Convergence with Capsule §7.4 (Micro Scale: Christological Ground Zero), which carries `5.75 × 360 = 2070` — registered as convergence, not novelty |
| P8 correspondence state | `ΔQ(P2 → P8) = 2081 − 11 = 2070`; printed P8 row `2076 BC / 2106 BC / AD 2065 / AD 2095` | Convergence with Capsule §7.4 (“P8 at micro level: 2070 prophetic days”) and the printed File_32 / File_33 / File_35 namespace |
| 40-year Mirror-gap law state | `(Q + 25) − (Q − 15) = 40` for every quad with `N⁻₁` on the AD side (P3 onward); verified at P3 (`126 − 86`), P4 (`216 − 176`), P5 (`846 − 806`), P8 (`2106 − 2066`); at P8: Noah’s death `2106 BC` → Isaac’s birth `2066 BC` | P1 / P2 lie outside the law’s regime (`N⁻₁` BC-side) |
| Revelation 12 1260 P5–P8 span state | `ΔQ(P5 → P8) = 2081 − 821 = 1260` (distinct from the ladder increment `+1260 = 2 × 630`; File_42 finding F2); `{2106, 2066} − 1260 = {846, 806}`; the wilderness pair `1446–1406 BC` decomposes both lines `660 + 600` | Registered `1260` operator (File_13 / File_16 / File_17); Capsule §7.4 micro-scale `1260` line convergent in subject, distinct in mechanism |
| P6 Joshua mid-split state | `B⁺(P6) = 1481 − 5 = 1476 BC` = the registered derived Joshua birth node (`1406 + 70`; File_03); `ΔQ(P5 → P6) = 660`; `ΔQ(P6 → P8) = 600`; the split stands 30 off the exact half `630` | File_03 dependency |
| Terah +60 macro-mirror display state | `30 BC ↔ AD 31` window: boundary-to-boundary span 60; the covenant-Mirror display `30 BC ↔ AD 30` is the same window (start-to-start cell reading `30 + 30 − 1 = 59`); the file’s preserved thesis identifies this macro-mirror window as the source of the established `+60` Terah variant — structural inference; the `+60` variant itself is established (§31.83; Capsule MT `+60` states) | Named macro-Mirror display, not an ordinary prime rail (§31.135 boundary) |

States opened by dependency only (controlling registrations unchanged): Key-of-23 Foundational Prime Set (§31.140A; File_37); Key-of-23 completion (File_36 §2.2; File_33; File_37); P1 Exceptional Origin (File_32; File_36); Herodian Temple field (File_33 P47); Johannine Temple 46/50 (File_13); Exodus 1472 → 1600 (File_05); Genealogical MirrorVolume and Seal700 (File_43; §§31.57b–31.57e — guarded locally by Machine Guard [FILE_42 MATTSPAN PROVENANCE], the same boundary as [FILE_43 SPAN SPLIT] / §31.57d).

Machine Guards registered:

Machine Guard [FILE_42 EARLY-CLUSTER BASE ENGINE]:
For programmatic generation of A-space coordinates for early-cluster bases, always use the definitive formula `A := 1 − B`. The recursive differencing demonstration (`A(P2) − 90`) is an illustrative proof of geometric continuity, but the absolute formula `A = 1 − (Q − 5)` is the required computational engine to prevent propagation drift in loops.

Machine Guard [AD 31 GEOMETRY OUTPUT]:
AD 31 in this file is a prime-geometry outcome converted to a civil date: the restored Mod-30 spacing at the P1 singularity rendered from the AD member of the `1 BC / AD 1` covenant hinge (`A = +1`; `+1 + 30 = +31`), met again by the Johannine `+50` display from the `20 BC` field center (§4.2). It is not a new consequential node for the ministry or the death of Christ. The Christological ministry / Passion field remains AD 26t, AD 27n, AD 29t, AD 30n, AD 33n (and AD 35n to complete a generation), depending on an AD 30 or especially an AD 33 Friday crucifixion, under File_13 / File_31 display-state controls (Register 31.55). The phase-resolved members pair as ministry-start fields: AD 26t / AD 27n stand `3.5` / `3` years before an AD 30n crucifixion (`26t → 30n = 3.5`; `27n → 30n = 3`), in parallel to AD 29t / AD 30n standing `3.5` / `3` years before the optionally preferred AD 33n crucifixion (`29t → 33n = 3.5`; `30n → 33n = 3`). Do not export AD 31 into that field. (Author direction June 11 2026; phase-resolved members confirmed by author June 11 2026.)

Machine Guard [POSITIVE-BASE BRIDGE SCOPE]:
The `+30` positive-base bridge (`A(base) + 30 = 36 − Q`) exists only where a prime-space 30-closure exists: natively at P2 (`2Q + 8 = 30` only at `Q = 11`; output AD 25, coinciding with `N⁻₂ = Q + 14`) and by Key-of-23 restoration at P1 (`5 − (−25) = 30`; output AD 31). The polarity closure for `Q > 11` is `2Q + 8` (210; 390; 1650; …), never 30. Do not apply the positive-base bridge to P3 onward; values such as `66 BC`, `156 BC`, `786 BC` are unlicensed and must not be generated.

Machine Guard [6 BC READING DUALITY]:
The registered and controlling label for `6 BC` is the P2 birth anchor (Capsule §1.18; File_36; §2.2 above). A conception-reading exists as a File_42 typological layer and does not replace the registered label. Under it: `6n BC + 270 days` (`9 × 30`) lands at the close of 6 BC, exactly 5 years (`6n → 1n` same-side: `6 − 1 = 5`) before the tradition-anchored chain at `1n BC` (Annunciation; March 25 + 9 calendar months = December 25 exact; schematic April 1 + 270 = January 1, the ~6-day slack absorbed by the 8-day Birth → Naming window). Whether `6n BC` denotes birth or conception is left open — the ambiguity may be by design; only the December 25, 1 BC tradition is certain, as tradition. Do not export the conception-reading as a replacement of the birth-anchor label. (Author direction, June 12 2026.)

Guard-scope note: the section references inside the verbatim guard texts above (§4.2; §2.2; §5) resolve within `File_42 Final`. The local guards [FILE_42 SPACE SEPARATION], [FILE_42 P2 NON-STRADDLING], [FILE_42 P1 NON-INHERITANCE], and [FILE_42 MATTSPAN PROVENANCE] remain defined in File_42 §§0.3–7; each restates a boundary already registered (§31.128 mapping; §31.133 split; File_32 / File_36 P1 displays; §31.57d span provenance).

Reconciliation record (registered; closes the §D.6 hold): the §D.6 decision record held the v3.8 attribution of the `Q − 5` base rail to FILE_42 “for reconciliation when File_42 is mounted.” File_42 entered the repository as File_42 Final on June 12 2026, and the hold is closed as convergence: `B := Q − 5` in File_42 §0.3 is the geometry-first source-history statement of the same base rail that Restart Capsule §1.18 carries as `B_n := Q_n − 5` (Base / Physical) and that the File_32 / File_36 active namespace carries as `B⁺ = Q − 5` (§31.128). No competing origin exists. Active prime-to-civil routing is unchanged: standard routing remains the `Q − 5` base rail through File_36 / File_32, exactly as Machine Guard [FILE_40 S-DATE SCOPE] states; the §D.6 guard text requires no edit.

Novelty-verification record (Procedures §18; exhaustive token search of Restart Capsule v11.7 and Register v1.6, June 12 2026): `AD 31`, `26t`, `27n`, `AD 26`, and `midnight` return zero hits in both control files — registered here as new control-file tokens; `2070` is found capsule-governed at Capsule §7.4 (Micro Scale: Christological Ground Zero; `5.75 × 360 = 2070` days; “P8 at micro level”) — the 2070 seed-span and P8 correspondence states are therefore registered as convergence with §7.4, not novelty; `1650` appears in the Capsule only as the Appendix A.3 inverse constant (`1650 ↦ 5610`), unrelated to the polarity gap — the 1650 backbone control is File_51a §16.10; `4146` is established (Capsule AdamApp30 marker `[4146 → 4116] BC`; §31 File_34 SKL `+1260` display) — no creation-field novelty.

Decision record (registered): AD 31 classified as prime-geometry output converted to a civil date, not a ministry / Passion node (June 11 2026); AD 26t / AD 27n included as phase-resolved ministry-start members (June 11 2026); the second ministry pair rendered `29t/30n → AD 33n`, author-confirmed (June 11 2026); §5 wording adjusted so the established `+60` Terah variant is not presented as foreign to repository claims (June 11 2026); June 12 2026 author directions: midnight-datum phase protocol adopted (P1 Julian New Year; P2 onward Nisan); AD 31 / AD 30 unified as dual labels of one boundary instant; §§5A–5F composed under author direction (tri-scale structure; ±2 onset window; nativity structure with the reading duality and the §5C.5 Johannine Mirror inversion; polarity-gap ladder and 1650 spine; 2070 seed-span, P8 correspondence, and the Noah → Isaac 40-year Mirror-gap law; Revelation 12 1260 span with the P6 Joshua split); author-message corrections `1866 → 2066` and day → year applied under §5.1 assumed affirmation and logged in File_42 §5F.

Correction record (registered): the sole logged source correction of the File_42 revision — the v10.1 source guard referenced “the A-space continuity rule defined in §1.4,” a dangling pointer (no §1.4 existed in the source); repaired under Procedures §5.1 to “the A-space continuity rule of this file,” carried in Machine Guard [FILE_42 SPACE SEPARATION].

Pointer note: File_42 Final §0 (working state register and §0.3 phase protocol) and §§5A–5F remain the controlling precedent and carry the full guard set, the resolution records, and the post-final Pressure Test record (batteries A–H; findings F1 / F2 applied; verdict pass).

## D.8 File_48 Convention Entry — Cumulative Prime Harmonic (condensed)

Registered at File_48 finalization (June 12 2026). File_48 v5.0C is Final; condensed edition. The entry registers
only File_48-specific states. The Formation Wobble is not redefined here: it is already registered at §31.138 and
in the File_00 Formation-Wobble statistical-scan state, both File_32-controlled, and File_48 uses it by dependency.

Novelty-verification record (Procedures §18; token search of Register v1.7 and Restart Capsule v11.7, June 12 2026):
`Formation Wobble`, `pairwise k-difference`, and the values `+50.669` / `+153.333` return hits at §31.138 and the
File_00 scan-state — registered below as convergence with §31.138, not novelty. `floor rule`, `membership
observation`, and the File_48 statistical-candor cluster return zero hits as File_48-keyed states — registered as
new File_48 entries. `Recomputed conditional probability calculation` exists as a File_33 label; File_48 uses it by
reference, not redefinition. `mod 210` / `mod-210` corridor language exists at the File_00 prime-quad center-corridor
state (`15`, `105`, `195 mod 210`); File_48's residue-class observation concerns quadruplet START classes (`11`,
`101`, `191`) and is recorded below as a File_32-scope cross-file note, not a redefinition of the corridor state.

### D.8.1 File_48 statistical-candor control cluster

The §2.0 control cluster governing every probability statement in File_48:
- Floor rule: a probability of the form `k/n` is an exact-enumeration rank — the observed configuration scored at
  least as high as `k` of the `n` decadal translations, including itself. `1/n` means unique global maximum, the
  resolution limit of the test, not a free-standing chance estimate.
- Conditionality: every File_48 probability is conditional on the stated target list, cycle set, rail configuration,
  list range, hub, and null convention, all chosen with the data visible.
- Documented sensitivity: the null-convention choice (Q-shift vs rigid) moves results roughly 2×; the published §2.2
  configuration moves from rank 4 of 31500 to rank 1033 of 441000 (`1 in 7875` to `1 in 427`) across documented
  alternatives.
- Documented multiplicity: across the 12 recorded §2.4 combinations, exact max-statistic correction moves the best
  nominal `1 in 2208` to `1 in 675` — a ceiling on the documented dimensions only, not a floor.
- Target-curation disclosure: documented in-sample forks (Methuselah dual node; Isaac excluded; 17→23 expansion;
  begetting ages only when divisible by 10; three-node expansion defined after the P13 observation) cannot be priced
  in; accordingly File_48 claims no unconditional probability anywhere.

Claim-status: methodological control state. Scope: File_48 only. Do not generalize the cluster to other files unless
a controlling dependency explicitly broadens it.

### D.8.2 File_48 Formation-Wobble dependency (convergence with §31.138)

File_48 §3.10 carries the 23-Wobble as a finding controlled by Final File_32, not locally derived. The values
converge with §31.138 (P23 `+50.669%` over n = `10`–`30`; peak `+153.333%` at n = `10`; baseline by n ≈ `48`).
File_48 adds an early-window reading — enrichment concentrated across the P1–P12 band where File_48's cumulative
sums are built, peaking near n = `9`–`10`, decaying to baseline by n ≈ `48` — carried as structural inference
dependent on the File_32 metric, not as a new state. The former File_48 §4.4.2 adjacent-gap phrasing was a wording
error (adjacent-gap divisibility by 23 sits at baseline) and is corrected to the pairwise k-difference metric; it is
not a competing measurement.

Machine Guard [FILE_48 WOBBLE DEPENDENCY]:
File_48 is not a second authority for Formation Wobble values. Cite Final File_32 (and §31.138) for any wobble
figure. File_48's early-window/decay reading is a File_32-dependent structural inference; do not promote it to a
standalone statistical state, and do not treat the corrected §4.4.2 adjacent-gap note as an alternative metric.

### D.8.3 File_48 membership-observation state

The structural-inference observation that the quadruplets the chronology projects into the Genesis-derived range
(`14466 BC` down to `1406 BC`) are the same low-ordinal set that carries the 23-enrichment: regular `Q − 5`
projection P2–P13; cumulative-sum projection P2–P10. This is an observation about shared membership of the early
quadruplets, not a claim that the date-spans between them are divisible by 23 (they generally are not; e.g.
`1866 → 12996 BC = 11130`).

Claim-status: structural inference; not load-bearing. Do not export this as a date-span divisibility claim or as
evidence of authorial design; it is a membership coincidence at the start of the sequence, left for the reader.

### D.8.4 File_48 condensed-edition state

File_48 v5.0C is a condensed edition. The v4.0 source — both analysis scripts, the run log, the full §§3–9
homiletic prose, the §10 constants glossary, §11, and the P17–P30 three-node rows — is the archived narrative
reference (routed to the Repository_Change_Archive at finalization). Statistical content carried into v5.0C was
recomputed by exact enumeration at the 2026-06-12 revision. The `Recomputed conditional probability calculation`
label (File_33 definition) governs the §2.2 and §2.3 reproduced p-values. The source labels `[ESTABLISHED]` /
`[LOCKED]` are retained as source-retained theorem-status labels only.

### D.8.5 Cross-file note — mod-210 residue classes (File_32-scope; recorded, not resolved)

File_48 records under its Recommended cross-file updates that prime-quadruple START values occupy exactly three
residue classes mod 210 (`11`, `101`, `191`) across the full tested range, with no post-P103 transition in start
residues. This bears on any file wording that describes a mod-210 "settling" beginning after P103. It is recorded
here for reconciliation when File_32 is next opened; it is not resolved in the Register and does not alter the
File_00 prime-quad center-corridor state (`15`, `105`, `195 mod 210`), which concerns corridor centers, not start
classes.

## D.9 File_53 Convention Entry — BJ 22-Fold Creation-Genealogy Witness

Registered after File_53 Finalization and post-final Pressure Test (June 18 2026). Source file: `File_53 Final; post-final pressure tested — The Book of Jubilees as a 22-Fold Creation-Genealogy Witness`.

Register-scope note: This entry registers File_53-specific modal-state vocabulary and Machine Guards. It does not revise BJ chronology, File_20's BJ fourth-witness controls, File_51c's macro-rounded witness state, File_54's comparative Luke / BJ state, or the Restart Capsule's File_53 summary. No Style Guide or Restart Capsule update is required from File_53.

Exact-token registration record (Procedures §18; mechanical string search of Register v1.8 and Restart Capsule v11.8, June 18 2026): `File_53` was already present in both controls before this registration (Register 2; Capsule 13). `Cainan expunged` and `Cainan-expunged` were Capsule-governed terms before this registration (Capsule 5 and 2; Register 0 and 0). `Kainan 57` / `57-year` were Capsule-governed before this registration (Capsule 1 and 2; Register 0 and 0). The eight File_53 Machine Guard labels listed in §D.9.4 returned zero hits in both controls before this registration. They are registered here as File_53 guards without altering existing Capsule chronology or generic Register definitions.

### D.9.1 File_53 working state register

The following File_53 working state register is registered as the File_53 local state vocabulary. These states are simultaneous, non-competing working states unless a local section explicitly narrows the working state.

The following states are active in `File_53`. They are simultaneous, non-competing working states unless a local section explicitly narrows the working state.

| State | Primary value or form | Node-class / function | Handling in `File_53` |
|---|---:|---|---|
| BJ / Jubilees witness state | Book of Jubilees | comparative chronological witness | Not canonical Hebrew inspiration; preserves possible chronological and symbolic insight. |
| BJ 22-fold Creation-genealogy state | `22 works` / `22 heads` | internal symbolic architecture | Primary BJ structure. |
| Cainan retained state | Kainan present in Jubilees 8 | historical / narrative ledger | Explains survival of Watcher astral lore after the Flood. |
| Cainan expunged state | Cainan omitted from sacred 22-count | theological / covenantal ledger | Preserves the 22-fold creation / genealogy symmetry. |
| 13th solar-position state | Cainan / Shelah / Sun | calendrical-theological position | Cainan corrupts the luminary position; Shelah receives the purified position. |
| Kainan 57-year compression state | `2482 BC → 2425 BC = 57` | possible chronological expunction | Opens secondary compressed ledger; does not replace primary trunk. |
| Secondary compressed ledger | `3799 BC → 1399 BC = 2400` | Cainan-expunged ledger | Subordinate to the primary `2450` Fall-to-rest trunk. |
| BJ Fall-to-rest trunk | `3849 BC → 1399 BC = 2450` | primary theological frame | Eden ruined after seven years to land-rest after seven-year Conquest threshold. |
| BJ Creation-to-Conquest trunk | `3856 BC → 1406 BC = 2450` | companion `File_51c` trunk | Retained as BJ macro-rounded witness; not the operative Fall-to-rest refinement in this file. |
| BJ Creation-week dual-state protocol | `3863–3856 BC`; `3861–3854 BC` | endpoint model and Adam-Day model | Both models must be preserved. |
| BJ Adam-Day +30 overlay | `3891–3884 BC`; single anchor `3886 BC` | apparent-age overlay | Local overlay; not a replacement for BJ Creation-week states. |
| SP cumulative state | SP cumulative Creation / Flood / Shem nodes | cumulative comparison | Opened only in cumulative synchronization sections. |
| MT cumulative state | MT cumulative Flood / Shem / Creation nodes | cumulative comparison | Opened only in cumulative synchronization sections. |
| LXX cumulative state | LXX cumulative Creation nodes | cumulative comparison | Opened only in cumulative synchronization sections. |
| Restored 2nd Cainan state | `+460` | variant / cumulative restoration | Does not overwrite omitted / expunged states. |
| Key-of-23 Priestly expansion state | `25/23` | exact-ratio expansion | Do not decimalize. |
| Prophetic expansion state | `70/69` | exact-ratio expansion | Do not decimalize. |
| Mirror corroboration state | BC / AD stress-test | secondary support | Never primary proof. |
| Providential / over-intended synchronization state | repository-level convergence | theological / providential meaning | Must not be promoted to likely BJ authorial intent. |

### D.9.2 File_53 section-local routing states

The following section-local state labels appear in File_53 outside the working state register. They are registered for retrieval and routing; they do not override the controlling files named in File_53 §0 and in the Restart Capsule.

| State / label | File_53 function | Control boundary |
|---|---|---|
| Cainan retained / Cainan expunged two-ledger state | Composite label for the historical / narrative and theological / covenantal ledgers | Use Machine Guard [FILE_53 CAINAN LEDGER NON-COLLAPSE]. |
| secondary Cainan-expunged ledger | Local label for the compressed Cainan-expunged field | Subordinate to the primary `2450` trunk. |
| Jared / Watcher comparison state | Comparative Watcher-lore field linking BJ Jared and cumulative spans | Providential synchronization unless a local section narrows the claim. |
| BJ / Jubilees calendar state | Local 364-day and 7/13 calendar framing | Does not redefine File_17 or File_20 calendar controls. |
| 7/13 harmonic weave state by repository dependency | File_53 use of the 7/13 grammar | Dependency-controlled; not redefined here. |
| BJ Abraham chronology state | BJ Abrahamic chronology and land-token frame | File_01 / File_20 / Genesis 15 controls remain active where opened. |
| Genesis 15 bounded interpretive field | Local Abraham / Sarah / land-token interpretation | Theological and structural reading; not a replacement for base chronology. |
| Levi hidden-23rd-generation state | Levi becomes the hidden 23rd generation after Cainan expunction | Supports the priestly `23 → 25` field; does not alter the 22-count where Jacob remains #22. |
| BJ Flood week state / BJ Flood year state | BJ Flood comparisons in cumulative and Mirror sections | Use only within the active BJ / cumulative / Mirror state. |
| BJ Flood typology | Flood / dove / Christological gestation comparison | Typological reading by File_13 / File_14 comparison. |
| File_53-local `14926 BC` derived ladder display | Local Mirror-ladder display retained under dependency note | Not a repository-scope primary anchor unless explicitly promoted by author direction. |
| Mirror secondary-support state | Part III proof-burden boundary | Use Machine Guard [FILE_53 MIRROR SECONDARY SUPPORT]. |

### D.9.3 File_53 claim hierarchy and burden rule

File_53 uses the following interpretive hierarchy:

| Level | Function |
|---|---|
| Textually explicit | Directly stated in Jubilees |
| Likely authorial | Strongly implied by BJ's structure and ideology |
| Plausibly authorial | Fits BJ's calendrical / genealogical grammar but is not explicit |
| Providential / over-intended | Coherent beyond likely authorial awareness |
| Mirror corroboration | Secondary stress-test only; never primary proof |

Claim-burden rule: the primary proof-burden remains the BJ internal architecture of 22 works / 22 heads, Cainan retained historically but expunged covenantally, the 13th solar-position logic, and the Fall-to-rest trunk. Mirror calculations, Key-of-23 expansions, cumulative MT/SP/LXX synchronizations, and Christological typology may corroborate or explain the structure, but they do not carry the primary proof-burden for likely BJ authorial design.

### D.9.4 File_53 Machine Guards

Eight Machine Guard entries are present in the pressure-tested File_53 source. The local `BJ CREATION-WEEK DUAL STATE` guard restates the file-level BJ Creation-week dual-state guard at the section where the state becomes active; it is counted separately because it appears as a separate Machine Guard block in File_53.

Machine Guard [FILE_53 CAINAN LEDGER NON-COLLAPSE]:
Cainan retained and Cainan expunged are simultaneous, non-competing ledgers. The historical / narrative ledger preserves Cainan; the theological / covenantal ledger omits Cainan from the sacred 22-count. Do not collapse the retained state into the expunged state or the expunged state into a denial of Cainan's historical function.

Machine Guard [FILE_53 COMPRESSED LEDGER NON-SUBSTITUTION]:
The Cainan-expunged `2400` field is a secondary compressed ledger. It does not replace the primary BJ Fall-to-rest trunk `3849 BC → 1399 BC = 2450`, nor the `File_51c` Creation-to-Conquest trunk `3856 BC → 1406 BC = 2450`.

Machine Guard [FILE_53 BJ CREATION-WEEK DUAL STATE]:
Do not flatten the two BJ Creation-week models into one state. The endpoint model is `3863–3856 BC`; the Adam-Day model is `3861–3854 BC`; the Adam-Day `+30` overlay is `3891–3884 BC` with single anchor `3886 BC`.

Machine Guard [FILE_53 MIRROR HIERARCHY]:
Mirror calculations in Part III are secondary stress-tests. They may corroborate a pattern already grounded in BJ's textual and structural field, but they must not be used as the primary proof of BJ authorial intent.

Machine Guard [FILE_53 CROSS-AXIS OPERATOR]:
Where a BC/AD cross-axis span is measured as a civil span, use `BC + AD − 1`. A-space / Protocol 1 Mirror labels must remain distinct from same-side BC subtraction and from ordinary civil cross-axis measurement.

Machine Guard [FILE_53 49/50 NON-COLLAPSE]:
BJ literal jubilees remain 49 years. Macro-50 structure may be opened where the file explicitly invokes `2450 = 50 × 49` or `2500 = 50 × 50`, but the macro-50 register does not erase the literal 49-register.

Machine Guard [BJ CREATION-WEEK DUAL STATE]:
Do not flatten these two BJ Creation-week models into one state.

Machine Guard [FILE_53 MIRROR SECONDARY SUPPORT]:
Do not treat Part III as the proof-burden carrier for the file. The primary proof-burden remains the BJ internal architecture of 22 works / 22 heads, Cainan retained historically but expunged covenantally, and the Fall-to-rest trunk.

### D.9.5 File_53 pressure-test count correction

Pressure-test record: File_53 post-final Pressure Test passed on June 18 2026. The only publication-level correction was the Revision Log count of Machine Guards: the Final file contained eight Machine Guard entries, while the Revision Log and cross-file update line said seven. The pressure-tested File_53 source corrected the count to eight. This correction did not create, delete, rewrite, merge, or reinterpret any Machine Guard.

### D.9.6 Pointer note

File_53 Final; post-final pressure tested remains the controlling source for full argument context, calculations, state transitions, dependency notes, and local claim-status labels. This Register entry is a routing and guard-control entry only. The Repository_Change_Archive update for File_53 remains pending by author direction and will be handled as a separate full replacement.

## D.10 File_50c Convention Entry — Bipartite Grid Landing and SKL/Berossus Naturalization

Registered after File_50c Finalization and post-final Pressure Test (June 18 2026). Source file: `File_50c Final; post-final pressure tested — The Bipartite Grid Landing and SKL/Berossus Naturalization`.

Register-scope note: This entry registers File_50c-specific modal-state vocabulary, the executable exact-strike inventory, hit-cardinality vocabulary, corrected scan-state totals, the source-parenthetical admission state, coordinate-family compression, the half-step tie convention note, and File_50c Machine Guards. It does not revise File_34's SKL/Berossus derivation, File_32 / File_33 prime-polarity outputs, File_21's SKL Unified Protocol, File_41's Jachin state by Register dependency, or Restart Capsule formulas.

Exact-token registration record (Procedures §18; mechanical string search of Register v1.9 and Restart Capsule v11.8, June 18 2026): `Bipartite Grid Landing` was already present in both controls before this registration (Capsule 5; Register 2). `0-Strike` was Capsule-governed before this registration (Capsule 1; Register 0). `Concave Mirror` was Capsule-governed before this registration (Capsule 1; Register 0). `Dual-Polarity 0-Strike`, `Sparsity Proof`, and `Cluster Compression` returned zero hits in both controls before this registration. They are registered here as File_50c-local vocabulary without altering existing Capsule formulas or generic Register definitions.

### D.10.1 File_50c working state register

The following File_50c working state register is registered as the File_50c local state vocabulary. These states are simultaneous, non-competing working states unless a local section explicitly narrows the active state.

| State | Function in `File_50c` | Local handling |
|---|---|---|
| File_50a naturalized-grid state | Supplies `P_nat` and `V_90` | Input state; not a File_32 polarity output. |
| P2 naturalization-zero / Grid Origin state | `P2 = 11`; `P_nat = 0`; civil grid datum `6 BC` | Mathematical origin of the ordinary grid operator. |
| P1 no-naturalization bypass / Civil Datum state | Returns civil `AD 1` | Exceptional state; do not execute through the ordinary Base-90 operator. |
| Bipartite negative-catch state | `R ∈ [−360,0)` | Signed target pair `{M, M − 720}` before civil conversion. |
| Bipartite positive-catch state | `R ∈ (0,+360]` | Signed target pair `{M, M + 720}` before civil conversion. |
| Exact-strike signed tri-position state | `R = 0` | Signed target positions `{M, M − 720, M + 720}` before absolute-value conversion. |
| `+720 scale-state` / rigid `±720` bracket | Cosmic SKL / Berossus bracket | Distinct from the local SKL `+2` rail. |
| Sumerian A / SKL Short | Whole-year `17980` state; physical beginning `20886 BC` by dependency | Current term for source `SKL-1` / `List 1 — Standard`. |
| Sumerian B / SKL Long | Whole-year `24510` state; physical beginning `27366 BC` by dependency | Current term for source `SKL-2` / `List 2 — +6480 Variant`. |
| Berossus List A | Berossus target matrix | Textual names and source-retained list identity; coordinates remain computed states. |
| SKL-superimposed Berossus framework | Source `Berossus-B` target matrix | Comparative framework; not a second independent textual source unless separately source-controlled. |
| Biblical restoration-anchor state | `536 BC` | Historical restoration anchor; distinct from `486 BC`. |
| Berossus computational-anchor state | `486 BC` | Figurative / computational projection anchor; distinct from `536 BC`. |
| Source-parenthetical admission state | Parenthetical coordinates in the two Berossus end-of-23-kings rows | Admitted in the File_50c scan because the source P12 and P14–P16 assignments depend on them; their derivation remains dependency-controlled. |
| Coordinate-family target-identity state | One named matrix row and its full admitted rail family; coordinate-identical duplicate rows collapse | Controls the `28 → 16` cluster compression. |
| Target-hit multiplicity state | `Single`, `Dual`, `Double Dual`, contiguous cluster, and `Outer Clamp` | `Single` and `Dual` count admitted coordinate intersections; cluster and clamp labels describe structural grouping. |
| Statistical scan state | P1–P100 target-intersection count and coordinate-family compression | Corrected executable result: `72` silent, `28` hitting, `16` structural targets; boundary `22 → 11`; intermediate `6 → 5`. |
| P23 Naturalized Grid meta-anchor state | File_50a civil landing `31686 BC` | Distinct from File_32 / File_33 P23 positive- and negative-polarity outputs. |
| Jachin Projective Boundary / Temple-Time Pillar state | `AD 29515` | Structural Pillar coordinate, not an ordinary historical anchor. |
| Concave Mirror / structural meta-anchor state | Relates `31686 BC` and `AD 29515` | Not automatically Protocol 1, File_36 target-Mirror, or inverse-number logic. |
| Textual-name / computed-coordinate firewall | Separates ancient names from repository-generated dates | Names and list identities are source material; civil target coordinates are computations. |

### D.10.2 File_50c operator and scan-state controls

File_50c uses the File_50a input values:

$$P_{nat}=P-11.$$

$$V_{90}=round(P_{nat}/90)\times90.$$

The Bipartite Grid Landing operator is:

$$G=-V_{90}.$$

$$M=round(G/720)\times720.$$

$$R=G-M.$$

| Condition | Signed target-space output | Civil-coordinate conversion |
|---|---|---|
| `R ∈ [−360,0)` | `{M, M − 720}` | `Civil BC = \|Target\| + 6` |
| `R ∈ (0,+360]` | `{M, M + 720}` | `Civil BC = \|Target\| + 6` |
| `R = 0` | `{M, M − 720, M + 720}` | `Civil BC = \|Target\| + 6` |

Executable exact-strike inventory after File_50c pressure testing: P2, P6, P13, P17, P19, P23, P56, P73, P78, P83, P91, and P98. P1 is source-history only in the inherited exact-strike list because P1 is bypass-controlled. P83 is executable because `389520 = 541 × 720`.

Corrected File_50c scan state:

| Field | Corrected value |
|---|---:|
| Silent indices | `72` |
| Hitting indices | `28` |
| Structural target families | `16` |
| Boundary concentration | `22 → 11` |
| Intermediate concentration | `6 → 5` |

Half-step tie convention note: when `V_90 = 720q + 360`, two signed `M` values are equally near. File_50c preserves exact integer arithmetic and an invariant unordered civil bracket in all such cases. An ordered `base` / `companion` display requires a documented tie convention if a downstream implementation needs ordered signed-`M` labels.

### D.10.3 File_50c Machine Guards

Machine Guard [FILE_50C P1 BYPASS]:
P1 is the Civil Datum at `AD 1` under the File_50a no-naturalization bypass. Do not execute P1 through `P_nat`, `V_90`, or the ordinary Bipartite Grid Landing operator. Source v3.0 may be quoted as listing P1 in its exact-strike inventory, but P1 is source-history only in that list and is not an executable exact strike.

Machine Guard [FILE_50C PRIME-POLARITY NAMESPACE]:
`Dual polarity` and signed target-space in this file describe the Bipartite `±720` landing around `M`. They are not File_32 / File_33 raw prime-space cross-polarity, positive-polarity `Q − 5 / Q + 25`, negative-polarity / signed A-space `Q − 16 / Q + 14`, or Coordinate Mirror / Protocol 1.

Machine Guard [FILE_50C P23 NATURALIZED GRID]:
P23 `31686 BC` is the File_50a Naturalized Grid civil landing. Do not collapse it with P23 positive-polarity `31716/31746 BC`, negative-polarity AD `31705/31735`, raw prime-space cross-polarity, or secondary Coordinate Mirror displays.

Machine Guard [FILE_50C +2 / +720 FIREWALL]:
The SKL `+2 local rail` is a two-year local coordinate shift. The `+720 scale-state` is the cosmic `720 = 2 × 360` interval used by the Bipartite bracket. They are related scale states but are not interchangeable operators.

Machine Guard [FILE_50C GENERATED BRACKET / LIST HIT]:
The Bipartite operator generates a two-position signed bracket, or an exact-strike signed tri-position. `Single` and `Dual` describe one or two admitted civil-coordinate intersections, not generation multiplicity. The scan admits primary and source-parenthetical coordinates in ancient-list rows, excludes the contextual `536 BC` / `486 BC` anchor rows and File_50c §5 meta-anchors, and compresses hits by coordinate-family row identity. Coordinate-identical duplicate rows do not create additional targets.

Machine Guard [FILE_50C RESTORATION / COMPUTATIONAL ANCHORS]:
`536 BC` is the biblical restoration-anchor state. `486 BC` is the Berossus computational-anchor state. They are not an uncertainty pair, one slash-pair node, or interchangeable historical anchors.

Machine Guard [FILE_50C TEXTUAL TARGET / COMPUTED COORDINATE]:
SKL and Berossus names and list ordering are source-retained textual or reception-history data. The BC coordinates in File_50c Appendix A are repository calculations or dependency-controlled target states. Do not present the computed coordinates as direct tablet wording.

Machine Guard [FILE_50C JACHIN / CONCAVE MIRROR]:
Jachin `AD 29515` is a Projective Boundary / Temple-Time Pillar coordinate, not an ordinary historical anchor. The Concave Mirror relation in File_50c §5 is a structural meta-anchor state and does not by itself open Protocol 1, File_36 target-Mirror, rounded mod-10 Mirror, or inverse-number operators.

### D.10.4 File_50c cross-file routing

- Use `File_50a` and Register §D.16 for the P1–P100 Naturalized Master Table, naturalization, Base-90 grid, Civil clutch, P2 naturalization-zero state, and P1 bypass.
- Use `File_50c` for Bipartite Grid Landing, exact-strike signed tri-position, source-parenthetical admission, target-hit multiplicity, coordinate-family target identity, and the `72 / 28 / 16` scan state.
- Use `File_34` for the full SKL / Berossus derivation, source-state vocabulary, `536 BC` / `486 BC` firewall, and `+2 local rail` / `+720 scale-state` firewall.
- Use `File_32` and `File_33` for Prime–Civil / polarity namespace controls. Do not substitute File_50c Naturalized Grid outputs for File_32 / File_33 positive- or negative-polarity coordinates.
- Use `File_21` and the File_41 Register entry for Jachin `AD 29515` as Projective Boundary / Temple-Time Pillar state. File_50c uses Jachin only in the structural meta-anchor relation.
- Treat `Sparsity Proof`, `Cluster Compression`, and `Concave Mirror` as source-retained or File_50c-local structural labels with claim-status control; do not promote them to formal probability or direct ancient-authorial-intent claims.

## D.11 File_55 Convention Entry — Moses-Node Thirds Primer and Cumulative Marker Ledger

Registered at File_55 finalization after full pressure testing, direct comparison with Final `File_23`, source-working-record comparison, and the bounded post-revision cross-file audit of June 20 2026; expanded after the post-final SKL / Berossus and hybrid-Creation amendments of June 21 2026; expanded again after the June 22 2026 standard SKL Creation rail-contraction, homogeneous Actual-MT / SKL `430`-pair, and adjacent-state robustness amendments; public-clean control registration closed June 27 2026. Source file: `File_55 Final — The Third Structure — The Moses-Node Primer across Rounded, Regular, and Cumulative Biblical Chronology`.

Register-scope note: This entry registers File_55-specific modal-state vocabulary, the Moses-node thirds-primer operator, its direction and node-class controls, the complete paired Rounded cumulative marker-ledger states, the restored-Cainan `+460 → +320 / +280` marker-displacement state, the File_23 Cainan `460` / schematic-`480` firewall, the expanded SKL / Berossus Creation–Flood–terminal macro-thirding sample, the hybrid `275766 BC` Creation overlay, the June 22 standard SKL Creation rail-contraction and adjacent-state robustness material, the methodological / null-test boundary, and the File_55 Machine Guards registered below. It does not revise File_23's halving scaffold, File_51a's Rounded Scaffold, File_02 / File_22 cumulative chronology, File_34's SKL / Berossus derivation, or the File_50-series external chronology.

Exact-token registration record (Project Procedures §18; mechanical string search of State_Vocabulary_Register v1.10, Restart Capsule v11.9, and Style Guide v2.5, June 20 2026): `File_55`, `Moses-node`, `third marker`, `+320 / +280`, `175206 BC`, `23526 BC`, and `8886 BC` returned zero hits in all three controls. `8880` returned zero hits in the controls but is already present elsewhere in the repository in a distinct File_51a Mirror state; File_55's Enosh usage is therefore state-distinct convergence, not numeric novelty. The values `+460`, `2300`, `2580`, `8640`, `7200`, `8000`, `25920`, and the `960 → 480 → 240 → 120` scaffold are established or dependency-controlled values; File_55 registers new operator and state combinations rather than claiming those numbers as new.

Post-final amendment registration note (June 21 2026): The new File_55 external states register the ordered Rail-1 / Rail-2 / Rail-3 bracket `D + 720 / D / D − 720`, `2160 = LCM(720, 3 × 360) = 30 × 72`, the variant SKL Alulim / Adam and Berossus Alorus / Adam markers, the hybrid `34566 + 241200 = 275766` origin, the `184326 BC` marker, the `30 + 20 + 30` terminal compression, the signed `486 → 806 → 1446 BC` row, and the retained Ubara-Tutu / Noah exception. These are state additions and clarifications, not changes to the underlying File_21 / File_24 / File_34 source values.

Standard SKL Creation registration note (June 27 2026): The June 22 standard SKL Creation amendment registers the `262086 → 2886 BC = 259200 = 10 × 25920` state, the `±720 → ±480` full rail-bracket contraction, selected `mod2160 → mod1440`, apparent-rail `+30 → +20`, nested Key-of-23 boundary `750 / 720 / 690 → 500 / 480 / 460`, the `430 / 432` dual-precessional state, and the `3706 / 3226 / 2746 BC` adjacent-state robustness bracket. These are File_55-specific applications of the Moses-node thirds-primer to dependency-controlled SKL / Berossus inputs. They do not alter File_34, File_21, File_24, or File_51a.

Second exact-token registration record (Project Procedures §18; mechanical string search of State_Vocabulary_Register v1.13, Restart Capsule v11.12, Style Guide v2.5, and Project Procedures v3.2, June 27 2026): `scaffold-mechanics`, `Mirror-mechanics`, `conquest-week rounded-block`, `standard SKL Creation`, `10 × 25920`, `mod2160 → mod1440`, `±720 → ±480`, `+30 → +20`, `750 / 720 / 690`, `500 / 480 / 460`, `430 / 432 dual`, `3706 / 3226 / 2746`, and `adjacent-state robustness` returned zero exact hits in the current controls. `File_51b`, `AD 1406`, `25920`, `430`, `432`, `720`, and `1446 BC` are established or dependency-controlled; this update registers state combinations and guard language rather than claiming the underlying values as new.

### D.11.1 File_55 working state register

| State | Primary coordinate or operator | Function in `File_55` |
|---|---:|---|
| Moses-node thirds-primer state | `1486 / 1446 / 1406 BC` | Fixed three-node selector; each integer input chooses the unique node congruent to it modulo `3`. |
| One-third marker / remaining two-thirds state | `T = (D − N)/3`; `M = D − T`; `R = M − N = 2T` | Direction-aware thirding operator; signed values are retained for inputs later than the selected node. |
| Adamic `960 / 930 / 900` thirds state | `960`; `930`; `900` | Distinguishes apparent-age, textual-lifespan, and underlying Rounded Creation units. |
| Key-of-23 ideal-day overlay | `920 × 25/23 = 1000` | Ordinary whole-year expansion; does not open Residue Protocol. |
| Halving-to-thirding terminal bifurcation | `960 → 480 → 240 → 120`; then `120 → 60 / 80` | File_23 schematic-halving dependency terminating in Moses’ textual `120`. |
| Flood / Cainan shared-marker state | `4836 → 3706 → 1446`; `4856 → 3706 → 1406` | Shared marker-label with distinct inputs, nodes, and node-classes. |
| First-Adam / Flood-Cainan / second-Adam reciprocal pivot | `10126 BC`; `5296 / 4836 BC`; `6 BC` | Cross-state reversal of the `5290 / 4830` spans; does not collapse Flood node-classes. |
| Cumulative-to-Regular Shem translation | `5436 → 4106`; `5431 → 4116` | Rounded cumulative reaches Rounded Regular; Actual cumulative reaches the MT Year-6 state. |
| Psalm 90 `70 / 80` interpretive convergence | `70`; `80`; watch motif | Textual and typological convergence; does not carry the primer’s arithmetic burden. |
| Moses-node distribution state | With Cainan `11 / 7 / 10`; without Cainan `9 / 7 / 11` | Exact row-count property of the complete paired Rounded cumulative ledger. |
| Flood-marker `2300 / 2580` Daniel comparison | `3706 → 1406 = 2300`; `4026 → 1446 = 2580` | Structural equal-half comparison through `1150 / 1290`; not a rewritten textual datum. |
| Rounded cumulative Aaron `+5` local comparison | `14011 / 14041 / 4841 BC` | File_51a-controlled local comparison generated by rounded Aaron `125`; does not replace the Moses line. |
| Rounded Regular MT base | `4106 BC` | Primary opening Creation scaffold. |
| Actual Regular MT standard base | `4114 BC` | Literal MT comparison base with Shem `+2` restored. |
| MT Year-6 / formation-of-Adam state | `4116 BC` | Distinct Creation-side node; reached by actual cumulative Shem thirding. |
| Localized apparent-age overlay | `+30` | Applied to Adam / Creation only unless a local row explicitly says otherwise. |
| Terah variant | `+60` | Moves Terah and all earlier Regular dates; under same-node thirding moves the marker by `40`. |
| Restored Regular 2nd Cainan | `+130` | Begetting-age insertion in Regular chronology; not the cumulative lifespan insertion. |
| Restored Cumulative 2nd Cainan | `+460` | Lifespan insertion in cumulative space; shifts earlier cumulative coordinates by `460`. |
| Rounded cumulative Flood / Shem-death boundary | `4836 BC` | Base Rounded cumulative Flood / end-of-Shem block / Arphaxad boundary. |
| Restored-Cainan Flood / Shem-death upper boundary | `5296 BC` | `4836 + 460`; upper member of the restored cumulative Flood Week and shifted end-of-Shem / Arphaxad boundary. |
| Restored cumulative Cainan life | `4856–4396 BC` | Local `460`-year Cainan lifespan from birth/start to death/Shelah boundary. |
| Paired cumulative third-marker ledger | No-Cainan / restored-Cainan columns from Adam through Moses | Complete Rounded comparison table; the `+460` cascade stops at Shelah. |
| Cainan marker-displacement state | `+460 → +320 / +280` | Marker change determined by the accompanying Moses-node transition. |
| Restored-Cainan Flood marker | `4026 BC` | Direct marker of `5296 BC`; secondarily `2580 = 2 × 1290 = 6 × 430` to Exodus `1446 BC`. |
| Restored-Cainan Enosh marker | `8886 BC` | `1260` below `10146 BC`; `8880` to the `6 BC` Christ-birth hinge. |
| Enosh precessional-day component display | `4300 + 4300 + 40 = 8640` | Isolates the two `4300` blocks and the `40`-year Christ-generation while treating the intervening `280` as gestational component. |
| Enosh Moses / Christ contrast | `8606 → 1406 = 7200`; `8886 → 1526 = 7360 → 8000`; `8886 → 6 = 8880` | Contrasts Moses' birth / terminal year with the Christ-birth hinge under the Enosh marker field. |
| SKL Flood macro-thirding | `27366 / 20886 BC → 1446 BC` | Applies the primer to the Sumerian B / SKL Long and Sumerian A / SKL Short Physical Flood anchors; preserves the `25920 / 19440` precessional forms. |
| Standard SKL Creation macro-thirding | `262086 → 175206 → 1446 BC` | Produces the direct middle-rail marker and opens the marker-to-marker `172000 = 400 × 430` comparison with Rounded Regular `3206 BC`. |
| Standard SKL Creation `10 × 25920` state | `262086 → 2886 BC = 259200` | Middle SKL Creation and terminal rails form `10 × 25920 = 720 × 360 = 600 × 432`. |
| SKL Creation rail-contraction state | `262806 / 262086 / 261366 → 175686 / 175206 / 174726 BC` | The File_55 primer contracts `±720 → ±480`; every marker span to `1446 BC` is `mod480`, while the exact Rail-1 `mod2160` member contracts to `mod1440`. |
| SKL apparent-rail contraction state | `+30 → +20`; `262836 / 262116 / 261396 → 175706 / 175226 / 174746 BC` | The apparent source rail is retained at two-thirds strength and lands on the `2906 / 2886 BC` SKL terminal pair through the Rail-1 markers. |
| SKL nested Key-of-23 boundary state | `750 / 720 / 690 → 500 / 480 / 460` | Adjacent source and marker rail fields reproduce `690 × 25/23 = 750` and `460 × 25/23 = 500`. |
| SKL Creation `430 / 432` dual-precessional state | `262116 − 4116 = 258000 = 600 × 430 = 200 × 1290`; `175226 − 3226 = 172000 = 400 × 430`; `259200 = 600 × 432 → 172800 = 400 × 432` | Preserves the homogeneous Actual-MT / SKL `+30` alternate pair, the standard `72`-year state, and the secondary Rounded marker alignment `175206 − 3206 = 172000` without merging their node-classes. |
| SKL Creation adjacent-state robustness bracket | `175706 / 175226 / 174746 BC → 3706 / 3226 / 2746 BC`; stable span `172000 = 400 × 430` | Shows that the contracted SKL Creation field remains coherent across adjacent state alternatives without collapsing node-classes or asserting intentional design. |
| SKL Alulim / Adam four-state rail | `262086 / 262806 / 268566 / 269286 BC` | Distinguishes standard, exact Rail-1, variant, and variant Rail-1 Creation coordinates. Shared Adam-figure language does not collapse the states. |
| SKL / Berossus ordered `±720 / mod2160` rail | Rail-1 `D + 720`; Rail-2 `D`; Rail-3 `D − 720`; `2160 = 3 × 720 = 6 × 360 = 30 × 72` | Exactly one qualifying adjacent rail has a span to `1446 BC` divisible by `2160`; the exact member is operator-expected, while marker significance is evaluated separately. |
| Variant SKL Alulim / Adam macro-thirding | `269286 → 180006 → 1446 BC` | Exact `124 × 2160` Rail-1 span; marker is `490 × 360 = 420²` from `3606 BC` and `500 × 360` from `6 BC`. |
| Hybrid Berossus / SKL Alulim Creation overlay | `34566 + 241200 = 275766`; `275766 → 184326 → 1446 BC` | File_34 controls the hybrid origin; File_55 controls the thirding. The marker stands `181440 = 7 × 25920` from `2886 BC`. |
| Berossus Alorus / Adam primary precessional pair | `466566 → 2166 BC = 1290 × 360`; `466566 → 6 BC = 1296 × 360` | Displays `18` alternate / standard precessional years through the `430 × 3 / 432 × 3` pair. |
| Berossus Alorus / Adam Rail-3 thirding | `465846 → 311046 → 1446 BC` | Exact `215 × 2160` Rail-3 span; reforms the `18`-year pair into `12` precessional years through doubled `430 / 432` forms. |
| SKL terminal `30 + 20 + 30` compression | `2936 → 2906 → 2886 → 2856 BC`; `3606 → 2886 → 1446 BC` | The first complete `2160 = 30 × 72` span thirds to `1440 = 20 × 72`, landing on the established `2886 BC` `+30` rail. |
| Berossus computational-anchor reverse thirding | `486 → 806 → 1446 BC` | Signed later-than-node application: `1446 − 486 = 960`; two-thirds `640`; marker `806 BC`, which stands `720` years from Moses' birth `1526 BC`. |
| Ubara-Tutu / Noah exception state | `39486 − 1446 = 52 × 720 + 600` | Named retained miss in the deep-time `±720 / mod2160` sample. Do not regularize or suppress it. |
| Logical-sample / compression boundary | Creation / first king; Flood; post-Flood `23 kings` terminus | File_55 records representative junctures rather than every external resonance. Omitted detail remains dependency-controlled. |
| Berossus Flood macro-thirding | `34566 → 23526 → 1446 BC` | Produces `33120 = 23 × 1440`, with remaining two-thirds `22080 = 23 × 960`; the Key of 23 expands the full span to `36000`. |
| Fourth-trumpet stellar / precessional comparison | `25920 → 8640 + 17280`; `36000 → 12000 + 24000` | Extends the one-third / two-thirds investigation to dependency-controlled Babylonian macro-chronology; the imported SKL field is read through the repository’s standard precessional register. |
| Short-sojourn comparison | `−215` | Regular / Rounded comparison state; does not alter native cumulative lifespan totals. |
| Rounded Cumulative MT Moses line | Adam `14006 BC`; Flood `4836 BC`; Moses death `1406 BC` | Rounded lifespans stacked backward from Moses’ death. |
| Actual Cumulative MT Moses line | Adam `14006 BC`; Flood `4831 BC`; Moses death `1406 BC` | Actual lifespans stacked backward from Moses’ death. |
| Schematic halving table, no Cainan | Adam apparent boundary `15686 BC` | File_23 mod-`120` schematic overlay. Not Actual or Rounded chronology. |
| Schematic halving table, with Cainan | Adam apparent boundary `16166 BC` | File_23 schematic overlay. Its with-/without-Cainan heads differ by a schematic `480`-year table displacement; Cainan remains `460`, and `480` is not Cainan’s lifespan. |
| LXX +60 Creation comparison | `5546 BC` | Used for the `1380 + 2760` one-third / two-thirds display. |
| Temple comparison node | `966 BC` | 1 Kings 6:1 construction anchor. |
| Christ-birth hinge | `6 BC` | Same-side comparison endpoint in selected rows. |

### D.11.2 Moses-node operator and File_23 Cainan boundary

The File_55 primer uses the fixed nodes:

```markdown
1486 BC; 1446 BC; 1406 BC
```

For integer input date `D`, select the unique node `N` for which:

$$
D-N\equiv0\pmod 3.
$$

Define:

$$
T=\frac{D-N}{3},
$$

$$
M=D-T=N+2T,
$$

$$
R=M-N=2T.
$$

The ordinary display is:

```markdown
D → M → N
```

For an input later than its selected node, retain the signed result; do not force a positive span. If two endpoints route to the same node, the marker-to-marker lifespan is exactly two-thirds of the source lifespan. A uniform same-node shift contracts by two-thirds, so `+60 → +40`. If an input shift also changes the selected node, use the full operator rather than a same-node shortcut.

State guard — File_23 Cainan `460` / schematic `480`:
File_23 does not assign Cainan a `480`-year lifespan. Cainan remains `460`. In File_23, `480` belongs to the rounded Shem-to-Eber + Cainan `4+1` average-class and to the schematic `480`-year displacement between the with-/without-Cainan halving-table heads. Neither display replaces File_55's cumulative restored-Cainan operator `+460`.

Anchor-tier note:
The File_55 year-labels `175206 BC`, `175686 BC`, `179526 BC`, `180006 BC`, `23526 BC`, `184326 BC`, `311526 BC`, `311046 BC`, `18726 BC`, `14406 BC`, `10126 BC`, `10146 BC`, `9806 BC`, `9826 BC`, `8886 BC`, `8606 BC`, `4026 BC`, `3706 BC`, `3206 BC`, and `806 BC` are derived third markers or local anchoring nodes under Register §D.3. They are not automatically Repository endpoint anchors and do not require promotion to the Restart Capsule Primary anchors inventory merely because they anchor later File_55 comparisons. External inputs such as `268566 / 269286 / 466566 / 465846 / 275766 / 39486 / 3606 BC` retain their File_21 / File_24 / File_34 node-classes and are not redefined by File_55.

### D.11.3 File_55 Machine Guards

Machine Guard [REGULAR `+130` / CUMULATIVE `+460`]:
Restored 2nd Cainan contributes `130` in Regular begetting chronology and `460` in cumulative lifespan chronology. Do not substitute one value for the other.

Machine Guard [LIFESPAN VS. CHRONOLOGY FIREWALL]:
Cumulative lifespan dates are biographical volumes stacked end-to-end. They are not ordinary historical birth dates, and they must not be conflated with sequential narrative spans such as the `430`-year sojourn.

Machine Guard [APPARENT-AGE LOCALIZATION]:
The `+30 Apparent Age` is a localized Adamic overlay. It does not propagate through later patriarchal dates unless a local schematic state explicitly defines such propagation.

Machine Guard [THIRD MARKER NODE-CLASS]:
A third marker is a derived coordinate. It may coincide with a historical, schematic, Rounded, Actual, prophetic, or typological node, but identity requires agreement of state, node-class, and operator.

Machine Guard [SHARED YEAR-LABEL]:
A year label shared by multiple states is not automatically the same node. `3706 BC`, `4106 BC`, `4116 BC`, and other repeated labels must retain their source state and operator.

Machine Guard [FLOOD / SHEM-DEATH / CAINAN NODE-CLASS]:
`4836 BC` is the base Rounded cumulative Flood / Shem-death / Arphaxad boundary; `5296 BC` is its restored-Cainan `+460` counterpart; `4856 BC` is restored cumulative Cainan's birth / start; and `4396 BC` is Cainan's death / Shelah boundary. The shared third marker `3706 BC` belongs specifically to `4836 BC` and `4856 BC`. Do not treat `5296 BC` as a third input to that same overlap.

Machine Guard [RESTORED-CAINAN CASCADE LIMIT]:
The cumulative `+460` shifts Adam through the Arphaxad / Flood / Shem-death boundary. It does not propagate beyond the local Cainan life `4856–4396 BC`; Shelah and all later cumulative coordinates remain unchanged.

Machine Guard [SKL / BEROSSUS DEPENDENCY]:
The SKL and Berossus dates used in this file are imported whole-year physical / computational anchors governed by `Restart Capsule v11.11`, `File_21`, `File_34`, and the `File_50-series`. `File_55` applies its local thirds primer to those anchors; it does not rederive the external chronologies or open their full variant matrices.

Machine Guard [MACRO-THIRDING NODE / CROSS-LAYER FIREWALL]:
The imported SKL and Berossus inputs in §7.16 route directly through the `1446 BC` Exodus node. The `175206 BC → 3206 BC = 172000` relation is a secondary marker-to-marker comparison across SKL and Rounded Regular MT states. The `14406 BC` harmonic is `10×` only in its span to `6 BC`: `14400 = 10 × 1440`; it is not the decimal multiplication of the year-label `1446 BC`.

Machine Guard [FOURTH-TRUMPET TYPOLOGY]:
The comparison with Revelation 8:12 is a structural / typological reading. `25920` is the standard precessional-year span under the repository's `72`-year precessional-day convention; `36000` is a Key-of-23-expanded sexagesimal macro-span. Do not collapse them into one physical astronomical unit or treat the arithmetic as proof of direct SKL, Berossian, or Johannine authorial coordination.

Machine Guard [HYBRID BEROSSUS / SKL CREATION OVERLAY]:
`275766 BC` is a derived hybrid node: the Berossus Flood anchor `34566 BC` plus the SKL eight-kings antediluvian span `241200`. It does not replace the native SKL Creation anchors `262086 / 268566 BC`, the Berossus Alorus / Adam anchor `466566 BC`, or the Berossus ten-kings span `432000`. Its File_55 third marker is `184326 BC`. The seven-precessional-year span is `184326 → 2886 BC`, not `275766 → 2886 BC`. The arithmetic does not prove that Berossus consciously used the thirding process.

Machine Guard [SKL `+2` / `+720` / `mod2160` FIREWALL]:
The local SKL `+2` rail and the cosmic `±720` scale-state are related by the `360:1` scalar but are not interchangeable operators. In §7.16, `±720` names an ordered cosmic bracket and `mod2160` tests the span to `1446 BC`. It does not authorize a floating `720`-year adjustment elsewhere.

Machine Guard [MOD2160 EXPECTATION / LANDING SIGNIFICANCE]:
For a qualifying span `D − 1446 = 720k`, the ordered `±720` bracket changes `k` to `k + 1`, `k`, and `k − 1`; exactly one is divisible by `3`. The existence of one exact `2160` member is therefore structurally expected. The resulting third marker, endpoint harmonics, and cross-state landings must be evaluated separately and do not by themselves establish intentional design.

Machine Guard [SKL TERMINAL BASE / `+30` RAIL]:
`2906 BC` and `2856 BC` are the SKL Short / Long physical bases. `2936 BC` and `2886 BC` are their `+30 Apparent Mode` rails. The strict `3606 → 2886 → 1446 BC` `mod2160` display uses `2886 BC`; it does not convert the physical `2856 BC` anchor into the apparent rail.

Machine Guard [PRECESSIONAL-DAY DUAL STATE]:
The standard precessional day is `72` years. The alternate day is `71⅔ = 215/3` years. Use each only where explicitly opened. The alternate display may corroborate a `430 / 432` pairing but must not correct or replace the standard `72`-year state.

Machine Guard [BEROSSUS `486 BC` CORRIDOR]:
`486 BC` is the Berossus computational anchor paired by the `2370` corridor with the SKL Long physical base `2856 BC`. The SKL Long `+30` rail `2886 BC` maps to `516 BC`, not `486 BC`. The local `486 → 806 → 1446 BC` thirding remains valid without calling `486 BC` the strict equivalent of `2886 BC`.

Machine Guard [UBARA-TUTU EXCEPTION]:
Ubara-Tutu / Noah is retained as the named exception to the deep-time `±720 / mod2160` sample. Do not silently regularize the row or suppress it from a later corpus sweep.

Machine Guard [EXTERNAL SAMPLE COMPRESSION]:
Section 7.16 records a logical sample of Creation / first king, Flood, and post-Flood terminal junctures. It does not enumerate every SKL / Berossus anchor resonance. The compression is editorial: the omitted detail remains dependency-controlled, not absent or disconfirmed.

Machine Guard [PRECESSIONAL UNIT STATE]:
`72` years is the standard precessional day. `71⅔ = 215/3` years is the alternate display. Do not average, merge, or use one to correct the other.

Machine Guard [SHEM TWO-STATE PIVOT]:
`2556 BC` and `1958 BC` belong to different Shem reckoning members. Their `598`-year difference is a cross-state pivot display, not Shem’s literal lifespan.

Machine Guard [DANIEL TEXT / EQUAL-HALF DISPLAY]:
The canonical textual figures are `2300` in Daniel 8:14 and `1290` in Daniel 12:11. The equations `2300 = 1150 + 1150` and `2580 = 1290 + 1290` are structural equal-half displays. Do not rewrite Daniel 8:14 as a direct textual statement of `1150` days, and do not rewrite Daniel 12:11 as a direct textual statement of `2580` days.

Machine Guard [GESTATION / CIVIL-SPAN UNIT FIREWALL]:
The `280 / 320` marker displacements are chronological year-values. Their comparison with gestational day-numbers is a biological-fractal typological overlay. The `6 BC → AD 35` relation is a `40`-year civil span. The `8640` precessional-day display is a component display, `4300 + 4300 + 40`, that excludes the intervening `280` gestational component; the continuous civil cross-axis span from `8886 BC` to `AD 35` remains `8920`. The Moses-facing `7200` and `7360 → 8000` comparisons are endpoint comparisons from Enosh markers; they are not the same operation as the direct primer routing of those markers. Do not treat days and years as the same physical unit, do not use the optional `71⅔` display to correct the standard `72`-year precessional day, and do not let the typological overlay replace the underlying marker arithmetic.

Machine Guard [SKL / BEROSSUS MACRO-THIRDING]:
The ordinary direct third markers are `18726 BC`, `14406 BC`, `175206 BC`, `179526 BC`, `23526 BC`, `184326 BC`, and `311526 BC`, all routed through `1446 BC`. The rail-normalized markers are `175686 BC`, `180006 BC`, `311046 BC`, `2886 BC`, and signed reverse marker `806 BC`. Values such as `172000`, `14400`, `36000`, `12000`, `24000`, `176400`, `180000`, `181440`, `182880`, `241200`, `274320`, `309600`, and `311040` are secondary spans, source spans, or expansions, not replacement civil-date nodes. Preserve direct marker, rail-normalized marker, remaining span, expanded span, and cross-state comparison as distinct node-classes.

Machine Guard [SKL CREATION RAIL CONTRACTION]:
The ordered source bracket is `262806 / 262086 / 261366 BC`; its ordered marker bracket is `175686 / 175206 / 174726 BC`. The full bracket contracts `±720 → ±480`, so every marker span to `1446 BC` is divisible by `480`. Only the source Rail-1 span is divisible by `2160`, and only its marker span is divisible by `1440`. Do not discard the flanking `480` markers merely because they are not the selected `mod1440` member.

Machine Guard [SKL `+30 → +20` KEY-OF-23 BOUNDARY]:
The source apparent rail `+30` contracts to a marker offset `+20`. The cross-span fields `750 / 720 / 690` and `500 / 480 / 460` are bounded adjacent-rail matrices, not new universal rail spacings. Their outer boundaries are linked by `690 × 25/23 = 750` and `460 × 25/23 = 500`.

Machine Guard [SKL CREATION `430 / 432` CROSS-STATE]:
Preserve `430 = 6 × 71⅔` and `432 = 6 × 72` as alternate and standard precessional states. `172000 = 400 × 430` and `172800 = 400 × 432` are distinct exact spans. The primary alternate row is homogeneous: `262116 / 4116 BC → 175226 / 3226 BC`, all through `1446 BC`. The Rounded relation `262086 BC → 175206 BC → 1446 BC` beside `4106 BC → 3206 BC → 1406 BC` is secondary marker-level corroboration only. `3226 BC` and `3206 BC` remain derived coordinates under their stated node-classes and primer nodes.

Machine Guard [SKL ADJACENT-STATE ROBUSTNESS RESTRAINT]:
The `175706 / 175226 / 174746 BC → 3706 / 3226 / 2746 BC` bracket is an adjacent-state robustness observation under `172000 = 400 × 430`. It does not collapse the Rounded cumulative Flood / Cainan marker, Actual MT Year-6 marker, and adjacent apparent-rail markers into one node, and it does not assert intentional design. Preserve the bracket as state-controlled robustness, not proof burden.

Machine Guard [INVESTIGATION SCOPE]:
`File_55` is confined to the chronological, arithmetic, structural, comparative, and theological states defined in its header and state register. Do not import source methodology that this file has not opened.

Machine Guard [NO STATE COLLAPSE]:
Do not collapse Actual Regular, Rounded Regular, Actual Cumulative, Rounded Cumulative, restored-Cainan, apparent-age, schematic-halving, LXX, SP, SKL, Berossus, Temple, Christ-hinge, or prophetic comparison states because they share a date or number.

Machine Guard [KEY-OF-23 WHOLE-YEAR STATE]:
The `25/23` and `70/69` calculations in this file are ordinary whole-year Key-of-23 expansions. They do not open Residue Protocol, decimal residue, physical-day residue, or Mirror mechanics.

Machine Guard [TYPOLOGY NON-SUBSTITUTION]:
Watchers, Nephilim, Psalm 90, Revelation 8 / 12, Temple, Christ, Conquest, SKL, and Berossus interpretations explain thematic fit. They do not substitute for the stated arithmetic or source chronology.

### D.11.4 Methodological / null-test boundary

Source-control note:
The arithmetic results in this file do not by themselves establish that the Moses-node primer detects intentional chronological design. The selector always produces an output; the recognized-number vocabulary is dense; many rows use Rounded chronology or admitted state choices; and several recurring effects—including unique routing, `+60 → +40`, and same-node two-thirds contraction—are algebraic consequences of the operator.

The source working record identifies the LXX Shem-death `1290 → 430` row and Abraham’s call `2091 → 1876 → 1446` as the two lowest-slack examples. Even these remain selected instances rather than evidence until tested against a null chronology.

In the external sample, one exact `mod2160` member within a qualifying `±720` bracket is algebraically expected. The evidentiary objects are therefore the marker landings, endpoint harmonics, and retained misses. Ubara-Tutu / Noah remains explicitly retained as a miss in the current sample.

The required test is to predefine the counted operations, target-number vocabulary, admitted variants, and hit criterion; sweep all real inputs while retaining misses; and compare the resulting hit rate with the identical procedure applied to shuffled and random chronologies. Unless the observed rate exceeds the null rate, the hypothesis remains unestablished and is not asserted. If the test does not separate the chronology from chance, the line of inquiry is dropped; if it does, the two lowest-slack cases are the appropriate starting point for a stronger argument.

### D.11.5 File_55 cross-file routing

- Use `File_55` for the Moses-node thirds-primer, direct and rail-normalized third markers, paired Rounded cumulative marker ledgers, Cainan marker displacement, Flood `2300 / 2580` comparison, Enosh `7200 / 8000 / 8640 / 8880` field, the bounded SKL / Berossus Creation–Flood–terminal sample through `±720 / mod2160`, the standard SKL Creation rail contraction, the apparent-rail `+30 → +20` contraction, the `430 / 432` dual-precessional state, and the adjacent-state robustness bracket.
- Use `File_23` for the controlling `960 → 480 → 240 → 120` schematic halving scaffold, Shem / Peleg boundary logic, and the original `479` actual / `480` rounded `4+1` average comparison. Cainan remains `460`.
- Use `File_51a` for the Rounded Scaffold, Actual / Rounded comparison boundaries, localized `+30 Apparent Age`, and the distinction between Regular `+130` and cumulative `+460` restored Cainan.
- Use `File_02`, `File_18`, and `File_22` for cumulative chronology, Flood / Shem boundaries, source tables, and cumulative harmonic controls.
- Use `File_34`, `File_21`, `File_24`, `File_26`, and the `File_50-series` for full SKL / Berossus chronology, precessional units, external variant matrices, and imported-anchor derivation. File_34 controls `34566 + 241200 = 275766`; File_55 applies the primer but does not replace those derivations.
- Use `File_33` only for the dependency-controlled `5290 / 4830` Christ-facing field opened by File_55. Do not import the broader prime-polarity or Mirror system unless explicitly opened.
- Treat File_55's fourth-trumpet, Watchers, gestation, Psalm 90, Temple, Christ, and Conquest correspondences as classified typological or providential readings. They do not carry the arithmetic burden.

### D.11.6 Pointer note

Final File_55 with the June 21 2026 post-final SKL / Berossus and hybrid-Creation amendments and the June 22 2026 standard SKL Creation rail-contraction / adjacent-state robustness amendments remains the controlling source for the complete ledgers, equations, state transitions, source-integration record, Audit notes, and open research items. Amended File_34 remains the controlling source for the origin of `275766 BC`. This Register entry supplies retrieval vocabulary, operator routing, state boundaries, and File_55 Machine Guards. The formal shuffled / random null comparison remains an open research item, not a source-control or finalization blocker. The Repository_Change_Archive update remains pending until the current Archive source is supplied.


### D.12 File_52a / File_52b inverse-number architecture and technical supplement registration

Source-control note:
Final post-final pressure-tested `File_52a` and Final post-final pressure-tested `File_52b` are now the controlling File_52a / File_52b sources. `File_52a` states the inverse-number architecture. `File_52b` supplies dense technical tables, constrained chains, secondary corroborations, no-placeholder and reverse-jubilee material, Cainan overlays, SKL / Pillar interoperability notes, and appendices. The two files are companion files; neither replaces `File_51a`, `File_51b`, or `File_52c`.

#### D.12.1 Registered File_52a / File_52b state labels

| State | Primary values / forms | Handling |
|---|---:|---|
| File_52a inverse-number architecture state | base-10 reversal; Rounded inverse axiom; trunk / branch hierarchy | `File_52a` controls the architecture and executable trunk hierarchy. |
| File_52b technical supplement state | dense tables; constrained chains; appendices | `File_52b` supplies technical detail and secondary corroboration without replacing `File_52a`. |
| Base-10 reversal state | digit reversal; mod-9 difference law | The difference between a number and its digit reversal is divisible by `9`. |
| Rounded inverse axiom state | off-lattice raw inverse normalized to mod-5 | Use where a rounded span is inverted and re-applied chronologically. |
| Placeholder-preserving inverse state | `30 → 30`; `60 → 60`; `100 → 100`; `500 → 500`; `130 → 310` | Primary inverse register for the File_52a architecture and File_52b primary tables. |
| No-placeholder compression state | `0501 + 006 + 0501 = 1008`; secondary compressed chains | Secondary confirmation only; must be explicitly labeled. |
| Reverse-jubilee register state | `2700 = 54 × 50`; `54 → 45`; `3656 BC` | Secondary reverse-jubilee corroboration; does not replace raw year-span inversion. |
| Regular trunk inverse state | `2700 → 7200`; `1406 + 7200 = 8606 BC` | Trunk-level inverse state controlled by File_52a. |
| Cumulative trunk inverse state | `12600 → 62100`; `1406 + 62100 = 63506 BC` | Trunk-level inverse state controlled by File_52a. |
| Placeholder backbone state | `1050 + 600 + 1050 → 5010 + 600 + 5010 = 10620`; `1406 + 10620 = 12026 BC` | Route-independent regular backbone state. |
| `9900` transfer bridge state | `14006 BC → 4106 BC = 9900`; `2700 + 9900 = 12600` | Cum/reg bridge state; do not confuse with actual `9890` File_22 bridge. |
| Regular inverse begetting-chain state | inverse begetting ages from Adam to Jacob | Technical chain controlled by File_52b; Jacob remains terminal son-date. |
| Inverse death-date structural notation state | inverse birth-date plus inverse rounded lifespan | Structural notation only; not ordinary biological death chronology. |
| Cumulative inverse lifespan-chain state | 26-patriarch inverse lifespan chain from `1406 BC` | Technical chain controlled by File_52b. |
| Cainan regular overlay state | regular inverse begetting `+310` overlay | Overlay state; does not replace the base regular inverse line. |
| Cainan cumulative insertion state | cumulative inverse lifespan `460 → 640` | Overlay state; does not replace the base cumulative inverse line. |
| Apparent-age propagation state | intrinsic `4136/4106`; intrinsic `14036/14006`; inherited `1436/1406` | `+30` rides only on load-bearing nodes; intrinsic and inherited states remain distinct. |
| Mirror compatibility boundary state | File_52a §14 and File_52b §13 | Mirror material must not be converted into ordinary civil arithmetic unless the active operator is explicitly civil. |
| Removed inherited §13.1 row state | former active rows removed from proof use | The inherited non-load-bearing `AD 1405` Mirror rows are not active proof rows after File_52a / File_52b finalization. |
| SKL / Pillar corroborative environment state | File_52b §12 and Appendix C | Corroborative, dependency-controlled, and not the main generative engine of File_52a / File_52b. |
| Appendix A decadal mirror-test state | `244 / 271`; `805 / 991`; `720 / 1261` | Technical support for File_52a triadic field; not a full statistical proof of the inverse system. |
| Appendix B four-triad mean-field state | `11006`; `17006`; `23006` | Raw technical data for File_52c square / gate-field interpretation. |
| Appendix C inverse Pillar / Babylonian field state | inverse Pillars; Babylonian six-node field | Technical supplement; full Pillar / SKL / Berossus derivation remains dependency-controlled. |

#### D.12.2 File_52a Machine Guards

Machine Guard [FILE_52A INVERSE / MIRROR FIREWALL]:
Inverse-number reversal is a base-10 transformation of a span or structured value. It is not Protocol 1 Mirror target generation, not Protocol 2 phase Mirror, and not civil cross-axis span-counting.

Machine Guard [FILE_52A CIVIL SPAN EXECUTION]:
When an executable BC-to-AD civil span is opened, use `BC + AD − 1 = total`. Do not use ordinary signed subtraction across the epoch boundary unless an explicitly labeled A-space calculation is being performed.

Machine Guard [FILE_52A A-SPACE DISPLAY]:
A-space may be used to display mirror coordinates or signed cross-axis relations. A-space display does not replace civil BC/AD labels in ordinary chronological statements.

Machine Guard [FILE_52A ROUNDED INVERSE AXIOM]:
When a rounded span is inverted and the raw inverse falls off the mod-5 lattice, normalize to the nearest mod-5 value before reapplying the span chronologically.

Machine Guard [FILE_52A PLACEHOLDER FORCE]:
Placeholder-preserving inversion is the primary register of `File_52a`. Do not strip trailing zeros unless a local section explicitly opens File_52b no-placeholder compression.

Machine Guard [FILE_52A NO-PLACEHOLDER COMPRESSION]:
No-placeholder compression belongs to `File_52b` as a secondary technical confirmation. It does not replace placeholder force and does not become the default operator in `File_52a`.

Machine Guard [FILE_52A ANCHOR RULE]:
Regular inverse chains are reckoned from `1406 BC`, `1446 BC`, or `6 BC` only where the section states the active anchor. The cumulative inverse lifespan chain summary is reckoned from `1406 BC` by File_52b dependency.

Machine Guard [FILE_52A TRUNK-FIRST HIERARCHY]:
The inverse architecture is trunk-first. Do not promote branch coincidences above the regular trunk `2700 → 7200`, the cumulative trunk `12600 → 62100`, or the `9900` transfer bridge.

Machine Guard [FILE_52A APPARENT-AGE PROPAGATION]:
The Adamic `+30` overlay is meaningful only where it rides on an already load-bearing node. Intrinsic Creation-pole use and inherited inverse-pole use must remain distinct.

Machine Guard [FILE_52A FILE_52B BOUNDARY]:
Dense tables, constrained chains, no-placeholder readings, reverse-jubilee corroborations, SKL / Pillar interoperability, and appendices remain File_52b-controlled. `File_52a` states the architecture and cites File_52b rather than reproducing its full proof burden.

Machine Guard [FILE_52A FILE_52C BOUNDARY]:
Second-order closure, inverse-of-the-inverse chains, and full square / gate-field interpretation remain File_52c-controlled unless a local section explicitly opens a bounded preview.

Machine Guard [FILE_52A AUDIT NOTE REQUIRED]:
Do not silently correct arithmetic, Mirror arithmetic, operator mismatch, or state logic. Preserve the source statement and add an audit note where needed.

#### D.12.3 File_52b Machine Guards

Machine Guard [FILE_52B TECHNICAL SUPPLEMENT BOUNDARY]:
`File_52b` supplies technical tables and dense corroborations for `File_52a`. Do not promote `File_52b` into a replacement architecture file, and do not import `File_52c` second-order interpretation into `File_52b` except as dependency-bound raw data.

Machine Guard [FILE_52B INVERSE / MIRROR FIREWALL]:
Base-10 inverse-number reversal is not Mirror target generation and not ordinary civil cross-axis elapsed-span arithmetic. A line may use inverse reversal, A-space Mirror display, or civil `BC + AD − 1`; the active operator must be explicit.

Machine Guard [FILE_52B CIVIL SPAN EXECUTION]:
For executable cross-axis spans, enforce `BC + AD − 1 = total`. Do not silently treat a Mirror label as an executable civil-span equation.

Machine Guard [FILE_52B PLACEHOLDER PRIMARY REGISTER]:
Placeholder-preserving inversion is the primary register unless a section explicitly opens no-placeholder compression. Examples include `30 → 30`, `60 → 60`, `100 → 100`, `500 → 500`, and `130 → 310`.

Machine Guard [FILE_52B NO-PLACEHOLDER SECONDARY REGISTER]:
No-placeholder compression confirms the primary placeholder system but does not compete with it. Label no-placeholder operations explicitly and do not retroject them into primary inverse tables.

Machine Guard [FILE_52B ROUNDED INVERSE AXIOM]:
When a rounded span is inverted and the raw inverse falls off the mod-5 lattice, normalize to the nearest mod-5 value before reapplying it chronologically. Do not silently replace the normalized inverse with the raw off-lattice inverse.

Machine Guard [FILE_52B ANCHOR RULE]:
Regular inverse chains are reckoned from `1406 BC` or `1446 BC` only when specified. The cumulative inverse lifespan chain is reckoned from `1406 BC`. Do not transfer one anchor to another section without restating the state.

Machine Guard [FILE_52B INVERSE DEATH-DATE STRUCTURAL NOTATION]:
Inverse death-dates are structural notation generated by inverse birth-date plus inverse rounded lifespan. They need not behave as ordinary historical death-dates. Do not use them as biological-history claims.

Machine Guard [FILE_52B CAINAN OVERLAY NON-REPLACEMENT]:
The regular `+310` inverse-begetting overlay and cumulative `640` inverse-lifespan insertion are overlay states. They do not replace the base regular or cumulative inverse lines.

Machine Guard [FILE_52B APPARENT-AGE PROPAGATION]:
The Adamic `+30` applies only where riding on a load-bearing node. Preserve the intrinsic / inherited distinction; do not attach `+30` to every attractive date.

Machine Guard [FILE_52B SKL / PILLAR CORROBORATIVE ENVIRONMENT]:
SKL, Berossus, and Pillar material in this file is corroborative and dependency-controlled. Full derivation belongs to the SKL / Berossus / Pillar control files unless a local section explicitly opens a bounded calculation.

Machine Guard [FILE_52B FILE_52C BOUNDARY]:
Appendix B supplies raw four-triad mean-field data for `File_52c`. The square-gate interpretation and second-order closure remain `File_52c` responsibilities.

Machine Guard [FILE_52B AUDIT CONTROL]:
Do not silently correct uncertain arithmetic or operator conflicts. Use `Audit note:` or dependency-boundary wording. Non-load-bearing rows may be removed from active proof use when author direction requires it.

#### D.12.4 File_52a / File_52b cross-file routing

- Use `File_52a` for inverse-number architecture, the base-10 reversal operator, the Rounded inverse axiom, the `9900` transfer bridge, trunk-first hierarchy, regular and cumulative trunk inversions, and the local File_52a argument structure.
- Use `File_52b` for dense technical tables, regular inverse begetting chronology, cumulative inverse lifespan chronology, no-placeholder chains, reverse-jubilee checks, Cainan overlays, terminal-digit concentration, apparent-age survey, decadal mirror-test counts, four-triad mean-field raw data, and inverse Pillar / Babylonian-field technical material.
- Use `File_52c` for second-order inverse closure and square / gate-field interpretation. Do not import File_52c conclusions into File_52a or File_52b unless a section explicitly opens that dependency.
- Use `File_51a` for Rounded Scaffold and mod-5 source controls. Use `File_51b` for formal Mirror mechanics. Use `File_54` for comparative later-file claim-status and Mirror role classification.
- Use `File_17`, `File_18`, `File_21`, and `File_34` for prophetic span grammar, raw chronological data, SKL Unified Protocol, and SKL / Berossus / Pillar derivations where those states are opened.
- The inherited non-load-bearing `AD 1405` Mirror rows formerly active in File_52a §14 and File_52b §13.1 are not active proof rows in the final post-final pressure-tested sources. Do not restore them without an explicit author decision.

#### D.12.5 Pointer note

Final post-final pressure-tested `File_52a` and `File_52b` remain the controlling sources for their complete headers, pass ledgers, state registers, File maps, equations, tables, and audit notes. This Register entry supplies retrieval vocabulary, operator routing, state boundaries, and File_52a / File_52b Machine Guards. No arithmetic value, anchor, modal state, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary is changed by this registration.



## D.13 File_51a / File_51b Handoff and Rounded Conquest-Block Display Guard

Registered June 27 2026 as a bounded public-clean control update after `File_51a` public-clean completion. Source file: `File_51a Public Clean v1.1`.

### D.13.1 Registered state labels

| State | Primary values / forms | Handling |
|---|---:|---|
| File_51a Rounded Scaffold mechanics state | mod-5 Rounded Scaffold; `4106 BC`; `4136 BC`; cumulative rounded layer | `File_51a` controls the Rounded Scaffold base state and its local mod-5 mechanics. It does not carry the full Cross-Polarity Mirror proof burden. |
| File_51b Mirror mechanics state | Cross-Polarity Mirror; deep-time translation engines; Protocol 1 / Protocol 2 by local definition | `File_51b` controls formal Mirror mechanics where those systems are opened. Do not retroject File_51b mechanics into File_51a as File_51a's primary proof layer. |
| Conquest-week rounded-block display convention | visible labels `AD 1406 / AD 1401`; strict Protocol 1 targets `AD 1405 / AD 1400` | When File_51a §§16.7–16.8 or later File_51b reuse these labels, `AD 1406 / AD 1401` are rounded Conquest-week block display labels only. They do not create a second Mirror protocol or civil-span operator. |

### D.13.2 Machine Guard

Machine Guard [FILE_51A / FILE_51B HANDOFF]:
`File_51a` supplies the Rounded Scaffold and its mod-5 mechanics. `File_51b` supplies Cross-Polarity Mirror and deep-time translation mechanics. Do not read `File_51b` Mirror mechanics back into `File_51a` as the scaffold's proof layer, and do not reduce `File_51a` scaffold states to Mirror states when `File_51b` cites them. Where `AD 1406 / AD 1401` are reused from File_51a §§16.7–16.8, they are rounded Conquest-week block display labels. Strict Protocol 1 remains `1406 BC → AD 1405` and `1401 BC → AD 1400`; `BC + AD − 1` remains the civil cross-axis span operator.

### D.13.3 Cross-file routing

- Use `File_51a` for Rounded Scaffold mechanics, mod-5 state controls, Actual / Rounded boundaries, localized `+30 Apparent Age`, `+60 Terah`, cumulative rounded-layer controls, and the §16.7–§16.8 rounded Conquest-block display convention.
- Use `File_51b` for formal Cross-Polarity Mirror and deep-time translation mechanics when that file is opened.
- Use `File_52a` and `File_52b` for inverse-number architecture. Inverse-number reversal is not a Mirror protocol and is not imported into `File_51a` as a primary operator.
- Use `File_54` for later comparative claim-status restraint and Mirror role classification where Luke / BJ / Rounded Scaffold synthesis is opened.

## D.14 File_51b Convention Entry — Cross-Polarity Mirror and Deep-Time Translation Engines

Registered June 27 2026 after `File_51b` Finalization and post-final Pressure Test. Source file: `File_51b_Final_Post_Final_Pressure_Tested.md`.

Source-control note:
`File_51b` is the controlling file for formal Cross-Polarity Mirror mechanics and deep-time translation-engine logic. It acts upon the Rounded Scaffold states controlled by `File_51a`. It bridges toward inverse-number architecture but does not replace `File_52a` or `File_52b`, which remain controlling for inverse-number architecture and its technical supplement.

### D.14.1 Registered File_51b state labels

| State | Primary values / forms | Handling |
|---|---:|---|
| File_51b identity state | Cross-Polarity Mirror; Deep-Time Translation Engines | Final; post-final pressure tested June 27 2026. |
| Former File_51a §14 source-history state | former §14 material | Source-history provenance only; local numbering now belongs to `File_51b`. |
| File_51a Rounded Scaffold dependency state | `4106 BC`; `4136 BC`; `4166 BC`; `14006 BC` | Use `File_51a` for Rounded Scaffold mechanics and mod-5 source controls. |
| File_51a §23.7 mod-5 boundary dependency | 5-year mod-5 blocks; rounded boundary-state rendering | `File_51b` may cite this rule; `File_51a` remains controlling for the mod-5 scaffold. |
| Cross-Polarity Mirror mechanics state | BC/AD axis; A-space; target-generation | Core File_51b state. Target-generation must be named before applying the civil span formula. |
| A-space coordinate state | `A = 1 − n`; `n BC → AD (n − 1)` | Used for pure rounded Protocol 1. |
| Protocol 1 state | pure rounded A-space integer Mirror | Use only where both sides are pure rounded states. |
| Protocol 2 state | Tishri/Nisan midpoint-pivot Mirror | Use where actual dates, mixed actual/rounded states, or phase structure are active. Protocol 2 is not a blanket `+1` correction to Protocol 1. |
| Civil cross-axis span state | `BC + AD − 1` | Universal executable civil span after the AD target is generated. |
| Circumcision Fulcrum / Jan-1 datum state | Dec 31, 1 BC / Jan 1, AD 1 | Historical/civil hinge; not a second span operator. |
| `1876 BC` Nexus Mirror state | `1876 BC → AD 1875`; `5980`; `6000`; `5520` | Dependency-controlled by `File_16`, `File_18`, and `File_51a` where needed. |
| 215/430-cycle companion state | `1876/1871 BC`; `1936/1931 BC`; `2081/2076 BC`; `6391/6386 BC`; `4841/4836 BC` | Preserve slash-pairs and node-classes. |
| `8280` cross-modal centricity lock state | `8280 = 23 × 360` | Cross-modal Mirror lock; not a pure rounded self-mirror. |
| MT / LXX `1380` textual-variance ladder state | `8280`; `9660`; `11040`; `1380` | LXX source reconstruction noted; interpretation remains repository-local. |
| Proportional Mirror Partition state | `1656 + 6624 = 8280`; `1:4:5` | Flood-boundary partition; preserve phase-state labels. |
| Pure rounded A-space mirror state | `n BC → AD (n − 1)` | Controls rounded mirror rows where pure rounded state is active. |
| Enoch cross-mirror bisection state | `8496 BC`; `3546 BC`; `8131 BC`; `3181 BC`; `3121 BC`; `6665` | Dependency-controlled by `File_22` / `File_31` where broader Enoch meaning is opened. |
| `11111` self-mirror state | LXX +60 Terah actual Year-6 `5556 BC`; `5556 + 5556 − 1 = 11111`; full-envelope boundary through AD `5555` | Author-resolved: actual Year 6, not rounded. The `−1` reflects full Year-6 envelope handling and is not a fudging error. |
| `11100` rounded corridor state | `5551 BC → AD 5550 = 11100` | Pure rounded boundary-state corridor; distinct from `11111` and `11110`. |
| `11110` mixed-modal translation span state | `4111 / 4106 BC → AD 7000 / AD 7005` | Operational translation corridor; distinct from literal self-mirror and pure rounded corridor. |
| `11106 BC` singularity state | convergence of `11111 / 11110 / 11100` from Incarnation boundary | BC-side source-point; not a second Creation date. |
| `11109` fractional boundary state | inner boundary of the translation engine | Route-independent convergence state; keep distinct from `11110`, `11111`, and `11100`. |
| Biological / Levitical purification arithmetic state | `270`; `280`; `320`; `360`; `40`; `80`; `7 + 33`; `14 + 66` | Route through File_12 / File_13 / File_14 dependencies. `270 = 9 × 30`; `280 = 40 × 7`; male `7 + 33 = 40`; female `14 + 66 = 80`. |
| Raw translation-engine chassis state | `11100 − 60 = 11040`; `8 × 1380`; `11040 × 70/69`; `11040 × 25/23` | Textual-variance engine; exact-ratio expressions require stated operator. |
| Cumulative transfer constant state | `14006 − 4106 = 9900`; `1200`; `21000`; `22200` | Bridge toward File_52a inverse architecture. |
| `11111` palindrome / inverse bridge state | `11111` as base-10 palindrome | File_51b may state the local palindrome fact; inverse-number architecture remains controlled by File_52a / File_52b. |
| LXX source-reconstruction witness boundary | Henry B. Smith Jr. as external LXX reconstruction witness | Smith supports LXX Genesis chronology reconstruction only; he is not the source of File_51b Mirror mechanics or interpretation. |

### D.14.2 File_51b Machine Guards

Machine Guard [FILE_51B / FILE_51A HANDOFF]:
`File_51a` controls Rounded Scaffold mechanics. `File_51b` controls formal Cross-Polarity Mirror mechanics and deep-time translation-engine logic. Do not retroject File_51b Mirror argument burden into File_51a as its primary scaffold argument.

Machine Guard [FILE_51B / FILE_52A HANDOFF]:
`File_51b` may bridge toward inverse-number architecture, but `File_52a` controls inverse-number architecture and `File_52b` controls the technical supplement. Do not make File_51b the inverse-number control file.

Machine Guard [PROTOCOL 1 / PROTOCOL 2 NON-COLLAPSE]:
Protocol 1 is pure rounded A-space integer Mirror. Protocol 2 is Tishri/Nisan midpoint-pivot Mirror. They are not interchangeable, and Protocol 2 is not a blanket `+1` correction to Protocol 1.

Machine Guard [CIVIL CROSS-AXIS SPAN]:
After the AD target-state is generated, every executable civil cross-axis span uses `BC + AD − 1`. Do not introduce a second span operator.

Machine Guard [AD 1406 / AD 1401 DISPLAY]:
Where `AD 1406` or `AD 1401` appears by File_51a §§16.7–16.8 precedent, it is a rounded Conquest-week block display label. Strict Protocol 1 targets remain `1406 BC → AD 1405` and `1401 BC → AD 1400`.

Machine Guard [11111 / 11110 / 11100 / 11109 NON-COLLAPSE]:
`11111`, `11110`, `11100`, and `11109` are distinct translation-engine states. Do not average, harmonize, or silently substitute one for another.

Machine Guard [LXX 5556 ACTUAL YEAR-6 SELF-MIRROR]:
The `5556 BC` member of the `11111` self-mirror state is the LXX +60 Terah actual Year-6 state, not a rounded state. The display `5556 + 5556 − 1 = 11111` is a convenient same-numeral expression of the full Year-6 envelope. It must not be corrected to a rounded `4166 BC`-type state or treated as a fudging error.

Machine Guard [11111 PALINDROME / INVERSE BRIDGE]:
`11111` is a base-10 palindrome. File_51b may state this as a bridge observation, but File_52a and File_52b remain the controlling files for inverse-number architecture. Do not promote File_51b into the inverse-number check file.

Machine Guard [11106 BC SINGULARITY NON-REIFICATION]:
`11106 BC` is a compact BC-side source-point generated by the convergence of translation corridor states. It is not a second Creation date and must not replace any MT, SP, LXX, or Rounded Creation anchor.

Machine Guard [LXX SOURCE-RECONSTRUCTION BOUNDARY]:
Smith may support reconstruction of the LXX Genesis chronological layer. Smith is not the source of File_51b Mirror mechanics, the translation-engine interpretation, inverse-number architecture, or comparative reading.

Machine Guard [BIOLOGICAL / LEVITICAL CALENDAR-SCALING RESTRAINT]:
Biological and Levitical calendar-scaling material remains dependency-controlled by File_12, File_13, File_14, File_20, File_51c, and File_53 where those arguments are opened. Gestation may be expressed as `270` days (`9 × 30`) or `280` days (`40 × 7`) according to the active state. Leviticus 12 divides the male purification state as `7 + 33 = 40` and the female purification state as `14 + 66 = 80`; thus `280 + 40 = 320` for the male completion display and `280 + 80 = 360` for the female completion display. These are state-labeled calendar-scaling comparison rows, not independent claims.

Machine Guard [NO SILENT ARITHMETIC CORRECTION]:
Do not silently correct visible arithmetic, state logic, year labels, route logic, or source-control wording. If an equation or route appears inconsistent, retain it with an Audit note until verification or author decision resolves it.

### D.14.3 Cross-file routing

- Use `File_51a` for Rounded Scaffold mechanics, mod-5 source controls, actual / rounded boundaries, localized `+30 Apparent Age`, `+60 Terah`, cumulative rounded-layer controls, and `File_51a` §23.7 5-year mod-5 block logic.
- Use `File_51b` for formal Cross-Polarity Mirror mechanics, Protocol 1 / Protocol 2 target-generation, civil span target discipline, `8280` cross-modal centricity, and the `11111 / 11110 / 11100 / 11109` translation-engine family.
- Use `File_52a` and `File_52b` for inverse-number architecture, base-10 digit reversal, placeholder force, no-placeholder technical supplement, and downstream inverse proof burden.
- Use `File_12`, `File_13`, `File_14`, `File_20`, `File_51c`, and `File_53` for biological / Levitical / Jubilees / Creation-week comparison states where those materials are opened.
- Use `File_16`, `File_18`, and `File_22` for `1876 BC` Nexus controls, raw chronological table values, cumulative MT, and Rounded Scaffold non-retrojection.
- Use `File_21` and `File_34` for SKL / Berossus / precessional dependency controls where deep-time material touches those systems.
- Use `File_54` for later comparative synthesis style, Mirror role classification, inverse appendix restraint, and claim-status calibration.

### D.14.4 Pointer note

Final post-final pressure-tested `File_51b` remains the controlling source for complete equations, body sections, state transitions, audit notes, pressure-test record, and Revision Log. This Register entry supplies retrieval vocabulary, operator routing, state boundaries, and File_51b Machine Guards. No arithmetic value, anchor, active modal-state logic, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary is changed by this registration. The Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.


## D.15 File_44 Compact Convention Entry — Chronological Scaling Laws `10x / 100x`

Registered June 27 2026 as a bounded public-clean cross-file update after Files_41–45 public-clean cleanup. Source files: `File_44_Public_Clean_v1_0.md` and `File_44_Final_Post_Final_Pressure_Tested.md`.

Source-control note:
`File_44` controls scaling-law vocabulary and state boundaries for macro-harmonic displays that depend especially on `File_42`, `File_43`, and `File_32` / `File_33`. It does not introduce new civil anchors, does not replace the genealogy locks in `File_43`, and does not import File_45 exact-sweep statistics back into File_44.

### D.15.1 Registered File_44 state labels

| State | Primary values / forms | Handling |
|---|---:|---|
| File_44 scaling-law protocol split state | `10x`; `100x`; File_42 / File_43 / File_32 / File_33 dependencies | Scaling vocabulary is state-bound. File_44 classifies patterns already dependent on upstream files; it does not create new anchors. |
| P2 rails-window / A-space bridge split state | `6 BC / 36 BC`; `6 BC / AD 25` | The positive rails-window and negative-polarity / signed A-space bridge remain separate. Do not chain `36 BC ↔ 6 BC ↔ AD 25` as one operator. |
| P2 local Operator A / Operator B restraint | Operator A = P2 positive rails-window; Operator B = P2 negative-polarity / signed A-space bridge | These labels are local to the P2 split and must not be exported to `300/299`, `70/69`, scaling-law operators, or later prime-grid systems. |
| MirrorVolume span-provenance state | `8050 = 5250 + 2800` | Uses `LukeIntervals = 5250` and `MattSpan_BirthHinge = 2800` only. It is not formed from `MattSpan_Carrier = 2870`. |
| Expanded volume / Seal700 state | `8050 × 25/23 = 8750`; `8750 − 8050 = 700 = 10 × 70` | Seal700 is a generated coefficient, not an independent genealogy span and not a substitute input. |
| `12558` exact-ratio expansion state | `14004 BC → 1446 BC = 12558`; `12558 × 300/299 = 12600`; `12558 × 70/69 = 12740`; `12740 ÷ 26 = 490` | Maintained exact-ratio locks by dependency. These are not P2 Operator A / B labels. |
| Creation-head 10x dependency-controlled triad state | `5556 BC`; `5256 BC`; `5956 BC`; `4556 BC`; `400/700/1000` | Arithmetic is verified and dependency-controlled. File_44 preserves and classifies the triad but does not independently authorize these as new anchors. |
| LXX Maximum Year-6 / Adam-state display state | `5556 BC` | File_14-controlled node-label. Not the LXX endpoint `5554 BC`, the Year-7-start boundary `5555 BC`, a generic LXX endpoint, or the File_54 rounded `Year-6 +60` state unless explicitly opened. |
| `5256 BC` shared date-label state | `5256 BC` | Locally Shadow Creation bracket; by File_54 Luke Shadow Creation / LukeIntervals from `6 BC`; by File_43 ledger boundary / cumulative-cube endpoint where opened. Coordinated but non-identical node-classes. |
| Flood Gear `1050` ladder state | `4556 → 3506 → 2456 → 1406`; each step `1050` | BC-only same-side arithmetic. The gear-language function is structural inference. |
| Schematic Noah / literal MT / subgear non-collapse state | schematic `4556 BC` / `3506 BC`; literal MT Flood / Arphaxad `2456 BC`; MT Noah death subgear `2106 BC` | These states are related but not interchangeable. The subgear observations do not substitute into the ladder. |
| Display-only macro motif state | `4200→4900`; `18200 = 50 × 364`; display-only `30 + 40` | Arithmetically true display motifs. They are not locks, target generators, or substitution rules. |
| mod-50 gearbox display state | MT / SP / LXX offset commentary; mod-50 visibility | Commentary / shared-tradition evidence / theological note / providential synchronization where opened. It does not prove direct scribal intent or collapse witness traditions. |
| LXX source-reconstruction witness boundary | Henry B. Smith Jr. as external LXX Genesis 5 / 11 reconstruction witness | Smith supports LXX reconstruction only; he is not the source of File_44 scaling-law interpretation, MirrorVolume, Seal700, SKL / Berossus integration, or providential synthesis. |
| File_43 / File_44 / File_45 proof-burden boundary | File_43 genealogy locks; File_44 scaling controls; File_45 exact-sweep statistics | Use File_43 for genealogy-node locks and File_44 for scaling-law controls. Do not import File_45 statistics back into File_44 or use File_44's Creation-head triad as File_45 target proof unless a later file explicitly opens that dependency. |

### D.15.2 File_44 Machine Guards

Machine Guard [FILE_32 / FILE_33 TERMINOLOGY]:
When prime-grid, P2, polarity, rails-space, A-space, or Mirror language is active, use the File_32 / File_33 Prime–Civil / Polarity Namespace. Positive-polarity date-output / rails-space `B/M`, negative-polarity / signed A-space date-output, prime-space cross-polarity, Coordinate Mirror / Protocol 1, and civil cross-axis arithmetic remain distinct.

Machine Guard [P2 NON-STRADDLING]:
Do not chain `36 BC ↔ 6 BC ↔ AD 25` as one continuous operator. The P2 positive rails-window is `6 BC / 36 BC`. The P2 negative-polarity / signed A-space bridge is `6 BC / AD 25`.

Machine Guard [P2 LOCAL LABELS]:
Where `File_42` or `File_43` uses local labels `Operator A` and `Operator B`, those labels refer only to the P2 split and must not be reused for unrelated ratio expansions or scaling-law operators.

Machine Guard [SPAN PROVENANCE]:
MirrorVolume `8050` uses `LukeIntervals = 5250` and `MattSpan_BirthHinge = 2800` only.

Machine Guard [MATTHEAN CARRIER PROHIBITION]:
`MattSpan_Carrier = 2870` must not be substituted into `8050`, `8750`, or Seal700.

Machine Guard [SEAL700 STATUS]:
Seal700 is generated by expansion difference: `8750 − 8050 = 700 = 10 × 70`. It is not an independent genealogy span and not a substitute input.

Machine Guard [TRIAD DISPLAY RESTRAINT]:
Triads or generational displays using `40`, `70`, and `100` are not locks unless independently authorized by the active dependency state. Display motifs do not generate anchors.

Machine Guard [ROUNDED MOTIF NON-RETROJECTION]:
Rounded or visibility conventions such as `18200 = 50 × 364` under rounded anchors do not retroject into actual chronology, Flood Gear, MirrorVolume, or Seal700.

Machine Guard [MT / SP / LXX WITNESS STATES]:
Where MT, SP, and LXX are compared, preserve them as distinct textual / chronological witness states. Do not collapse them into one literal chronology, and do not overstate demonstrable scribal intent unless a local source-critical dependency supplies that evidence.

Machine Guard [5556 BC NODE-CLASS]:
In `File_44`, `5556 BC` is the LXX Maximum Year-6 / Adam-state display by File_14 dependency. It is not the LXX Maximum endpoint display `5554 BC`, the Year-7-start boundary `5555 BC`, or a generic LXX endpoint.

Machine Guard [5256 BC SHARED DATE-LABEL]:
In `File_44`, `5256 BC` is a shared date-label. Locally it functions as the Shadow Creation bracket. By File_54 dependency it is Luke Shadow Creation / LukeIntervals from `6 BC`. By File_43 dependency it may function as a ledger boundary / cumulative-cube endpoint. These are coordinated but non-identical node-classes.

Machine Guard [CREATION-HEAD TRIAD CONDITION]:
The `400/700/1000` Creation-head triad is arithmetically verified and dependency-controlled. `File_44` may preserve and classify the triad, but it does not introduce or independently authorize `5556 BC`, `5256 BC`, `5956 BC`, or `4556 BC` as new anchors.

### D.15.3 Cross-file routing

- Use `File_32` / `File_33` for Prime–Civil / Polarity Namespace, P2 rails-space, P2 signed A-space, Coordinate Mirror / Protocol 1, and civil cross-axis arithmetic.
- Use `File_42` as geometry-first source-history support for the P2 dual-operator split, Key-of-23 expansion background, MirrorVolume, and Seal700.
- Use `File_43` for LukeIntervals, MattSpan_BirthHinge, MattSpan_Carrier, MirrorVolume, Seal700, P2 non-straddling, and genealogy-node proof burden.
- Use `File_14` for the LXX Maximum `5556 BC` Year-6 / Adam-state display boundary.
- Use `File_54` for Luke Shadow Creation / Rounded Scaffold comparison controls where `5256 BC` appears in rounded or comparative context.
- Use `File_15` for authorized `40 / 70 / 100` generational-unit state; File_44 display motifs do not independently authorize those units as locks.
- Use `File_17` and `File_22` for exact-ratio grammar and cumulative MT harmonic-scalar controls where `12558`, `12600`, `12740`, and `490` are opened.
- Use `File_51a` for Rounded Scaffold non-retrojection and rounded visibility motifs such as `18200 = 50 × 364`.
- Use `File_45` only as the downstream exact-sweep file. File_45 may cite File_44 for scaling-law controls, but File_44 does not import File_45 statistics.

### D.15.4 Pointer note

Final post-final pressure-tested `File_44` and `File_44_Public_Clean_v1_0.md` remain the controlling sources for complete equations, body sections, state transitions, audit notes, pressure-test status, and publication-clean source text. This Register entry supplies retrieval vocabulary, operator routing, state boundaries, and File_44 Machine Guards. No arithmetic value, anchor, active modal-state logic, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary is changed by this registration. The Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.


## D.16 File_50a Compact Convention Entry — Prime Quadruplet Naturalized Master Table

Registered June 27 2026 as a bounded File_50 public-clean control-registration follow-up. Source file: `File_50a_50b_Public_Clean_v1_1_Control_Registered.md`, Part A, preserving `File_50a` Final summary / source-pointer content and the full P1–P100 Naturalized Master Table.

Register-scope note: This entry registers File_50a-specific modal-state vocabulary, operator routing, and Machine Guards for direct AI retrieval. It does not re-run the File_50a table, revise File_50c's Bipartite Grid Landing operator, change File_32 / File_33 prime-polarity outputs, or promote File_50a civil outputs into independent chronological anchors.

### D.16.1 Registered File_50a state labels

| State | Primary values / forms | Handling |
|---|---:|---|
| File_50a geometric data-layer state | P1–P100 Naturalized Master Table | File_50a supplies the reusable prime-grid input table for the File_50 branch. It is an input/data layer, not an executable chronology by itself. |
| Naturalized Master Table retrieval state | indices `1–100`; prime-quadruplet starts; `Original`; `Naturalized`; Grid; Civil; Diff | The table is retained in full in the public-clean combined File_50a_50b source. Future retrieval should route to File_50a / §D.16 for table vocabulary rather than relying only on File_50c §D.10. |
| Prime-quadruplet start input state | source label OEIS A007530; `P` / `Q` prime-quadruplet start | Input sequence for File_50a naturalization. Do not treat `P` / `Q` as a civil date until the File_50a operator state is named. |
| Naturalization state | `P_nat = P − 11` | Anchors the ordinary system at P2. This is File_50a's naturalized-grid operator, distinct from File_32 / File_33 polarity-date projection. |
| Base-90 grid state | `V = round(P_nat / 90) × 90`; `V_90` | Nearest multiple-of-90 grid value. File_50c may consume this as `V_90`, but File_50a controls the table input. |
| Civil clutch state | `Civil = V + 6`; `Civil = V_90 + 6` | Converts the grid value to the File_50a civil display. Civil outputs are chronology outputs or comparison coordinates controlled by their local chronology files when used as anchors. |
| Three-state Diff / gear-selector state | `D = P_nat − V ∈ {0, +30, −30}` | The Diff value is a state selector. It is not a general tolerance band and not a free `+30` Apparent Age or SKL rail operator. |
| P2 naturalization-zero / Grid Origin state | P2 `P = 11`; `P_nat = 0`; Civil `6 BC` | Mathematical origin of the ordinary File_50a grid. Do not collapse with File_32 / File_33 P2 rails-window or P2 A-space bridge unless that dependency is explicitly opened. |
| P1 no-naturalization bypass / Civil Datum state | P1 returns `AD 1` | Exceptional state. P1 is not executed through the ordinary `P_nat`, Base-90, or File_50c Bipartite Grid Landing path. |
| Civil scale-reading state | Civil display from `V + 6` | File_50a display / comparison scale; not a stand-alone chronological proof. |
| Sumerian-360 scale-reading state | Sumerian / `360`-scale interpretation by File_50a source context | Structural or typological scale reading unless a controlling SKL file opens executable SKL mechanics. |
| Cosmic-25920 scale-reading state | precessional / `25920` scale interpretation by File_50a source context | Structural or typological scale reading. It does not replace File_21 / File_34 precessional and SKL / Berossus controls. |
| Mod-7 admission-constraint state | File_50a source-level filter | Retained as File_50a source vocabulary. The full apparatus remains source-pointer / raw-source controlled unless opened by a later file. |
| File_50b interpretive-synthesis dependency state | File_50b reads File_50a outputs | File_50b remains an interpretive synthesis layer and has no separate Part D entry in this pass. |
| File_50c executable handoff state | File_50c consumes `P_nat` and `V_90` | File_50c controls Bipartite Grid Landing, exact strikes, target hits, scan totals, and File_50c Machine Guards at §D.10. |

### D.16.2 File_50a operator controls

The reusable File_50a operator set is:

$$P_{nat}=P-11.$$

$$V=round(P_{nat}/90)\times90.$$

$$Civil=V+6.$$

$$D=P_{nat}-V.$$

For ordinary File_50a rows after P1, the Diff state is:

```markdown
D ∈ {0, +30, −30}
```

P1 remains outside the ordinary operator path. P2 is the naturalization-zero / Grid Origin state.

### D.16.3 File_50a Machine Guards

Machine Guard [FILE_50A DATA-LAYER BOUNDARY]:
File_50a is the geometric data and input-table layer for the File_50 branch. Do not treat File_50a civil displays as independent repository anchors unless a controlling chronology file or downstream File_50 operator explicitly opens that coordinate as a target state.

Machine Guard [FILE_50A P1 BYPASS]:
P1 returns the exceptional Civil Datum `AD 1`. Do not execute P1 through `P_nat`, Base-90 rounding, ordinary Diff-state classification, or File_50c Bipartite Grid Landing.

Machine Guard [FILE_50A P2 GRID ORIGIN]:
P2 with `P = 11` and `P_nat = 0` is the File_50a Grid Origin. Do not collapse this state with File_32 / File_33 P2 positive rails-window `6 BC / 36 BC`, P2 negative-polarity / signed A-space bridge `6 BC / AD 25`, or Coordinate Mirror unless the local file explicitly opens that separate dependency.

Machine Guard [FILE_50A DIFF STATE NON-COLLAPSE]:
The Diff values `0`, `+30`, and `−30` are File_50a gear-selector states. Do not convert them into arbitrary rounding tolerance, File_34 `+30 Apparent Mode`, File_51a `+30 Apparent Age`, or a generic chronological correction.

Machine Guard [FILE_50A PRIME-POLARITY FIREWALL]:
File_50a Naturalized Grid outputs are not File_32 / File_33 positive-polarity date outputs, negative-polarity / signed A-space outputs, prime-space cross-polarity, rails-space `B/M`, or Coordinate Mirror. A shared prime index such as P23 does not create coordinate identity.

Machine Guard [FILE_50A FILE_50C HANDOFF]:
Use File_50c and Register §D.10 for Bipartite Grid Landing, exact-strike signed tri-position, target-hit multiplicity, coordinate-family compression, corrected `72 / 28 / 16` scan state, and nearest-720 half-step tie handling. File_50a supplies the input table and operators only.

Machine Guard [FILE_50A SCALE-READING CLAIM-STATUS]:
Civil, Sumerian-360, and Cosmic-25920 scale readings in File_50a are structural inference or typological / providential interpretation unless the local file supplies an executable operator and dependency path. They do not establish demonstrated causation, formal probability, or direct ancient authorial intent by themselves.

Machine Guard [FILE_50A RAW-SOURCE POINTER]:
The public-clean combined File_50a_50b source carries the full Naturalized Master Table and repository-side controls. Remaining raw apparatus and raw rewrite issues stay source-pointer / audit-controlled and must not be silently resolved in the Register.

### D.16.4 Cross-file routing

- Use `File_50a` / Register §D.16 for the P1–P100 Naturalized Master Table, `P_nat`, Base-90 grid `V` / `V_90`, Civil clutch, Diff states, P2 Grid Origin, P1 bypass, and File_50a scale-reading vocabulary.
- Use `File_50b` for interpretive synthesis of File_50a outputs. File_50b still has no separate Part D entry unless a later pass requires direct retrieval of its synthesis vocabulary.
- Use `File_50c` / Register §D.10 for Bipartite Grid Landing, exact-strike execution, target intersections, coordinate-family compression, corrected scan totals, and File_50c Machine Guards.
- Use `File_32` / `File_33` for Prime–Civil / polarity namespace controls. File_50a naturalization does not replace positive- or negative-polarity prime-date outputs.
- Use `File_21` and `File_34` for SKL / Berossus derivation, SKL Short / Long states, `+2` / `+720` firewalls, and deep-time source-control where those states are opened.

### D.16.5 Pointer note

`File_50a_50b_Public_Clean_v1_1_Control_Registered.md`, Part A, remains the controlling public-clean source for File_50a table content, source-pointer boundaries, audit notes, and local claim-status controls. This Register entry supplies compact retrieval vocabulary and Machine Guards only. No arithmetic value, table value, anchor, active modal-state logic, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary is changed by this registration. The Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.



## D.17 File_56 Convention Entry — Creation Decade Protocol / SP Enochian Day-4 Phase-Anchor

Registered July 2 2026 at File_56 author-invoked Finalization, by author direction, from the Creation-decade discussion record. Source file: `File_56_Creation_Decade_Protocol_SP_Enochian_Day4_Phase_Anchor.md` (Final; Finalization July 2 2026; full pressure test scheduled by author).

Register-scope note: This entry registers File_56-specific modal-state vocabulary, operator routing, and Machine Guards for direct AI retrieval, and records the author-directed cross-file amendments routed to File_07 §1, File_18 §6C.1, and the File_12 bibliography vector. It does not re-run the File_18 cumulative tables, does not alter the File_11 §9 Watcher Bridge, and does not promote File_56 decade-boundary labels into repository endpoint anchors.

### D.17.1 Registered File_56 state labels

| State | Primary values / forms | Handling |
|---|---|---|
| SP Enochian Day-4 phase-anchor state | SP cumulative envelope `13403–13396 BC` phase-labeled Days 4–10; implied Day 1 `13406 BC` | Cumulative-only. Coexists with the default Day-1 / Year-7 reading of File_18 §6C. Does not propagate to SP regular chronology (`4206–4199 BC` remains Day-1 / Year-7). Orthogonal to the SP Toggle ON inclusive-reckoning operator. |
| Creation decade envelope | Days 1–10; DecNorm `10`; DecSpread `10.5 = 3.5 + 7 = 3.5 × 3` | Decade extension of EnvNorm `7` / EnvSpread `7.5`. The `10.5` display preserves the Priestly-Enochian `3.5` intersection at decade scale without opening the phase-resolved state. |
| Day-10 terminus node-class | MT `14001 BC`; LXX `14891 BC`; SP `13396 BC` | Each equals Day-1 start minus `10`. SP Day-10 carries `13396 − 1296 = 12100 = 110 × 110`. |
| Implied Day-1 label state | `13406 BC`; restored-Cainan `13866 BC` | Display labels, not repository endpoint anchors. `13406 − 1406 = 12000`; `13406 − 1446 = 11960 = 299 × 40`. |
| Aaron-member Adam selection (SP) | Aaron member `13401 BC = 13403 − 2` bears the Adam / sixth-day (Friday) label; Moses member `13398 BC` retained as machinery anchor | The `+3` phase shift is exactly absorbed by the Aaron/Moses gap `3`. File_11 §9 (`13398 − 4198 = 9200; × 25/23 = 10000`) is unchanged. |
| Decade segment-marking states | SP `3+7`; MT / LXX `7+3` | All traditions recognize the full ten-year field; each marks a different seven-year segment. |
| 299-multiple Exodus lock state | SP `11960 = 299 × 40 → 12000`; MT `12558 = 299 × 42 → 12600`; LXX `13455 = 299 × 45 → 13500` | Same-side spans to `1446 BC` under `300/299`. Residues `40 / 42 / 45` are conversion gains with typological labels only. `12558 × 10 = 125580` is a File_33 converging witness. |
| 1495 dual-expansion state | `14901 − 13406 = 1495 = 23 × 65 = 299 × 5`; `× 25/23 = 1625`; `× 300/299 = 1500` | LXX / SP Day-1 gap; gains `+130` (LXX / Cainan signature) and `+5`. |
| SP restored-Cainan Day-1 state | `13866 BC = 13406 + 460`; `12420 = 69 × 180 → × 70/69 = 12600`; `1035 = 23 × 45` to LXX | Cumulative `+460` rail only. Generates the temple pair `966 BC / 516 BC` (`10 × 1290` / `10 × 1335`; differential `450`) and the messianic chain `1266 BC → 6 BC`. |
| 605 phase-invariant gap state | `14011 − 13406 = 605`; `14471 − 13866 = 605`; `605 = 600 + 5 = 5 × 11 × 11` | Invariant under Cainan restoration on both sides. Fives-block reading is exploratory. |
| 12000 Creation–Conquest transposition state | `13406 − 12000 = 1406`; boundary-for-boundary overlay; Nisan 10 entry-ordinal resonance (Joshua 4:19) | Same-side BC subtraction; not a Mirror protocol. `12000` already a controlling BJ-field value (File_53 §§23–24); `13406 ↔ 1406` decimal echo under File_25. |
| Priestly Day-1 origin state | `336` Priestly year from Day 1, Sunday to Sabbath, light without the sun | Etiology for the File_12 Saturday-evening / Sunday-onset vector; theological / structural framing; File_12 vectors unaltered. |
| Solar Day-4 origin state | `364` / harmonic `360` solar calendar from Day 4, Wednesday, the sun as lamp of the light | Etiology for the File_12 Wednesday-morning vector; Revelation 21:23 / 22:5 typology. |
| 12600 dual-convergence boundary state | MT and SP restored-Cainan converge on `12600` to `1446 BC`; LXX excluded | Record the LXX exclusion as structural datum; the LXX stands on the `299 × 45 / 13500` rail. |
| Antediluvian 364 presupposition state | 1 Enoch as shadow of antediluvian 364 usage; Jude 14–15 (1 Enoch 1:9); comparison rail 1 Enoch 18:16; 21:6; 2 Peter 2:4; Jude 6 | Tier-1 theological claim on the File_20 §11.1 rail; not load-bearing for the arithmetic. |
| Egyptian-refinement etiology state | Egyptian `360 + 5 = 365` refined to `360 + 4 = 364 = 52 × 7`; lunar-quarter Sabbath seed | Tier-2 historical-critical inference; author-flagged exploratory theory. |
| Pre-exilic solar-priority hypothesis state | Solar mechanism from the Exodus era; Babylonian lunarization post-exile; 1 Enoch 73–74 / 78–79 as transition witness | Tier-2 historical-critical inference under the divided Jaubert / VanderKam versus Ben-Dov / Wacholder witness field. |
| Mirror-test landing state | `13406n BC` / `13407t BC` → `AD 1406t` / `AD 1407n`; span `14812 = 23 × 23 × 28`; `14812 × 25/23 = 16100 = 7 × 2300` | Protocol Mirror civil cross-axis display under `BC + AD − 1` with same-suffix pairing. Distinct from the same-side 12000 transposition; `AD 1406` / `AD 1407` are Mirror display labels, not repository endpoint anchors. |
| Mirror-test principle | one sufficient landing per pattern field | All File_56 patterns must land meaningfully in the Mirror; the File_56 §6A landing is the sufficient demonstration for the decade field. A meaningfulness criterion for pattern admission, not an anchor generator. |

### D.17.2 File_56 operator controls

The decade boundary ladder for a tradition with Day-1 start boundary `S`:

```markdown
Day n start = S − (n − 1)
Day n end = S − n
```

The Day-1 ↔ Day-4 phase shift is `+3` whole years at the boundary register. It is a distinct node-class from the File_12 `3.5`-day liturgical straddle.

### D.17.3 File_56 Machine Guards

Machine Guard [SP DAY-4 PHASE-ANCHOR SCOPE]:
Cumulative-only. SP regular chronology remains Day-1 anchored (`4206–4199 BC`; Year-7 completion `4199 BC`); the Toggle ON inclusive-reckoning operator remains distinct and orthogonal.

Machine Guard [DECADE ENVELOPE NON-REPLACEMENT]:
Decade envelopes coexist with, and do not overwrite, the canonical 7-year envelopes of File_18 §§6B–6D. The default Day-1 / Year-7 reading remains active wherever the phase-anchor state is not named.

Machine Guard [AARON-MEMBER ADAM SELECTION]:
The `+3` phase shift and the Aaron/Moses gap `3` coincide; the coincidence is corroborating structure, not identity. Do not derive one from the other; do not migrate the Adam / sixth-day label from `13401 BC` to `13398 BC` under the phase state.

Machine Guard [PHASE-SHIFT / STRADDLE FIREWALL]:
The 3-year phase shift (whole-year boundary register) and the `3.5`-day liturgical straddle (File_12 sub-day register) are distinct node-classes. DecSpread `10.5 = 3.5 + 7` relates them without merging them.

Machine Guard [TRACK A 1411 PROTECTION]:
The MT Day-10 register (`14001 BC`; derived label `1401 BC`) must not substitute into the File_06 Track A lock; the protected target remains `1411 BC` with `14011 − 1411 = 12600`.

Machine Guard [CUMULATIVE CAINAN +460 RAIL]:
Cumulative Cainan restoration uses the `460` lifespan insertion only; never `+130` on the cumulative rail.

Machine Guard [KINGS TEMPLE ANCHOR CONTROL]:
`966 BC` and `516 BC` remain File_08 Kings-controlled anchors; the `10x` Daniel spans are providential synchronization, not derivations of those dates.

Machine Guard [RESIDUE THEME RESTRAINT]:
The `40 / 42 / 45` residues are `300/299` conversion gains with typological labels; they are not added historical years.

Machine Guard [12000 TRANSPOSITION OPERATOR]:
`13406 − 12000 = 1406` is same-side BC subtraction; do not restate as a Mirror protocol.

Machine Guard [MIRROR-TEST BOUNDARY]:
The File_56 §6A Mirror landing runs under `BC + AD − 1` with same-suffix pairing (`13406n → AD 1407n`; `13407t → AD 1406t`); the unpaired `13406 + 1406 − 1 = 14811` does not check. Do not collapse the Mirror landing with the same-side 12000 transposition. `AD 1406` / `AD 1407` are Mirror display labels, not repository endpoint anchors.

Machine Guard [300/299 HARMONIC SOLAR INSTRUMENT]:
`300/299` is the harmonic Enochian / solar conversion instrument, not exact solar equivalence (`364 × 300/299 = 365.2174` against solar display `365.24`). Residue-field meaning is deferred by author direction.

Machine Guard [12600 DUAL-CONVERGENCE BOUNDARY]:
The `12600` convergence belongs to MT and SP restored-Cainan only; the LXX is excluded and must not be forced onto that constant.

Machine Guard [TIER FIREWALL]:
Tier-1 presuppositional and Tier-2 historical claims motivate but do not carry the Tier-3 arithmetic; the phase-anchor state stands on the arithmetic alone.

Machine Guard [FILE_11 BRIDGE PRESERVATION]:
The File_11 §9 Watcher Bridge is unchanged; the Moses member `13398 BC` remains its input.

### D.17.4 Cross-file routing and routed amendments

- Use `File_56` / Register §D.17 for the Creation decade ladder, tradition decade tables, Aaron-member Adam selection, 299 Solar Ladder, 12000 transposition, restored-Cainan decade states, segment-marking pattern, and tier firewall.
- `File_07` §1 carries the author-directed SP Day-4 exception amendment to the Day-7 endpoint axiom (July 2 2026; pressure test pending).
- `File_18` §6C.1 carries the author-directed SP Day-4 phase-anchor display amendment (July 2 2026; pressure test pending); the §6C default table is unchanged.
- `File_12` bibliography vector carries the Jaubert addition and divided-witness note (July 2 2026; pressure test pending); File_12 vectors, straddle, and source-control map are unchanged.
- Use `File_11` §9, `File_20` §§6H / 11, `File_53` §§17–24, `File_06` §7, `File_33`, `File_25`, and `File_08` as unchanged dependencies per the File_56 cross-reference index.

### D.17.5 Pointer note

`File_56` (Final) is the controlling source for its own states; this Register entry supplies compact retrieval vocabulary, Machine Guards, and the routed-amendment record. No pre-existing arithmetic value, table value, anchor, modal-state logic, node-class, sign convention, operator, Mirror protocol, theological claim, or dependency boundary is changed by this registration. The Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.


## D.18 Regular / Cumulative Page-Publication Convention Entry — July 2026 Bounded Pass

Scope: registers the state vocabulary, conventions, and guards created by the July 2026 Regular / Cumulative chronology-page rewrites and the sixteen-file bounded corpus pass (Files 01, 03, 07, 13, 14, 16, 17, 18, 20, 22, 44, 46, 51a, 51b, 53, 56). Vocabulary registration only; the controlling files hold the derivations.

### D.18.1 Terminology deprecation

"Long" / "Short" are vestigial synonyms for Cumulative / Regular. Retained only as legacy URL tokens and as one-time page alias clauses; not for corpus or heading use. Collision guard: external text-critical literature uses "long / short chronology" for the LXX-versus-MT begetting-figure distinction — a different axis; do not import that sense.

### D.18.2 Registered states, by controlling file

| State cluster | Controlling file / section |
|---|---|
| SOR MT-sub-variant; SOR landing-lattice; Covenant–Nexus shared-label; SOR variant-lattice / Danielic completion; Flood / Cainan cascade (MT-preserving graft); minimal-Kainan invariance; Abram / Sarah 500/490; 80-jubilee triple-rod; Danielic eight-cycle; twin-2450 pairing | `File_20` §7A |
| LXX original-Lamech; Lamech transform-pair `(182, 595, 777) ↔ (188, 565, 753)`, quanta `+6 / −30 / −24`; dual-locality; `777` overlay-row (row-local); `430`-alignment justification; Flood-marker convention set; Kohath / Amram silence | `File_18` §4A / §6D |
| Ezekiel date-string (day-quartet `5 \| 7 \| 10 \| 12 = 2 + 3 + 2`; `2550 = 1260 + 1290`; `2730 = 430 + 2300`) | `File_17` §13A |
| Deep-time overlay application; twenty-of-years pair; staged-Flood display; double-expansion licensing corollary; Adam-field expansion (`920 → 1000`) | `File_46` §6A |
| `12558` dual-placement; page-derived ×10 display; backbone field-harmonic; exact-ratio increment corollary `23n → +2n` | `File_44` §3.1 / §6A |
| Adam-field Mirror lock (`23 / 24 × 920`); Shem `±2` lock-key; providential claim-split | `File_51b` §7A |
| Cumulative apparent-span (`14041–14034`; `14036`); five-arm `10080` lattice; Revelation-11 ×10 template; inferred secondary-node class (`1441 BC`); Joshua register-conversion; center-jewel | `File_22` §9A |
| Composite-head Joshua lock (`13896 → 1296 = 12600`) | `File_56` §7A |
| Conception-anchor rail; preincarnate / Son-of-Man node labels; SOR rod-termini classification; temple AD-image display | `File_13` §7.4 / §8 |
| Consecutive-birth interval application (`File_51b` §14 controlling) | `File_14` §2A |
| Levi / Judah post-Jacob span | `File_03` §2.3A |
| SOR register extension; Benjamin `1906` birth-label | `File_01` |
| Four-roads shared-label display (`1876 BC`) | `File_16` §2A |
| Unified Enoch accounting; SP Enoch 364-day compression; relocated Enoch display block | `File_53` §14A |
| SOR `−5` mod-5 absorption | `File_51a` |
| SOR full-stack `4116 BC` shared-label note | `File_07` |

### D.18.3 Guards registered

- Non-propagation: the Lamech `777` overlay is row-local, confined by the LXX Exodus lock `14901 − 1446 = 13455 = 299 × 45` (`File_56` dependency).
- Non-collapse set: `File_46` §5 lattice-local Cainan non-lock versus deep-time `+460`; `6624` two-forms (`File_51b` §7 partition remainder versus `File_46` §6A overlay span); `8600` two-forms (span `2 × 4300` versus precessional season `120 × 71⅔`); `14036 / 14041 / 14046`; `2700 / 2730` day-scale versus year-scale; BJ `AD 30` frame versus SOR rod termini; P2 / `AD 25` non-chaining (`File_44`); shared year-labels `1876` (four events), `1846`, `1906`, `1656` (three kinds), `3856`, `4026`, `3896`, `4116`, `1871`.
- The `File_13` §0.4 straddling prohibitions are expressly unaffected.

### D.18.4 Supersessions (author-ruled; non-silent-repair)

1. `File_18` regular-table guard against Lamech `777` — superseded for the regular table; retained for the cumulative chain value.
2. `File_18` item 11 and §4.1 prior clarification — superseded in matching terms.
3. `File_20` informal SOR "−215-sub-variant" framing — superseded by the MT-sub-variant reclassification (`−220 = −(215 + 5)`).

### D.18.5 Principle candidate (pending)

Explicit-versus-inferred node principle — inferred figures (the `+30` apparent age; `1441 BC`) admitted where the text's own devices warrant the selection and independently dated texts exhibit the same signature; deliberate scribal economy leaves secondary nodes unstated. Routed to `File_15` / Style Guide commentary (`File_22` Recommended 0A); registered here as pending, not active.

### D.18.6 Routing

Use the controlling files and sections of §D.18.2 for all derivations. Repository_Change_Archive insert-block issued with this pass; Restart Capsule v11.18 pending as the pass's final step.

---

## D.19 Ezekiel Endpoint / Intercalation-Cycle Convention Entry — July 2026

Scope: registers the vocabulary of the deferred-item-6 resolution and the 360-day intercalation cycle. Controlling files hold the derivations.

### D.19.1 Registered states, by controlling file

| State cluster | Controlling file / section |
|---|---|
| 360-day intercalation cycle state (`14610 = 40 × 365.25`; identities `2190 = 6 × 365`, `1470 = 3 × 490`); counted-or-not option principle; `1260 / 1290` same-half-week corollary; year-scale instantiation display (AD 25n exact closures `2190` / `1470`) | `File_12` §7A |
| Siege-frame derivation state (`1075 = 2.5 × 430`; inclusive close `1655 / 1656`); leap-month scheme-instance state; the `2580 = 430 + 2150 = 430 × 6` sibling with the Lamech cross-scale twin display; §13A.8 endpoint closure (fall of the temple, intercalated form; four Danielic forms complete in both orders); SP year-mirror display state (three chains at `1876 / 1846` heads) | `File_17` §13A (v5.3) |

### D.19.2 Guards

Julian-mean equivalence versus the `File_09` Julian Jan-1 phase-coordinate proof — distinct objects sharing a word; do not collapse. The `2730` dual reading (leap-month form `2700 + 30`; Enochian-register form `7.5 × 364`) — no collapse. Any leap-month instance declares its placement (a longer window may cross one or two cycle-ends). `File_13` §0.4 straddling prohibitions unaffected.

### D.19.3 Claim-splits

The intercalation scheme: author-declared system / structural inference (reconstructed, not text-stated; witnessed by its forced structure and Julian-mean landing). The intended-option reading of Ezekiel: historical-critical inference. The SP year-mirror: structural inference / providential synchronization. The day-for-year license: textual datum (Ezek. 4:6). The siege-duration `2.5` years: author ruling / historical-critical inference.

### D.19.4 Open-item ledger

`File_17` §13A.8 — closed (endpoint derived, July 2026). No new open items.

---

## Revision Log (v1.20)

- Date: July 6 2026.
- Basis: the deferred-item-6 resolution discussion; File_12 §7A and File_17 v5.3 bounded amendments; focused pressure tests (15/15 PASS).
- Changes: (1) §D.19 registered; (2) version / status lines updated. No pre-existing definition, Machine Guard, arithmetic value, anchor, modal state, node-class, sign convention, operator, slash-pair, range, envelope, Mirror protocol, claim-status label, theological claim, or dependency boundary altered.

## Revision Log (v1.19)

- Date: July 6 2026.
- Basis: author-directed bounded control-file update from the Regular / Cumulative chronology-page publication discussion record and the sixteen-file bounded corpus pass.
- Changes: (1) Regular / Cumulative Page-Publication Convention Entry registered as Part D §D.18; (2) the D.18.2 state clusters registered by controlling file; (3) the D.18.3 non-propagation and non-collapse guard set registered; (4) three author-ruled supersessions recorded per the non-silent-repair rule; (5) the Long / Short terminology deprecation registered; (6) the explicit-versus-inferred node principle recorded as pending; (7) version / status lines updated. No pre-existing definition, Machine Guard, arithmetic value, anchor, modal state, node-class, sign convention, operator, slash-pair, range, envelope, Mirror protocol, claim-status label, theological claim, or dependency boundary altered.

## Revision Log (v1.18)

- File: `State_Vocabulary_Register_v1.18`
- Revision date: July 2 2026
- Prior version: v1.17 retained unaltered as the prior-version record.
- Passes completed: bounded control-file update registering the File_56 Convention Entry at author-invoked Finalization, from the author-directed Creation-decade discussion record; same-pass supersession of the interim same-day Draft registration before installation; Machine Guard drafting; routed-amendment recording for File_07 §1, File_18 §6C.1, and the File_12 bibliography vector; pointer audit; machine diff.
- Changes: (1) `File_56` Convention Entry registered as Part D §D.17; (2) nineteen File_56 state labels registered, including the SP Enochian Day-4 phase-anchor state (cumulative-only), Creation decade envelope with DecNorm 10 / DecSpread 10.5, Day-10 terminus node-class, implied Day-1 label states, Aaron-member Adam selection, 299-multiple Exodus lock state, 605 phase-invariant gap state, 12000 transposition state, Mirror-test landing state, Mirror-test principle, and the Tier states; (3) fourteen File_56 Machine Guards registered; (4) author-directed cross-file amendments to File_07 §1, File_18 §6C.1, and the File_12 bibliography vector recorded as routed amendments pending pressure test; (5) version/status lines updated.
- Arithmetic status: no pre-existing arithmetic changed. The File_56 arithmetic-control ledger introduces new displayed calculations, all machine-verified at drafting and carried by File_56 §0.6.
- Anchor status: no Primary anchors inventory change. The File_56 decade-boundary values (`13406 BC`; `13866 BC`; `14001 BC`; `14891 BC`; `13401 BC`; `1401 BC`) and the Mirror display labels (`AD 1406t`; `AD 1407n`) are derived display labels or Tier-2/local coordinates under §D.3, governed by File_56 and this entry; `13398 BC`, `13396 BC` context, and the tradition envelopes remain governed by their existing controlling files.
- Cross-file status: Restart Capsule v11.16 and Style Guide v2.5 pointer refresh are supplied as full replacements. File_07, File_18, and File_12 receive bounded author-directed amendments supplied as full replacements, pressure tests pending. No content amendment is required for File_00, File_06, File_08, File_11, File_20, File_25, File_33, File_53, File_55, or Project Procedures v3.2. Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.
- Status: v1.18 active; File_56 Final at author-invoked Finalization July 2 2026; full pressure test scheduled by author.

## Revision Log (v1.17)

- File: `State_Vocabulary_Register_v1.17`
- Revision date: June 27 2026
- Prior version: v1.16 retained unaltered as the prior-version record.
- Passes completed: bounded File_50 public-clean control-registration follow-up for `File_50a`; direct-retrieval routing audit against File_50a_50b and File_50c; Machine Guard drafting; pointer audit; machine diff.
- Changes: (1) `File_50a` Compact Convention Entry registered as Part D §D.16; (2) File_50a naturalized-grid state, Naturalized Master Table retrieval state, naturalization, Base-90 grid, Civil clutch, three-state Diff, P2 Grid Origin, P1 bypass, scale-reading states, Mod-7 admission-constraint state, and File_50c handoff state registered; (3) eight File_50a Machine Guards registered; (4) File_50a / File_50b / File_50c routing recorded; (5) version/status lines updated.
- Arithmetic status: no underlying arithmetic changed; no new arithmetic claim was introduced by this registration.
- Anchor status: no Primary anchors inventory change is required. File_50a civil outputs and table values are data-layer / coordinate-output states governed by File_50a and downstream controlling files, not newly promoted repository endpoint anchors.
- Cross-file status: Restart Capsule v11.15 and Style Guide v2.5 pointer refresh are supplied as replacements. File_50a_50b, File_50c, and File_50d_50e public-clean files receive pointer-refresh / control-registration updates only. No content amendment is required for File_32, File_33, File_34, File_39, File_40, File_49, File_51a, File_54, or Project Procedures v3.2. Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.
- Status: v1.17 active.

## Revision Log (v1.16)

- File: `State_Vocabulary_Register_v1.16`
- Revision date: June 27 2026
- Prior version: v1.15 retained unaltered as the prior-version record.
- Passes completed: bounded public-clean cross-file update for `File_44`; exact-token extraction from `File_44` state register and Machine Guards; proof-burden routing check against `File_43` / `File_44` / `File_45`; pointer audit; machine diff.
- Changes: (1) `File_44` Compact Convention Entry registered as Part D §D.15; (2) File_44 scaling-law protocol split state, P2 split controls, MirrorVolume / Seal700 provenance, `12558` exact-ratio expansion state, Creation-head 10x dependency-controlled triad, Flood Gear `1050` ladder, display-only macro motif, mod-50 gearbox display state, LXX Maximum Year-6 / Adam-state display, `5256 BC` shared date-label, and MT / SP / LXX witness-state commentary registered; (3) twelve File_44 Machine Guards registered; (4) File_43 / File_44 / File_45 proof-burden routing recorded; (5) version/status lines updated.
- Arithmetic status: no underlying arithmetic changed; no new arithmetic claim was introduced by this registration.
- Anchor status: no Primary anchors inventory change is required. File_44 values registered here are existing File_44 local states, dependency-controlled anchors, display motifs, or exact-ratio values under the controlling Final file.
- Cross-file status: No content amendment is required for `File_44`, `File_43`, `File_45`, `File_32`, `File_33`, `File_42`, `File_14`, `File_15`, `File_17`, `File_22`, `File_51a`, `File_54`, Restart Capsule v11.14, Style Guide v2.5, or Project Procedures v3.2. Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.
- Status: v1.16 active.

## Revision Log (v1.15)

- File: `State_Vocabulary_Register_v1.15`
- Revision date: June 27 2026
- Prior version: v1.14 retained unaltered as the prior-version record.
- Passes completed: bounded post-revision cross-file audit for Final post-final pressure-tested `File_51b`; exact-token Register / Restart Capsule search; Machine Guard routing check; pointer audit; machine diff.
- Changes: (1) `File_51b` Convention Entry registered as Part D §D.14; (2) formal Cross-Polarity Mirror mechanics, Protocol 1 / Protocol 2 target-generation, civil span target discipline, `8280`, LXX `5556 BC` actual Year-6 `11111` self-mirror, `11111 / 11110 / 11100 / 11109`, `11106 BC`, `11109`, biological / Levitical calendar-scaling restraint, File_51a §23.7 dependency, and File_52a / File_52b inverse-bridge firewall registered; (3) twelve File_51b Machine Guards registered; (4) Restart Capsule pointer advanced to v11.14; (5) version/status lines updated.
- Arithmetic status: no underlying arithmetic changed; no new arithmetic claim was introduced by this registration.
- Anchor status: no automatic promotion of File_51b translation-engine values to repository endpoint anchors. Key File_51b values are routed as state labels, local translation-engine constants, or display-state values under the controlling Final file and this Register entry.
- Cross-file status: Restart Capsule v11.14 and Style Guide v2.5 pointer refresh are supplied as full replacements. No content amendment is required for `File_51a`, `File_52a`, `File_52b`, `File_54`, `File_55`, `File_00`, `File_12`, `File_13`, `File_14`, `File_16`, `File_18`, `File_20`, `File_21`, `File_22`, `File_31`, `File_34`, or Project Procedures v3.2. Repository_Change_Archive update remains pending until the current Archive source is supplied or explicitly authorized.
- Status: v1.15 active.

## Revision Log (v1.14)

- File: `State_Vocabulary_Register_v1.14`
- Revision date: June 27 2026
- Prior version: v1.13 retained unaltered as the prior-version record.
- Passes completed: bounded public-clean control update for `File_51a` and `File_55`; exact-token novelty search; Machine Guard routing check; pointer audit; machine diff.
- Changes: (1) File_55 §D.11 expanded for the June 22 standard SKL Creation rail-contraction, selected `mod2160 → mod1440`, `±720 → ±480`, `+30 → +20`, nested Key-of-23 boundary, `430 / 432` dual-precessional state, and `3706 / 3226 / 2746 BC` adjacent-state robustness bracket; (2) four File_55 Machine Guards added for rail contraction, `+30 → +20` Key-of-23 boundary, `430 / 432` cross-state handling, and adjacent-state robustness restraint; (3) File_51a / File_51b handoff and rounded Conquest-block display guard registered as §D.13; (4) Restart Capsule pointer advanced to v11.13; (5) version/status lines updated.
- Arithmetic status: no underlying arithmetic changed; no new arithmetic claim was introduced by this registration.
- Anchor status: no Primary anchors inventory change is required. The added File_55 labels are derived marker states, Tier-2/local coordinates, or display-state labels under §D.3. `AD 1406 / AD 1401` are display labels, not repository endpoint anchors.
- Cross-file status: Restart Capsule v11.13 and Style Guide v2.5 pointer refresh are supplied as full replacements. No content amendment is required for `File_34`, `File_21`, `File_24`, `File_54`, `File_52a`, `File_52b`, or Project Procedures v3.2. Raw `File_51b` is not required for this handoff registration; it remains needed only when `File_51b` itself is cleaned.
- Status: v1.14 active.

## Revision Log (v1.13)

- File: `State_Vocabulary_Register_v1.13`
- Revision date: June 26 2026
- Prior version: v1.12 retained unaltered as the prior-version record.
- Passes completed: bounded post-revision cross-file audit for Final post-final pressure-tested `File_52a` and `File_52b`; exact-token Register / Restart Capsule search; Machine Guard routing check; pointer audit; machine diff.
- Changes: (1) File_52a / File_52b Convention Entry registered as Part D §D.12; (2) File_52a inverse-number architecture states and twelve Machine Guards registered; (3) File_52b technical-supplement states and thirteen Machine Guards registered; (4) File_52a / File_52b / File_52c dependency routing registered; (5) inherited non-load-bearing `AD 1405` Mirror-row removal recorded as a boundary state; (6) Restart Capsule pointer advanced to v11.12; (7) version/status lines updated.
- Arithmetic status: no underlying arithmetic changed; no new arithmetic claim was introduced by this registration.
- Anchor status: no automatic promotion of File_52a / File_52b inverse nodes to repository endpoint anchors. Existing File_52a / File_52b coordinates remain controlled by the final file sources and by the existing Restart Capsule inverse-number architecture.
- Cross-file status: Restart Capsule v11.12 and Style Guide v2.5 pointer refresh are supplied as full replacements. No content amendment is required for File_00, File_51a, File_51b, File_52c, File_52d, File_52e, File_53, File_54, File_17, File_18, File_21, File_34, or Project Procedures v3.2. Repository_Change_Archive update remains pending until the current Archive source is supplied.
- Status: v1.13 active.

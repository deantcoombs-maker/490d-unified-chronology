File: File_23
Title: The Tebah Protocol (The Source of the 3.5)
Status: Final; sampled post-equalization pressure test complete; post-final pressure tested; post-pressure Shem / Peleg full-lifespan node clarification applied and focused pressure tested; post-pressure Peleg chiasm / AD 35 mod-120 cycle clarification applied and focused pressure tested; post-pressure 460-cycle / LXX-MT Mirror slot clarification applied and focused pressure tested; post-focused-pressure Shem-to-Eber + Cainan 480-class clarification applied and focused pressure tested; post-focused-pressure Adam 930 / Peleg 230-460 Mirror clarification applied with 230-only diagnostic restraint and focused pressure tested; minor 460-list amendment applied with pressure-test status retained; minor 16136 BC / 36 BC / AD 36 Christological mod-10 amendment applied and verified during trust-status equalization; File_24 cross-file dependency update applied and focused pressure tested; File_34 post-final citation-control update applied and focused pressure tested June 3 2026
Publication-cleanup status: Public-clean Markdown source; archival amendment history routed to Repository_Change_Archive.
Pressure-test status: Current; sampled post-equalization pressure test complete for File_23; focused pressure test complete for the File_24 cross-file dependency update, the minor 16136 BC / 36 BC / AD 36 Christological mod-10 amendment, and public-clean cleanup; File_34 post-final citation-control update focused pressure tested June 3 2026; prior focused pressure-test results retained.
Register Agreement status: Checked against State_Vocabulary_Register v1.17 and active companion controls; convergence / pointer update only; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries changed.
Source-history status: Prior argument-control layer Argument base; lifespan-halving reprocessing integrated; intentionally incomplete before restart reprocessing
Primary domain: SKL; Comparative; Prophetic; Calendar; Theological; Mirror
Traditions: MT; LXX by comparison; SKL; Hebrew Bible; Danielic / prophetic by dependency; NT / Christological by comparison
Canonical source: Markdown
Primary anchors: `tebah`; En-tarah-ana; `3 months`; `3.5 days`; `360`; `1260`; `3`; `3.5`; `7`; `12600`; `1/3600`; Adam `930`; Moses `120`; `1050`; `1/12`; `30 + 930 = 960`; approximate `960/480/240/120` lifespan-halving scaffold; Noah `950`; Noah age `480`; Genesis 6:3 `120`; Shem `600`; Arphaxad `438`; Shelah `433`; Eber `464`; Shem-to-Eber average `483.75`; Shem-to-Eber + Cainan average `479`; rounded Shem-to-Eber + Cainan average `480`; Peleg `239`; Reu `239`; Peleg/Reu `239/240`; 2nd Cainan `460`; `24` intervening generations; Shem death / Arphaxad birth `5606/6086 BC`; Peleg death / Reu boundary `3926 BC`; Peleg chiasm `2400 + 240 + 2400`; `10080`; `10800`; `11760`; `1680`; `2160`; `2520`; `3920`; `13 × 360`; AD 35 rounded Christ-death comparison; `12` mod-120 node slots; `11` distinct mod-120 date labels; `5` AD 35 mod-360 landings; 2nd Cainan Adam slot `16166/15206 BC`; Adam textual `930` schematic slot `16136/15206 BC`; MT Adam `930` mirror pair `4116/3186 BC/AD`; LXX Adam `930` mirror pair `5496/4566 BC/AD`; no-Cainan Adam slot `15686/14726 BC`; mod-10 Mirror display `BC + AD − 2`; `19320`; `20700`; `21000`; `21600`; `19044`; LXX Jared `960`; LXX Noah `1656`; Peleg / Adam / Moses `230/460` Mirror integration; `2760`; `5520`; `6000`; `9660`; `9800`; `6900`; `4600`; `8280`; `12650`; `7130`; `11730`; Moses age-40 failure / wedding `1486 BC`; Temple `966 BC`; `36 BC`; AD 36; cumulative Flood `4836 BC`; `9890`; `14720`; `16000`; `16100`; `16170`; `2300 × 7`; `35 × 460`; `33 × 490`; `36 BC → AD 36 = 70`
Related files: File_00; File_02; File_07; File_08; File_10; File_12; File_13; File_14; File_15; File_16; File_17; File_18; File_19; File_20; File_21; File_22; File_24; File_34; File_51a; File_54; Restart Capsule v11.15; State Vocabulary Register v1.17; 490d Repository Style Guide v2.5; Project Procedures v3.2
Major operators: Day-Year Scalar `1:360`; `3.5 × 360 = 1260`; ordinary Aaron/Moses `3`-year branch; Aaron/Moses phase-resolved `L = 3.5` branch; `12600 = 10 × 1260`; `12600 ÷ 3600 = 3.5`; `930 + 120 = 1050`; `12600 ÷ 12 = 1050`; `30 + 930 = 960`; `960 = 8 × 120`; approximate halving scaffold `960 → 480 → 240 → 120`; `600 − 120 = 480`; `2843 − 480 = 2363 BC`; `2363 − 2243 = 120`; `(600 + 438 + 433 + 464) ÷ 4 = 483.75`; `(600 + 438 + 460 + 433 + 464) ÷ 5 = 479`; rounded comparison `(600 + 440 + 435 + 465) ÷ 4 = 485`; rounded MT + Cainan comparison `(600 + 440 + 460 + 435 + 465) ÷ 5 = 480`; Peleg `239 ≈ 240`; Reu `239 ≈ 240`; `15686 − 5606 = 10080`; `16166 − 6086 = 10080`; `14726 − 3926 = 10800`; `14006 − 3926 = 10080`; `3926 − 1406 = 2520`; `3926 − 6 = 3920`; `15686 − 3926 = 11760`; `5606 − 3926 = 1680`; `6086 − 3926 = 2160`; `2160 + 2520 = 4680 = 13 × 360 = 390 × 12`; `6566 − 4166 = 2400`; `4166 − 3926 = 240`; `3926 − 1526 = 2400`; `6 BC → AD 35 = 40`; `16166 BC → AD 35 = 16200 = 45 × 360`; `14726 BC → AD 35 = 14760 = 41 × 360`; `6086 BC → AD 35 = 6120 = 17 × 360`; `3926 BC → AD 35 = 3960 = 11 × 360`; `1406 BC → AD 35 = 1440 = 4 × 360`; `14006 BC → AD 35 = 14040 = 39 × 360`; mod-10 Mirror display `BC + AD − 2`; `15206 BC → AD 4116 = 19320 = 42 × 460`; `15206 BC → AD 5496 = 20700 = 45 × 460`; `19320 × 25/23 = 21000`; `20700 × 70/69 = 21000`; `16166 BC → AD 4836 = 21000`; `16166 BC → AD 5436 = 21600 = 60 × 360`; `20700 − 1656 = 19044`; `19044 × 25/23 = 20700`; `15686 − 966 = 14720 = 32 × 460`; `14726 − 6 = 14720 = 32 × 460`; `16136 − 36 = 16100 = 7 × 2300 = 35 × 460 = 230 × 70`; `16136 BC → AD 36 = 16170 = 33 × 490 = 33 × 7 × 70`; `36 BC → AD 36 = 70` under mod-10 Mirror display; `14720 × 25/23 = 16000`; `14726 − 4836 = 9890 = 23 × 430`; `16166 − 30 = 16136`; `16136 − 15206 = 930`; `4116 − 3186 = 930`; `5496 − 4566 = 930`; `16136 BC → AD 3186 = 19320`; `16136 BC → AD 4566 = 20700`; `4166 − 1406 = 2760 = 23 × 120 = 12 × 230 = 6 × 460`; `6086 − 1486 = 4600 = 10 × 460`; `4166 BC → AD 4116 = 8280 = 18 × 460`; `4116 BC → AD 1406 = 5520 = 24 × 230 = 12 × 460`; `5520 × 25/23 = 6000`; `5496 BC → AD 4166 = 9660 = 42 × 230 = 21 × 460`; `9660 × 70/69 = 9800 = 20 × 490`; `5496 BC → AD 1406 = 6900 = 30 × 230 = 15 × 460`; `6900 × 70/69 = 7000`; `6086 BC → AD 6566 = 12650 = 55 × 230`; `5606 BC → AD 1526 = 7130 = 31 × 230`; `15656 − 3926 = 11730 = 51 × 230`; `3.5 + 3.5 = 7`; `7 ÷ 2 = 3.5`
Major modal states: file identity state; File_34 post-final citation-control state; Post-pressure Shem / Peleg full-lifespan node clarification state; Tebah / ark typological bridge state; source-retained lexical claim state; SKL appended fractional-datum state; En-tarah-ana fractional-tail state; SKL whole-year non-active dependency state; Day-Year Scalar state; prophetic half-week state; Priestly-Enochian comparison dependency state; Mosaic hiding state; Aaron/Moses ordinary three-year branch; Aaron/Moses `L = 3.5` phase-resolved branch; source-retained integer-floor display state; cumulative MT trunk state; `1/3600` half-week scale state; patriarchal boundary-lifespan state; `1/12` flank-sum state; Adam apparent-age `960` state; schematic halving-table state; no-Cainan / with-Cainan schematic overlay state; Shem halving-juncture state; Shem death / Arphaxad birth marker state; Cainan / LXX comparison state; Peleg division-hinge state; Peleg death / Reu boundary marker state; Priestly 336 / prophetic 360 comparison state; BJ / Fall-of-man 6+7 typological comparison state; Peleg-centered 2400 + 240 + 2400 chiasm state; AD 35 rounded Christ-death mod-120 culmination state; AD 35 mod-360 selected-subset state; double-6086 Shem node-class state; 12-node / 11-distinct-date mod-120 register state; Post-focused-pressure Shem-to-Eber + Cainan 480-class clarification state; 2nd Cainan 460-cycle comparison state; mod-10 Mirror display state; MT / LXX 21000 conversion state; Adam textual 930 mirror-pair comparison state; LXX Jared 960-slot comparison state; LXX Noah 1656 expansion state; no-Cainan Temple-switch comparison state; 16136 BC / 36 BC / AD 36 Christological mod-10 comparison state; Peleg / Adam / Moses 230-460 Mirror integration state; minor 460-list amendment state; 230-only diagnostic restraint state; cumulative Flood 9890 comparison state; Key-of-23 comparison state; Noah `480` warning / halving-forecast state; Genesis 6:3 dual-`120` state; Shem-to-Eber average-lifespan state; Shem-to-Eber + Cainan 480-class state; Peleg / Reu 239/240 transition state; MT-primary halving-tradition state; approximate lifespan-halving scaffold state; actual / rounded lifespan comparison state by `File_51a`; actual / rounded / schematic mod-120 firewall state; same-side schematic BC span state; bounded civil cross-axis comparison state; bounded anchor-corroboration state; bounded Mirror / Pillar comparison state; Projective Pillar coordinate comparison state; secondary Aaron-side check state; Aaron/Moses terminal age-pair state; halving-node judgment-transition state; Abrahamic-period cap note; progressive feebleness theological state; Creation-to-Aaron/Moses half-week symmetry state; Solar/Lunar Straddle comparison state; no-formal-Mirror local state; no-Residue local state; bounded Rounded Scaffold comparison state
Revision basis: 490d Repository Style Guide v2.5; Restart Capsule v11.15; State Vocabulary Register v1.17; Project Procedures v3.2; `File_00` foundation / archive-routing precedent; `File_51a`; `File_54`; prior finalized source; Repository_Change_Archive (non-controlling history)
Current refresh note: Public-clean trust-status equalization aligns active control pointers to 490d Repository Style Guide v2.5, Restart Capsule v11.15, State_Vocabulary_Register v1.17, and Project Procedures v3.2. Register Agreement status is recorded in the header; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed. Detailed pass logs, pressure-test records where applicable, pointer-refresh history, and replacement-workflow notes are preserved in Repository_Change_Archive.

# File_23 — The Tebah Protocol (The Source of the 3.5)

## Related Repository Files

| File | Function for `File_23` |
|---|---|
| `File_00` | Foundational controls for Solar/Lunar Straddle language, calendar-state discipline, Mirror / civil / inverse firewalling, and no-collapse modal-state rules. |
| `File_02` | Flood/SKL terminology, Sumerian A / SKL Short and Sumerian B / SKL Long labels, and `File_34` dependency boundary for full SKL / Berossus derivation. |
| `File_07` | Controls Creation / Adam states, `+30 Apparent Age`, Adam `930 + 70` reception-history framing, MT Jared offset vocabulary, LXX Jared comparison where opened by dependency, and Creation-week-to-half-week comparison vocabulary where Adamic design symmetry is opened. |
| `File_08` | Controls Kings / Temple ledger vocabulary where the no-Cainan Adam slot is compared with the `966 BC` Temple construction anchor by the `32 × 460` span. |
| `File_10` | Controls external-validation and LXX source-reconstruction witness language where LXX chronology is materially referenced. |
| `File_12` | Calendrical-physics control for Solar/Lunar Straddle language, Priestly-Enochian half-week projection, and the `3.5` half-week register. |
| `File_13` | Controls Christological date-state vocabulary where `6 BC`, AD 35 rounded Christ-death comparison language, Passion-state distinction, Temple-body typology, or Christological endpoint comparison is opened. |
| `File_14` | Controls biological / LXX comparison vocabulary where Jared, Noah, LXX Creation, or LXX lifespan rounding is invoked by dependency. |
| `File_15` | Controls the Generational Axiom, including Moses death age `120` and the Mosaic `40` authorized unit; used here only for boundary-lifespan and Mosaic-age source control, not for importing the full generational axiom. |
| `File_16` | Controls the 1876 Nexus, Cainan `130/460` distinction, cumulative Cainan / Famine field, and Key-of-23 macro-vector restraint where the 460-cycle comparison touches broader Cainan material. |
| `File_17` | Prophetic span-component control for `360`, `1260`, `3.5` prophetic years, the Enochian `1274` firewall by dependency, and related Danielic half-week vocabulary. |
| `File_18` | Raw data and typological precedent for the `tebah` / ark bridge between Noah and Moses, with arithmetic burden kept separate from typological meaning. It also supplies dependency context for LXX / Cainan / cumulative table labels used in the 460-cycle comparison. |
| `File_19` | Controls Temple `430` / literal post-exilic and Christological Temple-comparison vocabulary where the no-Cainan Temple-switch row cites `966 BC` and Temple-body typology. |
| `File_20` | Controls BJ / Jubilees comparison context where the author-supplied `6 + 7` Fall-of-man typological note is retained. This does not make BJ proof-bearing in `File_23`. |
| `File_21` | Controls the SKL appended fractional-datum state: `3 months and 3.5 days`, distinct from whole-year SKL table arithmetic. |
| `File_22` | Controls the cumulative MT trunk `12600`, the ordinary Aaron/Moses `3`-year branch, and the phase-resolved Aaron/Moses `L = 3.5` branch. |
| `File_24` | Forward companion file. `File_23` supplies the Tebah Protocol and `3.5` source-state that `File_24` extends into the re-scoped First Dynasty of Kish cipher, including the En-tarah-ana bridge as a biblical-interplay example. |
| `File_34` | Final; post-final pressure-tested dependency-controlled source for full SKL / Berossus derivation, month/day textual variants, En-tarah-ana source metadata, and Appendix B phase-chain / corridor vocabulary where explicitly opened. `File_23` uses this dependency only for the SKL appended fractional-datum and Tebah / `3.5` source-state. |
| `File_51a` | Controls the Rounded Scaffold and actual / rounded lifespan comparison layer where File_23 opens rounded lifespan dates. It does not replace actual MT dates, cumulative states, SKL states, or prophetic half-week states. |
| `File_54` | Controls Luke / Rounded / BJ comparative context, including AD 35 contraction / rounded Christ-death comparison vocabulary where cited. It does not replace `File_13` as Christological date-state controller or `File_51a` as Rounded Scaffold controller. |

Dependency note:
`File_23` may state the local Tebah Protocol and SKL fractional logic needed for its argument, but full SKL / Berossus derivation remains controlled by `File_34`. `File_21` indexes the SKL appended fractional datum, while `File_22` controls the downstream Aaron/Moses `3` / `3.5` and `12600` cumulative states.

File_34 post-final citation-control note:
`File_34 Final; post-final pressure tested` now controls the full SKL / Berossus derivation, the SKL appended `3 months and 3.5 days` source state, En-tarah-ana source metadata, Appendix B phase-chain dependency, and the `2370` / `30 + 20 + 30` restoration-corridor vocabulary where explicitly opened. `File_23` opens only the Tebah / `3` / `3.5` fractional-source bridge and does not import File_34 Appendix B corridor or Pillar machinery into its local proof.

LXX source-control note:
LXX chronology is now materially referenced in the post-pressure 460-cycle comparison. Henry B. Smith Jr.'s reconstruction of the LXX Genesis 5 and 11 chronology is therefore an important external source-reconstruction witness for the LXX layer. Smith is not the source of `File_23`'s harmonic interpretation, Tebah Protocol, halving-node argument, Mirror display, SKL / Berossus integration, or providential synthesis.

## 0. File-function

`File_23` identifies the proposed cosmic source of the Mosaic `3` / `3.5` interval by joining the biblical `tebah` ark/basket motif with the SKL appended fractional datum `3 months and 3.5 days`.

The file functions as a short bridge between `File_22` and `File_24`:

| Direction | Function |
|---|---|
| Backward to `File_22` | Supplies the source-code layer for the Aaron/Moses `3.5` phase-resolved straddle and the `12600` cumulative echo. |
| Forward to `File_24` | Prepares the downstream First Dynasty of Kish cipher by isolating the fractional SKL payload. In the completed `File_24`, the `16560` harmonic is retained as a biblical-interplay example rather than the file's sole governing thesis. |

Publication-cleanup note:
This public-clean source preserves the file's Final, pressure-tested, post-final, focused-pressure-tested, current publication-clean, and dependency-controlled status labels in the header. Detailed pass ledgers, pressure-test records, post-final patch records, pointer-refresh history, and replacement-workflow notes are archived rather than repeated in the public body.

Authorial clarification note:
The prior arithmetic-control review audit item concerning the source phrase `12.6 / 3.5 harmonic` is resolved for the active `File_23` argument. The active expression is now the `1/3600` scale relation between the cumulative `12600`-year trunk and the prophetic half-week:

```markdown
12600 years ÷ 3600 = 3.5 years
3.5 years × 360 days/year = 1260 days
```

The author further clarifies that this `1/3600` relation is included to demonstrate symmetrical design. The design argument now also includes the outer lifespan flank: Adam begins the 26-patriarch line with a lifespan of `930` years, Moses ends it with a lifespan of `120` years, and `24` generations stand between them. Their combined boundary lifespans are `930 + 120 = 1050`, which is `1/12` of the full `12600`-year cumulative trunk. When Adam's `+30 Apparent Age` state is included, Adam's boundary display becomes `30 + 930 = 960`, exactly `8 × 120`, yielding the approximate halving scaffold `960 → 480 → 240 → 120`. The intermediate values remain approximate as ideal halving stages, but the current post-pressure clarification makes the full lifespans of Shem and Peleg intentional node markers rather than incidental witnesses. Shem's death / Arphaxad birth at `5606/6086 BC` and Peleg's death / Reu boundary at `3926 BC` are therefore retained as accepted schematic nodes alongside Adam and Moses.

This is an author-resolved argument clarification. It adds symmetrical-design framing and exact local arithmetic checks, but it does not create a new chronology operator, silently regularize the approximate halving stages, or retroactively change arithmetic-control review arithmetic.

Terminology note:
Legacy source phrases such as “source code,” “Micro-Flood,” and “AI Cross-Reference Vector” are retained as source vocabulary. Where needed, this revision places them inside repository-style headings and dependency notes rather than deleting or expanding them.

Modal-state rule:
A shared value or label such as `3.5`, `3 months`, `1260`, or `12600` does not create identity unless state, node-class, tradition, and operator agree.

Argument-control rule:
The `tebah` motif and Micro-Flood language explain the theological meaning of the structure. Arithmetic facts, scalar operators, and dependency-controlled source states carry the computational burden. The argument-control layer therefore classifies the file's design language as typological reading, structural inference, theological note, or providential synchronization rather than as direct proof of human scribal intent.

### 0.1 Working state register

The following states are active in `File_23`. They are simultaneous, non-competing states unless a local section explicitly narrows the working state.

| State | Primary values / forms | Node-class / function | Handling in `File_23` |
|---|---:|---|---|
| File identity state | `File_23`; The Tebah Protocol; Final; post-final pressure tested; post-pressure clarifications applied | bridge file / source-state file | Preserved as a finalized bridge file. The current source is not `File_24` and does not execute the `16560` harmonic. |
| Post-pressure Shem / Peleg full-lifespan node clarification state | Adam, Shem, Peleg, and Moses full lifespans; Shem death / Arphaxad birth `5606/6086 BC`; Peleg death / Reu boundary `3926 BC` | author-supplied node clarification / post-pressure patch state | Active in the current replacement source. It clarifies that full lifespans mark the halving structure, especially Shem and Peleg death-boundaries. It does not convert schematic coordinates into actual dates or Rounded Scaffold dates and remains preserved under the file header status. |
| Post-pressure Peleg chiasm / AD 35 mod-120 cycle clarification state | `6566 → 4166 → 3926 → 1526`; AD 35; `12` node slots; `11` distinct date labels; `5` mod-360 landings | author-supplied cycle clarification / post-pressure patch state | Active in the current replacement source. It adds Peleg-centered chiasm and AD 35 cycle comparisons without changing the preserved final status. It does not open formal Mirror, Residue Protocol, or Rounded Scaffold retrojection. |
| Post-pressure 460-cycle / LXX-MT Mirror slot clarification state | `16166–15206 BC`; `15686–14726 BC`; 2nd Cainan `460`; mod-10 Mirror display `BC + AD − 2`; `19320`; `20700`; `21000`; `21600`; `19044`; `14720`; `16000`; `9890` | author-supplied cycle clarification / post-pressure patch state | Active in the current replacement source. It adds 460-cycle, MT / LXX Mirror-display, LXX Jared / Noah, no-Cainan Temple-switch, and cumulative Flood comparison material without changing the preserved final status. It does not open formal Mirror protocol, Residue Protocol, or Rounded Scaffold retrojection. |
| Post-focused-pressure Shem-to-Eber + Cainan 480-class clarification state | Shem `600`; Arphaxad `438`; 2nd Cainan `460`; Shelah `433`; Eber `464`; actual average `479`; rounded average `480`; Peleg / Reu `239/240` | author-supplied 480-class clarification / post-focused-pressure patch state | Active in the current replacement source. It clarifies that the MT Shem-to-Eber line, when restored 2nd Cainan is included as a `4+1` member, belongs to the `480` class by actual average `479` and rounded average `480`. It also clarifies that the switch to Peleg / Reu `239/240` marks the `480 → 240` division. This state is preserved under the file header status. |
| 2nd Cainan 460-cycle comparison state | 2nd Cainan `460`; `32 × 460`; `30 × 460`; `42 × 460`; `45 × 460` | Cainan-lifespan / Key-of-23 comparison field | Active only in the bounded §5.9.3b–§5.9.3c comparison. The `460` value may compare actual LXX / Cainan lifespan and cumulative Cainan themes by dependency, but it does not replace regular `+130` begetting insertion or cumulative `+460` insertion states in other files. |
| Mod-10 Mirror display state | `BC + AD − 2`; source display `6 BC → AD 6 = 10` | rounded / mod-10 Mirror display convention | Active only in §§5.9.3b–5.9.3d where the author explicitly invokes the mod-10 Mirror display. It is distinct from ordinary civil cross-axis span `BC + AD − 1`; it does not open Protocol 1, Protocol 2, inverse-number reversal, or proof-bearing Mirror target-generation. |
| MT / LXX `21000` conversion state | `19320 × 25/23 = 21000`; `20700 × 70/69 = 21000`; `16166 BC → AD 4836 = 21000` by mod-10 Mirror display | Key-of-23 / cross-tradition comparison state | Retained as structural inference / providential synchronization. The MT and LXX rows converge by different exact-ratio operators and remain distinct witness states. |
| Adam textual `930` mirror-pair comparison state | schematic Adam `16136–15206 BC`; MT Adam `4116–3186 BC/AD`; LXX Adam `5496–4566 BC/AD`; `19320`; `20700` | direct Adam `930` mirror-pair comparison | Current author clarification. It clarifies that the schematic actual-lived Adam `930` slot begins after the `+30` apparent-age head and aligns with MT and LXX Adam `930` mirror pairs under bounded mod-10 display. It does not replace actual MT or LXX chronology. |
| Peleg / Adam / Moses `230/460` Mirror integration state | Peleg `4166 BC`; Moses `1406 BC`; Moses age-40 failure / wedding `1486 BC`; MT Adam `4116 BC`; LXX Adam `5496 BC`; Shem `6086/6566/5606 BC`; `2760`; `4600`; `5520`; `8280`; `9660`; `6900`; `12650`; `7130`; `11730` | half-cycle / full-cycle comparison field | Current author clarification with 230-only diagnostic restraint and minor 460-list amendment. The rows that are full `460` multiples attach to the known 2nd-Cainan `460` cycle stream and remain the strongest examples. Rows that are multiples of `230` but not `460` are retained as arithmetic facts / exploratory diagnostics and are downweighted unless tied to a recognized `460` path. The foregrounded 460-list is representative, not exhaustive. |
| LXX Jared `960` slot comparison state | MT Jared offset `460`; LXX Jared offset `960`; LXX Jared mirror label `4536`; Jared rounded lifespan `960` by dependency | LXX / Jared / Watcher-lore comparison | Retained as dependency-controlled comparison material. It aligns the LXX Adam-to-Jared `960` with the schematic Adam `960` slot, but it does not source-prove Watcher chronology locally. |
| LXX Noah `1656` expansion state | LXX Noah birth offset `1656`; `20700 − 1656 = 19044`; `19044 × 25/23 = 20700` | LXX / Noah / Key-of-23 comparison | Retained as author-supplied Gear 2 / Gear 3 display pending source-control if made proof-bearing. |
| No-Cainan Temple-switch comparison state | `15686 → 966`; `14726 → 6`; `32 × 460`; `14720 × 25/23 = 16000`; `966 − 6 = 960` | Temple / Christ comparison state | Retained as bounded structural and typological comparison. The `966 BC` Temple anchor remains dependency-controlled by Kings / Temple files and does not replace the no-Cainan schematic dates. |
| `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison state | `16136 − 36 = 16100`; `16100 = 7 × 2300 = 35 × 460 = 230 × 70`; `16136 BC → AD 36 = 16170 = 33 × 490`; `36 BC → AD 36 = 70` under mod-10 display | Christological / Creation-week / mod-10 Mirror comparison state | Minor amendment. This row foregrounds the schematic Adam actual-lived `930` start as a Creation-of-Adam comparison node whose same-side span to `36 BC` and bounded mod-10 span to AD 36 are Christologically significant. It does not replace `6 BC`, AD 35, actual Passion states, Luke's `36 BC` rail, or formal Mirror protocol. |
| Cumulative Flood `9890` comparison state | `14726 − 4836 = 9890 = 23 × 430` | cumulative Flood / Decimal Bridge comparison | Retained as dependency-controlled comparison with the cumulative Flood field. It does not alter `File_22` Decimal Bridge controls or cumulative Flood table values. |
| Peleg-centered `2400 + 240 + 2400` chiasm state | `6566 − 4166 = 2400`; `4166 − 3926 = 240`; `3926 − 1526 = 2400` | schematic chiasm centered on Peleg full lifespan | Treat as structural inference inside the schematic overlay. Peleg's `240` central span is the rounded / schematic form of the textual `239`; the outer `2400` wings are same-side BC schematic spans. |
| Double-`6086 BC` Shem node-class state | no-Cainan Shem birth / start `6086 BC`; with-Cainan Shem death / Arphaxad birth `6086 BC` | shared date-label with two node-classes | Count as two node-class slots when the 12-node mod-120 register is active, but as one distinct date label when counting distinct mod-120 coordinates. Do not collapse the two Shem states into one actual event. |
| AD 35 rounded Christ-death mod-120 culmination state | `6 BC → AD 35 = 40`; selected schematic nodes to AD 35 | rounded Christological comparison label | Bounded by `File_13` / `File_54` dependency. AD 35 is not used as the actual Passion date in this file and does not replace AD 30 / AD 33 Christological states elsewhere. |
| AD 35 mod-360 selected-subset state | `16166`; `14726`; `6086`; `3926`; `1406`; auxiliary `14006` | diagnostic subset within the mod-120 register | Five of the 11 distinct schematic mod-120 labels land on AD 35 by exact `360`-year multiples. The normal cumulative MT Creation date `14006 BC` also lands by `39 × 360` as a separate actual / cumulative comparison, not as an extra schematic node. |
| Tebah / ark typological bridge state | Noah's Ark; Moses' basket; `tebah` | Hebrew lexical / typological bridge | Preserved as the narrative lock joining Flood and Exodus birth narratives. It does not by itself carry the arithmetic burden. |
| Source-retained lexical claim state | `tebah` appears in Noah / Moses ark contexts | lexical source claim | Preserved as source-retained pending citation-control. Do not use the lexical claim alone as proof of the arithmetic structure. |
| SKL appended fractional-datum state | `3 months and 3.5 days` | SKL textual month/day payload | Preserved as dependency-controlled by `File_21` / `File_34`. Whole-year SKL arithmetic remains distinct unless a later section opens it. |
| En-tarah-ana fractional-tail state | `3 months`; `3.5 days` | fractional payload foregrounded by this file | The source foregrounds the month/day tail. Full En-tarah-ana reign metadata remains dependency-controlled by `File_21` / `File_34`. |
| SKL whole-year non-active dependency state | Sumerian A / SKL Short; Sumerian B / SKL Long; whole-year SKL totals by dependency | SKL table / deep-time state | Named only for source-control. `File_23` does not open whole-year SKL date-generation, `+30 Apparent Mode`, `+2 local rail`, `+720 scale-state`, or the File_21 four-state matrix. |
| Day-Year Scalar state | `1:360`; `3.5 × 360 = 1260` | scalar conversion / prophetic half-week output | Arithmetic-control review confirms the local product. The output is handled as `1260` prophetic days / `3.5` prophetic years under the 360-day register. This local scalar does not by itself open the full File_21 SKL `360:1` date-generation matrix. |
| Prophetic half-week state | `1260` days; `3.5` prophetic years; `360`-day prophetic year | Danielic / prophetic register | Preserved by `File_17` dependency. Distinct from the Enochian `1274 = 3.5 × 364` state. |
| Priestly-Enochian comparison dependency state | liturgical `3.5` days → `3.5` years by day-year projection | File_12 comparison / explanatory state | May clarify how a `3.5`-day straddle can project into a prophetic half-week, but it remains dependency-controlled and must not be confused with the File_34 SKL month/day datum. |
| Mosaic hiding state | `3 months`; Exodus 2:2 | biblical textual datum / typological bridge | Preserved as the Mosaic counterpart to the SKL `3 months` component. |
| Aaron/Moses ordinary branch | `3`; Aaron `1529n BC`; Moses `1526n BC` by dependency | ordinary Nisan/Nisan birth-label branch | Preserved by `File_22`; not corrected into `3.5`. Unsuffixed local prose remains ordinary label-language unless a Nisan/Tishri state is explicitly opened. |
| Aaron/Moses phase-resolved branch | `L = 3.5`; Aaron `1530t BC`; Moses `1526n BC` by dependency | Tishri/Nisan priestly straddle | Preserved by `File_22`; not collapsed into the ordinary `3`-year branch. |
| Source-retained integer-floor display state | ordinary `3` as integer floor of `3.5` | explanatory display / source wording | Arithmetic-control review confirms that `floor(3.5) = 3`. The phrase remains an explanatory display, not a new chronology operator. Argument role is classified in the argument-control layer. |
| Cumulative MT trunk state | `12600`; `10 × 1260` | cumulative Creation-to-Conquest echo | Preserved by `File_22`; not treated as ordinary regular chronology. |
| `1/3600` half-week scale state | `12600 ÷ 3600 = 3.5`; `3.5 × 360 = 1260` | cumulative-to-prophetic scale relation | Author-resolved and preserved in the argument-control layer. The cumulative `12600`-year trunk contains the `3.5` prophetic-year / `1260`-day half-week as a `1/3600` scaled miniature. This replaces the prior `12.6 / 3.5` slash phrase for active argument use. |
| Patriarchal boundary-lifespan state | Adam `930`; Moses `120`; `24` intervening generations | outer lifespan flank of the 26-patriarch line | Author-supplied symmetrical-design state. Adam and Moses flank the `12600` cumulative trunk as first and last patriarchs in this display. The `24` intervening generations are retained as author-supplied table logic and not locally rederived. |
| `1/12` flank-sum state | `930 + 120 = 1050`; `12600 ÷ 12 = 1050` | boundary-lifespan fraction of cumulative trunk | Exact arithmetic display. It shows the Adam/Moses boundary pair as `1/12` of the `12600` cumulative trunk. |
| Adam apparent-age `960` state | `30 + 930 = 960`; `960 = 8 × 120` | Apparent Age plus Adam lifespan display | Uses the `+30 Apparent Age` vocabulary by `File_07` / `File_00` dependency. It is a structural display, not a replacement for Adam's textual lifespan `930`. |
| Approximate lifespan-halving scaffold state | about `960/480/240/120`; Adam `960`; Shem `600`; Peleg / Reu `239/240`; Moses `120`; Shem-to-Eber average `483.75`; Shem-to-Eber + Cainan average `479/480` | lifespan decline / symmetrical-design scaffold | Preserved with the qualifier “about.” Exact arithmetic applies only to the ideal scaffold values. The current patches clarify that Adam, Shem, Peleg, and Moses are full-lifespan node markers; the Shem-to-Eber + Cainan average makes the `480` class explicit while preserving actual lifespans rather than regularizing them. |
| Schematic halving-table state | no-Cainan column; with-Cainan column; `15686/16166 BC`; `6086/6566 BC`; Peleg `4166 BC` | schematic overlay / lifespan-stage display | Active only where §5 opens the schematic overlay. It does not replace actual MT, LXX, cumulative MT, or Rounded Scaffold chronology. |
| No-Cainan / with-Cainan schematic overlay state | No 2nd Cainan; With 2nd Cainan; optional inserted 2nd Cainan `480` block | comparison columns inside the halving schematic | Preserve as two display states. Do not treat the with-Cainan column as ordinary LXX chronology or the no-Cainan column as a full actual MT table. |
| Shem halving-juncture state | Shem `6086 BC`; Shem `6566 BC`; one inserted `480` block | first schematic transition from the `960` stage to the `480` stage | These are schematic halving-table positions, not ordinary Shem birth dates. The `6086/6566 BC` pair is state-dependent and not a range. |
| Shem death / Arphaxad birth marker state | no-Cainan `5606 BC`; with-Cainan `6086 BC`; `10080 = 28 × 360 = 30 × 336` | Shem full-lifespan boundary / Arphaxad start marker | Current author clarification. In cumulative-style table logic, Shem death is also the Arphaxad birth/start boundary. These nodes are accepted schematic coordinates and remain firewalled from actual and Rounded Scaffold dates. |
| Cainan / LXX comparison state | 2nd Cainan; `130`; `460`; native / restored Cainan comparison | Cainan-register comparison state | Active where §5.7.3 and §5.8.5 open Cainan comparison. It does not replace the local no-Cainan schematic state, whole LXX chronology, regular `+130`, or cumulative `+460`. |
| Key-of-23 comparison state | `25/23`; `70/69`; `300/299`; `23 ×`; `69 ×`; `299 ×` displays | exact-ratio comparison state | Active in later halving-node comparison displays as preserved source text. Arithmetic-control review verifies the displayed exact-ratio calculations without promoting them into Residue Protocol or a new chronology operator. |
| Bounded civil cross-axis comparison state | `BC + AD − 1`; `AD 25`; `AD 16165`; `AD 29395` | ordinary civil cross-axis span-counting | Active only inside bounded Christ / Temple, AD 35 rounded Christ-death, and Mirror / Pillar comparison displays in §§5.9.3, 5.9.3a, and 5.9.5. It is not formal Mirror target-generation, 10x Mirror scaling, or inverse-number reversal. |
| Bounded anchor-corroboration state | Exodus; Conquest; Christ; Temple; Rounded Scaffold; Mirror; Pillar comparison anchors | comparison-only support state | Used to gather major-anchor confirmations without turning every clean mod-120 or Key-of-23 display into a proof-bearing row. |
| Bounded Mirror / Pillar comparison state | `AD 16165`; `AD 29395`; Protocol 1 named by dependency | comparison-only cross-axis state | Active only in §5.9.5. It does not open formal Mirror target-generation as a local proof protocol. |
| Projective Pillar coordinate comparison state | `AD 29395`; Jachin / Boaz Projective Boundary field by dependency | projective / dependency-controlled comparison coordinate | Preserved as a bounded comparison to a File_21 / Temple-Time Pillar coordinate, not as ordinary historical chronology. |
| Secondary Aaron-side check state | Aaron `123`; Moses `120`; `123 × 120`; `80 × 120`; `43 × 120` | terminal age-pair comparison | Secondary overlay only. The main schematic remains Moses-centered because Moses supplies the terminal `120` lifespan. |
| Halving-node judgment-transition state | Adam; Shem; Peleg; Moses | narrative transition / judgment state | Preserved as structural / theological classification of the selected halving heads. It is not an arithmetic selection rule by itself. |
| Abrahamic-period cap note state | Abraham birth `2166 BC`; Exodus `1446 BC`; six Mosaic `120` units | covenantal background state | Abraham remains covenantally central but is not a halving head inside the schematic. |
| Noah `480` warning / halving-forecast state | Noah age `480`; `600 − 120 = 480`; Flood warning `120` | halving forecast / Flood-to-Moses bridge | Author-supplied expansion. Noah receives the 120-year warning at age `480`, which marks the first visible half-stage after Adam-with-apparent-age `960`. |
| Genesis 6:3 dual-`120` state | `120` years to Flood; `120` maximum lifespan / Moses | textual / theological dual reading | Preserved as authorial interpretation: the `120` announces both the remaining years to the Flood and the terminal human lifespan embodied by Moses. |
| Shem-to-Eber average-lifespan state | Shem `600`; Arphaxad `438`; Shelah `433`; Eber `464`; average `483.75` | no-Cainan approximate post-Flood half-stage | Arithmetic checks. This four-name no-Cainan group is treated as a visible “about `480`” generation cluster, not an exact halving law. |
| Shem-to-Eber + Cainan 480-class state | Shem `600`; Arphaxad `438`; 2nd Cainan `460`; Shelah `433`; Eber `464`; actual average `479`; rounded average `480` | MT + Cainan `4+1` lifespan class | Active by post-focused-pressure author clarification. The listed actual values sum to `2395`, giving `2395 ÷ 5 = 479`; rounded values sum to `2400`, giving `2400 ÷ 5 = 480`. This strongly supports Shem-to-Eber as the `480` class without converting the schematic overlay into exact table law. |
| Peleg division state | Peleg `239`; rounded `240`; name associated with division | division / halving clue | Peleg divides the halving stages and his lifespan is approximately `240`, the third ideal stage. The name clue is typological / lexical and should receive citation-control later if made proof-bearing. |
| Peleg / Reu `239/240` transition state | Peleg `239`; Reu `239`; rounded `240` | repeated lifespan / `480 → 240` transition witness | Active by post-focused-pressure author clarification. Reu repeats Peleg's `239` lifespan, and both round to `240`, strengthening the transition from the Shem-to-Eber `480` class into the Peleg / Reu `240` class. |
| MT-primary halving-tradition state | MT without restored Cainan; MT + restored 2nd Cainan; LXX / SP subordinate comparison | tradition-priority guard | The local halving pattern is locked by MT with or without restored Cainan. LXX and SP may corroborate or compare by dependency, but they do not replace the MT halving scaffold in `File_23`. |
| Peleg death / Reu boundary marker state | `3926 BC`; `10800`; `10080`; `2520`; `3920`; `11760`; `1680`; `2160`; `13 × 360` | Peleg full-lifespan boundary / division-marker state | Current author clarification. Peleg death / Reu boundary is an accepted schematic node, not merely an incidental row. It remains schematic unless another local state is explicitly opened. |
| Priestly 336 / prophetic 360 comparison state | `10080 = 28 × 360 = 30 × 336`; `10800 = 30 × 360`; `1680 = 5 × 336`; `2160 = 6 × 360`; `2520 = 7 × 360` | calendar-register comparison inside schematic node display | Used to compare the Shem and Peleg death-boundaries through Prophetic `360` and Priestly `336` registers. It is not a new chronology operator and does not import the full File_12 Priestly Ledger proof. |
| BJ / Fall-of-man `6 + 7` typological comparison state | `6 × 360 + 7 × 360 = 13 × 360`; author-supplied `6 + 7` Fall-of-man comparison | typological / BJ-dependent comparison note | Preserved as theological / typological comparison. It requires source-control if made proof-bearing and does not alter the schematic table arithmetic. |
| Peleg-centered `2400 + 240 + 2400` chiasm state | `6566 BC → 4166 BC → 3926 BC → 1526 BC`; `2400 + 240 + 2400` | schematic chiasm / Peleg full-lifespan center | Current author clarification. Peleg's `240`-year schematic lifespan is centered between two `2400` spans from Shem's with-Cainan birth to Moses' birth. This remains schematic and does not convert the row into actual chronology or Rounded Scaffold chronology. |
| AD 35 rounded Christ culmination state | `AD 35`; `6 BC → AD 35 = 40`; mod-120 node-cycle landing | bounded rounded Christ-death / contraction comparison | Current author clarification. `AD 35` functions as a rounded Christ-death / contraction culmination comparable to File_54's rounded-death / contraction field. It does not revise actual Passion-date states, open formal Mirror protocol, or make schematic coordinates actual / Rounded Scaffold dates. |
| Mod-120 / mod-360 cycle diagnostic state | `12` node entries; `11` distinct date labels; `5` mod-360 landings to `AD 35` | exploratory diagnostic / structural comparison | Retained as a slightly elevated cycle-density observation. It is not formal statistical proof and should remain exploratory unless a later file supplies a formal model. |
| Actual / rounded lifespan comparison state | actual and rounded lifespans both total `12600`; File_51a actual / rounded cumulative dates | Rounded Scaffold comparison layer | Active only where explicitly named. It does not replace actual MT dates, cumulative MT state, SKL state, or prophetic half-week state. |
| Actual / rounded / schematic mod-120 firewall state | actual MT dates; File_51a Rounded Scaffold dates; schematic mod-120-year span coordinates | chronology-state firewall / display-state guard | Finalization guard. Actual dates, Rounded Scaffold dates, and schematic mod-120 span displays are three separate states. A clean multiple of `120` does not make an actual date rounded, and a schematic table coordinate does not become actual or rounded chronology unless a local row explicitly opens that state. |
| Progressive feebleness theological state | lifespan descent from Adam to Moses | theological interpretation | Preserved as theological note: the decreasing ages imply the increasing feebleness of mankind. This does not replace arithmetic proof or source-control. |
| Creation-to-Aaron/Moses half-week symmetry state | Creation `7` years/days; Aaron/Moses `3.5`-year spread | terminal half-week design comparison | Links Creation and Adam to Aaron/Moses by a full-week / half-week relation. This remains structural inference / theological design language, not formal Mirror protocol. |
| Solar/Lunar Straddle comparison state | `3.5 + 3.5 = 7`; Day `7` versus Day `7.5` | Creation-side comparison / theological structure | Preserved by `File_00` / `File_12` / `File_22` dependency. Formal Mirror protocol is not opened locally. |
| Micro-Flood synthesis state | Moses' birth as “Micro-Flood” | typological / theological synthesis | Preserved as source conclusion and classified in argument-control layer as typological / theological language. It does not open formal Mirror protocol or carry arithmetic proof by itself. |
| No-formal-Mirror local state | no formal Mirror target generated as proof protocol; bounded §5.9.5 comparison only | Mirror firewall | `File_23` uses comparison and typology, not Protocol 1, Protocol 2, phase-resolved Mirror coordinate-completion, inverse-number reversal, or civil cross-axis computation as local proof mechanics. The §5.9.5 Mirror / Pillar material is a bounded anchor-corroboration note. |
| No-Residue local state | no physical residue or residual-display arithmetic | Residue Protocol firewall | Do not import Residue Protocol merely because `3.5`, `1260`, or SKL language appears. |
| Bounded Rounded Scaffold comparison state | actual / rounded lifespan dates and rounded lifespans by `File_51a` | comparison-only rounded layer | Active only in the lifespan-halving expansion where explicitly named. Clean values, 10x echoes, SKL states, and prophetic half-week values do not thereby become Rounded Scaffold values. |
| Forward `File_24` companion state | First Dynasty of Kish cipher; En-tarah-ana bridge; `16560` biblical-interplay example | forward dependency | `File_24` is named as the downstream Kish-cipher file. `File_23` does not execute or revise the 23-kings cipher table, the En-tarah-ana coordinate family, or the `16560` biblical-interplay displays. |

### 0.1a Node-class and display-state register

| Value / label / display | Node-class | Active state | Do not collapse with |
|---|---|---|---|
| `tebah` | Hebrew lexical / typological label | Tebah / ark typological bridge state | arithmetic operator; SKL source-control; proof of scalar arithmetic |
| Noah's Ark | Flood deliverance node | Tebah / ark typological bridge state | Moses' basket as same event-node |
| Moses' basket | Mosaic deliverer-birth node | Tebah / ark typological bridge state | Noah's Ark as same event-node |
| `3 months` in SKL | appended fractional month component | SKL appended fractional-datum state | Mosaic hiding period as identical source event; ordinary quarter-year chronology unless locally opened |
| `3 months` in Exodus 2:2 | Moses hiding period | Mosaic hiding state | SKL month component as same textual tradition |
| `3.5 days` in SKL | appended fractional day component | SKL appended fractional-datum state | File_12 liturgical `3.5`-day straddle; Aaron/Moses `L = 3.5`; prophetic `3.5` years |
| liturgical `3.5` days | Priestly-Enochian phase-offset | File_12 comparison dependency state | File_34 / SKL appended `3.5` days unless File_34 mechanics are explicitly opened |
| `3.5` prophetic years | half-week span | Prophetic half-week state | SKL `3.5` days; ordinary Aaron/Moses `3`; Solar/Lunar comparison half |
| `3` years | ordinary Aaron/Moses birth-label branch | Nisan/Nisan branch | phase-resolved `L = 3.5` branch as correction or replacement |
| `L = 3.5` | phase-resolved Aaron/Moses branch | Tishri/Nisan priestly straddle | ordinary `3`-year branch; File_12 Tishri Recess 6-month phase-offset; SKL `3.5` days |
| `1260` | prophetic half-week output | `3.5 × 360` prophetic register | `12600` cumulative MT trunk; Enochian `1274`; Residue Protocol output |
| `12600` | cumulative MT trunk echo | `10 × 1260` by `File_22` dependency | ordinary regular chronology; Rounded Scaffold; `1260` as same node |
| `12600 ÷ 3600 = 3.5` | cumulative-to-prophetic scale display | `1/3600` half-week scale state | `10 × 1260` decimal echo as identical node-class; literal slash division |
| `3.5 × 360 = 1260` | prophetic-day miniature | `1/3600` half-week scale state / prophetic half-week state | whole-year SKL table arithmetic; Enochian `1274`; Residue Protocol output |
| Adam `930` | textual lifespan datum / beginning boundary | patriarchal boundary-lifespan state | Adam apparent-age `960`; Adam `130` Minor Note; cumulative Adam date `14006 BC` |
| Moses `120` | textual lifespan datum / terminal boundary | patriarchal boundary-lifespan state | Mosaic `40` authorized unit; Aaron/Moses `3` / `L = 3.5`; Moses birth `1526 BC` |
| `1050` | Adam/Moses flank-sum | `1/12` flank-sum state | ordinary lifespan total of one person; `1050` as a new chronology anchor |
| `30 + 930 = 960` | apparent-age plus lifespan display | Adam apparent-age `960` state | Adam textual lifespan `930`; Rounded Scaffold; ordinary date correction |
| `960/480/240/120` | approximate halving scaffold | approximate lifespan-halving scaffold state | exact table law; exact lifespan row for Shem, Arphaxad, or Peleg |
| No 2nd Cainan / With 2nd Cainan | schematic overlay columns | schematic halving-table state | actual MT or LXX chronology unless locally state-labeled |
| `15686 BC`; `16166 BC` | Adam apparent-age schematic heads | no-Cainan / with-Cainan schematic overlay state | actual Adam birth; cumulative Creation date; Rounded Scaffold Creation date |
| `15656 BC`; `16136 BC` | Adam textual-lifespan schematic starts | Adam apparent/textual boundary display | Adam's textual lifespan `930` as a lone value; actual chronology |
| `6086 BC`; `6566 BC` | schematic Shem birth / start pair | Shem halving-juncture state | actual Shem birth; Shem death / Arphaxad birth marker; Shem lifespan `600`; Flood date |
| `5606 BC`; with-Cainan `6086 BC` | Shem death / Arphaxad birth accepted node | Shem death / Arphaxad birth marker state | actual Flood date; actual Arphaxad birth; Rounded Scaffold date; Shem birth `6086/6566 BC` as same node-class |
| double `6086 BC` | no-Cainan Shem birth / start and with-Cainan Shem death / Arphaxad birth | double-`6086 BC` Shem node-class state | one generic Shem event; actual Flood date; actual Arphaxad birth; rounded date |
| `4166 BC` in §5 | schematic Peleg birth / division boundary / Rounded Scaffold `+60` comparison node | Peleg division-hinge state | actual Peleg birth; Creation endpoint; generic Rounded Scaffold value outside the local comparison |
| `3926 BC` | Peleg death / Reu boundary accepted node | Peleg death / Reu boundary marker state | actual Peleg death date; Rounded Scaffold date; Reu as proof-bearing halving node |
| `6566 → 4166 → 3926 → 1526` | Peleg-centered `2400 + 240 + 2400` chiasm | Peleg-centered `2400 + 240 + 2400` chiasm state | actual chronology; formal Mirror protocol; ordinary lifespan table |
| `12` mod-120 node slots / `11` distinct date labels | node-count distinction | 12-node / 11-distinct-date mod-120 register state | 12 distinct date labels; statistical proof; Rounded Scaffold node count |
| `25/23`; `70/69`; `300/299` | exact-ratio comparison operators | Key-of-23 comparison state | Residue Protocol; ordinary rounding; formal Mirror protocol |
| `AD 25` | Christological comparison display | bounded civil cross-axis comparison state | ordinary local historical anchor; formal Mirror target |
| `AD 35` | rounded Christ-death / mod-120 culmination comparison display | AD 35 rounded Christ-death mod-120 culmination state | actual Passion date; local Christological anchor replacing `File_13`; formal Mirror target |
| `36 BC`; AD 36 | Christological mod-10 Mirror comparison labels | `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison state | Luke `36 BC` rail; actual Passion date; AD 35 rounded Christ-death label; local Christological anchor replacing `File_13`; formal Mirror target |
| `AD 16165` | Protocol 1 mirror target-label by dependency | bounded Mirror / Pillar comparison state | locally generated proof-bearing Mirror target; ordinary historical AD anchor |
| `AD 29395` | Projective Pillar coordinate by dependency | projective Pillar coordinate comparison state | ordinary historical AD anchor; `AD 29515` File_19 actual Pillar state unless that dependency is opened |
| Shem `600`; Arphaxad `438`; Peleg `239` | approximate midpoint witnesses | approximate lifespan-halving scaffold state | exact halving stages unless a local table proves them |
| Noah age `480` | warning-age / halving forecast | Noah `480` warning / halving-forecast state | Noah lifespan `950`; Flood date; ordinary maximum-lifespan `120` |
| Genesis 6:3 `120` | Flood-warning / lifespan-cap display | Genesis 6:3 dual-`120` state | Mosaic `120` as a separate textual lifespan unless the dual-reading state is active |
| Shem-to-Eber average `483.75` | approximate post-Flood half-stage | Shem-to-Eber average-lifespan state | exact `480` stage; Rounded Scaffold average `485`; MT + Cainan `479/480` class as same calculation |
| Shem-to-Eber + Cainan actual average `479` | MT + Cainan `4+1` post-Flood half-stage | Shem-to-Eber + Cainan 480-class state | no-Cainan average `483.75`; Rounded Scaffold average; LXX / SP witness states |
| Shem-to-Eber + Cainan rounded average `480` | File_51a-style rounded lifespan comparison | Shem-to-Eber + Cainan 480-class state; bounded Rounded Scaffold comparison state | actual lifespans; schematic mod-120 coordinates; exact table law |
| Peleg `239` / rounded `240` | division-stage lifespan marker | Peleg division state / actual-rounded comparison | exact midpoint law; new date anchor |
| Reu `239` / rounded `240` | repeated Peleg-class lifespan marker | Peleg / Reu `239/240` transition state | Reu as new halving head; exact midpoint law; schematic date anchor |
| `10080` | Priestly `30 × 336` / Prophetic `28 × 360` comparison span | Shem death / Arphaxad birth marker state; Peleg death / Reu boundary marker state | actual `+30 Apparent Age` as an ordinary same-unit span; Rounded Scaffold correction |
| `10800` | `30 × 360` prophetic comparison span | Peleg death / Reu boundary marker state | `10080` Priestly 336 comparison; exact lifespan halving law |
| `11760` | `24 × 490 = 240 × 49` no-Cainan Adam apparent head to Peleg death span | Peleg death / Reu boundary marker state | actual chronology; formal Mirror span |
| `6 × 360 + 7 × 360 = 13 × 360` | Shem / Peleg / Moses schematic decomposition | BJ / Fall-of-man `6 + 7` typological comparison state | formal Mirror protocol; Residue Protocol; literal BJ chronology unless source-control opens it |
| `5` of `11` distinct mod-120 labels landing on AD 35 by `360`-year multiples | diagnostic / elevated-subset observation | AD 35 mod-360 selected-subset state | formal statistical proof; random-probability model; correction of non-360 nodes |
| `14006 BC → AD 35 = 14040 = 39 × 360` | actual cumulative MT comparison with the AD 35 cycle | AD 35 mod-360 selected-subset state; cumulative MT comparison | schematic mod-120 node slot; Rounded Scaffold date; actual Passion date |
| `16166–15206 BC` | with-Cainan Adam apparent/textual 960-slot | 2nd Cainan 460-cycle comparison state; Adam apparent-age `960` state | ordinary actual chronology; no-Cainan slot; formal Mirror target |
| `15686–14726 BC` | no-Cainan Adam apparent/textual 960-slot | no-Cainan Temple-switch comparison state; Adam apparent-age `960` state | with-Cainan slot; actual MT dates; Rounded Scaffold dates |
| `BC + AD − 2` | mod-10 Mirror display operator | mod-10 Mirror display state | civil cross-axis `BC + AD − 1`; Protocol 1 Mirror target-generation; inverse-number reversal |
| `AD 4116`; `AD 5496`; `AD 4836`; `AD 5436`; `AD 4536` | mod-10 Mirror display labels of MT / LXX / cumulative nodes | mod-10 Mirror display state; MT / LXX `21000` conversion state | ordinary historical AD anchors; actual BC dates; Protocol 1 generated targets unless another file opens that protocol |
| `19320`; `20700`; `21000`; `21600`; `19044` | 460-cycle / Key-of-23 comparison spans | MT / LXX `21000` conversion state; LXX Noah `1656` expansion state | civil cross-axis spans; Residue Protocol; formal statistical proof |
| `16136–15206 BC` | schematic Adam actual-lived `930` slot after `+30` apparent age | Adam textual `930` mirror-pair comparison state | `16166–15206 BC` apparent-age slot; actual MT Adam chronology; LXX Adam chronology |
| `4116/3186 BC/AD`; `5496/4566 BC/AD` | MT and LXX Adam `930` mirror-pair display labels | Adam textual `930` mirror-pair comparison state; mod-10 Mirror display state | ordinary historical AD anchors; Protocol 1 target-generation; LXX Jared `960` comparison as the same node-class |
| LXX Jared `960` | LXX Adam-to-Jared offset / rounded Jared-lifespan comparison | LXX Jared `960` slot comparison state | Adam apparent-age `960` as the same node; MT Jared `460`; proof-bearing Watcher chronology |
| LXX Noah `1656` | LXX Noah birth offset / Gear display | LXX Noah `1656` expansion state | MT Flood `1656` unless locally opened; SKL Flood date; actual chronology replacement |
| `966 BC`; `6 BC` Temple-switch endpoints | Temple anchor and Christ anchor reached by no-Cainan Adam slot | no-Cainan Temple-switch comparison state | actual schematic Adam coordinates; formal Mirror protocol; Rounded Scaffold nodes |
| `16136 BC → 36 BC = 16100`; `16136 BC → AD 36 = 16170`; `36 BC → AD 36 = 70` | Creation-of-Adam / Christological mod-10 comparison display | `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison state | ordinary civil cross-axis span; Luke `36 BC` rail; actual Passion chronology; AD 35 mod-360 subset; Protocol 1 target-generation |
| `4166 BC`; `1406 BC`; `1486 BC`; `AD 1406`; `AD 4116`; `4116 BC`; `5496 BC`; `AD 4166`; `AD 6566`; `AD 1526` | Peleg / Adam / Moses / Shem mod-10 Mirror and same-side comparison labels | Peleg / Adam / Moses `230/460` Mirror integration state; minor 460-list amendment state | actual date replacement; civil cross-axis rows unless explicitly using `BC + AD − 1`; formal Mirror target-generation |
| `12650`; `7130`; `11730`; `2760`; `4600`; `5520`; `8280`; `9660`; `6900` | `230/460` comparison spans | Peleg / Adam / Moses `230/460` Mirror integration state; minor 460-list amendment state | Residue Protocol; Rounded Scaffold correction; proof-bearing statistical result |
| `9890 = 23 × 430` | cumulative Flood / Decimal Bridge comparison span | cumulative Flood `9890` comparison state | File_22 actual Decimal Bridge if not locally opened; rounded `9900`; ordinary Temple `430` alone |
| Actual MT dates | textual / actual chronology coordinates | actual MT date state where locally opened | Rounded Scaffold dates; schematic mod-120 span coordinates; SKL dates; Mirror coordinates |
| Rounded Scaffold dates | File_51a rounded comparison coordinates | File_51a actual / rounded comparison state | actual MT dates; schematic mod-120 span coordinates; SKL dates; Mirror coordinates |
| Schematic mod-120-year spans | display coordinates generated by the halving-table overlay | schematic halving-table state; actual / rounded / schematic mod-120 firewall state | actual MT dates; Rounded Scaffold dates; cumulative MT state; SKL dates; Mirror coordinates |
| `3.5 + 3.5 = 7` | completion-pair comparison | Solar/Lunar Straddle comparison state | formal Mirror protocol; ordinary span between date anchors |
| “source code” | source metaphor | upstream source-state vocabulary | technical software claim; proof-bearing operator |
| “Micro-Flood” | typological / theological label | Micro-Flood synthesis state | literal Flood event; arithmetic proof by itself |
| `16560` | forward companion / File_24 biblical-interplay value | `File_24` forward dependency | active File_23 arithmetic or local executable span |

### 0.1b Operator and protocol register

| Operator / protocol | Local status | Handling in `File_23` |
|---|---|---|
| Day-Year Scalar `1:360` | Active | Used only for the source display `3.5 × 360 = 1260`. Arithmetic-control review confirms the product and the local prophetic-day unit-state. |
| Prophetic-year `360` register | Active | Governs `1260` as `3.5` prophetic years where the prophetic half-week state is active. |
| File_21 SKL `360:1` scalar | Dependency-aware but not fully opened | `File_23` uses the local `3.5 × 360` display. It does not open the full File_21 four-state SKL date-generation matrix. |
| Whole-year SKL table arithmetic | Not locally active | Whole-year totals, Sumerian A / SKL Short, Sumerian B / SKL Long, `+30 Apparent Mode`, `+2 local rail`, and `+720 scale-state` remain dependency controls. |
| Ordinary Aaron/Moses `3` branch | Active by dependency | Preserved as ordinary Nisan/Nisan branch. |
| Aaron/Moses `L = 3.5` branch | Active by dependency | Preserved as phase-resolved Tishri/Nisan priestly branch. |
| Integer-floor display | Source-retained only | Preserved as explanatory source wording; not promoted to new operator in modal-state review. |
| Cumulative expansion `10 × 1260 = 12600` | Active by dependency | Preserved as cumulative MT echo controlled by `File_22`. |
| `1/3600` half-week scale relation | Active by author clarification | The argument-control layer preserves `12600 ÷ 3600 = 3.5` and `3.5 × 360 = 1260` to state the cumulative-to-prophetic scale relation. This is argument clarification, not a new chronology operator. |
| Boundary-lifespan sum | Active by author clarification | Uses `930 + 120 = 1050` and `12600 ÷ 12 = 1050` to display Adam and Moses as a `1/12` flank of the cumulative trunk. |
| Apparent-age lifespan display | Active by author clarification | Uses `30 + 930 = 960` and `960 = 8 × 120` to show Adam-with-apparent-age as an exact 8x counterpart to Moses `120`. |
| Approximate halving scaffold | Active as structural inference | Uses ideal values `960 → 480 → 240 → 120`, while preserving Shem-to-Eber averages, the MT + Cainan `479/480` class, and Peleg / Reu `239/240` as stage witnesses. Do not convert “about” into exact equality. |
| Noah warning-age calculation | Active as author expansion | Uses Noah age at Flood `600` minus the Genesis 6:3 warning span `120` to display `600 − 120 = 480`. |
| Actual Noah warning date | Active under MT Minimum-Regular actual date state | Uses `2843 − 480 = 2363 BC`; then `2363 − 2243 = 120` to the Flood. This is a same-side BC display in the actual MT Minimum-Regular state, not a Rounded Scaffold value. |
| Shem-to-Eber average | Active as approximate structural display | `(600 + 438 + 433 + 464) ÷ 4 = 483.75`, showing the no-Cainan post-Flood cluster near the ideal `480` half-stage. |
| Rounded Shem-to-Eber average | Active only under File_51a rounded-lifespan comparison | `(600 + 440 + 435 + 465) ÷ 4 = 485`; this confirms the same approximate half-stage without replacing actual lifespans. |
| MT + Cainan Shem-to-Eber average | Active by post-focused-pressure author clarification | `(600 + 438 + 460 + 433 + 464) ÷ 5 = 479`, using the checked sum `2395`; this clarifies the `4+1` Shem-to-Eber + Cainan group as a `480`-class witness. |
| Rounded MT + Cainan Shem-to-Eber average | Active only under File_51a rounded-lifespan comparison | `(600 + 440 + 460 + 435 + 465) ÷ 5 = 480`; this confirms the rounded `480` class while preserving actual lifespans and the actual / rounded / schematic firewall. |
| Peleg / Reu rounding display | Active as actual / rounded comparison | Peleg `239` and Reu `239` each round to `240`, marking the transition from the Shem-to-Eber `480` class into the Peleg / Reu `240` class while preserving actual textual lifespans. |
| Shem death / Arphaxad birth marker display | Active by post-pressure author clarification | Uses `15686 − 5606 = 10080 = 28 × 360 = 30 × 336` and `16166 − 6086 = 10080` to mark Shem death / Arphaxad birth as accepted schematic nodes. These are not actual or Rounded Scaffold dates. |
| Peleg death / Reu boundary marker display | Active by post-pressure author clarification | Uses `14726 − 3926 = 10800 = 30 × 360`, `14006 − 3926 = 10080 = 30 × 336`, `3926 − 1406 = 2520 = 7 × 360`, `3926 − 6 = 3920 = 8 × 490`, and `15686 − 3926 = 11760 = 24 × 490 = 240 × 49`. |
| Shem-to-Peleg-to-Moses decomposition | Active by post-pressure author clarification | Uses `5606 − 3926 = 1680 = 5 × 336`, `6086 − 3926 = 2160 = 6 × 360`, and `2160 + 2520 = 4680 = 13 × 360 = 390 × 12` to display the `6 + 7` decomposition. |
| Peleg-centered `2400 + 240 + 2400` chiasm | Active by current post-pressure author clarification | Uses `6566 − 4166 = 2400`, `4166 − 3926 = 240`, and `3926 − 1526 = 2400`. Peleg's full schematic lifespan forms the central `240` span between equal `2400` wings from with-Cainan Shem birth to Moses birth. |
| AD 35 rounded Christ-death mod-120 culmination | Active as bounded Christological comparison | Uses civil cross-axis counting from `6 BC` to AD 35: `6 + 35 − 1 = 40`. AD 35 is a rounded Christ-death comparison label and does not replace actual Passion date-states. |
| AD 35 mod-360 selected-subset display | Active as bounded diagnostic comparison | Uses civil cross-axis `BC + AD − 1`: `16166 + 35 − 1 = 16200 = 45 × 360`; `14726 + 35 − 1 = 14760 = 41 × 360`; `6086 + 35 − 1 = 6120 = 17 × 360`; `3926 + 35 − 1 = 3960 = 11 × 360`; `1406 + 35 − 1 = 1440 = 4 × 360`. The actual cumulative `14006 + 35 − 1 = 14040 = 39 × 360` is an auxiliary comparison, not a schematic node. |
| 12-node / 11-distinct-date mod-120 register | Active as node-count display | Counts the double `6086 BC` as two node-class slots, giving `12` mod-120 nodes but `11` distinct date labels. The observation that `5` of the `11` distinct labels also land on AD 35 by `360`-year multiples is diagnostic, not formal statistical proof. |
| 2nd Cainan 460-cycle comparison | Active by current post-pressure author clarification | Uses `16166 − 1446 = 14720 = 32 × 460`, `15206 − 1406 = 13800 = 30 × 460`, mod-10 Mirror displays of `42 × 460` and `45 × 460`, and no-Cainan Temple-switch displays. This is comparison-state material, not a table-generation operator. |
| Mod-10 Mirror display `BC + AD − 2` | Active only in §§5.9.3b–5.9.3d | Used where the author invokes the rounded / mod-10 Mirror display in which `6 BC → AD 6 = 10`. Ordinary civil cross-axis counting remains `BC + AD − 1`. Do not use this display to silently recalculate bounded civil cross-axis rows in §§5.9.3, 5.9.3a, or §5.9.5. |
| MT / LXX `21000` conversions | Active by current post-pressure author clarification | Uses `19320 × 25/23 = 21000`, `20700 × 70/69 = 21000`, and `16166 BC → AD 4836 = 21000` under the mod-10 Mirror display. Retain as structural inference / providential synchronization. |
| LXX Jared / Noah comparison operators | Active by current post-pressure author clarification | Uses `5496 − 960 = 4536`, `16166 + 4536 − 2 = 20700`, `20700 − 1656 = 19044`, and `19044 × 25/23 = 20700`. LXX source-control remains dependency-controlled. |
| No-Cainan Temple-switch display | Active by current post-pressure author clarification | Uses `15686 − 966 = 14720 = 32 × 460`, `14726 − 6 = 14720 = 32 × 460`, `966 − 6 = 960`, `14720 × 25/23 = 16000`, and `14726 − 4836 = 9890 = 23 × 430`. The Temple / body typology remains theological / typological support. |
| `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison | Active by minor author amendment | Uses same-side BC display `16136 − 36 = 16100 = 7 × 2300 = 35 × 460 = 230 × 70`, same-side AD mirror display `AD 16136 → AD 36 = 16100`, mod-10 Mirror display `16136 + 36 − 2 = 16170 = 33 × 490 = 33 × 7 × 70`, and central mod-10 display `36 BC → AD 36 = 36 + 36 − 2 = 70`. This is bounded Christological / Creation-week comparison, not ordinary civil cross-axis arithmetic and not Protocol 1 target-generation. |
| Adam `930` mirror-pair display | Active by current post-focused-pressure author clarification | Uses `16166 − 30 = 16136`, `16136 − 15206 = 930`, `4116 − 3186 = 930`, `5496 − 4566 = 930`, `16136 + 3186 − 2 = 19320`, and `16136 + 4566 − 2 = 20700`. This display clarifies that the schematic Adam textual `930` slot aligns directly with MT and LXX Adam `930` mirror pairs. |
| Peleg / Adam / Moses `230/460` Mirror integration | Active by current post-focused-pressure author clarification, with 230-only diagnostic restraint and minor 460-list amendment | Uses `4166 − 1406 = 2760 = 23 × 120 = 12 × 230 = 6 × 460`, `6086 − 1486 = 4600 = 10 × 460`, `4166 + 4116 − 2 = 8280 = 18 × 460`, `4116 + 1406 − 2 = 5520`, `5520 × 25/23 = 6000`, `5496 + 4166 − 2 = 9660`, `9660 × 70/69 = 9800`, `5496 + 1406 − 2 = 6900`, `6900 × 70/69 = 7000`, `6086 + 6566 − 2 = 12650`, `5606 + 1526 − 2 = 7130`, and `15656 − 3926 = 11730`. Full `460` rows remain the high-value comparison examples; isolated `230` rows that are not also `460` multiples are diagnostic only. The foregrounded 460-list is not exhaustive. |
| Textual Adam half audit | Audit-controlled | Exact textual-lifespan half is `930 ÷ 2 = 465`. The ideal `480` stage comes from the active apparent-age state: `(30 + 930) ÷ 2 = 480`. |
| Creation-to-half-week comparison | Active as design comparison | Uses `7 ÷ 2 = 3.5` to relate Creation's full week to the Aaron/Moses half-week spread. This is not formal Mirror arithmetic. |
| Completion pair `3.5 + 3.5 = 7` | Comparison state | Preserved as Solar/Lunar Straddle comparison, not formal Mirror target-generation. |
| Same-side BC span | Active in state-labeled displays | Used for ordinary Aaron/Moses birth-label arithmetic and for schematic / actual / rounded / Key-of-23 comparison displays in §5 where both endpoints are BC. Arithmetic-control review verifies the visible same-side §5 displays. |
| Schematic mod-120-year span display | Active only as schematic overlay | The §5 halving table uses same-side BC subtraction and clean `120` / `240` / `480` / `960` units as display coordinates. These spans are not actual MT chronology and not File_51a Rounded Scaffold chronology unless that state is explicitly opened in the local row. |
| Key-of-23 ratio-family comparison | Active as preserved comparison display | `25/23`, `70/69`, and `300/299` appear in §§5.7–5.10 as state-labeled comparison operators. Arithmetic-control review verifies the displayed exact-ratio conversions while preserving them as comparison-state operators, not Residue Protocol. |
| Civil cross-axis span `BC + AD − 1` | Bounded active in §§5.9.3, 5.9.3a, and 5.9.5 | Used only for bounded Christ / Temple and Mirror / Pillar comparison displays after endpoint labels have been supplied by dependency. This does not open formal Mirror protocol, 10x Mirror scaling, or inverse-number reversal as a proof-bearing local operator. |
| Formal Mirror protocol | Not active as local proof protocol | §5.9.5 may name a Protocol 1 mirror target by dependency, but `File_23` does not generate formal Mirror targets as proof mechanics. Protocol 1, Protocol 2, phase-resolved Mirror coordinate-completion, and 10x Mirror scaling remain outside local proof burden. |
| Inverse-number reversal | Not active | No inverse-number operator is present. |
| Residue Protocol | Not active | No physical residue, conceptual residual-display, or residual day-year lift is opened. |
| Rounded Scaffold / File_51a comparison | Bounded active state | Active only where `File_23` cites actual / rounded lifespan dates or rounded lifespans from `File_51a`. It does not retroject rounded status into SKL, prophetic, cumulative, Mirror, or clean-value displays. |

### 0.1c Modal-state processing rules

1. The `tebah` bridge is typological and theological. It warrants comparison but does not replace arithmetic, source-control, or dependency-state verification.
2. The SKL `3 months and 3.5 days` datum is an appended fractional payload. It must remain distinct from whole-year SKL table totals.
3. The `3 months` in Exodus 2:2 and the SKL `3 months` component are paired by typology and structural inference, not by textual tradition identity.
4. SKL `3.5` days, Priestly-Enochian `3.5` days, prophetic `3.5` years, and Aaron/Moses `L = 3.5` are related but distinct node-classes.
5. The ordinary Aaron/Moses `3`-year branch and the phase-resolved `L = 3.5` branch are simultaneous representations. Neither cancels the other.
6. The source wording that the ordinary `3` is the integer floor of `3.5` checks as `floor(3.5) = 3`, but it is not promoted to a new chronology operator.
7. The `1260` prophetic half-week and the `12600` cumulative MT trunk echo remain distinct. The 10x relation does not make them the same node-class. The argument-control layer preserves the author-resolved `1/3600` expression: `12600 ÷ 3600 = 3.5` prophetic years and `3.5 × 360 = 1260` days.
8. The Adam/Moses boundary-lifespan state uses textual lifespan values `930` and `120` as the outer flank of the 26-patriarch display. The `24` intervening generations are retained as author-supplied table logic and not locally rederived in `File_23`.
9. The Adam apparent-age display `30 + 930 = 960` uses the repository `+30 Apparent Age` vocabulary by dependency. It does not replace Adam's textual lifespan `930` and does not create a new regular chronology.
10. The lifespan-halving scaffold is approximate except where ideal arithmetic is explicitly stated. Preserve the word “about” for `960/480/240/120` and for the Noah / Shem-to-Eber / Peleg staging.
11. The full lifespans of Adam, Shem, Peleg, and Moses are now active node markers by post-pressure author clarification. In the schematic table, death-boundaries may function as markers as well as birth / start boundaries: Shem death / Arphaxad birth at `5606/6086 BC`, Peleg death / Reu boundary at `3926 BC`, and Moses death / Conquest at `1406 BC`. These are schematic coordinates unless a local row explicitly opens actual or rounded chronology.
11a. The Shem-to-Eber + Cainan `4+1` actual average belongs to the MT + Cainan 480-class state. The listed actual lifespans sum to `2395`, giving `2395 ÷ 5 = 479`; rounded lifespans sum to `2400`, giving `2400 ÷ 5 = 480`. Do not preserve the source phrase `2394/5=479` as executable arithmetic.
11b. Peleg and Reu both display `239` actual years and rounded `240`; this supports the `480 → 240` transition but does not make Reu a separate halving head.
11c. MT without restored Cainan and MT + restored 2nd Cainan are the primary tradition states for the File_23 halving pattern. LXX and SP remain subordinate comparison witnesses unless a local dependency explicitly opens them as controlling states.
11d. The `6566 → 4166 → 3926 → 1526` Peleg chiasm is a schematic comparison: `2400 + 240 + 2400`. It does not convert the halving overlay into actual chronology or Rounded Scaffold chronology.
11e. The double `6086 BC` display is a node-class distinction. It may be counted as two node slots where no-Cainan Shem birth and with-Cainan Shem death / Arphaxad birth are both active, but it remains one distinct date label for distinct-date counts.
11f. AD 35 is a rounded Christ-death comparison label in this file. It may be used as a civil cross-axis terminal for bounded comparison, but it does not replace actual Passion date-states and does not open formal Mirror protocol.
11g. The 460-cycle / LXX-MT Mirror slot clarification uses mod-10 Mirror display arithmetic `BC + AD − 2`, not ordinary civil cross-axis arithmetic. The source display `6 BC → AD 6 = 10` is therefore a rounded / mod-10 display; the ordinary civil span would be `6 + 6 − 1 = 11`.
11h. MT, LXX, cumulative, and Temple nodes in §§5.9.3b–5.9.3c remain dependency-controlled comparison states. They do not convert schematic Adam slots into actual chronology, Rounded Scaffold chronology, or formal Mirror protocol.
11i. The Adam textual `930` mirror-pair display distinguishes the apparent-age head `16166 BC` from the actual-lived schematic slot `16136–15206 BC`. It may compare that schematic `930` with MT `4116–3186 BC/AD` and LXX `5496–4566 BC/AD` mirror pairs, but it does not replace MT or LXX actual chronology.
11j. The Peleg / Adam / Moses `230/460` Mirror integration uses both same-side BC subtraction and bounded mod-10 Mirror display. Full `460` multiples carry the primary comparison weight. Rows that are `230` multiples but not `460` multiples are diagnostic half-cycle observations and are downweighted unless they attach to a known `460` path; they do not open Residue Protocol, formal Mirror target-generation, or Rounded Scaffold retrojection.
11k. The author-supplied `6085 BC` Shem display is audit-controlled. The active File_23 schematic label is `6086 BC`; under the active mod-10 display `6086 + 6566 − 2 = 12650`. The civil-style equivalent `6085 + 6566 − 1 = 12650` may explain the shorthand, but `6085 BC` is not promoted to a new File_23 node.
11l. The `16136 BC` / `36 BC` / AD 36 comparison is a bounded Christological mod-10 display. The same-side span `16136 − 36 = 16100` is executable; the cross-axis display `16136 BC → AD 36 = 16170` uses the mod-10 operator `BC + AD − 2`, not ordinary civil `BC + AD − 1`. The label `36 BC` must not be collapsed with Luke's `36 BC` rail unless a local section explicitly opens that dependency.
12. Noah age `480` belongs to the Flood-warning / halving-forecast state. It is not Noah's lifespan and not the same node as the final Mosaic `120` lifespan.
13. The source phrase “`930/2=480`” is audit-controlled. Exact `480` follows from Adam-with-apparent-age `960`, not from Adam's textual `930` alone: `930 ÷ 2 = 465`, while `960 ÷ 2 = 480`.
14. Genesis 6:3 `120` may be read here as both the remaining years to the Flood and the final lifespan cap represented by Moses. This is a theological / structural interpretation, not a replacement of the textual datum.
15. Actual MT dates, File_51a Rounded Scaffold dates, and schematic mod-120-year span coordinates are three separate states. Actual dates do not become Rounded Scaffold dates because they land on clean values; Rounded Scaffold dates do not overwrite actual MT dates; schematic mod-120 spans remain display coordinates unless the local row explicitly opens an actual or rounded state.
16. Peleg's name / division clue supports the halving / dividing theme as typological or lexical corroboration. It does not create a new arithmetic operator.
17. The Creation-to-Aaron/Moses comparison relates the full Creation week `7` to the Aaron/Moses `3.5` half-week spread. It does not open formal Mirror protocol, inverse-number logic, or proof-bearing civil cross-axis protocol. The later §5.9.5 Mirror / Pillar note is a bounded anchor-corroboration comparison only and uses civil cross-axis arithmetic only inside that bounded comparison state.
18. The schematic halving-table dates in §5 are display coordinates. They do not replace actual MT dates, File_51a Rounded Scaffold dates, cumulative MT states, SKL states, or Mirror coordinates, even when they form clean multiples of `120`.
19. Key-of-23 ratios in §§5.7–5.10 are retained as dependency-aware comparison operators. Arithmetic-control review verifies the displayed arithmetic but does not promote them into Residue Protocol.
20. `AD 25` in §5.9.3 is a bounded Christological comparison display. It is not formal Mirror target-generation and is not a new local Christological anchor.
20a. AD 35 in §5.9.3a is a bounded rounded Christ-death comparison display. The observation that selected mod-120 schematic nodes land on AD 35 by exact `360`-year multiples is diagnostic and structural; it is not a formal probability model and not a proof-bearing Christological chronology.
21. `AD 16165` in §5.9.5 is a bounded Protocol 1 target-label dependency. It supplies a Mirror label for comparison only. The local elapsed spans still use ordinary civil cross-axis arithmetic `BC + AD − 1`.
22. `AD 29395` in §5.9.5 is a dependency-controlled Projective Boundary / Pillar coordinate. It is not ordinary historical chronology and is not locally generated by `File_23`.
23. `File_23` prepares `File_24` but does not revise or execute the `File_24` 23-kings Kish cipher, En-tarah-ana coordinate-family displays, or `16560` biblical-interplay examples.

### 0.1d Sign-convention, slash-pair, and display-state register

| Display / convention | Active state | Handling in `File_23` |
|---|---|---|
| Same-side BC subtraction | active in ordinary Aaron/Moses branch and state-labeled §5 displays | Use higher BC date minus lower BC date where both endpoints are BC. Arithmetic-control review verifies the later §5 calculations under this sign convention. |
| Civil cross-axis span `BC + AD − 1` | bounded §§5.9.3, 5.9.3a, and 5.9.5 comparison states | Use only inside bounded Christ / Temple and Mirror / Pillar comparison notes. It does not open formal Mirror proof protocol. |
| Mod-10 Mirror display `BC + AD − 2` | bounded §§5.9.3b–5.9.3d comparison state | Use only where the source explicitly invokes the rounded / mod-10 display in which `6 BC → AD 6 = 10`. Do not apply it to ordinary civil cross-axis rows. |
| No 2nd Cainan / With 2nd Cainan | schematic halving-table columns | Treat as two schematic overlay states, not as a literal range and not as replacement actual or Rounded Scaffold chronologies. |
| Slash-pairs such as `15686/16166 BC`, `6086/6566 BC`, `14036/14006 BC`, and `4136/4106 BC` | paired display labels | Preserve as paired state labels. Do not collapse them into a range, average, or single corrected date. |
| Arrow sequence `960 → 480 → 240 → 120` | ideal halving scaffold | Treat as stage sequence, not as a chronological date range. |
| `1526 → 1486 → 1446 → 1406` | Moses internal stage display | Treat as a schematic same-side sequence of Mosaic life stages, not as a slash-pair or range. |
| `AD 25`, AD 35, AD 36, `AD 16165`, and `AD 29395` | bounded cross-axis / mod-10 / Projective Pillar comparison labels | Treat as dependency-controlled comparison targets. They do not become ordinary local historical anchors. AD 35 is a rounded Christ-death comparison label, not an actual Passion-date replacement. AD 36 in the `16136 BC` comparison uses bounded mod-10 display and does not replace actual Passion states. |
| `6566 → 4166 → 3926 → 1526` | Peleg-centered schematic chiasm | Treat as same-side BC schematic display: `2400 + 240 + 2400`. It is not a literal genealogy table or formal Mirror protocol. |
| `12` node slots / `11` distinct date labels | mod-120 node-count distinction | Count double `6086 BC` as two node-class slots only when the Shem birth / Shem death distinction is active. Do not count it as two distinct date labels. |
| `File_24` Kish cipher / `16560` | forward companion display | Mentioned only as forward dependency. The 23-kings Kish cipher, En-tarah-ana coordinate-family displays, and `16560` examples remain inactive in `File_23`. |

### 0.2 File map

| Section | Function | Active state / operator |
|---|---|---|
| Related Repository Files | Names controlling dependencies and forward companion file | dependency tracking |
| §0 | Defines file function, working states, node-class register, operator register, arithmetic-control and argument-control ledgers, and retrieval controls | file-level modal states; source-history controls; post-pressure clarification controls; minor amendment controls |
| §1 | Identifies the Tebah narrative lock | `tebah`; Noah / Moses typological bridge |
| §2 | Isolates the SKL fractional datum | `3 months and 3.5 days`; En-tarah-ana fractional-tail state |
| §3 | Records the Day-Year Scalar | `3.5 × 360 = 1260`; prophetic half-week state |
| §4 | Records the Mosaic output | `3 months`; ordinary `3`; phase-resolved `L = 3.5` |
| §5 | Preserves the cumulative echo and reprocessed lifespan-halving design around Adam/Moses brackets and the primary full-lifespan halving nodes Shem and Peleg | `12600`; `10 × 1260`; `1/3600`; `1/12`; Adam `930`; Adam apparent-age `960`; Moses `120`; schematic table `15686/16166 BC`; Shem birth/start `6086/6566 BC`; Shem death / Arphaxad birth `5606/6086 BC`; Peleg birth/start `4166 BC`; Peleg death / Reu boundary `3926 BC`; `2400 + 240 + 2400`; AD 35 mod-120 cycle; 460-cycle / LXX-MT Mirror slot comparison; `16136 BC → 36 BC / AD 36`; `10080`; `10800`; `2520`; Cainan `130/460`; Key-of-23 comparison; bounded Christ / Temple and Mirror / Pillar comparisons; actual / rounded File_51a comparison |
| §6 | States the local synthesis | Micro-Flood typological / theological synthesis |
| §7 | Preserves the cross-reference vector | `File_22`; `File_00`; source logic chain |
| Audit / unresolved / cross-file controls | Records audit notes, unresolved issues, recommended cross-file updates, and archive-routing note | dependency tracking; publication cleanup |

### 0.3 Claim-status control

This file uses claim-status language only where it prevents collapse between textual data, arithmetic statements, typological readings, structural inference, bounded comparison, and theological synthesis.

| Label | Local function in `File_23` |
|---|---|
| Textual datum | Biblical or SKL datum preserved from the source or dependency. |
| Textual / lifespan datum | Adam `930`, Noah `950`, Shem `600`, Arphaxad `438`, Shelah `433`, Eber `464`, Peleg `239`, and Moses `120` where used as lifespan markers. |
| Arithmetic fact | Direct calculation checked in arithmetic-control review, or added in the post-pressure patch arithmetic additions, under the active state and operator. |
| Structural inference | Pattern relation between the Tebah bridge, SKL fraction, Mosaic hiding period, Aaron/Moses straddle, cumulative `12600`, Adam/Moses boundary flank, Shem / Peleg full-lifespan halving nodes, and the Adam-to-Moses lifespan descent. |
| Approximate structural display | The about `960/480/240/120` lifespan-halving scaffold and the Noah / Shem-to-Eber / Peleg staging where approximation remains active; full-lifespan marker status does not turn approximate stage values into exact table law. |
| Bounded comparison | Major-anchor, Key-of-23, Christ / Temple, AD 35 rounded Christ-death, mod-10 Mirror display, MT / LXX 21000 conversion, Mirror / Pillar, or Rounded Scaffold comparison retained for corroboration without becoming the local proof protocol. |
| Diagnostic observation | A frequency or subset observation, such as `5` of `11` distinct mod-120 labels landing on AD 35 by `360`-year multiples or a high-density 460-cycle cluster, retained as structurally notable but not as a formal statistical result unless a defined model is supplied. |
| Rounded comparison datum | File_51a rounded lifespan or rounded cumulative date used only when the Rounded Scaffold comparison state is explicitly active. |
| Schematic display datum | A mod-120 table coordinate or span used only inside the halving-table overlay. It is not actual chronology and not Rounded Scaffold chronology by default. |
| Typological reading | Noah / Moses ark correspondence, Peleg division clue, transition / judgment-node reading, and Micro-Flood synthesis. |
| Theological note | Progressive lifespan decline, human feebleness, New Adam / Prophet-like-Moses reading, and design-oriented theological synthesis. |
| Providential synchronization | Coherence across arithmetic, typological, and theological layers beyond demonstrable human scribal intent. |
| Dependency note | Claim controlled by another repository file, especially `File_21`, `File_22`, `File_34`, or `File_51a`. |
| Audit note | Value, unit-state, phrase, source-control issue, section reference, or lexical claim requiring later review. |

Claim-status rule:
The `tebah` motif explains the theological meaning of the structure. The arithmetic burden remains with stated values, scalar operators, and dependency-controlled files. Bounded comparisons in §§5.9.3, 5.9.3a, and 5.9.5 may corroborate the design argument, but they do not open formal Mirror proof mechanics or civil cross-axis proof as the file's primary protocol.

### 0.4 Machine Guards for `File_23`

Machine Guard [FILE_23 TEBAH / ARITHMETIC BURDEN]:
The `tebah` ark/basket motif is a typological bridge. It may warrant the Flood-to-Moses overlay inside this file, but it does not replace arithmetic, SKL source-control, or dependency-state verification.

Machine Guard [FILE_23 SKL FRACTIONAL PAYLOAD]:
The SKL appended fractional datum `3 months and 3.5 days` is distinct from whole-year SKL totals. Do not import whole-year SKL arithmetic, SKL Short / Long table totals, `+30 Apparent Mode`, `+2 local rail`, `+720 scale-state`, or the File_21 four-state matrix into this file unless a later pass explicitly opens that state.

Machine Guard [FILE_23 FILE_34 POST-FINAL BOUNDARY]:
`File_23` uses `File_34 Final; post-final pressure tested` for SKL / Berossus source-control, but it does not locally open File_34 Appendix B phase-chain arithmetic, the `2370` corridor, the `30 + 20 + 30` restoration corridor, Pillar / AD target spans, or Sanctuary Fractal machinery.

Machine Guard [FILE_23 `3.5` NODE-CLASS FIREWALL]:
SKL `3.5` days, Priestly-Enochian liturgical `3.5` days, prophetic `3.5` years, Aaron/Moses `L = 3.5`, and Solar/Lunar comparison halves are related by shared `3.5` vocabulary but are not the same node-class. Restate the active state whenever the argument shifts.

Machine Guard [FILE_23 `3` / `3.5` NON-COLLAPSE]:
The Aaron/Moses relation has two coordinated states: the ordinary `3`-year birth-label branch and the phase-resolved `L = 3.5` branch. Do not silently correct one into the other.

Machine Guard [FILE_23 PROPHETIC / CUMULATIVE FIREWALL]:
`1260` belongs locally to the prophetic half-week state. `12600` belongs to the cumulative MT trunk state by `File_22` dependency. The decimal relation `10 × 1260 = 12600` is a structural echo, not a collapse of node-class.

Machine Guard [FILE_23 LIFESPAN-HALVING RESTRAINT]:
The Adam-to-Moses lifespan descent may be displayed as an approximate symmetrical scaffold: Adam `30 + 930 = 960`, Noah at age `480`, Shem / Arphaxad / Shelah / Eber averaging `483.75`, the MT + Cainan Shem-to-Eber `4+1` group averaging `479` actual and `480` rounded, Peleg / Reu `239` / rounded `240`, Moses `120`, and idealized stages about `960/480/240/120`. Preserve the qualifier “about.” Do not turn the scaffold into exact table law, do not correct the lifespans to fit the ideal stages, and do not replace Adam's textual lifespan `930` with the apparent-age display `960`.

Machine Guard [FILE_23 FULL-LIFESPAN NODE MARKERS]:
The post-pressure patch clarifies that Adam, Shem, Peleg, and Moses are full-lifespan halving-node markers. Do not restrict Shem and Peleg to their birth / start positions. In the schematic overlay, Shem death / Arphaxad birth at `5606/6086 BC` and Peleg death / Reu boundary at `3926 BC` are accepted display nodes. These remain schematic mod-120 coordinates unless the local row explicitly opens actual or Rounded Scaffold chronology.

Machine Guard [FILE_23 MT + CAINAN 480-CLASS CONTROL]:
The Shem-to-Eber + Cainan `4+1` group uses MT with restored 2nd Cainan as the primary comparison state: Shem `600`, Arphaxad `438`, 2nd Cainan `460`, Shelah `433`, and Eber `464`. The checked actual sum is `2395`, so `2395 ÷ 5 = 479`; the rounded sum is `2400`, so `2400 ÷ 5 = 480`. Treat the author-supplied `2394/5=479` as audit-controlled shorthand, not executable arithmetic. This state strengthens Shem's placement in the `480` class without replacing the no-Cainan average, without regularizing actual lifespans, and without making LXX or SP the controlling tradition for this local pattern.

Machine Guard [FILE_23 NOAH `480` / GENESIS 6:3 CONTROL]:
Noah age `480` is a warning-age and halving-forecast state, not Noah's lifespan. Genesis 6:3 `120` may be interpreted in this file as both the years remaining to the Flood and the maximum human lifespan represented by Moses, but this dual interpretation must be labeled as structural / theological unless a later source-control pass makes it proof-bearing.

Machine Guard [FILE_23 ADAM `930` / APPARENT-AGE `960` AUDIT]:
Do not write `930 ÷ 2 = 480` as an arithmetic fact. Exact halving to `480` requires the apparent-age state: `30 + 930 = 960`; `960 ÷ 2 = 480`. Adam's textual lifespan remains `930`.

Machine Guard [FILE_23 FILE_51a ROUNDED COMPARISON]:
When actual and rounded dates are displayed together, the active state is `File_51a` rounded comparison. Rounded dates and rounded lifespans do not replace actual MT dates, actual lifespans, cumulative MT state, SKL state, prophetic half-week state, or formal Mirror protocol.

Machine Guard [FILE_23 ACTUAL / ROUNDED / SCHEMATIC MOD-120 FIREWALL]:
Actual MT dates, File_51a Rounded Scaffold dates, and schematic mod-120-year span coordinates are distinct states. The halving table may use clean multiples of `120`, `240`, `480`, and `960` as schematic display coordinates, but those coordinates are not actual chronology and not Rounded Scaffold chronology unless the local row explicitly opens that state. Do not identify a date as Rounded merely because it is clean, mod-120, or harmonically regular, and do not use rounded dates to correct actual MT or schematic rows.

Machine Guard [FILE_23 PELEG CHIASM / DOUBLE-6086 CONTROL]:
The `6566 → 4166 → 3926 → 1526` display is a schematic Peleg-centered chiasm: `2400 + 240 + 2400`. The `6086 BC` label may function as no-Cainan Shem birth / start and as with-Cainan Shem death / Arphaxad birth under different schematic states. Count it as two node-class slots only when that state distinction is active; do not treat it as two distinct date labels or as a single actual historical event.

Machine Guard [FILE_23 AD 35 MOD-120 CULMINATION]:
AD 35 is a rounded Christ-death comparison label used to test mod-120 and mod-360 cycle culmination. It does not replace actual Passion date-states, does not create a new local Christological anchor, and does not open formal Mirror target-generation. The observation that `5` of `11` distinct schematic mod-120 labels land on AD 35 by exact `360`-year multiples is diagnostic unless a formal statistical model is supplied.

Machine Guard [FILE_23 MOD-10 MIRROR DISPLAY CONTROL]:
The 460-cycle / LXX-MT Mirror slot clarification uses the rounded / mod-10 Mirror display `BC + AD − 2`, illustrated by `6 BC → AD 6 = 10`. This is not the ordinary civil cross-axis span operator. Ordinary civil spans remain `BC + AD − 1`. Do not use the mod-10 display to recalculate civil rows, generate Protocol 1 targets, open Protocol 2, or introduce inverse-number reversal.

Machine Guard [FILE_23 460-CYCLE / LXX-MT SLOT CONTROL]:
The with-Cainan Adam slot `16166–15206 BC` and no-Cainan Adam slot `15686–14726 BC` may function as high-density 460-cycle comparison nodes. The MT, LXX, Jared, Noah, cumulative Flood, Temple, Adam `930`, Peleg / Shem `230/460`, and Moses age-40 `460` rows in §§5.9.3b–5.9.3d are bounded structural-inference / providential-synchronization displays. They do not prove that `16166–15206 BC` is the only possible 960-year slot, do not replace LXX source-control, and do not retroject Rounded Scaffold, Residue Protocol, or formal Mirror mechanics into File_23.

Machine Guard [FILE_23 ADAM 930 / 230-460 MIRROR CONTROL]:
The Adam textual `930` mirror-pair display and the Peleg / Adam / Moses `230/460` integration use bounded mod-10 Mirror display only where explicitly stated. The apparent-age head `16166 BC`, the schematic actual-lived slot `16136–15206 BC`, the MT Adam mirror pair `4116/3186 BC/AD`, and the LXX Adam mirror pair `5496/4566 BC/AD` are comparison states, not replacements for actual chronology. Full `460` multiples attach to the known 2nd-Cainan `460` cycle stream and carry the primary evidential weight. Rows that are multiples of `230` but not `460` are retained as arithmetic facts / exploratory diagnostics and are downweighted unless a local row connects them to a recognized `460` path. The foregrounded `460` examples are representative, not exhaustive. Do not use these rows to open formal Mirror target-generation, ordinary civil cross-axis revision, Residue Protocol, Rounded Scaffold retrojection, or a new LXX controlling state.

Machine Guard [FILE_23 16136 / 36 BC / AD 36 CHRISTOLOGICAL MOD-10 CONTROL]:
The `16136 BC → 36 BC / AD 36` comparison belongs to the bounded Christological / Creation-week mod-10 display state. Same-side `16136 − 36 = 16100 = 7 × 2300 = 35 × 460 = 230 × 70` is an executable same-side display. Cross-axis `16136 BC → AD 36 = 16170 = 33 × 490 = 33 × 7 × 70` and `36 BC → AD 36 = 70` use the mod-10 Mirror operator `BC + AD − 2`, not ordinary civil `BC + AD − 1`. Do not collapse `36 BC` with Luke's `36 BC` rail, AD 36 with actual Passion-date states, or this comparison with Protocol 1 target-generation.

Machine Guard [FILE_23 NO MIRROR / RESIDUE / ROUNDED RETROJECTION]:
`File_23` does not open formal Mirror protocol, inverse-number reversal, Residue Protocol, or proof-bearing civil cross-axis protocol. The §5.9.3 Christ / Temple material, §5.9.3a AD 35 material, §§5.9.3b–5.9.3d mod-10 Mirror display material, the `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison, and §5.9.5 Mirror / Pillar material are bounded comparison displays. The Rounded Scaffold is opened only as a bounded File_51a comparison layer for actual / rounded lifespan material. Clean values, 10x echoes, SKL displays, and completion-pair displays remain local-state displays unless a later section explicitly opens another protocol.

### 0.5 Arithmetic-control ledger

Arithmetic-control scope:
This pass checks visible calculations and unit-state displays already active in `File_23` after modal-state review. It verifies arithmetic under the stated local operators only. It does not supply independent source provenance for the displayed anchors, does not open `File_24`, does not open whole-year SKL date-generation, does not open formal Mirror target-generation as a proof protocol, does not open Residue Protocol, and does not promote Rounded Scaffold logic beyond the bounded `File_51a` comparison layer.

| Calculation / display | Active state / operator | Arithmetic-control result | Handling in `File_23` |
|---|---|---|---|
| `3.5 × 360 = 1260`; `1260 ÷ 360 = 3.5` | Day-Year Scalar; prophetic half-week | Checks | `1260` is handled locally as prophetic days, equal to `3.5` prophetic years under the 360-day register. |
| `1529 − 1526 = 3` | ordinary Aaron/Moses Nisan/Nisan birth-label branch | Checks by same-side BC subtraction | Dependency-controlled by `File_22`; this is the ordinary branch and is not corrected into `3.5`. |
| `1530t BC → 1526n BC = 3.5` | Aaron/Moses phase-resolved Tishri/Nisan branch | Checks only under the phase-resolved dependency state | This is not ordinary integer label subtraction. It requires the Nisan/Tishri phase operator supplied by dependency. |
| `floor(3.5) = 3` | source-retained integer-floor display | Checks | Preserved as explanatory display. It is not promoted into a new chronology operator. |
| `10 × 1260 = 12600`; `12600 ÷ 10 = 1260` | cumulative expansion / File_22 dependency | Checks | Structural echo from prophetic half-week to cumulative MT trunk. Node-classes remain distinct. |
| `12600 ÷ 3600 = 3.5`; `3.5 × 360 = 1260` | author-resolved `1/3600` scale relation | Checks | Shows the prophetic half-week miniature as `1/3600` of the full `12600`-year cumulative trunk. |
| `930 + 120 = 1050`; `12600 ÷ 12 = 1050` | Adam/Moses boundary-lifespan sum; `1/12` flank state | Checks | Displays the first and last patriarchal lifespans as a single boundary flank equal to `1/12` of the cumulative trunk. |
| `30 + 930 = 960`; `960 ÷ 120 = 8`; `960 = 8 × 120` | Adam apparent-age display | Checks | Uses `+30 Apparent Age` only for symmetrical-design display. Adam's textual lifespan remains `930`. |
| `960 ÷ 2 = 480`; `480 ÷ 2 = 240`; `240 ÷ 2 = 120` | ideal halving scaffold | Checks | Ideal arithmetic checks. Actual lifespan witnesses remain approximate and are not silently regularized. |
| `930 ÷ 2 = 465`; `960 ÷ 2 = 480` | Adam textual lifespan versus apparent-age state | Checks | The exact `480` stage requires the apparent-age state. The phrase `930/2=480` remains invalid as exact arithmetic. |
| `600 − 120 = 480` | Noah warning-age / Genesis 6:3 state | Checks | Noah receives the `120`-year warning at age `480` if the Flood occurs when Noah is `600`. |
| `2843 − 480 = 2363 BC`; `2363 − 2243 = 120` | actual MT Minimum-Regular Noah date state | Checks | Gives the actual warning-date display in the MT Minimum-Regular table state. |
| `15686 − 6086 = 9600 = 10 × 960`; `16166 − 6566 = 9600 = 10 × 960` | Shem halving-juncture schematic overlay | Checks | The no-Cainan and with-Cainan Adam-to-Shem schematic blocks both preserve the `9600` / `10 × 960` display. |
| `6086 − 1406 = 4680 = 13 × 360`; `6566 − 1526 = 5040 = 14 × 360 = 7 × 720`; `5040 − 4680 = 360`; `480 − 120 = 360` | Shem and prophetic-year comparison | Checks | The with-Cainan span preserves one additional `360`-year prophetic component. |
| `6566 − 586 = 5980`; `5980 = 13 × 460`; `5980 = 46 × 130`; `5980 × 300/299 = 6000 = 10 × 600` | Shem / Cainan / Fall of Jerusalem comparison | Checks | Exact `300/299` comparison checks. This is Key-of-23 comparison state, not Residue Protocol. |
| `4166 − 1406 = 2760`; `2760 = 23 × 120`; `2760 = 69 × 40`; `2760 × 70/69 = 2800 = 70 × 40` | Peleg-to-Moses / Conquest comparison | Checks | Peleg's schematic position preserves `120`, `40`, and `70/69` comparison grammar. |
| `2760 × 25/23 = 3000`; `3000 − 2760 = 240`; `1406 + 3000 = 4406 BC`; `4406 − 4166 = 240`; `4166 − 3926 = 240` | Peleg divided by Priestly Expansion | Checks | Exact `25/23` comparison checks. The `240` halves remain comparison display, not new date-generation operator. |
| `2760 × 70/69 = 2800`; `1406 + 2800 = 4206 BC`; `4206 − 4166 = 40` | Peleg `40`-year hinge | Checks | Exact `70/69` comparison checks. |
| `16166 − 4206 = 11960 = 299 × 40`; `11960 × 300/299 = 12000 = 300 × 40`; `16166 − 12000 = 4166 BC` | Peleg / `300/299` hinge | Checks | Exact `300/299` comparison checks. This remains Key-of-23 / Enochian comparison state, not Residue Protocol. |
| `16166 − 4206 = 11960`; `16136 − 4176 = 11960`; `4206 − 4176 = 30`; `16166 − 16136 = 30` | Peleg and `30` apparent-age groove | Checks | The displayed `30` offsets check. |
| `16166 − 5586 = 10580`; `16136 − 5556 = 10580`; `10580 = 23 × 460`; `10580 × 25/23 = 11500`; `11500 − 10580 = 920 = 460 + 460` | LXX / Cainan comparison | Checks | Exact `25/23` comparison and doubled `460` display check. |
| `2166 − 1446 = 720 = 6 × 120` | Abrahamic-period cap note | Checks | Same-side BC span checks under the local comparison state. |
| `16166 − 1446 = 14720`; `14720 = 23 × 640`; `14720 × 25/23 = 16000 = 40 × 400` | with-Cainan Adam to Exodus | Checks | Exact `25/23` comparison checks. |
| `15206 − 1406 = 13800 = 10 × 1380`; `13800 × 70/69 = 14000`; `1406 + 14000 = 15406 BC` | Adam death / Seth boundary to Conquest | Checks | Exact `70/69` comparison checks. |
| `14006 BC → 6 BC = 14000`; `15406 BC → 1406 BC = 14000` | same-side BC comparison display | Checks | Both are same-side BC spans; no cross-axis operator is involved. |
| `15686 − 6 = 15680 = 32 × 490`; `15656 BC → AD 25 = 15680 = 32 × 490`; `14726 − 6 = 14720 = 23 × 640` | no-Cainan Adam to Christ and Temple | Checks | The `15656 BC → AD 25` row checks by civil cross-axis `15656 + 25 − 1 = 15680`; the other two rows check by same-side BC subtraction. |
| `15686 − 15656 = 30`; `6 BC → AD 25 = 30` | Adam / Christ `30`-year groove | Checks | The second row checks by civil cross-axis `6 + 25 − 1 = 30`. |
| `16166 − 1446 = 14720`; `15686 − 966 = 14720`; `16166 − 15686 = 480`; `1446 − 966 = 480`; `966 − 6 = 960`; `480 + 960 = 1440` | Temple comparison | Checks | Same-side BC displays check. |
| `15686 − 1406 = 14280`; `15686 − 960 = 14726`; `1406 + 120 = 1526`; `14726 − 1526 = 13200 = 8 × 1650` | interior `1650` grammar | Checks | The internal stripped-span calculation checks. |
| `15686 − 14036 = 1650`; `15656 − 14006 = 1650`; `15686 − 4136 = 11550 = 7 × 1650`; `15656 − 4106 = 11550 = 7 × 1650`; `4106 − 2456 = 1650` | Rounded Scaffold compatibility display | Checks | Same-side BC spans check. Rounded comparison remains bounded to the `File_51a` comparison layer. |
| `15686 BC → AD 16165 = 15686 + 16165 − 1 = 31850`; `31850 = 650 × 49 = 65 × 490` | bounded Mirror comparison / civil cross-axis | Checks | Civil cross-axis arithmetic checks. This does not open formal Mirror target-generation as a local proof protocol. |
| `15686 − 6 = 15680 = 32 × 490`; `6 BC → AD 16165 = 6 + 16165 − 1 = 16170 = 33 × 490 = 330 × 49` | Christ division inside bounded Mirror comparison | Checks | Same-side and civil cross-axis displays check. |
| `15686 BC → AD 29395 = 15686 + 29395 − 1 = 45080`; `45080 = 23 × 1960 = 92 × 490`; `45080 × 25/23 = 49000 = 100 × 490` | bounded Pillar comparison / Projective Boundary coordinate | Checks | Civil cross-axis and exact `25/23` expansion check. `AD 29395` remains dependency-controlled and projective. |
| `AD 29395 − 49000 years = 19606 BC`; `19606 − 6 = 19600 = 400 × 49 = 40 × 490` | bounded Pillar backward expansion | Checks | Cross-axis backward-count display checks because `49000 − 29395 + 1 = 19606`. |
| `16166 − 1406 = 14760`; `14760 = 123 × 120 = 41 × 360` | with-Cainan Aaron-side endpoint check | Checks | Same-side BC and factor displays check. |
| `16166 − 6566 = 9600 = 80 × 120 = 800 × 12`; `6566 − 1406 = 5160 = 43 × 120 = 430 × 12`; `9600 + 5160 = 14760 = 123 × 120 = 1230 × 12` | Aaron/Moses terminal age-pair decomposition | Checks | Same-side BC, factor, and sum displays check. |
| `15689 − 1406 = 14283`; `14283 = 3 × 69²`; `14283 × 70/69 = 14490`; `14490 × 70/69 = 14700`; `14700 = 300 × 49 = 30 × 490` | secondary Aaron ordinary `+3` overlay | Checks | Exact double `70/69` comparison checks. Secondary overlay remains non-primary. |
| `600 + 438 + 433 + 464 = 1935`; `1935 ÷ 4 = 483.75` | Shem-to-Eber actual average | Checks | No-Cainan actual lifespan average remains an approximate witness near `480`. |
| `600 + 440 + 435 + 465 = 1940`; `1940 ÷ 4 = 485` | `File_51a` rounded-lifespan comparison | Checks | No-Cainan rounded comparison remains bounded and does not replace actual lifespans. |
| `600 + 438 + 460 + 433 + 464 = 2395`; `2395 ÷ 5 = 479` | MT + Cainan Shem-to-Eber `4+1` average | Checks with audit note | The listed actual values average exactly `479`. The source phrase `2394/5=479` is audit-controlled because the listed values sum to `2395`, not `2394`. |
| `600 + 440 + 460 + 435 + 465 = 2400`; `2400 ÷ 5 = 480` | rounded MT + Cainan Shem-to-Eber `4+1` average | Checks | Rounded comparison places the group exactly in the `480` class while preserving actual lifespans and the File_51a bounded rounded-comparison state. |
| Peleg actual `239`; Reu actual `239`; rounded `240` | Peleg / Reu transition / rounded comparison state | Checks as approximate display | Peleg and Reu each repeat the `239/240` class, marking the transition from the Shem-to-Eber `480` class to the Peleg / Reu `240` class. |
| `7 ÷ 2 = 3.5`; `3.5 + 3.5 = 7` | Creation-to-Aaron/Moses half-week comparison | Checks | Completion-pair comparison only; not formal Mirror arithmetic. |

Post-pressure arithmetic additions:

| Calculation / display | Active state / operator | Patch result | Handling in `File_23` |
|---|---|---|---|
| `15686 − 5606 = 10080`; `10080 = 28 × 360 = 30 × 336` | Shem death / Arphaxad birth marker; Priestly 336 / prophetic 360 comparison | Checks | No-Cainan Shem death / Arphaxad birth is accepted as a schematic node. The `10080` span represents Adam's `+30` apparent-age component under the Priestly `336` register while also equaling `28 × 360`. |
| `16166 − 6086 = 10080`; `10080 = 28 × 360 = 30 × 336` | with-Cainan Shem death / Arphaxad birth marker | Checks | With Cainan, `6086 BC` is also Shem death / Arphaxad birth. It remains a schematic display coordinate, not actual or Rounded Scaffold chronology. |
| `14726 − 3926 = 10800`; `10800 = 30 × 360` | Peleg death / Reu boundary marker | Checks | Peleg death stands `30 × 360` from Adam death / Seth boundary in the no-Cainan schematic. |
| `14006 − 3926 = 10080`; `10080 = 30 × 336` | Peleg death to normal cumulative MT Creation / Adam birth comparison | Checks | Peleg death also stands `30 × 336` from `14006 BC`, the normal cumulative MT Creation / Adam birth date. This remains a schematic comparison, not a date correction. |
| `3926 − 1406 = 2520`; `2520 = 7 × 360` | Peleg death to Moses death / Conquest | Checks | Peleg death stands one prophetic week of years from Moses death / Conquest. |
| `3926 − 6 = 3920`; `3920 = 8 × 490` | Peleg death to Christ anchor comparison | Checks | Same-side BC comparison to the `6 BC` Christ anchor; no civil cross-axis operator is involved. |
| `15686 − 3926 = 11760`; `11760 = 24 × 490 = 240 × 49` | no-Cainan Adam apparent head to Peleg death | Checks | Peleg death divides the no-Cainan Adam apparent-age schematic by a Jubilee-scale span. |
| `5606 − 3926 = 1680`; `1680 = 5 × 336` | Shem death to Peleg death Priestly comparison | Checks | The no-Cainan Shem death / Arphaxad birth node stands `5 × 336` before Peleg death. |
| `6086 − 3926 = 2160`; `2160 = 6 × 360` | Shem birth / with-Cainan Shem death to Peleg death | Checks | `6086 BC` can mark no-Cainan Shem birth or with-Cainan Shem death / Arphaxad birth under its active state; the span to Peleg death is `6 × 360`. |
| `2160 + 2520 = 4680`; `4680 = 13 × 360 = 390 × 12` | Shem-to-Peleg-to-Moses decomposition | Checks | The Shem-to-Peleg and Peleg-to-Moses death / Conquest sequence breaks the `13 × 360` span into `(6 + 7) × 360`. The `6 + 7` application is typological / BJ-dependent, not proof-bearing arithmetic by itself. |
| `6566 − 4166 = 2400`; `4166 − 3926 = 240`; `3926 − 1526 = 2400` | Peleg-centered chiasm | Checks | With-Cainan Shem birth to Peleg birth, Peleg birth to Peleg death, and Peleg death to Moses birth form `2400 + 240 + 2400`, centering Peleg's full schematic lifespan. |
| `6 BC → AD 35 = 6 + 35 − 1 = 40` | AD 35 rounded Christ-death comparison | Checks | AD 35 stands `40` years from the `6 BC` Christ anchor by civil cross-axis counting. It remains a rounded Christ-death comparison label, not an actual Passion-date replacement. |
| `16166 BC → AD 35 = 16200 = 45 × 360`; `14726 BC → AD 35 = 14760 = 41 × 360`; `6086 BC → AD 35 = 6120 = 17 × 360`; `3926 BC → AD 35 = 3960 = 11 × 360`; `1406 BC → AD 35 = 1440 = 4 × 360` | AD 35 mod-360 selected-subset display | Checks | Five distinct schematic mod-120 date labels land on AD 35 by exact `360`-year multiples. The expected baseline under a simple one-in-three residue heuristic is about `11 ÷ 3 = 3.666`, so the observation is slightly elevated but diagnostic, not formal statistical proof. |
| `14006 BC → AD 35 = 14040 = 39 × 360` | cumulative MT comparison with AD 35 | Checks | The actual cumulative MT Creation / Adam birth date also lands on AD 35 by an exact `360` multiple. This is a separate cumulative comparison, not an added schematic node. |
| `16166 − 1446 = 14720 = 32 × 460`; `15206 − 1406 = 13800 = 30 × 460` | 2nd Cainan 460-cycle comparison | Checks | Same-side BC spans show the with-Cainan Adam apparent/textual slot aligned with Exodus and Moses death / Conquest by `460`-cycle multiples. |
| `15206 BC → AD 4116 = 15206 + 4116 − 2 = 19320 = 42 × 460`; `15206 BC → AD 5496 = 15206 + 5496 − 2 = 20700 = 45 × 460` | mod-10 Mirror display `BC + AD − 2` | Checks under mod-10 display | These are not ordinary civil cross-axis spans. The `BC + AD − 2` display is opened only in §5.9.3b. |
| `19320 × 25/23 = 21000`; `20700 × 70/69 = 21000` | MT / LXX `21000` conversion state | Checks | MT and LXX comparison rows converge on `21000` through different exact-ratio operators. |
| `16166 − 30 = 16136`; `16136 − 15206 = 930`; `4116 − 3186 = 930`; `5496 − 4566 = 930`; `16136 + 3186 − 2 = 19320`; `16136 + 4566 − 2 = 20700` | Adam textual `930` mirror-pair comparison | Checks under same-side subtraction and mod-10 display | The schematic Adam actual-lived `930` slot aligns with MT and LXX Adam `930` mirror pairs. The apparent-age head remains `16166 BC`; the actual-lived schematic slot begins at `16136 BC`. |
| `16136 − 36 = 16100 = 7 × 2300 = 35 × 460 = 230 × 70`; `AD 16136 → AD 36 = 16100`; `16136 BC → AD 36 = 16136 + 36 − 2 = 16170 = 33 × 490 = 33 × 7 × 70`; `36 BC → AD 36 = 36 + 36 − 2 = 70` | `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison | Checks under same-side subtraction and bounded mod-10 display | The same-side `16100` span and the mod-10 `16170` span are retained as Christologically significant comparison displays. Ordinary civil cross-axis arithmetic is not active for these rows. |
| `16166 BC → AD 4836 = 16166 + 4836 − 2 = 21000` | cumulative Flood / mod-10 Mirror comparison | Checks under mod-10 display | Cumulative Flood / Shem-death label reaches `21000` from the with-Cainan Adam apparent head. |
| `16166 BC → AD 5436 = 16166 + 5436 − 2 = 21600`; `21600 = 60 × 360`; `16166 − 2160 = 14006 BC` | cumulative Shem birth / mod-10 Mirror comparison | Checks with audit note | The source phrase `60 × 3600` is audit-controlled; executable arithmetic is `60 × 360 = 21600`. |
| `130 + 105 + 90 + 70 + 65 = 460`; `230 + 205 + 190 + 170 + 165 = 960`; `5496 − 960 = 4536`; `16166 + 4536 − 2 = 20700` | MT / LXX Jared comparison | Checks under stated sums and mod-10 display | MT Jared offset is `460`; LXX Jared offset is `960`; the LXX Jared mirror label reaches `20700`. Source-control remains dependency-controlled. |
| `20700 − 1656 = 19044`; `19044 × 25/23 = 20700` | LXX Noah `1656` expansion | Checks | Author-supplied LXX Noah birth offset is retained as Gear 2 / Gear 3 display pending source-control if made proof-bearing. |
| `15686 − 966 = 14720 = 32 × 460`; `14726 − 6 = 14720 = 32 × 460`; `15686 − 14720 = 966 BC`; `14726 − 14720 = 6 BC`; `966 − 6 = 960`; `14720 × 25/23 = 16000` | no-Cainan Temple-switch comparison | Checks | The no-Cainan Adam slot shifts the same `32 × 460` span to the Temple anchor and Christ anchor. Temple typology remains dependency-controlled. |
| `14726 − 4836 = 9890`; `9890 = 23 × 430` | cumulative Flood `9890` comparison | Checks | The no-Cainan Adam textual boundary / Seth start reaches the cumulative Flood label by a `23 × 430` span. This does not revise File_22 controls. |
| `4166 − 1406 = 2760`; `2760 = 23 × 120 = 12 × 230 = 6 × 460`; `4116 + 1406 − 2 = 5520`; `5520 = 24 × 230 = 12 × 460`; `5520 × 25/23 = 6000` | Peleg / Adam / Moses `230/460` integration | Checks under same-side subtraction, mod-10 display, and exact ratio | Peleg's schematic birth / division label, MT Adam, and Moses form a `2760` / `5520` comparison field. The checked `25/23` expansion of `5520` is `6000`. |
| `6086 − 1486 = 4600`; `4600 = 10 × 460` | Shem-to-Moses age-40 `460` companion | Checks by same-side BC subtraction | `1486 BC` is treated as Moses age-40 failure / wedding comparison label by dependency. This row is a full `460` multiple and therefore belongs to the high-weight `460` stream, not to the downweighted 230-only diagnostic set. |
| `4166 BC → AD 4116 = 4166 + 4116 − 2 = 8280`; `8280 = 18 × 460` | Peleg-to-MT-Creation-Mirror companion under mod-10 display | Checks under mod-10 display | `AD 4116` is a mod-10 Mirror display label of the MT Adam / Creation coordinate, not an ordinary historical AD anchor and not a Protocol 1 target. |
| `5496 + 4166 − 2 = 9660`; `9660 = 42 × 230 = 21 × 460`; `9660 × 70/69 = 9800 = 20 × 490`; `5496 + 1406 − 2 = 6900`; `6900 = 30 × 230 = 15 × 460`; `6900 × 70/69 = 7000` | LXX Adam / Peleg / Moses mod-10 Mirror companion | Checks under mod-10 display and exact ratio | LXX Adam, Peleg, and Moses integrate by full-cycle `460` multiples, with the `230` factors visible only as subordinate half-cycle structure. LXX remains comparison-state material. |
| `6086 + 6566 − 2 = 12650 = 55 × 230`; `5606 + 1526 − 2 = 7130 = 31 × 230`; `15656 − 3926 = 11730 = 51 × 230` | Shem / Adam / Peleg `230` half-cycle companion rows | Checks with audit note | The active File_23 label is `6086 BC`, not a new `6085 BC` node. These rows are `230`-only diagnostics and are downweighted because they do not attach to a known full-`460` path. |

arithmetic-control review result:
No arithmetic blocker was identified in the visible displays checked in arithmetic-control review. The pass verifies arithmetic only under the stated local displays and dependencies. Source provenance for `tebah`, En-tarah-ana, schematic table endpoints, LXX / Cainan comparison labels, Rounded Scaffold comparison labels, Protocol 1 target labels, and Projective Pillar coordinates remains dependency-controlled or source-control pending as already classified.

### 0.6 Argument-control ledger

argument-control layer argument-control layer scope:
This pass clarifies evidential role and proof burden. It does not alter the checked arithmetic, add new anchors, open `File_24`, change dependency assignments, or perform pressure testing.

| Claim / phrase | Claim-status | argument-control layer handling |
|---|---|---|
| `tebah` lexical bridge | source-retained textual / typological claim | Preserved as the narrative warrant for overlaying Noah and Moses. It does not carry arithmetic proof by itself and remains citation-control pending. |
| SKL `3 months and 3.5 days` | dependency-controlled textual datum | Preserved as SKL appended fractional-datum state by `File_21` / `File_34`; whole-year SKL matrix remains unopened. |
| `3.5 × 360 = 1260` | arithmetic fact under prophetic `360` register | Preserved as the local scalar output; distinct from SKL whole-year scaling and Enochian `1274`. |
| Moses hidden `3 months` | biblical textual datum / typological bridge | Preserved as the Mosaic counterpart to the SKL `3 months` component. |
| Ordinary Aaron/Moses `3` branch | arithmetic-compatible dependency-controlled display | Preserved as the ordinary Nisan/Nisan branch; not corrected into `3.5`. |
| Aaron/Moses `L = 3.5` | dependency-controlled phase-resolved state | Preserved as the Tishri/Nisan priestly branch by `File_22` dependency. |
| “Integer floor” | explanatory structural display | Retained as `floor(3.5) = 3`; not promoted to a chronology operator. |
| `12600 ÷ 3600 = 3.5` | arithmetic fact / author-resolved argument clarification | Retained as the active `1/3600` scale expression replacing prior ambiguous source-history wording. |
| Adam `930`; Moses `120` | textual / lifespan datum by dependency | Used as first/last patriarchal boundary values; not a substitute for cumulative dates or the Mosaic `40` generational unit. |
| `930 + 120 = 1050 = 12600 ÷ 12` | arithmetic fact / structural inference | Displays the Adam/Moses boundary pair as `1/12` of the full cumulative trunk. |
| `30 + 930 = 960 = 8 × 120` | arithmetic fact with apparent-age dependency | Used as a symmetrical-design display; Adam's textual lifespan remains `930`. |
| About `960/480/240/120` | approximate structural display | Preserved as approximate lifespan-halving scaffold; actual lifespan witnesses are not regularized to fit the ideal stages. |
| Schematic halving table | structural display / comparison overlay | Preserved as a schematic overlay, not actual MT chronology and not an exact table law. |
| Noah age `480`; Genesis 6:3 `120` | structural / theological interpretation | Preserved as Flood-warning / halving-forecast state. It remains source-control pending if later made proof-bearing. |
| Shem halving juncture | structural inference with Cainan / judgment comparison | Preserved as the first visible `960 → 480` halving node. Cainan and Fall-of-Jerusalem rows are comparison-state support. |
| Peleg division hinge | structural inference with typological / lexical note | Preserved as the `480 → 240` division node. The Peleg name clue is not proof-bearing without citation-control. |
| Shem / Peleg full-lifespan node clarification | author-supplied structural inference | Integrated as a post-pressure patch. Adam, Shem, Peleg, and Moses are full-lifespan node markers; Shem and Peleg death-boundaries may function as markers, not only their birth / start positions. |
| Shem-to-Eber + Cainan 480-class clarification | author-supplied structural inference / arithmetic fact | Integrated as a post-focused-pressure patch. The MT + Cainan `4+1` group from Shem through Eber averages `479` actual and `480` rounded, confirming the Shem-to-Eber group as the `480` class under the local halving scaffold. |
| Peleg / Reu `239/240` transition clarification | author-supplied structural inference / typological note | Integrated as a post-focused-pressure patch. Peleg and Reu repeat the `239` actual / `240` rounded lifespan, supporting the `480 → 240` division. Peleg's name and the phrase “in the days of Peleg” remain typological / lexical support requiring citation-control if made proof-bearing. |
| MT-primary halving-tradition clarification | tradition-state guard | Integrated as a post-focused-pressure patch. MT without Cainan and MT + Cainan supply the primary local halving pattern; LXX and SP remain subordinate comparison witnesses in `File_23`. |
| Shem death / Arphaxad birth `5606/6086 BC` | schematic display datum / structural inference | Preserved as accepted node pair under the schematic overlay. It remains firewalled from actual chronology and Rounded Scaffold chronology. |
| Peleg death / Reu boundary `3926 BC` | schematic display datum / structural inference with typological note | Preserved as an intended division marker. The Genesis wording “in the days of Peleg” is retained as author-supplied typological support; citation-control remains needed if made proof-bearing. |
| `6 × 360 + 7 × 360 = 13 × 360` | typological comparison / BJ-dependent note | Preserved as an author-supplied Fall-of-man comparison. It does not open BJ chronology as a local proof layer and does not create a new operator. |
| Peleg-centered `2400 + 240 + 2400` chiasm | structural inference / schematic comparison | Preserved as an author-supplied chiasm centered on Peleg's full schematic lifespan: Shem birth with Cainan → Peleg birth → Peleg death → Moses birth. It is not actual chronology and not formal Mirror protocol. |
| AD 35 rounded Christ-death mod-120 culmination | bounded Christological / rounded comparison | Preserved as an author-supplied cycle terminal. AD 35 functions as a rounded Christ-death comparison label by dependency, not as an actual Passion-date replacement. |
| `5` of `11` distinct mod-120 labels landing on AD 35 by `360`-year multiples | diagnostic observation | Preserved as structurally notable and slightly elevated over a simple `11 ÷ 3` expectation. It is not a formal statistical result unless a defined scan model is later supplied. |
| 460-cycle / LXX-MT Mirror slot field | high-density structural inference / providential synchronization | Retained as author-supplied post-pressure comparison material. The source term “hyperactive” is normalized to high-density comparison-state language and is not treated as formal statistical proof. |
| Mod-10 Mirror display `BC + AD − 2` | bounded display operator / audit-controlled Mirror comparison | Retained only in §5.9.3b. It does not replace ordinary civil cross-axis `BC + AD − 1`, does not open Protocol 1 / Protocol 2, and does not revise previous civil rows. |
| MT / LXX `21000` conversions | arithmetic fact plus structural inference | Retained as cross-tradition comparison: MT-side `19320 × 25/23 = 21000`; LXX-side `20700 × 70/69 = 21000`; cumulative side `16166 BC → AD 4836 = 21000` by mod-10 display. |
| LXX Jared `960` / Noah `1656` rows | dependency-controlled comparison | Retained with LXX source-control. Jared / Watcher and Noah offset claims require dependency control if made proof-bearing. |
| No-Cainan Temple-switch comparison | typological / structural comparison | Retained as bounded Temple / Christ comparison: `15686 → 966` and `14726 → 6` by `32 × 460`. Jesus / Temple-body typology remains theological support, not arithmetic proof. |
| Cumulative Flood `9890 = 23 × 430` companion | dependency-controlled structural comparison | Retained as a File_22-adjacent comparison without revising File_22's Decimal Bridge or cumulative Flood controls. |
| Key-of-23 rows | bounded comparison / arithmetic fact where checked | Retained as comparison-state material using `25/23`, `70/69`, and `300/299`. They are not Residue Protocol. |
| Christ / Temple rows in §5.9.3 | bounded civil cross-axis and major-anchor comparison | Retained as corroboration only. Civil cross-axis arithmetic was checked in arithmetic-control review but does not become the local proof protocol. |
| Mirror / Pillar rows in §5.9.5 | bounded Mirror / Pillar comparison; Projective Boundary dependency | Retained as comparison-only material. Formal Mirror target-generation is not locally opened. |
| Secondary Aaron-side check | secondary structural comparison | Retained because Aaron/Moses `3` / `3.5` is the file's opening problem; it remains secondary to the Moses-centered halving table. |
| Actual / rounded lifespan witnesses | actual / rounded comparison state by `File_51a` | Preserved as bounded support for the approximate halving theme; Rounded Scaffold does not overwrite actual MT or cumulative states. |
| Progressive lifespan-decline synthesis | theological note / providential synchronization | Preserved as theological interpretation, not as source-critical proof of human scribal intent. |
| Creation `7` → Aaron/Moses `3.5` | structural inference / theological design note | Links full-week Creation to half-week Aaron/Moses terminal spread without opening formal Mirror protocol. |
| `Micro-Flood` | typological reading / theological note | Preserved as source vocabulary and theological synthesis. It is not formal Mirror protocol and does not carry arithmetic proof by itself. |
| “designed to mirror” | theological note / providential synchronization | Preserved with claim-status restraint. It does not claim demonstrable scribal intent unless a source-critical dependency later supplies it. |
| `File_24` / Kish cipher / `16560` | forward dependency | Named only as downstream companion material; not executed in `File_23`. |

argument-control layer result:
The argument layer is classified as a controlled synthesis of textual data, arithmetic facts, structural inferences, typological readings, theological notes, providential synchronization, bounded anchor-corroboration, and dependency-controlled comparison states. No arithmetic, anchors, modal states, node-classes, sign conventions, operators, Mirror protocols, theological claims, or dependency assignments were intentionally changed.

## 1. The Tebah Narrative Lock

Active state: Tebah / ark typological bridge state.

Textual / typological datum:
The source identifies the Hebrew word `tebah` as appearing in only two narrative contexts: Noah's Ark in Genesis 6 and Moses' basket in Exodus 2.

The source describes this lexical lock as authorizing the overlay of Flood chronology onto the Exodus narrative. In argument-control layer claim-status terms, “authorizing” is classified as typological warrant / structural inference: it permits the Flood-to-Moses comparison inside this file, but it does not by itself prove the arithmetic pattern.

Modal-state note:
The lexical claim is preserved as source-retained. Citation-control remains pending. The `tebah` bridge does not become a chronological operator and does not replace SKL source-control.

Typological note:
Noah's Ark preserves a remnant through the waters of judgment. Moses' basket preserves the deliverer who will lead Israel through the waters of judgment and deliverance. This typological bridge explains why Flood chronology may be compared with the Mosaic birth narrative in this file, while the arithmetic burden remains with the stated SKL and prophetic values.

## 2. The SKL Fractional Datum

Active state: SKL appended fractional-datum state.

Textual datum:
The source identifies a specific fractional anomaly in the reign of En-tarah-ana: `3 months and 3.5 days`.

Dependency note:
In this file, the phrase `3 months and 3.5 days` is treated as the SKL appended fractional-datum state. Full source-control for En-tarah-ana, the SKL textual form, and SKL / Berossus derivation remains with `File_21`, `File_34`, and related SKL controls.

The local function of the datum is not to rederive the whole SKL system. Its function is to isolate the fractional payload that later reappears in the Mosaic field.

| Component | Active node-class | Local handling |
|---|---|---|
| `3 months` | SKL appended fractional month component | Compared typologically with Moses' `3`-month hiding period. |
| `3.5 days` | SKL appended fractional day component | Scaled locally through the Day-Year Scalar into the prophetic half-week display. |
| En-tarah-ana | SKL fractional-tail carrier | Reign metadata remains `File_21` / `File_34` controlled. |
| Whole-year SKL totals | SKL table / deep-time state | Not locally active in `File_23`. |

Modal-state note:
The SKL `3.5` days are not the same node-class as the File_12 liturgical `3.5`-day phase-offset. The two states are related by shared half-week vocabulary but remain dependency-distinct unless a later file explicitly opens the connection.

## 3. The Day-Year Scalar

Active state: Day-Year Scalar state; prophetic half-week state.

Active operator: Day-Year Scalar `1:360`.

| SKL input | Operator | Prophetic output |
|---|---|---|
| `3.5` days | `3.5 × 360 = 1260` | `1260` days = `3.5` prophetic years |

Arithmetic fact, arithmetic-control review verified:

```markdown
3.5 × 360 = 1260
1260 ÷ 360 = 3.5
```

Terminology note:
The source value is normalized to `1260` under repository no-comma number formatting. The arithmetic value is not changed.

Unit-state note:
The output `1260` is stated in the 360-day prophetic-year register. Locally this means `1260` prophetic days, or `3.5` prophetic years. This section does not open the Enochian `1274` register, the File_21 four-state SKL date-generation matrix, or Residue Protocol arithmetic.

## 4. The Mosaic Output

Active state: Mosaic hiding state; Aaron/Moses ordinary branch; Aaron/Moses `L = 3.5` phase-resolved branch.

The source identifies three Mosaic outputs from the Tebah / SKL fractional bridge.

| Mosaic output | Source form | Local handling |
|---|---|---|
| The hiding | Moses is hidden for `3 months` in Exodus 2:2 | Preserved as textual datum and Mosaic counterpart to the SKL `3 months` component. |
| The gap | Aaron and Moses have a nominal `3`-year age difference | Preserved as the ordinary Aaron/Moses branch. |
| The phase-resolved straddle | The `3`-year gap is read as the integer floor of the `3.5`-year scalar | Preserved as source-retained integer-floor display and as the `L = 3.5` phase-resolved branch by `File_22` dependency. |

Modal-state note:
The ordinary `3`-year branch and the phase-resolved `L = 3.5` branch are simultaneous states. The `3`-year birth-label branch is not a failed `3.5`, and the `3.5` phase-resolved branch is not a correction of the ordinary `3`.

Node-class note:
The ordinary branch is a Nisan/Nisan birth-label branch. The phase-resolved branch is a Tishri/Nisan priestly straddle. Do not recompute `L = 3.5` from printed integer labels alone.

arithmetic-control review note:
The ordinary branch checks by same-side BC subtraction:

```markdown
1529 − 1526 = 3
```

The integer-floor display checks mathematically:

```markdown
floor(3.5) = 3
```

The phase-resolved branch checks only when the Tishri/Nisan dependency state is active:

```markdown
1530t BC → 1526n BC = 3.5
```

Argument note:
The source phrase “integer floor” is arithmetic-compatible as display language because `floor(3.5) = 3`. The argument-control layer preserves it as a structural explanation for the ordinary `3`-year branch while keeping the phase-resolved `L = 3.5` branch distinct. It is not promoted to an independent chronology operator.

## 5. The Cumulative Echo and Lifespan-Halving Design

Active state: cumulative MT trunk state; `1/3600` half-week scale state; patriarchal boundary-lifespan state; Adam apparent-age `960` state; schematic halving-table state; Shem halving-juncture state; Peleg division-hinge state; actual / rounded lifespan comparison state; bounded anchor-corroboration state.

`File_23` now carries two related design layers:

1. the earlier Tebah / SKL layer, in which the SKL `3.5`-day fractional datum scales into the prophetic half-week;
2. the enlarged lifespan-halving layer, in which the Adam-to-Moses lifespan descent is displayed by the ideal scaffold `960 → 480 → 240 → 120`.

The controlling claim is restrained. The halving table does not prove design by itself, and it is not an exact replacement for actual textual lifespans. Because the schematic values are all built from `120`, many clean multiples are expected by construction. The file therefore focuses on the major brackets and true halving nodes only: Adam / Moses as the outer brackets, Shem as the first halving transition, and Peleg as the division hinge. Secondary harmonics that do not land on a halving node are excluded from the main thread.

### 5.1 The `1/3600` half-week miniature

Active state: `1/3600` half-week scale state; prophetic half-week state; cumulative MT trunk state.

The source-history phrase `12.6 / 3.5 harmonic` is no longer the active argument wording. The active expression is the author-resolved `1/3600` relation:

```markdown
12600 years ÷ 3600 = 3.5 years
3.5 years × 360 days/year = 1260 days
```

Arithmetic fact:
`12600 ÷ 3600 = 3.5`, and `3.5 × 360 = 1260`.

Structural inference:
The prophetic half-week is a scaled miniature of the full 26-patriarch cumulative trunk. The full trunk is `12600`; the miniature is `3.5` prophetic years, or `1260` prophetic days under the 360-day register.

Node-class control:
The `12600` state remains cumulative MT by `File_22` dependency. The `1260` state remains prophetic half-week by `File_17` dependency. The `1/3600` relation shows scale symmetry; it does not collapse the two node-classes.

### 5.2 The `1/12` Adam/Moses boundary flank

Active state: patriarchal boundary-lifespan state; `1/12` flank-sum state.

Adam begins the 26-patriarch line with a textual lifespan of `930`. Moses ends the line with a textual lifespan of `120`. Between them stand `24` intervening generations in the author-supplied display.

Arithmetic fact:

```markdown
930 + 120 = 1050
12600 ÷ 12 = 1050
```

Structural inference:
The first and last lifespans of the 26-patriarch trunk sum to `1050`, exactly `1/12` of the full `12600` cumulative lifespan total. Adam and Moses therefore flank the complete trunk by a clean fractional boundary.

Dependency note:
The full `12600` total remains controlled by `File_22` and the cumulative lifespan table. `File_23` uses the boundary-flank display; it does not locally rederive the complete 26-patriarch sum.

### 5.3 Adam apparent age and the ideal halving scaffold

Active state: Adam apparent-age `960` state; approximate lifespan-halving scaffold state.

When Adam's `+30 Apparent Age` state is included, Adam's display becomes:

```markdown
30 + 930 = 960
960 = 8 × 120
```

The ideal halving scaffold is therefore:

| Stage | Ideal value | Local witness | Handling |
|---|---:|---|---|
| 1 | `960` | Adam with `+30 Apparent Age`: `30 + 930 = 960` | Exact under apparent-age display. |
| 2 | `480` | Noah age `480`; Shem full-lifespan marker; Shem death / Arphaxad birth `5606/6086 BC` | Noah exact as warning age; Shem marks the schematic crossover into the `480` stage, and the current patch clarifies that Shem death also marks an accepted node. |
| 3 | `240` | Peleg actual `239`; rounded Peleg `240`; Peleg birth `4166 BC`; Peleg death / Reu boundary `3926 BC` | Approximate actual; exact under rounded / schematic comparison. The current patch clarifies that Peleg death is also an intended marker. |
| 4 | `120` | Moses `120` | Exact terminal lifespan. |

Arithmetic fact:

```markdown
960 ÷ 2 = 480
480 ÷ 2 = 240
240 ÷ 2 = 120
```

Audit note:
The source-history expression “`930/2=480`” should not be treated as exact arithmetic. Exact textual-lifespan half is:

```markdown
930 ÷ 2 = 465
```

The exact `480` stage is generated only when the active Adam apparent-age state is opened:

```markdown
30 + 930 = 960
960 ÷ 2 = 480
```

Structural inference:
The display begins with Adam as `960` under apparent-age state and ends with Moses at `120`. The intervening stage values are not rigid table law; they are treated as approximate witnesses, especially at Shem and Peleg.

### 5.4 The schematic halving table

Active state: schematic halving-table state; actual / rounded / schematic mod-`120` firewall state; bounded File_51a comparison state; restored / native 2nd Cainan comparison state.

The following table is a schematic overlay. It is not the actual MT table, not an actual date table, and not a Rounded Scaffold table. It does not replace actual lifespans or actual dates. It displays the halving stages as cumulative-style mod-`120` blocks in which the death of one patriarch functions as the start boundary of the next row, analogous to cumulative MT stacking.

The table is shown in two forms:

| Column | Function |
|---|---|
| No 2nd Cainan | MT-style 26-patriarch display without the inserted Cainan row. |
| With 2nd Cainan | LXX / restored-Cainan comparison display; 2nd Cainan adds one `480` block at the post-Flood `480` stage. |

| # | Patriarch / stage | Schematic span / stage | No 2nd Cainan | With 2nd Cainan |
|---:|---|---:|---:|---:|
| 1a | Adam, apparent-age boundary | `960` | `15686 BC` | `16166 BC` |
| 1b | Adam, textual lifespan begins | `930` | `15656 BC` | `16136 BC` |
| 2 | Seth | `960` | `14726 BC` | `15206 BC` |
| 3 | Enosh | `960` | `13766 BC` | `14246 BC` |
| 4 | Kenan / 1st Cainan | `960` | `12806 BC` | `13286 BC` |
| 5 | Mahalalel | `960` | `11846 BC` | `12326 BC` |
| 6 | Jared | `960` | `10886 BC` | `11366 BC` |
| 7 | Enoch | `960` | `9926 BC` | `10406 BC` |
| 8 | Methuselah | `960` | `8966 BC` | `9446 BC` |
| 9 | Lamech | `960` | `8006 BC` | `8486 BC` |
| 10 | Noah | `960` | `7046 BC` | `7526 BC` |
| 11 | Shem | `480` | `6086 BC` | `6566 BC` |
| 12 | Arphaxad | `480` | `5606 BC` | `6086 BC` |
| 12b | 2nd Cainan, optional inserted slot | `480` | — | `5606 BC` |
| 13 | Shelah | `480` | `5126 BC` | `5126 BC` |
| 14 | Eber | `480` | `4646 BC` | `4646 BC` |
| 15 | Peleg | `240` | `4166 BC` | `4166 BC` |
| 16 | Reu | `240` | `3926 BC` | `3926 BC` |
| 17 | Serug | `240` | `3686 BC` | `3686 BC` |
| 18 | Nahor | `240` | `3446 BC` | `3446 BC` |
| 19 | Terah | `240` | `3206 BC` | `3206 BC` |
| 20 | Abraham | `240` | `2966 BC` | `2966 BC` |
| 21 | Isaac | `240` | `2726 BC` | `2726 BC` |
| 22 | Jacob | `240` | `2486 BC` | `2486 BC` |
| 23 | Levi | `240` | `2246 BC` | `2246 BC` |
| 24 | Kohath | `240` | `2006 BC` | `2006 BC` |
| 25 | Amram | `240` | `1766 BC` | `1766 BC` |
| 26a | Moses, birth | `120` total | `1526 BC` | `1526 BC` |
| 26b | Moses, age `40` | stage 1 | `1486 BC` | `1486 BC` |
| 26c | Moses, age `80` / Exodus | stage 2 | `1446 BC` | `1446 BC` |
| 26d | Moses, age `120` / death / Conquest | stage 3 | `1406 BC` | `1406 BC` |

Arithmetic checks:

| Check | Calculation | Result |
|---|---:|---:|
| No-Cainan total from Adam apparent boundary to Conquest | `15686 − 1406` | `14280 = 119 × 120` |
| With-Cainan total from Adam apparent boundary to Conquest | `16166 − 1406` | `14760 = 123 × 120 = 41 × 360` |
| 2nd Cainan insertion | `16166 − 15686` | `480 = 4 × 120` |
| Moses internal stages | `1526 → 1486 → 1446 → 1406` | `40 + 40 + 40 = 120` |

Boundary-marker note:
In cumulative-style schematic stacking, the death of one patriarch may function as the start boundary of the next row. The current post-pressure clarification therefore treats the following as intended schematic nodes: Adam death / Seth boundary `14726 BC`, Shem death / Arphaxad birth `5606 BC` in the no-Cainan column and `6086 BC` in the with-Cainan column, Peleg death / Reu boundary `3926 BC`, and Moses death / Conquest `1406 BC`. These are schematic display coordinates unless a local row explicitly opens actual or rounded chronology.

Methodological restraint:
The table generates many predictable `120`, `240`, `480`, and `360` relations. The main argument therefore uses only the Adam / Moses brackets and the full-lifespan halving nodes Shem and Peleg. The Arphaxad / Flood-correspondence point yields interesting secondary harmonics, but it is excluded from the main thread because Arphaxad is not the halving node in this schematic. Shem is the crossover from the pre-diluvian `960` stage into the post-diluvian `480` stage. These coordinates remain schematic mod-`120` spans and must not be identified with either actual dates or File_51a Rounded Scaffold dates.

State guard:
These are schematic mod-120-year display spans. They are not actual MT dates and not File_51a Rounded Scaffold dates unless a local row explicitly opens that state. The table may compare with actual or rounded witnesses later, but it does not convert either witness layer into the schematic overlay.

### 5.5 Noah age `480` and Genesis 6:3

Active state: Noah `480` warning / halving-forecast state; Genesis 6:3 dual-`120` state; Tebah / ark typological bridge state.

Noah is the last patriarch who lives close to the Adam apparent-age stage `960`; his textual lifespan is `950`. At age `480`, Noah receives the Genesis 6:3 warning that man's years will be `120`.

Arithmetic fact:

```markdown
600 − 120 = 480
```

Actual MT Minimum-Regular date display:

```markdown
Noah birth: 2843 BC
Noah age 480: 2843 − 480 = 2363 BC
Flood: 2243 BC
2363 − 2243 = 120
```

Theological / structural interpretation:
The `120` of Genesis 6:3 functions in two directions inside this file:

1. it marks `120` years remaining to the Flood;
2. it anticipates the final `120`-year lifespan represented by Moses.

On this reading, Noah's warning node functions in two ways: it marks man's days being shortened to `120`, and it forecasts the halving scaffold by standing at the `480` stage, halfway down from Adam-with-apparent-age `960` and two halvings above Moses `120`.

Typological note:
The Noah warning node belongs to the same Flood-to-Moses typological field as the `tebah` bridge. Noah's Ark and Moses' basket remain distinct events, but both preserve life through water and judgment. The `120` warning and the Mosaic `120` lifespan join the Flood and Exodus deliverer fields without creating a new chronological operator.

### 5.6 Adam, Shem, Peleg, and Moses as textual and schematic lifespan markers

Active state: Adam apparent-age `960` state; Shem death / Arphaxad birth marker state; Peleg death / Reu boundary marker state; Mosaic `120` terminal state; actual / rounded / schematic mod-`120` firewall state.

The outer brackets are not only schematic. Adam and Moses remain tied to actual textual data while also serving the symbolic halving overlay. The current post-pressure clarification adds that Shem and Peleg also function through their full lifespans, not only through their birth / start positions.

| Marker | Actual / textual value | Schematic start / birth marker | Schematic death / boundary marker | Handling |
|---|---:|---:|---:|---|
| Adam | `930`, with `+30 Apparent Age` | `15656 BC`; apparent head `15686 BC` | `14726 BC` / Seth boundary | Adam's textual lifespan remains `930`; the apparent-age display supplies the ideal `960`. |
| Shem | `600` | `6086 BC` no-Cainan; `6566 BC` with-Cainan | `5606 BC` no-Cainan; `6086 BC` with-Cainan / Arphaxad boundary | Shem's full lifespan is intended as the first post-Flood halving-node marker. The death boundary aligns with Arphaxad's birth / start marker in the schematic table. |
| Peleg | `239`; rounded `240` | `4166 BC` | `3926 BC` / Reu boundary | Peleg's full lifespan is intended as the division marker. The death boundary is not merely incidental. |
| Moses | `120` | `1526 BC` | `1406 BC` / death / Conquest | Moses' actual lifespan is the terminal halving value. |

Structural inference:
Adam, Shem, Peleg, and Moses are therefore double-state witnesses. They remain tied to textual lifespan values while also functioning as schematic halving-node markers. This does not turn the schematic overlay into actual chronology or Rounded Scaffold chronology. It only clarifies that the marker logic includes death-boundaries as well as birth / start-boundaries.

Typological note:
The Peleg row is compatible with the wording that the earth was divided “in the days of Peleg,” not necessarily only at Peleg's birth. In this file, that supports treating Peleg's full lifespan, including the `3926 BC` death / Reu boundary, as part of the division hinge. This remains typological / lexical support pending citation-control if made proof-bearing.

### 5.7 Shem as the first halving juncture

Active state: Shem halving-juncture state; Cainan / LXX comparison state; Ezekiel `390/430` comparison state; Fall-of-Jerusalem judgment comparison state.

Shem is the first primary halving node after the Adam / Noah `960` stage. In the no-Cainan form Shem stands at `6086 BC`; in the with-Cainan form Shem stands at `6566 BC`. The shift is one inserted `480` block.

The first halving block itself magnifies Adam's apparent-age value:

```markdown
15686 − 6086 = 9600 = 10 × 960
16166 − 6566 = 9600 = 10 × 960
```

#### 5.7.1 Shem and the Ezekiel `390/430` field

The two Shem forms reproduce the `390/430` field at 12x scale while remaining inside the Mosaic `120` lattice:

| Shem state | Span to Moses' death / Conquest | Ezekiel / Sojourn form | Mosaic form |
|---|---:|---:|---:|
| No Cainan | `6086 − 1406 = 4680` | `12 × 390` | `39 × 120` |
| With Cainan | `6566 − 1406 = 5160` | `12 × 430` | `43 × 120` |
| Difference | `480` | `12 × 40` | `4 × 120` |

Structural inference:
The Cainan insertion supplies the scaled `40` component between `390` and `430`. This is treated as structurally compatible with Shem, who bridges the old world and the new world: the no-Cainan Shem node corresponds to the `390` judgment-bearing register, while the with-Cainan Shem node completes the full `430` Sojourn / deliverance register.

#### 5.7.2 Shem and the prophetic-year pair

The Shem pair also forms a `13/14` prophetic-year relation around Moses:

```markdown
6086 − 1406 = 4680 = 13 × 360
6566 − 1526 = 5040 = 14 × 360 = 7 × 720
```

The difference is exactly one prophetic year:

```markdown
5040 − 4680 = 360
480 − 120 = 360
```

Structural inference:
The with-Cainan Shem node is paired with Moses' birth, while the no-Cainan Shem node is paired with Moses' death / Conquest. The Cainan insertion exceeds Moses' lifespan by one prophetic year, producing the `13 × 360` / `14 × 360` pair.

#### 5.7.3 Shem, Cainan, and the Fall of Jerusalem

In the with-Cainan form, Shem also stands in a Cainan-shaped relation to the Fall of Jerusalem:

```markdown
6566 − 586 = 5980
5980 = 13 × 460
5980 = 46 × 130
5980 × 300/299 = 6000 = 10 × 600
```

Structural inference:
This is structurally notable because 2nd Cainan in the LXX / Jubilees tradition-field begets Shelah at `130` and lives `460` years, while Shem's actual lifespan is `600`. Thus the with-Cainan Shem node allows Cainan's `130/460` and Shem's `600` to converge at the judgment endpoint of `586 BC`.

Tradition note:
The association of 2nd Cainan with the recovery or transmission of forbidden Watcher knowledge belongs to the established Cainan / Jubilees tradition stream elsewhere in the repository. In `File_23`, this motif is not used to prove the arithmetic. It explains why the with-Cainan Shem node is theologically coherent when the span terminates at Judah's fall under judgment.

#### 5.7.4 Shem death / Arphaxad birth as accepted node

Active state: Shem death / Arphaxad birth marker state; Priestly 336 / prophetic 360 comparison state; actual / rounded / schematic mod-`120` firewall state.

The current post-pressure clarification makes Shem's full lifespan active in the halving-node argument. In the schematic table, Shem's death is also the start boundary for Arphaxad. This yields two accepted display nodes:

```markdown
No Cainan: Shem death / Arphaxad birth = 5606 BC
With Cainan: Shem death / Arphaxad birth = 6086 BC
```

The no-Cainan and with-Cainan displays both preserve the same `10080` relation from the Adam apparent-age head:

```markdown
15686 − 5606 = 10080
16166 − 6086 = 10080
10080 = 28 × 360 = 30 × 336
```

Structural inference:
The `10080` display allows the Adam `+30 Apparent Age` component to be represented as `30 × 336` in the Priestly Ledger register, while simultaneously appearing as `28 × 360` in the prophetic-year register. This makes the Shem death / Arphaxad birth node a meaningful full-lifespan marker, not merely a secondary table coordinate.

State guard:
The nodes `5606 BC` and `6086 BC` remain schematic display coordinates. They are not actual MT dates and not File_51a Rounded Scaffold dates unless a local row explicitly opens that state.

### 5.8 Peleg as the division hinge

Active state: Peleg division-hinge state; Rounded Scaffold `+60 Terah` comparison state; Key-of-23 comparison state; LXX / Cainan comparison state.

Peleg is the schematic point where the halving table divides the upper and lower halves of the structure:

```markdown
960 → 480 | 240 → 120
           Peleg
```

Peleg stands at `4166 BC` in both the no-Cainan and with-Cainan forms. This is the Rounded Scaffold `+60 Terah` Creation / Adam state by `File_51a` comparison, and it is one controlled local reason to treat Peleg as the visible division hinge.

Claim-status note:
Peleg's local role is structural inference supported by arithmetic comparison and typological name-language. The Peleg name-clue remains typological / lexical and should not carry the proof burden without citation-control.

#### 5.8.1 Peleg to Moses / Conquest

Arithmetic fact:

```markdown
4166 − 1406 = 2760
2760 = 23 × 120
2760 = 69 × 40
2760 × 70/69 = 2800 = 70 × 40
```

Structural inference:
Peleg to Moses' death / Conquest is the 23rd cycle of `120`, and the same span expands by `70/69` from `69 × 40` to `70 × 40`. This ties Peleg to Moses' `40 + 40 + 40` life-structure without making Peleg a Mosaic figure.

#### 5.8.2 Peleg divided by Priestly Expansion

Priestly Expansion divides Peleg symmetrically:

```markdown
2760 × 25/23 = 3000
3000 − 2760 = 240
1406 + 3000 = 4406 BC
4406 − 4166 = 240
4166 − 3926 = 240
```

Therefore:

```markdown
4406 BC → 4166 BC → 3926 BC
         240       240
```

Structural inference:
Peleg, whose name is associated with division, is centered between the expanded endpoint `4406 BC` and his own schematic death / Reu boundary at `3926 BC`. This is a restrained typological use of the name-clue; the arithmetic carries the burden.

#### 5.8.3 Peleg's `40`-year hinge

Prophetic Expansion from the Moses side shifts the Peleg boundary by `40` years:

```markdown
4166 − 1406 = 2760 = 69 × 40
2760 × 70/69 = 2800 = 70 × 40
1406 + 2800 = 4206 BC
4206 − 4166 = 40
```

From the Adam-with-Cainan side, the same `40` returns through the Enochian `300/299` register:

```markdown
16166 − 4206 = 11960 = 299 × 40
11960 × 300/299 = 12000 = 300 × 40
16166 − 12000 = 4166 BC
```

Thus Peleg forms a reversible `40`-year hinge:

```markdown
4166 BC → 4206 BC → 4166 BC
```

#### 5.8.4 Peleg and the `30`-year apparent-age groove

The same Peleg-expanded `4206 BC` aligns with the `+30 Apparent Age` groove:

```markdown
16166 − 4206 = 11960
16136 − 4176 = 11960
4206 − 4176 = 30
16166 − 16136 = 30
```

Structural inference:
The schematic Adam apparent-age pair `16166/16136 BC` lands against the MT Year-6 +60 apparent/textual pair `4206/4176 BC` by the same `11960` span. The full `30` of Adam's apparent age is therefore reflected as a comparison display in the Peleg / Creation-side groove. This does not open formal Mirror protocol.

#### 5.8.5 LXX / Cainan comparison

The with-Cainan schematic best represents the LXX comparison because LXX includes 2nd Cainan natively. The LXX Year-6 +60 apparent/textual pair yields:

```markdown
16166 − 5586 = 10580
16136 − 5556 = 10580
10580 = 23 × 460
10580 × 25/23 = 11500
11500 − 10580 = 920 = 460 + 460
```

Structural inference:
The LXX / Cainan comparison shifts the analysis from the Mosaic `40/120` register into the Cainan `460` register while preserving Key-of-23 grammar. The expansion amount is `2 × 460`, which is compatible with a Cainan-shaped comparison without replacing the local Peleg argument.

#### 5.8.5a Peleg death and the Shem-to-Moses decomposition

Active state: Peleg death / Reu boundary marker state; Shem death / Arphaxad birth marker state; Priestly 336 / prophetic 360 comparison state; BJ / Fall-of-man `6 + 7` typological comparison state.

The current post-pressure clarification makes Peleg's full lifespan active in the halving-node argument. Peleg's death / Reu boundary at `3926 BC` is therefore an accepted division node, not merely a secondary coordinate. This fits the textual wording that the earth was divided “in the days of Peleg,” not necessarily only at Peleg's birth.

The node connects to Adam, Shem, Moses, and the Christ anchor by several state-labeled displays:

```markdown
14726 − 3926 = 10800 = 30 × 360
14006 − 3926 = 10080 = 30 × 336
3926 − 1406 = 2520 = 7 × 360
3926 − 6 = 3920 = 8 × 490
15686 − 3926 = 11760 = 24 × 490 = 240 × 49
5606 − 3926 = 1680 = 5 × 336
6086 − 3926 = 2160 = 6 × 360
2160 + 2520 = 4680 = 13 × 360 = 390 × 12
```

Structural inference:
Peleg death at `3926 BC` stands `30 × 360` from Adam death / Seth boundary at `14726 BC`, and `30 × 336` from the normal cumulative MT Creation / Adam birth date `14006 BC`. It also stands `7 × 360` from Moses death / Conquest and `8 × 490` from the `6 BC` Christ anchor. From the no-Cainan Adam apparent-age head, it stands `11760 = 24 × 490 = 240 × 49`.

Shem / Peleg / Moses decomposition:
The same node divides the Shem-to-Moses prophetic-year span into `(6 + 7) × 360`. The `6086 BC` node can mark no-Cainan Shem birth or with-Cainan Shem death / Arphaxad birth under the active schematic state. From that node to Peleg death is `2160 = 6 × 360`; from Peleg death to Moses death / Conquest is `2520 = 7 × 360`; together they total `4680 = 13 × 360 = 390 × 12`.

Typological note:
The author-supplied `6 + 7` comparison is retained as a Fall-of-man typological note connected with BJ / Jubilees and related tradition context. It explains theological fit but does not make BJ chronology proof-bearing in `File_23` without source-control.

State guard:
The Peleg death node `3926 BC` remains a schematic mod-`120` display coordinate unless a local row explicitly opens actual or rounded chronology.


#### 5.8.5b Peleg-centered `2400 + 240 + 2400` chiasm

Active state: Peleg-centered `2400 + 240 + 2400` chiasm state; Peleg division-hinge state; double-`6086 BC` Shem node-class state; same-side schematic BC span state.

The author further clarifies that Peleg's schematic lifespan at `4166–3926 BC` sits inside a larger chiasm that runs from with-Cainan Shem birth to Moses birth:

```markdown
Shem birth with Cainan: 6566 BC
Peleg birth: 4166 BC
Peleg death / Reu boundary: 3926 BC
Moses birth: 1526 BC
```

Arithmetic fact:

```markdown
6566 − 4166 = 2400
4166 − 3926 = 240
3926 − 1526 = 2400
```

Therefore:

```markdown
6566 BC → 4166 BC → 3926 BC → 1526 BC
          2400        240        2400
```

Structural inference:
Peleg's full schematic lifespan occupies the center of the chiasm. The outer wings are equal `2400`-year spans, while Peleg's own full schematic lifespan supplies the central `240`. This reinforces the earlier Peleg division logic without turning the schematic overlay into actual chronology or Rounded Scaffold chronology.

Prophetic-year companion:
The same division principle is visible in the `6 × 360 + 7 × 360` decomposition already displayed in §5.8.5a:

```markdown
6086 − 3926 = 2160 = 6 × 360
3926 − 1406 = 2520 = 7 × 360
2160 + 2520 = 4680 = 13 × 360
```

State guard:
The `6566 → 4166 → 3926 → 1526` chiasm uses schematic mod-120 coordinates. It is not a literal MT, LXX, or Rounded Scaffold date table. The `6086 BC` label remains double-state: no-Cainan Shem birth / start in one row and with-Cainan Shem death / Arphaxad birth in another row.

#### 5.8.6 Halving nodes as transition / judgment nodes

The primary halving nodes are not selected only because they produce clean arithmetic. They also mark transition and judgment fields in the biblical narrative.

| Halving node | Schematic role | Narrative / theological transition |
|---|---|---|
| Adam | first bracket; apparent-age `960` | Fall of man; cursed ground; beginning of mortal descent. |
| Shem | first halving juncture; `960 → 480` | Bridge from the old world to the new world after the Flood. |
| Peleg | division hinge; `480 → 240`; full-lifespan marker with death / Reu boundary at `3926 BC`; divides the upper pair `960 → 480` from the lower pair `240 → 120` | Division / scattering field associated with Babel and the division of the earth. |
| Moses | terminal bracket; actual `120` | Plagues on Egypt, Exodus deliverance, wilderness judgment, and the Conquest / Canaan horizon at his death. |

Structural inference:
The halving nodes are therefore also narrative transition-points. Adam opens the cursed human condition; Shem crosses the Flood boundary through both birth and death / Arphaxad-boundary functions; Peleg marks the division of the nations through his full lifespan and death / Reu boundary; Moses brings the Abrahamic / Exodus field to its terminal covenantal and judgment horizon. This is why the main argument focuses on Adam, Shem, Peleg, and Moses, while omitting secondary rows that may be arithmetically interesting but do not mark a halving transition.

Abraham restraint note:
Abraham is not absent because he is unimportant. He is absent from the formal halving-node list because Moses functions as the extension and cap of the Abrahamic period. In the actual chronology, Abraham's birth at `2166 BC` stands exactly six Mosaic `120`-units before the Exodus:

```markdown
2166 − 1446 = 720 = 6 × 120
```

This preserves Abrahamic significance without making Abraham a halving node.



### 5.9 Adam/Moses bracket confirmations at major anchors

Active state: bounded anchor-corroboration state; Key-of-23 comparison state; Christological anchor comparison state; AD 35 rounded Christ-death mod-120 culmination state; Temple anchor comparison state; bounded Mirror / Pillar comparison state.

This subsection gathers major bracket-level corroborations. These are not proof by themselves. They are retained because they land on major repository anchor fields rather than on incidental mod-120 positions.

#### 5.9.1 With-Cainan Adam to Exodus

```markdown
16166 − 1446 = 14720
14720 = 23 × 640
14720 × 25/23 = 16000 = 40 × 400
```

Structural inference:
The with-Cainan Adam apparent-age head reaches the Exodus by `23 × 640`, and Priestly Expansion converts the span into `16000 = 40 × 400`. This structurally joins the wilderness `40` register with the `400`-year affliction / Egypt register.

#### 5.9.2 Adam death / Seth boundary to Conquest

```markdown
15206 − 1406 = 13800 = 10 × 1380
13800 × 70/69 = 14000
1406 + 14000 = 15406 BC
```

Comparison:

```markdown
14006 BC → 6 BC = 14000
15406 BC → 1406 BC = 14000
```

Structural inference:
The with-Cainan Adam textual boundary / Seth start expands into a `14000`-year Conquest-side span, creating a 10x macro-form of the `1400` years from Conquest to the `6 BC` Christ anchor.

#### 5.9.3 No-Cainan Adam to Christ and Temple

The no-Cainan column shifts the Adam bracket toward Christ and Temple:

```markdown
15686 − 6 = 15680 = 32 × 490
15656 BC → AD 25 = 15680 = 32 × 490
14726 − 6 = 14720 = 23 × 640
```

The `30`-year groove also aligns Adam and Christ:

```markdown
15686 − 15656 = 30
6 BC → AD 25 = 30
```

Temple comparison:

```markdown
16166 − 1446 = 14720
15686 − 966 = 14720
16166 − 15686 = 480
1446 − 966 = 480
966 − 6 = 960
480 + 960 = 1440
```

Structural inference:
With Cainan, Adam's apparent head maps to the Exodus by `23 × 640`. Without Cainan, the same span maps Adam's apparent head to the Temple construction anchor at `966 BC`, because the removed Cainan block is exactly matched by the `480` years from Exodus to Temple. The `966 BC → 6 BC` span of `960` then brings the Adamic `960` stage to the Christ anchor.


#### 5.9.3a AD 35 rounded Christ-death mod-120 culmination

Active state: AD 35 rounded Christ-death mod-120 culmination state; AD 35 mod-360 selected-subset state; 12-node / 11-distinct-date mod-120 register state; actual / rounded / schematic mod-120 firewall state; bounded civil cross-axis comparison state.

The current author clarification adds AD 35 as a rounded Christ-death / contraction comparison terminal. It stands `40` years from the `6 BC` Christ anchor by the repository civil cross-axis rule:

```markdown
6 BC → AD 35 = 6 + 35 − 1 = 40
```

This does not replace actual Passion date-states. It functions as a rounded Christ-death / contraction comparison label, similar in kind to rounded comparison values controlled elsewhere by the Rounded Scaffold and Luke / BJ comparative files.

The schematic mod-120 register contains `12` node-class slots, but only `11` distinct date labels because `6086 BC` is double:

| Half | Node-class slot | Date label | Civil span to AD 35 |
|---|---|---:|---:|
| Upper | Adam apparent head, with Cainan | `16166 BC` | `16200 = 135 × 120 = 45 × 360` |
| Upper | Adam apparent head, no Cainan | `15686 BC` | `15720 = 131 × 120` |
| Upper | Seth / Adam death boundary, with Cainan | `15206 BC` | `15240 = 127 × 120` |
| Upper | Seth / Adam death boundary, no Cainan | `14726 BC` | `14760 = 123 × 120 = 41 × 360` |
| Upper | Shem birth / start, with Cainan | `6566 BC` | `6600 = 55 × 120` |
| Upper | Shem birth / start, no Cainan | `6086 BC` | `6120 = 51 × 120 = 17 × 360` |
| Lower | Shem death / Arphaxad birth, with Cainan | `6086 BC` | `6120 = 51 × 120 = 17 × 360` |
| Lower | Shem death / Arphaxad birth, no Cainan | `5606 BC` | `5640 = 47 × 120` |
| Lower | Peleg birth / division hinge | `4166 BC` | `4200 = 35 × 120` |
| Lower | Peleg death / Reu boundary | `3926 BC` | `3960 = 33 × 120 = 11 × 360` |
| Lower | Moses birth | `1526 BC` | `1560 = 13 × 120` |
| Lower | Moses death / Conquest | `1406 BC` | `1440 = 12 × 120 = 4 × 360` |

Node-count note:
The table has `12` node-class slots and `11` distinct date labels. The center is the double `6086 BC` Shem birth / Shem death label, which marks the Flood-correspondence point in the schematic overlay. It must not be collapsed into one event-node.

Five of the `11` distinct date labels also reach AD 35 by exact prophetic-year multiples:

| Date label | Civil cross-axis span to AD 35 | Prophetic-year form |
|---|---:|---:|
| `16166 BC` | `16166 + 35 − 1 = 16200` | `45 × 360` |
| `14726 BC` | `14726 + 35 − 1 = 14760` | `41 × 360` |
| `6086 BC` | `6086 + 35 − 1 = 6120` | `17 × 360` |
| `3926 BC` | `3926 + 35 − 1 = 3960` | `11 × 360` |
| `1406 BC` | `1406 + 35 − 1 = 1440` | `4 × 360` |

Diagnostic observation:
Out of `11` distinct mod-120 date labels, five land on AD 35 by exact `360`-year multiples. A simple one-in-three residue expectation for a mod-120 set landing on mod-360 would be:

```markdown
11 ÷ 3 = 3.666...
```

The observed count of `5` is therefore slightly elevated under that simple heuristic. This is a diagnostic observation, not a formal statistical result.

Auxiliary cumulative comparison:
The actual cumulative MT Creation / Adam birth date also reaches AD 35 by an exact `360`-year multiple:

```markdown
14006 BC → AD 35 = 14006 + 35 − 1 = 14040
14040 = 39 × 360
```

State guard:
The AD 35 display does not convert schematic mod-120 coordinates into actual dates or Rounded Scaffold dates. It also does not turn AD 35 into the local actual Passion date. It is a bounded rounded Christ-death / civil cross-axis comparison label.

#### 5.9.3b With-Cainan Adam `960` slot and the 460-cycle Key-of-23 field

Active state: 2nd Cainan 460-cycle comparison state; mod-10 Mirror display state; MT / LXX `21000` conversion state; LXX Jared `960` slot comparison state; LXX Noah `1656` expansion state; Key-of-23 comparison state; bounded anchor-corroboration state.

The current author clarification treats the with-Cainan Adam apparent/textual slot `16166–15206 BC` as a high-density 460-cycle comparison field. The comparison begins with the local same-side spans already visible around the Exodus / Conquest field:

```markdown
16166 − 1446 = 14720 = 32 × 460
15206 − 1406 = 13800 = 30 × 460
```

The author then opens a bounded mod-10 Mirror display. In this display, the governing cross-axis display is:

```markdown
BC + AD − 2
```

This is the same kind of rounded / mod-10 display as the source comparison:

```markdown
6 BC → AD 6 = 6 + 6 − 2 = 10
```

Audit note:
This is not the ordinary civil cross-axis span. The ordinary civil span from `6 BC` to AD 6 is `6 + 6 − 1 = 11`. Therefore, all rows in this subsection using `BC + AD − 2` are labeled as mod-10 Mirror displays and must not be used to revise the ordinary civil cross-axis rows elsewhere in `File_23`.

Under that bounded mod-10 Mirror display, the Adam textual boundary `15206 BC` aligns with MT and LXX comparison labels by exact 460-cycle multiples:

```markdown
15206 BC → AD 4116 = 15206 + 4116 − 2 = 19320 = 42 × 460
15206 BC → AD 5496 = 15206 + 5496 − 2 = 20700 = 45 × 460
```

Key-of-23 conversions:

```markdown
19320 × 25/23 = 21000
20700 × 70/69 = 21000
```

Structural inference:
The MT and LXX comparison rows converge on `21000` by different exact-ratio operators. The MT-side row uses the Priestly `25/23` conversion; the LXX-side row uses the Prophetic `70/69` conversion. This convergence is retained as bounded structural inference / providential synchronization, not as proof by itself.

The same `21000` appears from the with-Cainan Adam apparent head to the cumulative MT Flood / Shem-death label by mod-10 Mirror display:

```markdown
16166 BC → AD 4836 = 16166 + 4836 − 2 = 21000
```

The cumulative Shem birth label extends the same display to `21600`:

```markdown
16166 BC → AD 5436 = 16166 + 5436 − 2 = 21600
21600 = 60 × 360
16166 − 2160 = 14006 BC
```

Audit note:
The source phrase “`21600 = 60 × 3600`” appears to intend the executable display `21600 = 60 × 360`. As written, `60 × 3600 = 216000`, not `21600`. This file therefore retains the authorial intent as an audit-controlled display and uses the checked arithmetic `60 × 360 = 21600`.

Adam textual `930` mirror-pair nuance:
The LXX Jared `960` groove below remains true, but the more direct alignment is Adam's own textual `930` lived years. In the with-Cainan schematic display, the apparent-age head is `16166 BC`; the actual-lived `930` segment begins after the `+30` apparent-age component:

```markdown
16166 − 30 = 16136 BC
16136 − 15206 = 930
```

MT Adam mirror-pair display:

```markdown
4116 − 3186 = 930
16136 BC → AD 3186 = 16136 + 3186 − 2 = 19320
15206 BC → AD 4116 = 15206 + 4116 − 2 = 19320
```

LXX Adam mirror-pair display:

```markdown
5496 − 4566 = 930
16136 BC → AD 4566 = 16136 + 4566 − 2 = 20700
15206 BC → AD 5496 = 15206 + 5496 − 2 = 20700
```

Structural inference:
The Adam `930` mirror-pair display slots the schematic Adam textual-lifespan segment `16136–15206 BC` against the MT Adam `930` mirror pair `4116/3186 BC/AD` and the LXX Adam `930` mirror pair `5496/4566 BC/AD`. This makes the Adam-to-Adam `930` correspondence the direct layer, while the LXX Jared `960` row remains a secondary `960` groove inside the same high-density comparison field.

State guard:
The `16136–15206 BC` display is not a new actual chronology. It is the actual-lived `930` segment inside the schematic Adam slot after the `+30` apparent-age head. The MT and LXX mirror-pair labels are comparison labels under the bounded mod-10 Mirror display, not actual AD historical anchors and not Protocol 1 targets.

LXX Jared comparison:
In the MT regular genealogy, Jared is born `460` years after Adam / Creation by the summed begetting-age display:

```markdown
130 + 105 + 90 + 70 + 65 = 460
```

In the LXX comparison state, Jared is born `960` years after Creation:

```markdown
230 + 205 + 190 + 170 + 165 = 960
5496 − 960 = 4536 BC
16166 BC → AD 4536 = 16166 + 4536 − 2 = 20700
```

Structural inference:
The LXX Jared `960` slot aligns the actual LXX Adam-to-Jared interval with the schematic Adam `960` slot in the with-Cainan mirror display. The author also notes Jared's Watcher-lore association and his `962`-year lifespan rounded to `960` in the Rounded comparison layer. Those links are retained as dependency-controlled comparison material and do not become local source-critical proof.

LXX Noah comparison:
The author-supplied LXX Noah birth offset is retained as a Gear 2 / Gear 3 display, depending on whether the local comparison uses mod-10 display or actual count-state:

```markdown
20700 − 1656 = 19044
19044 × 25/23 = 20700
```

Structural inference:
The LXX Noah `1656` offset subtracts from the `20700` LXX mirror span and then expands by `25/23` back to `20700`. This preserves the author's claim that the with-Cainan `16166–15206 BC` slot integrates strongly with MT, LXX, Jared, Noah, cumulative Shem / Flood, and Key-of-23 comparison grammar.

Claim-status note:
The source describes this field as “hyperactive.” `File_23` retains the substance of that observation but classifies it as high-density structural inference / providential synchronization. It is not a formal statistical proof and does not establish that the `16166–15206 BC` slot is the only possible 960-year placement without a defined exhaustive model.

#### 5.9.3c No-Cainan Temple-switch 460-cycle companion

Active state: no-Cainan Temple-switch comparison state; cumulative Flood `9890` comparison state; Key-of-23 comparison state; Temple anchor comparison state; bounded anchor-corroboration state; actual / rounded / schematic mod-120 firewall state.

The no-Cainan Adam apparent/textual slot `15686–14726 BC` shifts the same 460-cycle field down by `480` years, from the Exodus anchor to the Temple construction anchor:

```markdown
16166 − 15686 = 480
1446 − 966 = 480
15686 − 966 = 14720 = 32 × 460
14726 − 6 = 14720 = 32 × 460
```

Equivalent landing display:

```markdown
15686 − 14720 = 966 BC
14726 − 14720 = 6 BC
966 − 6 = 960
```

Christological mod-10 companion from the schematic Adam actual-lived start:

```markdown
16136 − 36 = 16100
16100 = 7 × 2300
16100 = 35 × 460
16100 = 230 × 70
AD 16136 → AD 36 = 16100
```

Bounded mod-10 cross-axis display:

```markdown
16136 BC → AD 36 = 16136 + 36 − 2 = 16170
16170 = 33 × 490
16170 = 33 × 7 × 70
36 BC → AD 36 = 36 + 36 − 2 = 70
```

Structural inference:
The `16136 BC` schematic actual-lived Adam start therefore has a same-side `16100`-year relation to `36 BC`, equal to `7 × 2300`, `35 × 460`, and `230 × 70`. Under the bounded mod-10 Mirror display, the same Adam start reaches AD 36 by `16170 = 33 × 490 = 33 × 7 × 70`, while the `36 BC → AD 36` center-span displays `70` years by the same mod-10 operator. This allows the Creation-week `7`, the `2300 × 7` / `230 × 70` pattern, the `33 × 490` pattern, and the central `70` to merge around the schematic Creation-of-Adam coordinate `16136 BC`.

State guard:
The `36 BC` label is retained here as a bounded Christological / mod-10 comparison label. It is not Luke's `36 BC` rail unless a local dependency explicitly opens that state, and AD 36 is not an actual Passion-date replacement. Ordinary civil cross-axis arithmetic would use `BC + AD − 1`; the displayed `16170` and `70` values require the bounded mod-10 operator `BC + AD − 2`. The author-supplied phrase that a mod-120 cycle also ends at `36 BC` is retained as comparison-state language; this patch does not locally rederive a full mod-120 cycle to `36 BC`.

Key-of-23 conversion:

```markdown
14720 × 25/23 = 16000
```

Structural inference:
Without Cainan, the same `32 × 460` span that carries the with-Cainan Adam apparent head to the Exodus instead carries the no-Cainan Adam apparent head to the Temple construction anchor `966 BC` and the no-Cainan Adam textual boundary to the `6 BC` Christ anchor. The Temple comparison is retained because Jesus' body / Temple typology supplies theological fit by `File_13` / `File_19` dependency; the arithmetic remains the controlling layer.

Cumulative Flood / Decimal Bridge companion:

```markdown
14726 − 4836 = 9890
9890 = 23 × 430
```

Structural inference:
The no-Cainan Adam textual boundary / Seth start reaches the cumulative Flood / Shem-death label `4836 BC` by `9890 = 23 × 430`. This links the no-Cainan Adam slot to the cumulative Flood field and Decimal Bridge vocabulary without rewriting `File_22` or converting the schematic Adam slot into actual chronology.

State guard:
The no-Cainan Temple-switch comparison is bounded. `966 BC` remains the Temple construction anchor by Kings / Temple dependency; `6 BC` remains the Christ anchor by Christological dependency; `4836 BC` remains cumulative Flood / Shem-death comparison by cumulative dependency. None of these rows converts the schematic Adam slot into an actual, Rounded Scaffold, or formal Mirror state.

#### 5.9.3d Peleg, Adam, Moses, and the `230/460` Mirror integration

Active state: Peleg / Adam / Moses `230/460` Mirror integration state; Peleg division-hinge state; Adam textual `930` mirror-pair comparison state; mod-10 Mirror display state; Key-of-23 comparison state; bounded anchor-corroboration state.

Peleg's `4166 BC` birth / division label and `3926 BC` death / Reu boundary already divide the halving scaffold. The present clarification adds that Peleg sits inside a dense `460`-cycle integration with Adam and Moses. The `230` rows are retained as half-cycle diagnostics, but rows that are multiples of `230` and not `460` are now explicitly downweighted unless they attach to an already recognized `460` path.

Peleg to Moses same-side display:

```markdown
4166 − 1406 = 2760
2760 = 23 × 120
2760 = 12 × 230
2760 = 6 × 460
```

Mod-10 Mirror companion through MT Adam and Moses:

```markdown
4116 BC → AD 1406 = 4116 + 1406 − 2 = 5520
5520 = 2 × 2760
5520 = 24 × 230
5520 = 12 × 460
5520 × 25/23 = 6000
```

Shem to Moses age-40 same-side companion:

```markdown
6086 − 1486 = 4600
4600 = 10 × 460
```

Peleg to MT Creation Mirror companion:

```markdown
4166 BC → AD 4116 = 4166 + 4116 − 2 = 8280
8280 = 18 × 460
```

Dependency note:
`1486 BC` is retained here as a Moses age-40 failure / wedding comparison label by dependency. `AD 4116` is a mod-10 Mirror display label under `BC + AD − 2`, not an ordinary historical AD anchor.

Structural inference:
The Peleg-to-Moses span `2760` is both `23 × 120` and `6 × 460`. Its doubled mod-10 Mirror companion `5520` reaches the MT Adam / Moses display and converts by `25/23` to `6000`. This ties Peleg, Adam, and Moses into the same `120`, `230`, and `460` register without changing the governing chronology state.

LXX Adam / Peleg mod-10 Mirror companion:

```markdown
5496 BC → AD 4166 = 5496 + 4166 − 2 = 9660
9660 = 42 × 230 = 21 × 460
9660 × 70/69 = 9800
9800 = 20 × 490
```

LXX Adam / Moses mod-10 Mirror companion:

```markdown
5496 BC → AD 1406 = 5496 + 1406 − 2 = 6900
6900 = 30 × 230 = 15 × 460
6900 × 70/69 = 7000
```

Shem and Adam `230` half-cycle diagnostic rows:

```markdown
6086 BC → AD 6566 = 6086 + 6566 − 2 = 12650
12650 = 55 × 230

5606 BC → AD 1526 = 5606 + 1526 − 2 = 7130
7130 = 31 × 230

15656 − 3926 = 11730
11730 = 51 × 230
```

Audit note:
The author-supplied phrase uses `6085 BC` for the first Shem mirror row. The active File_23 schematic node label is `6086 BC`. Under the active mod-10 Mirror display, `6086 + 6566 − 2 = 12650`; under ordinary civil-style counting, `6085 + 6566 − 1 = 12650`. This file therefore treats `6085 BC` as audit-controlled shorthand and uses the active File_23 node label `6086 BC`.

Audit note:
The source phrase “expands by `240` or `720` years” is retained as source-intent language but not made executable without a specified operator. The checked local conversions are:

```markdown
2760 × 25/23 = 3000
3000 − 2760 = 240

5520 × 25/23 = 6000
6000 − 5520 = 480
```

Structural inference:
The full `460` rows carry the main evidential weight because they attach to the known 2nd-Cainan `460` cycle stream and recur along major Adam, Peleg, Moses, MT, and LXX comparison paths. The `230`-only rows that are not also multiples of `460` are retained as arithmetic facts / exploratory diagnostics but are explicitly downweighted. Given the number of available nodes and their mod-10 Mirror counterparts, isolated `230` half-cycle hits may be random unless they connect to a recognized `460` path. The foregrounded strongest rows include, but are not exhaustive of: `19320 = 42 × 460`, `20700 = 45 × 460`, `9660 = 21 × 460`, `6900 = 15 × 460`, `14720 = 32 × 460`, `13800 = 30 × 460`, `2760 = 6 × 460`, `4600 = 10 × 460`, and `8280 = 18 × 460`.

State guard:
This subsection extends the already opened bounded mod-10 Mirror display. It does not open ordinary civil cross-axis revision, Protocol 1 target-generation, Protocol 2, inverse-number reversal, Residue Protocol, actual chronology replacement, LXX controlling chronology, or Rounded Scaffold retrojection.

#### 5.9.4 Interior `1650` grammar and Rounded Scaffold compatibility

Removing the two flanking patriarchs Adam and Moses from the no-Cainan span leaves the interior:

```markdown
15686 − 1406 = 14280
15686 − 960 = 14726
1406 + 120 = 1526
14726 − 1526 = 13200 = 8 × 1650
```

The same `1650` grammar links the halving schematic with the Rounded Scaffold:

```markdown
15686 − 14036 = 1650
15656 − 14006 = 1650
15686 − 4136 = 11550 = 7 × 1650
15656 − 4106 = 11550 = 7 × 1650
4106 − 2456 = 1650
```

Structural inference:
The halving schematic is not only compatible with the Rounded Scaffold at isolated dates. Its no-Cainan interior reproduces the Rounded Scaffold's `1650` backbone in an eightfold form, while the Adam apparent/textual pair also lands on the cumulative `14036/14006 BC` and rounded `4136/4106 BC` pairs by `1650` multiples. This remains bounded File_51a comparison material.

#### 5.9.5 Mirror and Pillar corroboration

Bounded Mirror note:
The following is a comparison-only Mirror / Pillar note. It does not open formal Mirror target-generation as a local proof protocol for `File_23`. Civil cross-axis spans use the repository rule `BC + AD − 1` where ordinary cross-axis counting is active.

Under standard Protocol 1 handling, the mirror of `16166 BC` is `AD 16165`. The no-Cainan Adam apparent head reaches that target by:

```markdown
15686 BC → AD 16165 = 15686 + 16165 − 1 = 31850
31850 = 650 × 49 = 65 × 490
```

Christ divides the same span:

```markdown
15686 − 6 = 15680 = 32 × 490
6 BC → AD 16165 = 6 + 16165 − 1 = 16170 = 33 × 490 = 330 × 49
```

Bounded comparison:
The Mirror comparison displays a Jubilee flow across the Christ hinge. The post-Christ portion is `330 × 49`, which corresponds to Cainan's `330` remaining years after begetting Shelah at `130`, while the whole span is `65 × 490`. This remains Mirror / Pillar corroboration, not local Mirror proof mechanics.

Pillar note:
The Jachin / Boaz Projective Boundary field uses `AD 29395` as a Pillar coordinate by dependency. The ordinary civil cross-axis span from the no-Cainan Adam skin to this Pillar coordinate is:

```markdown
15686 BC → AD 29395 = 15686 + 29395 − 1 = 45080
45080 = 23 × 1960 = 92 × 490
45080 × 25/23 = 49000 = 100 × 490
```

Expanding backward from the Pillar coordinate gives:

```markdown
AD 29395 − 49000 years = 19606 BC
19606 − 6 = 19600 = 400 × 49 = 40 × 490
```

Bounded comparison:
The Pillar field is retained as a large Temple-Time anchor. It expands the no-Cainan Adam skin into `100 × 490`, and the expanded endpoint `19606 BC` stands `400` jubilees from the `6 BC` Christ anchor. This is dependency-controlled Projective Boundary comparison material, not ordinary historical chronology.

### 5.10 Secondary Aaron-side check

Active state: secondary Aaron-side check state; Aaron/Moses ordinary `3` branch by dependency; Key-of-23 comparison state.

The primary halving schematic is centered on Moses because Moses supplies the terminal `120` lifespan. However, `File_23` begins with the Aaron/Moses `3` / `3.5` problem, so Aaron remains thematically relevant. The Aaron overlay does not break the structure.

End-of-brackets Aaron/Moses note:
In the with-Cainan form, the entire Adam apparent-age head to Conquest / Moses-death span is:

```markdown
16166 − 1406 = 14760
14760 = 123 × 120 = 41 × 360
```

Structural inference:
Moses supplies the repeated terminal unit `120`, while Aaron's actual lifespan `123` appears as the number of Mosaic `120` units in the completed with-Cainan bracket. Since Aaron dies immediately before the Conquest / Moses-death horizon by dependency, the endpoint field can display Moses' `120` and Aaron's `123` without inserting Aaron into the main halving table.

The same bracket decomposes through the Shem juncture in a way that recalls the Aaron/Moses ages at the Exodus and at death. Moses is `80` at the Exodus and `120` at death; Aaron is `83` at the Exodus and `123` at death. The first block from Adam to Shem supplies the initial `80 × 120`, while the Shem-to-Conquest span supplies the additional `43 × 120`:

```markdown
16166 − 6566 = 9600 = 80 × 120 = 800 × 12
6566 − 1406 = 5160 = 43 × 120 = 430 × 12
9600 + 5160 = 14760 = 123 × 120 = 1230 × 12
```

Secondary structural comparison:
The initial `80 × 120` is notable because Moses is `80` at the Exodus, and the completed with-Cainan bracket totals `123 × 120`, echoing Aaron's `123` at death. The extra `43 × 120` segment also preserves the `430` Sojourn / deliverance register at `12×` scale. This makes the completed bracket thematically capable of holding both Moses' `120` and Aaron's `123`, while the main table remains Moses-centered.

If Aaron's ordinary `+3` birth offset is allowed to pull the no-Cainan Adam apparent head from `15686 BC` back to `15689 BC`, the span to the Conquest / terminal Aaron-Moses field at `1406 BC` is:

```markdown
15689 − 1406 = 14283
14283 = 3 × 69²
14283 × 70/69 = 14490
14490 × 70/69 = 14700
14700 = 300 × 49 = 30 × 490
```

Structural comparison:
The Aaron overlay remains compatible with the Jubilee grammar, while remaining secondary to the main Moses-centered halving table. This note is included only because the local file already treats the Aaron/Moses `3` / `3.5` relation as part of its opening problem.

### 5.11 Actual and rounded lifespan witnesses retained

Active state: actual / rounded lifespan comparison state by `File_51a`; Shem-to-Eber average-lifespan state; Shem-to-Eber + Cainan 480-class state; Peleg / Reu `239/240` transition state; MT-primary halving-tradition state.

The schematic table is not intended to erase the actual lifespan witnesses. It sits beside them. The actual and rounded witnesses also do not convert the schematic mod-120 coordinates into actual chronology or Rounded Scaffold chronology.

The no-Cainan Shem-to-Eber group witnesses the approximate `480` stage:

```markdown
Shem = 600
Arphaxad = 438
Shelah = 433
Eber = 464
600 + 438 + 433 + 464 = 1935
1935 ÷ 4 = 483.75
```

No-Cainan rounded comparison under `File_51a`:

```markdown
Shem = 600
Arphaxad = 440
Shelah = 435
Eber = 465
600 + 440 + 435 + 465 = 1940
1940 ÷ 4 = 485
```

The MT + Cainan `4+1` group makes the `480` class explicit:

```markdown
Shem = 600
Arphaxad = 438
2nd Cainan = 460
Shelah = 433
Eber = 464
600 + 438 + 460 + 433 + 464 = 2395
2395 ÷ 5 = 479
```

Audit note:
The author-supplied phrase `2394/5=479` is not executable arithmetic from the listed values. The listed values sum to `2395`, not `2394`. This file therefore preserves the intended `479` average by the checked equation `2395 ÷ 5 = 479`.

MT + Cainan rounded comparison under `File_51a`:

```markdown
Shem = 600
Arphaxad = 440
2nd Cainan = 460
Shelah = 435
Eber = 465
600 + 440 + 460 + 435 + 465 = 2400
2400 ÷ 5 = 480
```

Structural inference:
The MT + Cainan `4+1` average confirms the Shem-to-Eber group as the `480` class in the local halving scaffold. It strengthens the sequence Adam `960` → Shem-to-Eber `480` → Peleg / Reu `240` → Moses `120` without converting approximate lifespan witnesses into an exact table law.

Peleg supplies the division clue, and Reu confirms the transition by repeating Peleg's lifespan:

```markdown
Peleg actual lifespan = 239
Peleg rounded lifespan = 240
Reu actual lifespan = 239
Reu rounded lifespan = 240
```

Typological / lexical note:
Peleg's name remains associated with division, and the text locates the division “in the days of Peleg.” In this file, that supports the death-boundary / full-lifespan use of Peleg as a division marker, not only Peleg's birth. This remains typological / lexical support unless a later citation-control pass makes the claim proof-bearing.

Tradition-state note:
MT without restored Cainan and MT + restored 2nd Cainan are the primary tradition states for the File_23 halving pattern. LXX and SP remain subordinate comparison witnesses in this local section. They may corroborate or compare by dependency, but they do not replace the MT halving scaffold.

Full-lifespan marker clarification:
The current patch clarifies that the full lifespans of Shem and Peleg are intended in the same broad way Adam and Moses are intended. Shem death / Arphaxad birth at `5606/6086 BC` and Peleg death / Reu boundary at `3926 BC` are therefore accepted schematic nodes. This clarification strengthens the lifespan-marker layer but does not convert the schematic coordinates into actual chronology or Rounded Scaffold chronology.

Claim-status:
The no-Cainan average `483.75`, no-Cainan rounded average `485`, MT + Cainan actual average `479`, MT + Cainan rounded average `480`, and Peleg / Reu `239/240` support the visibility of the halving theme. They do not turn the schematic overlay into an exact table law and do not make LXX or SP controlling traditions for the local scaffold.

### 5.12 Theological synthesis of the halving pattern

Active state: progressive feebleness theological state; providential-synchronization reading; typological / theological note.

The lifespan-halving pattern does not technically prove anything by itself. Its evidential role is suggestive and structural. The theological message, however, is coherent with the trunk of the pattern.

The old Adamic line is progressively shortened:

```markdown
960 → 480 → 240 → 120
```

The decreasing lifespans are interpreted here as signifying mankind's increasing feebleness while living on the ground cursed because of human failure. The pattern terminates in Moses, whose actual lifespan is `120` and whose life is divided into `40 + 40 + 40`. As §5.8.6 states, the selected halving heads are also transition / judgment heads: Adam, Shem, Peleg, and Moses. The current patch clarifies that these heads include full-lifespan boundary logic, especially Shem death / Arphaxad birth and Peleg death / Reu boundary. Abraham remains covenantally central, but he is not a halving head because the schematic tracks the bracket-openers, full-lifespan transition nodes, and division points rather than every major patriarch.

In the theological reading preserved here, Christ completes and reverses the pattern as the New Adam and the Prophet like Moses. Christ willingly has his life cut short in order to restore eternal life. The recurrence of `460` and `490` among the native `480` / mod-120 structure is therefore locally relevant: `480` belongs to the table's halving grammar, while `460` and `490` function as redemptive counter-threads woven through it.

Typological note:
The `6 × 360 + 7 × 360 = 13 × 360` decomposition is retained as an author-supplied Fall-of-man comparison: the `6 + 7` pattern is treated as a BJ / Jubilees-related typological note, not as local proof of the halving table. It explains theological fit while preserving arithmetic and source-control boundaries.

Claim-status restraint:
This is classified as structural inference and providential synchronization. It is not a claim that the table proves human scribal intent. It states that the halving schematic, when confined to Adam / Moses and the true halving nodes Shem and Peleg, coheres with the same mathematical grammar as the Rounded Scaffold, the Cainan `460` register, and the Jubilee `490` register.

### 5.13 From Creation week to Aaron/Moses half-week

Active state: Creation-to-Aaron/Moses half-week symmetry state; Solar/Lunar Straddle comparison state.

The same structural inference links Creation and Adam to Aaron/Moses. The chronology begins with the `7` years / days of Creation and ends this local line with the `3.5`-year Aaron/Moses spread.

Arithmetic fact:

```markdown
7 ÷ 2 = 3.5
3.5 + 3.5 = 7
```

Structural inference:
The full-week Creation frame and the terminal Aaron/Moses half-week spread show the chronology portioned out in exact and approximate fractions: `1/3600` for the prophetic half-week miniature, `1/12` for the Adam/Moses boundary-lifespan sum, about `960/480/240/120` for the lifespan descent, and `7 → 3.5` for the Creation-to-Mosaic half-week symmetry.

Dependency note:
This section uses Solar/Lunar Straddle and Creation-week comparison language. Formal Mirror protocol, inverse-number logic, and Residue Protocol are not promoted into local proof-bearing status. The bounded Mirror / Pillar comparisons in §5.9.5 are anchor-corroboration notes only and remain dependency-labeled.


## 6. Synthesis: Moses' Birth as Micro-Flood

Active state: Micro-Flood synthesis state; typological / theological claim-status.

The source conclusion is preserved with claim-status restraint:

> The birth of Moses is a Micro-Flood event designed to mirror the math of the SKL.

Typological / theological note:
In this file, “Micro-Flood” names the structural and theological reading in which Moses' birth narrative recapitulates the Flood pattern at smaller scale. The source interprets the Mosaic `tebah`, the `3 months` hiding period, the SKL `3.5`-day fraction, the prophetic `1260` output, the `1/3600` relation to `12600`, the `1/12` Adam/Moses boundary-lifespan flank, and the approximate lifespan-halving scaffold as one coordinated pattern.

Argument-control note:
The phrase “designed to mirror” is preserved as theological / providential synchronization language. It does not open formal repository Mirror protocol, 10x Mirror scaling, inverse-number reversal, or proof-bearing civil cross-axis protocol. The bounded §§5.9.3, 5.9.3a, and 5.9.5 comparisons remain the only local civil cross-axis displays. It also does not claim demonstrable scribal intent unless a later source-critical dependency supplies that evidence. The symmetrical-design material is therefore classified as structural inference and theological / providential synchronization, with exact arithmetic separated from approximate lifespan-stage display.

## 7. Cross-Reference Vector

### 7.1 Link to `File_22` — Cumulative MT Harmonics

Active state: `File_22` Aaron/Moses `3` / `L = 3.5` branch dependency; cumulative MT trunk dependency.

Source-retained cross-reference:
The `3.5` prophetic-years state in this file supplies the source-state layer for the `3.5`-year Aaron/Moses straddle defined in `File_22`. The legacy phrase “source-code layer” is retained as source vocabulary, not as a proof-bearing operator.

Logic chain:

```markdown
SKL input: 3.5 days
→ scalar output: 3.5 prophetic years / 1260 days
→ cumulative trunk echo: 12600 years
→ scale relation: 12600 ÷ 3600 = 3.5 years
→ boundary flank: Adam 930 + Moses 120 = 1050 = 12600 ÷ 12
→ apparent-age scaffold: 30 + 930 = 960 ≈ 960/480/240/120 → Moses 120
→ full-lifespan node clarification: Adam / Shem / Peleg / Moses; Shem death / Arphaxad birth 5606/6086 BC; Peleg death / Reu boundary 3926 BC
```

Dependency note:
`File_22` controls the downstream Aaron/Moses ordinary `3`-year branch, the phase-resolved `L = 3.5` branch, and the cumulative `12600` state. `File_23` supplies the upstream Tebah / SKL source-state.

### 7.2 Link to `File_00` — Master Index / Axioms

Active state: Solar/Lunar Straddle comparison state.

Source-retained cross-reference:
The hidden `3`-month period in the Mosaic birth narrative is compared with the Solar/Lunar Straddle of Creation, especially Day `7` versus Day `7.5`.

Logic chain:

```markdown
7 ÷ 2 = 3.5
3.5 + 3.5 = 7
```

Arithmetic fact:
The completion-pair display checks. The source identifies the hidden portion as the unrevealed half of the cycle.

Dependency note:
This section uses Solar/Lunar Straddle comparison language. Outside the bounded §5.9.3 Christ / Temple comparison, §5.9.3a AD 35 comparison, §§5.9.3b–5.9.3d mod-10 Mirror display comparisons, and §5.9.5 Mirror / Pillar comparison notes, formal Mirror protocol, inverse-number logic, Residue Protocol, and civil cross-axis arithmetic are not active in this local file unless a later pass explicitly opens them.

## Audit Notes

Audit note:
The lexical claim that `tebah` appears only in the Noah Ark and Moses basket contexts is preserved as a source-retained textual / typological claim. Citation-control and lexical verification remain for a later source-control pass or final cleanup.

Audit note:
The En-tarah-ana fractional datum is preserved as `3 months and 3.5 days`, but full SKL source-control remains dependency-controlled by `File_21` / `File_34`.

Audit note:
The unit-state of `3.5 × 360 = 1260` is locally resolved as `1260` prophetic days / `3.5` prophetic years under the 360-day register. SKL deep-time scaling remains dependency-controlled and is not opened locally.

Resolved authorial-direction note:
The prior arithmetic-control review audit item for `12.6 / 3.5 harmonic` is closed. The active `File_23` wording is now the `1/3600` relation: `12600 ÷ 3600 = 3.5` years, and `3.5 × 360 = 1260` days. No additional author-decision process is required for this item.

Audit note:
The Adam/Moses boundary-lifespan flank is author-supplied and arithmetic-clean: `930 + 120 = 1050` and `12600 ÷ 12 = 1050`. The `24` intervening generations are retained as author-supplied table logic and are not locally rederived in `File_23`.

Audit note:
The lifespan-halving scaffold is intentionally approximate. Exact checks apply to the ideal scaffold `960 → 480 → 240 → 120`, but Shem-to-Eber averages, the MT + Cainan `479/480` class, and Peleg / Reu `239/240` are approximate stage witnesses. Do not silently regularize those values.

Audit note:
The author-supplied Shem-to-Eber + Cainan shorthand `2394/5=479` is audit-controlled. The listed values Shem `600`, Arphaxad `438`, 2nd Cainan `460`, Shelah `433`, and Eber `464` sum to `2395`, not `2394`; the executable display is `2395 ÷ 5 = 479`. The rounded comparison remains exact: `2400 ÷ 5 = 480`.

Audit note:
The MT + Cainan `4+1` average strongly supports the Shem-to-Eber group as the local `480` class. This does not turn the approximate lifespan-halving scaffold into an exact table law and does not make LXX or SP the controlling tradition for the local pattern.

Audit note:
Peleg and Reu both have actual lifespan `239` and rounded lifespan `240`. The repeated `239/240` supports the `480 → 240` transition, while Peleg remains the named division marker. Reu is not promoted into a separate halving head.

Audit note:
The post-pressure Shem / Peleg clarification adds full-lifespan marker status for Shem and Peleg. Shem death / Arphaxad birth `5606/6086 BC` and Peleg death / Reu boundary `3926 BC` are accepted schematic nodes, but they remain schematic display coordinates unless a local row explicitly opens actual or Rounded Scaffold chronology.

Audit note:
The `6566 → 4166 → 3926 → 1526` Peleg-centered chiasm is a schematic comparison. It has been arithmetically checked locally as `2400 + 240 + 2400` and included in the focused post-pressure patch pressure test.

Audit note:
AD 35 is used as a rounded Christ-death comparison label. It does not replace actual Passion date-states and does not open formal Mirror target-generation.

Audit note:
The observation that `5` of `11` distinct mod-120 date labels land on AD 35 by exact `360`-year multiples is diagnostic only. A formal statistical claim would require a defined scan model.

Audit note:
The BJ / Fall-of-man `6 + 7` note is retained as author-supplied typological comparison. It requires source-control if made proof-bearing and does not open BJ chronology as a local proof layer.

Audit note:
The 460-cycle / LXX-MT Mirror slot clarification uses the bounded mod-10 Mirror display `BC + AD − 2`. This is distinct from ordinary civil cross-axis arithmetic `BC + AD − 1`. The source display `6 BC → AD 6 = 10` is valid only under the rounded / mod-10 display state; ordinary civil counting would give `6 + 6 − 1 = 11`.

Audit note:
The source phrase `21600 = 60 × 3600` is audit-controlled. The checked local arithmetic is `21600 = 60 × 360`; `60 × 3600 = 216000`. The file uses the checked display and retains the phrase only as evidence of the author's intended 360-cycle comparison.

Audit note:
The LXX Jared `960` and LXX Noah `1656` rows are dependency-controlled comparison material. Smith-style LXX reconstruction source-control is now relevant because LXX chronology is materially referenced, but Smith does not supply the File_23 harmonic interpretation.

Audit note:
The no-Cainan Temple-switch comparison uses `966 BC` and `6 BC` as bounded dependency-controlled comparison anchors. It does not revise Kings / Temple chronology, Christological date-state controls, or the actual / rounded / schematic mod-120 firewall.

Audit note:
The `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison uses two operators. The same-side display `16136 − 36 = 16100` checks by ordinary same-side subtraction. The cross-axis displays `16136 BC → AD 36 = 16170` and `36 BC → AD 36 = 70` check only under the bounded mod-10 Mirror display `BC + AD − 2`; ordinary civil counting would produce different totals. The `36 BC` label is not collapsed with Luke's `36 BC` rail unless that dependency is explicitly opened. The author-supplied claim that the mod-120 cycle also ends at `36 BC` is retained as comparison-state language; the present patch does not locally rederive a full mod-120 path to `36 BC`.

Audit note:
The Adam textual `930` mirror-pair nuance is comparison-only. It distinguishes `16166 BC` as the apparent-age head from `16136–15206 BC` as the schematic actual-lived `930` slot, then compares that slot with MT `4116–3186 BC/AD` and LXX `5496–4566 BC/AD` mirror-pair displays. It does not revise MT or LXX actual chronology.

Audit note:
The Peleg / Adam / Moses `230/460` integration extends the already opened bounded mod-10 Mirror display only inside §§5.9.3b–5.9.3d. Full `460` multiples remain the strongest comparison examples because they attach to the known 2nd-Cainan `460` cycle stream. Rows that are multiples of `230` but not `460` are retained as arithmetic facts / exploratory diagnostics and are downweighted as likely random unless a recognized `460` path is supplied.

Audit note:
The author-supplied `6085 BC` Shem display is audit-controlled. The active File_23 schematic label remains `6086 BC`, because `6086 + 6566 − 2 = 12650` under mod-10 Mirror display. The civil-style equivalent `6085 + 6566 − 1 = 12650` may explain the shorthand, but `6085 BC` is not promoted to a new node.

Audit note:
The phrase “expands by `240` or `720` years” remains source-intent language unless a later pass supplies the exact `720` operator. The checked local conversions are `2760 × 25/23 = 3000`, an expansion by `240`, and `5520 × 25/23 = 6000`, an expansion by `480`.

Audit note:
The cumulative Flood `9890 = 23 × 430` companion row is comparison-only. It does not revise File_22 Decimal Bridge controls or convert `4836 BC` into a new local File_23 anchor.

Audit note:
The newly supplied phrase `930/2=480` is not arithmetically exact if Adam's textual `930` alone is active. Exact halving gives `930 ÷ 2 = 465`. The intended `480` stage is preserved through the active Adam apparent-age state: `30 + 930 = 960`; `960 ÷ 2 = 480`.

Audit note:
The Genesis 6:3 dual-`120` reading is author-supplied and theologically / structurally coherent within the halving theme, but its textual-source control should be reviewed if the claim is made proof-bearing in a later pass.

Audit note:
The meaning of Peleg's name as “division” is retained as a typological / lexical note. It should receive citation-control if the lexical claim becomes load-bearing.

Audit note:
The actual / rounded date comparison is imported from `File_51a`. It is bounded to the lifespan-halving section and should not be allowed to overwrite actual MT dates or the cumulative MT `12600` state.

Resolved finalization guard:
Actual MT dates, File_51a Rounded Scaffold dates, and schematic mod-120-year span coordinates are now explicitly firewalled. The schematic table remains a display overlay; clean mod-120 spans do not become Rounded Scaffold values, and rounded comparison dates do not correct actual or schematic dates.

Audit note:
The File_22 section reference in the source should be verified against the current final `File_22` section numbering in a later cross-reference cleanup pass.

Audit note:
Arithmetic-control review verifies the §5.9.3 Christ / Temple and §5.9.5 Mirror / Pillar civil cross-axis displays as bounded comparison-only material. The current patch adds §5.9.3a AD 35 civil cross-axis displays as bounded comparison-only material as well. argument-control layer classifies their evidential role as bounded anchor-corroboration / bounded Mirror-Pillar comparison. The checked rows use `BC + AD − 1` or the stated backward cross-axis count; the displays remain comparison-only and do not open formal Mirror proof mechanics.

Audit note:
argument-control layer classifies later halving-node Key-of-23 displays as comparison-state arithmetic and structural corroboration. They are not Residue Protocol and should not be generalized into a correction operator.

Audit note:
The minor 460-list amendment adds full `460` examples already governed by same-side BC subtraction or the locally opened mod-10 Mirror display. The new foregrounded rows check as `4166 − 1406 = 2760 = 6 × 460`, `6086 − 1486 = 4600 = 10 × 460`, and `4166 + 4116 − 2 = 8280 = 18 × 460`. This amendment does not change the prior focused pressure-test result.

## Unresolved Issues

| Issue | Final handling | Later handling |
|---|---|---|
| `tebah` lexical source-control | Preserved as source-retained textual / typological claim | Citation-control / final cleanup |
| En-tarah-ana textual provenance | Dependency-controlled by `File_21` / `File_34` | Source-control review only if proof burden changes |
| `3.5 × 360 = 1260` unit-state | Locally resolved as `1260` prophetic days / `3.5` prophetic years | Dependency review only if SKL deep-time scaling is opened |
| “Integer floor” language | Classified as explanatory structural display; not a new operator | No author-decision blocker |
| “Authorizes the overlay” wording | Classified as typological warrant / structural inference | No author-decision blocker |
| Solar/Lunar Straddle comparison | `3.5 + 3.5 = 7` checks; formal Mirror remains inactive | Cross-file cleanup only if `File_00` section label is needed |
| `File_22` section label | Preserved as dependency reference, not final section audit | Cross-reference cleanup |
| `24` intervening generations and lifespan-halving table provenance | Integrated as author-supplied design logic; not locally rederived from the full table | Citation-control / final cleanup if needed |
| Noah `480` / Genesis 6:3 dual-`120` source-control | Integrated and claim-status classified as theological / structural interpretation | Source-control review only if made proof-bearing |
| `930/2=480` shorthand | Audit-controlled; exact relation is `960 ÷ 2 = 480`, not textual `930 ÷ 2` | Keep audit note unless author rewrites source wording |
| Peleg name / division lexical control | Preserved as typological / lexical clue; now also supports full-lifespan boundary use by author clarification | Citation-control if proof-bearing |
| Shem death / Arphaxad birth node provenance | Integrated as author-supplied schematic node clarification | Keep schematic; source-control only if promoted beyond local display state |
| Peleg death / Reu boundary node provenance | Integrated as author-supplied schematic node clarification | Keep schematic; source-control only if promoted beyond local display state |
| BJ / Fall-of-man `6 + 7` source-control | Integrated as typological / theological comparison | Source-control if proof-bearing |
| Peleg chiasm source-control / pressure status | Integrated as author-supplied structural clarification and focused pressure tested | Keep schematic unless a later pass opens actual or rounded chronology |
| AD 35 rounded Christ-death comparison | Integrated as bounded comparison | Dependency-control remains through `File_13` / `File_54` if proof burden increases |
| AD 35 mod-360 subset frequency | Diagnostic only | Formal statistical treatment would require a defined scan model |
| `16136 BC` / `36 BC` / AD 36 Christological mod-10 comparison | Integrated as bounded minor amendment; focused pressure tested during trust-status equalization | Keep mod-10 display distinct from civil cross-axis arithmetic; do not collapse `36 BC` with Luke's `36 BC` rail unless dependency opens it |
| Adam `930` mirror-pair nuance | Integrated as bounded post-focused-pressure comparison and focused pressure tested | Keep comparison-only; do not revise MT or LXX actual chronology |
| Peleg / Adam / Moses `230/460` Mirror integration | Integrated as bounded post-focused-pressure comparison and focused pressure tested with 230-only diagnostic restraint | Keep bounded to §§5.9.3b–5.9.3d; downweight 230-only rows unless they attach to a recognized `460` path |
| `6085 BC` Shem shorthand | Audit-controlled; active File_23 schematic label remains `6086 BC` | Do not promote `6085 BC` to a new node unless a later state explicitly opens it |
| `240` / `720` expansion phrase | Audit-controlled because no executable `720` operator is supplied | Retain checked conversions `2760 × 25/23 = 3000` and `5520 × 25/23 = 6000` |
| 460-cycle / LXX-MT Mirror slot comparison | Integrated as bounded post-pressure comparison and focused pressure tested | Formal statistical or optimal-position claim would require a defined exhaustive model |
| Mod-10 Mirror display `BC + AD − 2` | Integrated only for §§5.9.3b–5.9.3d | Keep distinct from civil cross-axis `BC + AD − 1`; source-control if generalized |
| `21600 = 60 × 3600` source phrase | Audit-controlled; executable display is `21600 = 60 × 360` | Keep audit note unless author rewrites the phrase |
| LXX Jared `960` / Noah `1656` source-control | Integrated as dependency-controlled comparison | Smith / LXX source-control review if proof-bearing |
| No-Cainan Temple-switch comparison | Integrated as bounded Temple / Christ comparison | Keep `966 BC`, `6 BC`, and Temple-body typology dependency-controlled |
| Cumulative Flood `9890 = 23 × 430` comparison | Integrated as dependency-controlled comparison | Keep File_22 controls unchanged |
| Shem-to-Eber + Cainan `2395/5 = 479` average | Integrated as post-focused-pressure author clarification and focused pressure tested | Keep audit note for source shorthand `2394/5=479` |
| Peleg / Reu `239/240` transition | Integrated as structural comparison | Keep Reu as transition witness, not as separate halving head |
| MT-primary halving-tradition guard | Integrated as tradition-state control | Keep LXX and SP subordinate unless a local dependency explicitly promotes them |
| Actual / rounded date integration | Bounded File_51a comparison state; visible averages and span checks verified in arithmetic-control review; claim-status reviewed in argument-control layer | Keep bounded; no Rounded Scaffold retrojection |
| Actual / rounded / schematic mod-120 firewall | Added in finalization stage as finalization guard and pressure-tested post-final | Maintain as hard non-collapse rule in later cross-file indexing |
| Bounded §§5.9.3 / 5.9.3a / 5.9.5 civil cross-axis displays | Reclassified in modal-state review where present; §5.9.3a added in current post-pressure patch; visible arithmetic checked locally; claim-status comparison-only | Keep comparison-only; do not open formal Mirror proof mechanics |
| Later halving-node Key-of-23 displays | Classified in modal-state review, checked in arithmetic-control review, and retained in argument-control layer as comparison-state material | Keep as comparison-state; do not promote to Residue Protocol |

No additional author-decision process is required for the current post-focused-pressure patches. The three earlier post-pressure patches have received a focused pressure test, and the Shem-to-Eber + Cainan 480-class patch plus the Adam `930` / Peleg `230/460` Mirror patch are now focused pressure tested in §0.14. Further pressure testing is not required unless the source is reopened.

## Recommended Cross-File Updates

| Target | Recommendation |
|---|---|
| Glossary / state register | Add provisional `Tebah Protocol state`: a typological / structural state linking Noah's Ark and Moses' basket through the Hebrew `tebah` lexical field and the SKL fractional datum. |
| Glossary / operator register | Add provisional `SKL appended fractional-datum state`: `3 months and 3.5 days` as a textual payload distinct from SKL whole-year totals. |
| Glossary / node-class register | Add provisional `3.5 node-class firewall`: distinguishes SKL `3.5` days, Priestly-Enochian `3.5` days, prophetic `3.5` years, Aaron/Moses `L = 3.5`, and Solar/Lunar comparison halves. |
| Glossary / scale-state register | Add provisional `1/3600 half-week scale state`: `12600 ÷ 3600 = 3.5` years and `3.5 × 360 = 1260` days. |
| Glossary / structural-design register | Add provisional `1/12 Adam/Moses boundary-flank state`: Adam `930` plus Moses `120` equals `1050 = 12600 ÷ 12`. |
| Glossary / structural-design register | Add provisional `apparent-age lifespan-halving scaffold state`: Adam `30 + 930 = 960`, about `960/480/240/120`, with Shem / Arphaxad and Peleg as approximate stage witnesses. |
| Glossary / structural-design register | Add provisional `Creation-to-Aaron/Moses half-week symmetry state`: Creation `7` reduced to the Aaron/Moses `3.5` spread under structural / theological comparison. |
| Glossary / structural-design register | Add provisional `Noah 480 warning / halving-forecast state`: Noah age `480` as the midpoint forecast between Adam apparent-age `960` and Moses `120`. |
| Glossary / theological interpretation register | Add provisional `Genesis 6:3 dual-120 state`: `120` as both Flood-warning span and terminal human lifespan cap represented by Moses. |
| Glossary / structural-design register | Add provisional `Shem-to-Eber average half-stage`: `600/438/433/464` averaging `483.75`, with rounded comparison average `485`. |
| Glossary / structural-design register | Add provisional `Shem-to-Eber + Cainan 480-class state`: Shem `600`, Arphaxad `438`, 2nd Cainan `460`, Shelah `433`, and Eber `464` averaging `479` actual and `480` rounded. |
| Glossary / structural-design register | Add provisional `Peleg / Reu 239/240 transition state`: Peleg `239` and Reu `239`, each rounded to `240`, as the transition from the Shem-to-Eber `480` class to the lower `240` class. |
| Glossary / tradition-state register | Add provisional `File_23 MT-primary halving-tradition state`: MT without Cainan and MT + restored Cainan control the local halving scaffold; LXX and SP are subordinate comparison witnesses unless locally promoted. |
| Glossary / structural-design register | Add provisional `Peleg division-stage state`: Peleg `239` / rounded `240` as the division marker inside the approximate halving scaffold. |
| Glossary / structural-design register | Add provisional `File_23 full-lifespan halving-node state`: Adam, Shem, Peleg, and Moses function through full-lifespan boundary logic, not only through birth / start markers. |
| Glossary / structural-design register | Add provisional `Shem death / Arphaxad birth marker state`: `5606/6086 BC` as accepted schematic nodes representing Shem death and Arphaxad birth / start under the active no-Cainan or with-Cainan state. |
| Glossary / structural-design register | Add provisional `Peleg death / Reu boundary marker state`: `3926 BC` as an accepted division node and full-lifespan marker inside the schematic halving table. |
| Glossary / calendar-comparison register | Add provisional `File_23 Priestly 336 / Prophetic 360 comparison state`: spans such as `10080 = 30 × 336 = 28 × 360`, `10800 = 30 × 360`, `1680 = 5 × 336`, and `(6 + 7) × 360` are comparison displays, not new chronology operators. |
| Glossary / typological-comparison register | Add provisional `File_23 BJ / Fall-of-man 6+7 typological comparison state`: the `6 × 360 + 7 × 360 = 13 × 360` decomposition is a typological note requiring source-control if made proof-bearing. |
| Glossary / structural-design register | Add provisional `File_23 Peleg-centered 2400 + 240 + 2400 chiasm state`: `6566 → 4166 → 3926 → 1526` as a schematic chiasm centered on Peleg's full schematic lifespan. |
| Glossary / display-state register | Add provisional `File_23 double-6086 Shem node-class state`: `6086 BC` as both no-Cainan Shem birth / start and with-Cainan Shem death / Arphaxad birth under distinct schematic states. |
| Glossary / comparison-state register | Add provisional `File_23 AD 35 rounded Christ-death mod-120 culmination state`: AD 35 as a bounded rounded Christ-death comparison label, not an actual Passion-date replacement. |
| Glossary / diagnostic register | Add provisional `File_23 AD 35 mod-360 selected-subset diagnostic state`: `5` of `11` distinct mod-120 labels landing on AD 35 by exact `360`-year multiples, diagnostic only unless a formal scan model is supplied. |
| Glossary / comparison-state register | Add provisional `File_23 460-cycle / LXX-MT Mirror slot comparison state`: with-Cainan `16166–15206 BC` and no-Cainan `15686–14726 BC` operate as high-density 460-cycle comparison nodes. |
| Glossary / operator register | Add provisional `File_23 mod-10 Mirror display state`: bounded use of `BC + AD − 2`, distinct from civil cross-axis `BC + AD − 1` and formal Mirror target-generation. |
| Glossary / comparison-state register | Add provisional `File_23 MT / LXX 21000 conversion state`: `19320 × 25/23 = 21000`, `20700 × 70/69 = 21000`, and `16166 BC → AD 4836 = 21000` under mod-10 Mirror display. |
| Glossary / comparison-state register | Add provisional `File_23 LXX Jared 960-slot comparison state` and `File_23 LXX Noah 1656 expansion state`. |
| Glossary / comparison-state register | Add provisional `File_23 no-Cainan Temple-switch state`: `15686 → 966` and `14726 → 6` by `32 × 460`, with Temple-body typology dependency-controlled. |
| `File_08` / `File_19` cross-reference index | Note that `File_23` uses the `966 BC` Temple construction anchor only as bounded no-Cainan Temple-switch comparison material. |
| `File_10` / LXX source-control index | Note that `File_23` materially references LXX chronology in the 460-cycle comparison and requires Smith-style LXX reconstruction source-control if the rows become proof-bearing. |
| `File_22` cross-reference index | Note that `File_23` uses `4836 BC` and `9890 = 23 × 430` as bounded cumulative Flood / Decimal Bridge comparison material without revising File_22. |
| Glossary / comparison-state register | Add provisional `File_23 Adam 930 mirror-pair comparison state`: schematic Adam `16136–15206 BC` compared with MT `4116–3186 BC/AD` and LXX `5496–4566 BC/AD` under bounded mod-10 display. |
| Glossary / comparison-state register | Add provisional `File_23 Peleg / Adam / Moses 230-460 Mirror integration state`: Peleg, Adam, Shem, and Moses linked primarily by `460` cycle rows under same-side and bounded mod-10 Mirror displays, with `230`-only rows marked diagnostic and downweighted unless tied to a known `460` path. |
| Glossary / comparison-state register | Add provisional `File_23 non-exhaustive 460-cycle foregrounded-list state`: the §5.9.3d list highlights representative full `460` examples, including `2760`, `4600`, `8280`, `9660`, `6900`, `14720`, `13800`, `19320`, and `20700`, without claiming to exhaust all possible rows. |
| Glossary / audit-control register | Add provisional `File_23 6085/6086 Shem shorthand audit state`: author shorthand `6085 BC` remains audit-controlled while the active File_23 schematic label remains `6086 BC`. |
| `File_07` / Creation index | Note bounded MT Adam `4116–3186 BC/AD` mirror-pair comparison and the `16136 BC` Creation-of-Adam mod-10 companion if File_07 later indexes File_23 Adam `930` mirror material. |
| `File_10` / LXX source-control index | Note bounded LXX Adam `5496–4566 BC/AD` mirror-pair comparison if File_10 later indexes File_23 LXX `930` material. |
| `File_13` / Christological index | Note `36 BC` / AD 36 only as bounded File_23 mod-10 comparison material, not as a Passion-date replacement or Luke-rail revision. |
| `File_54` / Luke-Rounded comparison index | Note that File_23's `36 BC` label is not automatically the File_54 / Luke `36 BC` rail unless a local dependency explicitly opens that state. |
| Glossary / display-state register | Add provisional `File_23 no-Cainan / with-Cainan schematic overlay state`: paired schematic columns that must not replace actual MT, LXX, cumulative, or Rounded Scaffold chronology. |
| Glossary / display-state register | Add provisional `File_23 actual / rounded / schematic mod-120 firewall state`: actual MT dates, File_51a Rounded Scaffold dates, and schematic mod-120-year span coordinates remain distinct even when clean multiples align. |
| Glossary / comparison-state register | Add provisional `File_23 bounded civil cross-axis comparison state`: §§5.9.3 / 5.9.3a / 5.9.5 comparison-only use of `BC + AD − 1` without opening formal Mirror proof protocol. |
| Glossary / comparison-state register | Add provisional `File_23 Key-of-23 halving-node comparison state`: later §5 displays using `25/23`, `70/69`, and `300/299`; visible arithmetic checked in arithmetic-control review and argument-control classification while preserving comparison-only status. |
| Glossary / claim-status register | Add provisional `File_23 bounded anchor-corroboration state`: checked alignments at major repository anchors may support the argument but do not prove design by themselves. |
| `File_51a` cross-reference index | Note that `File_23` now cites the actual / rounded lifespan comparison layer and should be reviewed later to ensure Rounded Scaffold non-retrojection. |
| `File_13` cross-reference index | Note that `File_23` uses AD 35 only as a rounded Christ-death comparison label and does not replace File_13's actual Passion date-state controls. |
| `File_54` cross-reference index | Note that `File_23` cites AD 35 rounded Christ-death / contraction vocabulary only as bounded comparison material. |
| `File_20` cross-reference index | If the BJ / Fall-of-man `6 + 7` comparison remains recurring, add a dependency note that `File_23` uses it only as typological / theological comparison, not as local proof-bearing BJ chronology. |
| `File_21` cross-reference index | Confirm that `File_23` is listed as the Mosaic Bookend / Tebah Protocol file for the `3 months and 3.5 days` fractional payload. |
| `File_22` cross-reference index | Confirm that `File_23` supplies the upstream Tebah / SKL source-state for the Aaron/Moses `L = 3.5` branch, the `12600` echo, the `1/3600` scale expression, and the Adam/Moses `1/12` boundary-flank design. |
| `File_24` completed source | Carry forward the En-tarah-ana fractional-tail state into the completed `File_24` First Dynasty of Kish cipher. The `16560` harmonic is retained there as a biblical-interplay example without requiring `File_23` to redefine the Kish cipher or execute whole-year SKL date-generation. |

## Revision and archive note

This public-clean Markdown source removes only archival amendment history, pass logs, pressure-test repetition, pointer-refresh records, and obsolete replacement-workflow notes.

Detailed revision history is preserved in the `Repository_Change_Archive`.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed.

Final pressure-test status is preserved in the file header. Detailed pressure-test records are archived rather than repeated in the public file body.

File: File_01
Title: Abraham — Patriarch Generation 20 and the Fractal Pivot
Status: Final; post-final pressure test complete; 1876 Cainan macro-vector cross-file update applied; SOR register-extension / Benjamin label bounded update applied (July 2026)
Publication-cleanup status: Public-clean Markdown source; archival amendment history routed to Repository_Change_Archive.
Pressure-test status: Current; publication-clean verification complete; detailed pressure-test record archived rather than repeated in the public file body. Focused check complete for the SOR register-extension / Benjamin label bounded update.
Register Agreement status: Checked against State_Vocabulary_Register v1.18 and active companion controls; bounded SOR register-extension and Benjamin birth-label addition (§0.1 row; §2 register row and notes) only; no pre-existing arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries changed.
Primary domain: Regular; Cumulative; Comparative; Theological; Prophetic
Traditions: MT; SP; LXX; BJ; SOR
Canonical source: Markdown
Primary anchors: 1446 BC; 1876 BC; 2166 BC; 2091 BC; 1991 BC; 1951 BC; 1776 BC; 2435 BC; 2260 BC; 2438 BC; 2263 BC; 1981 BC; 1906 BC; 1806 BC; 1946 BC; 1846 BC; 1406 BC; 1231 BC; AD 60; 6 BC; AD 25/26; 3901–3894 BC; 3896 BC; 3894 BC; 3926 BC; 2238 BC; 1936 BC; 1656 BC; 2066 BC; 2006 BC
Related files: `File_00`; `File_05`; `File_16`; `File_18`; `File_51a`; `File_51c`; `File_53`; `File_54`; `File_20`; Restart Capsule v11.17
Major operators: +60 Terah; SP-215 / Sojourn 215; same-side BC span; civil cross-axis span; Cainan macro-vector B6 by dependency; regular-Cainan `460` same-side symmetry by dependency; +3.5 Aaron/Moses offset; 720 × 360 scaling; 40 × 49 Jubilee harmonic; 6 × 360 prophetic scaling; +30 Ministry Shift; `160 = 4 × 40`
Major modal states: MT Regular Abraham biography; MT Adjusted / SP-215 Abraham biography; SP Regular Abraham biography; LXX Regular death-tumbler witness; 1876 BC convergence year-label; Cainan macro-vector Abraham / 1876 dependency state; 1991/1776 BC death slash-pair; MT Cumulative Moses register; MT Cumulative Aaron register; BJ / Jubilees register; SOR / Seder Olam low-bound register; subordinate Ramesside Exodus state; Sarah harmonic state; cross-modal 390 harmonic; Messianic Week state; AD 25/26 ministry slash-pair / display-state; SOR MT-sub-variant classification by File_20 dependency; Benjamin 1906 birth-label state
Revision basis: 490d Repository Style Guide v2.5; Restart Capsule v11.17; State Vocabulary Register v1.18; Project Procedures v3.3; `File_00`; `File_16`; `File_18`; `File_51a`; `File_54`; prior finalized source; Repository_Change_Archive (non-controlling history)
Current refresh note: Public-clean trust-status equalization aligns active control pointers to 490d Repository Style Guide v2.5, Restart Capsule v11.17, State_Vocabulary_Register v1.18, and Project Procedures v3.3. Register Agreement status is recorded in the header; no arithmetic, anchors, modal states, node-classes, operators, sign conventions, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed. Detailed pass logs, pressure-test records where applicable, pointer-refresh history, and replacement-workflow notes are preserved in Repository_Change_Archive. Bounded SOR register-extension and Benjamin birth-label addition registered from the Regular-page publication discussion record.

# Abraham — Patriarch Generation 20 and the Fractal Pivot

## 0. File-function

`File_01` is an entity file for Abraham as Patriarch Generation 20 and as the Fractal Pivot within the repository chronology.

The file registers Abraham’s major chronological states across MT Regular, MT Adjusted / SP-215, SP Regular, LXX Regular death-tumbler witness, MT Cumulative, BJ / Jubilees, and SOR / Seder Olam witnesses.

This file does not replace the anchor definitions in `File_05`, the `1876 BC` convergence proof in `File_16`, the axiom and operator definitions in `File_00`, or the cumulative and rounded controls in `File_51a`.

### 0.1 Working state register

The following states are active in `File_01`. They are simultaneous, non-competing working states unless a local section explicitly narrows the working state.

| State | Primary date(s) | Node-class / function | Handling in `File_01` |
|---|---:|---|---|
| MT Regular Abraham biography | 2166 BC; 2091 BC; 1991 BC | Birth; Call at 75; Death at 175 | Base biography state for the 720-year fractal, Messianic Week, and ordinary MT Abraham row. |
| MT Adjusted / SP-215 Abraham biography | 1951 BC; 1876 BC; 1776 BC | adjusted Birth; adjusted Call; adjusted Death | Variant biography state; supports `1876 BC` Nexus and `1776 BC` death-tumbler member. |
| SP Regular Abraham biography | 1951 BC; 1876 BC; 1776 BC | SP Birth; SP Call; SP Death | Comparative SP witness; does not overwrite MT Regular. |
| LXX Regular death-tumbler witness | 1951 BC; 1876 BC; 1776 BC | LXX-associated Abraham death state | Final handling: `1776 BC` is retained as the LXX Regular member of the death-tumbler slash-pair. |
| 1876 BC convergence year-label | 1876 BC | Entry / Call / convergence Super-Anchor | Dependency-controlled by `File_16`; locally functions as Node A. |
| Cainan macro-vector Abraham / 1876 dependency state | 2091 BC; 1876 BC; 1661 BC; regular Cainan `2551–2091 BC` and `2336–1876 BC` by File_16 / File_18 dependency | Cainan macro-vector witness touching Abraham Call / Entry labels | `File_16` controls Category B / B6 classification; `File_18` supplies source values. In `File_01`, this state clarifies that the `1876 BC` Abraham Call / Entry label is a shared target, not a new Abraham biography row. |
| 1991/1776 BC death slash-pair | 1991 BC; 1776 BC | Death-state slash-pair | `1991 BC` = MT Regular Abraham Death; `1776 BC` = MT Adjusted / SP Regular / LXX Regular Abraham Death. The slash-pair is not a range. |
| MT Cumulative Moses register | 2435 BC; 2260 BC; 1406 BC | cumulative Abraham node; lifespan-close / successor node; Moses death endpoint | Pair arithmetic checks locally; `12600` remains dependency-controlled by `File_51a` and Restart Capsule v11.15. |
| MT Cumulative Aaron register | 2438 BC; 2263 BC; +3.5 | cumulative priestly display labels; Aaron/Moses offset | Integer display labels retained; exact `+3.5` offset preserved as underlying modal operator. |
| BJ / Jubilees register | 1981 BC; 1906 BC; 1806 BC; 1406 BC | BJ Birth; Call; Death; Conquest target | Expanded in Final source because dependency evidence supplies the Abrahamic 500/400 conquestal frame. |
| SOR / Seder Olam low-bound register | 1946 BC; 1846 BC; 1446 BC; extension labels 3901–3894 BC; 3896 BC; 3894 BC; 3926 BC; 2238 BC; 1936 BC; 1656 BC | Abraham node; Isaac / 400-year node; Exodus target; Creation-to-Entry extension labels | Retained as low-bound comparative register, not forced into a full Abraham biography row. Extended July 2026 with the File_20 §7A landing-lattice labels; the SOR is classified there as an MT sub-variant (`−220 = −(215 + 5)`), not a −215 sub-variant. `File_20` controls the protocol. |
| subordinate Ramesside Exodus state | 1231 BC; AD 60 | subordinate Exodus anchor; Rome Harmonic target | Dependency-controlled by `File_05`; Danielic register controlled by `File_16`, §7. |
| Sarah harmonic state | 4116 BC; 2156 BC; 1960 | Adam Year-6 state; Sarah MT Regular birth; Jubilee harmonic | Active Adam state is resolved as `4116 BC`; Sarah’s MT Regular birth is `2156 BC`. |
| cross-modal 390 harmonic | 2166 BC; 1776 BC | MT Regular Birth combined with adjusted / SP / LXX Death | Preserved as cross-modal harmonic, not as Abraham’s single-state lifespan. |
| Messianic Week state | 2166 BC; 6 BC; AD 25/26 | prophetic scaling / Creation-pattern source label | `Creation Mirror` remains source-retained Creation-pattern language, not File_00 Mirror protocol. |

Modal-state rule:
A shared date-label, number, or span does not create identity unless state, node-class, tradition, and operator agree.

Note: The Aaron/Moses branch has two legitimate representations:

1. an ordinary Nisan/Nisan birth-label relation preserving the three-year Aaron/Moses separation;
2. a phase-resolved Nisan/Tishri priestly straddle in which the Aaron branch may shift six months earlier, yielding the `L = 3.5` operator.

The `+3.5` operator must not erase the legitimate three-year branch, and the three-year integer labels must not erase the phase-resolved `+3.5` operator.


### 0.2 File map

| Section | Function | Active state / operator |
|---|---|---|
| §0 | Defines file function, working states, claim-status controls, Machine Guards, arithmetic controls, and argument controls. | file-level modal states; dependency-boundary discipline |
| §1 | Defines Node A and Node B and verifies the `1876 BC` Nexus and `1991/1776 BC` death slash-pair. | convergence year-label; death-tumbler slash-pair |
| §2 | Separates primary Abraham biography rows from cumulative and comparative registers. | MT Regular; MT Adjusted / SP-215; SP; LXX; MT Cumulative; BJ; SOR |
| §3 | Tests the 720-year fractal, Sarah `1960` harmonic, and Rome `1290` harmonic. | same-side BC span; civil cross-axis span |
| §4 | Preserves the `390` Abraham harmonic as cross-modal rather than single-state lifespan. | MT Regular birth plus adjusted / SP / LXX death-state |
| §5 | Preserves the Messianic Week and `AD 25/26` ministry slash-pair. | same-side BC span; `+30` Ministry Shift |
| §6 | Identifies the active cross-file dependencies. | `File_00`; `File_05`; `File_16` |
| §7 | Records final dependency boundaries for future revision. | dependency tracking |
| Revision and archive note | Records public-clean archive routing and preservation controls. | publication cleanup |

### 0.3 Publication-cleanup control

This public-clean source preserves the Final and post-final pressure-tested file status. Detailed finalization, pressure-test, file-map refresh, and 1876 Cainan macro-vector update records are archived rather than repeated in the public body.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, theological claims, or dependency boundaries are changed in this cleanup.

### 0.4 Claim-status register for `File_01`

| Label | Function in `File_01` |
|---|---|
| Textual datum | Source-transmitted tradition, age, anchor, or dependency statement preserved from the file or its controlling cross-reference. |
| Arithmetic fact | Direct calculation checked under the stated operator. |
| Conditional arithmetic fact | Calculation that checks only if the active state or node-class is supplied by dependency review. |
| Structural inference | Pattern inferred from repeated state-controlled alignments or harmonic recurrence. |
| Dependency-controlled | Claim preserved locally because `File_00`, `File_05`, `File_16`, `File_51a`, or another dependency supplies the controlling proof or operator. |
| Typological reading | Theological or symbolic correspondence anchored to the biblical chronology field. |
| Theological note | Theological meaning preserved without making the theology carry the arithmetic burden. |
| Source-history note | Legacy wording preserved for traceability after current repository terminology clarifies the active state. |
| Display-state note | A printed value or slash-pair member retained as a display state without replacing the exact operator. |
| Audit note | Value, state, source label, or dependency requiring later review. No blocking audit note remains in this Final source. |

Argument-control rule:
Arithmetic facts and textual/dependency data carry the base layer of the file. Structural inferences may support the argument when state, node-class, tradition, and operator are explicit. Theological and typological notes explain why a span or node matters but do not replace arithmetic or dependency controls.

### 0.5 Terminology controls

| Legacy wording | Final wording / handling |
|---|---|
| Terah +60 | `+60 Terah` when the structural variator is active. |
| Terah 70 +60 | Preserved only as a source-history compression; not made load-bearing in the MT Regular biography row. |
| SP-215 variants | SP-215 / Sojourn 215 variant. |
| Jubilees | BJ / Jubilees where the repository abbreviation is needed. |
| Seder Olam | SOR / Seder Olam where the repository abbreviation is needed. |
| Ramesside Exodus | subordinate Ramesside Exodus state. |
| AD 25/26 | retained as a slash-pair / display-state. |

### 0.6 Machine Guards for `File_01`

Machine Guard [ABRAHAM 1876 YEAR-LABEL]:

`1876 BC` is a shared year-label. It must not be treated as a single node merely because several systems land on it. In `File_01`, it functions locally as Node A, the Nexus, with Entry / Call language. By dependency, `File_16` expands the year-label into multiple node-classes. These are related convergence witnesses, not one collapsed event.

Machine Guard [ABRAHAM CAINAN MACRO-VECTOR DEPENDENCY]:

The Cainan macro-vector touches Abrahamic labels such as `2091 BC` and `1876 BC` by dependency, but it does not add a new Abraham biography row in `File_01`. `File_16` controls the B6 Category B argument. `File_18` supplies table-source values. Keep Abraham Call / Entry, regular Cainan death, lower-bound Entry, and Cainan macro-vector target labels distinct.

Machine Guard [ABRAHAM NODE B SLASH-PAIR]:

`1991/1776 BC` is a modal death slash-pair, not a range and not a corrected single date. `1991 BC` belongs to the MT Regular death row. `1776 BC` belongs to the MT Adjusted / SP Regular / LXX Regular death state.

Machine Guard [ABRAHAM CUMULATIVE ROWS]:

The MT Cumulative Moses and MT Cumulative Aaron rows are cumulative registers, not ordinary Abraham Birth / Call / Death biography rows. Do not insert a cumulative Call node unless a dependency or later author decision defines that node-class.

Machine Guard [ABRAHAM AARON +3.5]:

The Aaron/Moses priestly offset remains exact at `+3.5`. The printed `2438 BC` and `2263 BC` labels are integer display labels and must not be used to reduce the exact offset to `+3` or `+4`.

Machine Guard [ABRAHAM RAMESSES NON-REPLACEMENT]:

`1231 BC` is the subordinate Ramesside Exodus state. It is valid for the Rome Harmonic and dependency-controlled prophetic patterns, but it must not replace the primary `1446 BC` Exodus anchor used in the chronological matrix.

Machine Guard [ABRAHAM CROSS-MODAL 390]:

The `390` harmonic uses `2166 BC` from the MT Regular Abraham Birth state and `1776 BC` from the adjusted / SP / LXX death state. It is preserved as a cross-modal harmonic. It must not be silently reclassified as Abraham’s ordinary lifespan in a single chronology.

Machine Guard [ABRAHAM CREATION MIRROR LABEL]:

The source classification `Prophetic Scaling / Creation Mirror` is preserved in the Messianic Week section. In this file, this label is source-retained Creation-pattern language. It is not File_00 foundational Mirror protocol, Protocol 1 Mirror, Protocol 2 Mirror, Mirror coordinate-completion, or Mirror corroboration.

Machine Guard [ABRAHAM AD 25/26]:

`AD 25/26` is a slash-pair / display-state produced by the `+30` Ministry Shift. `AD 25` is the exact civil +30 landing from `6 BC`; `AD 26` remains a calendar or ministry display member.

### 0.7 Arithmetic-control summary

| Arithmetic type | Rule used in `File_01` |
|---|---|
| Same-side BC span | subtract lower BC date from higher BC date |
| Civil cross-axis span | `BC + AD − 1` |
| Factorization | direct multiplication or division |
| Slash-pair | check each member under its own state; do not collapse into one date |
| Cumulative register | check visible local spans; keep full trunk dependency-controlled |
| Display-state | preserve display label where exact operator remains active |
| Undefined dependency | mark as `Dependency note:` or `Display-state note:` |

Arithmetic-control result:
The explicit same-side and cross-axis calculations in the main body check under the stated operators. The cumulative `12600` trunk, Aaron `+3.5` display labels, `1876 BC` convergence proof, Daniel Quadruple Lock, and subordinate Ramesside anchor field remain dependency-controlled.

### 0.8 Argument-control summary

`File_01` argues by layering arithmetic fact, state-controlled structural inference, and theological or typological meaning.

| Argument layer | Function |
|---|---|
| Arithmetic base | The checked spans: `720`, `1960`, `1290`, `390`, `2160`, `30`, and row-level lifespan checks. |
| Modal-state control | The file preserves MT Regular, adjusted / SP, LXX, cumulative, BJ, SOR, Ramesside, and Messianic states without collapsing them. |
| Structural inference | Harmonics such as `720`, `390`, `2160`, and `1290` are treated as pattern claims after the arithmetic and modal state are named. |
| Theological / typological explanation | Sarah as New Eve, Father of Faith to Author of Faith, Ezekiel 4:5, and Daniel 12 explain meaning but do not carry the arithmetic burden. |
| Dependency-controlled proof | `1876 BC` convergence, Exodus `1446 BC` / `1231 BC`, Daniel Quadruple Lock, `+60 Terah`, SP-215 / Sojourn 215, and cumulative `12600` logic remain cross-file controlled. |

Argument-scope result:
The file’s claims are preserved with explicit claim-status labels and local burden-of-proof controls.

## 1. The Twin Pillars: The Binary Node System

| Node | Date label | Local node-class | States | Modal handling | Argument role |
|---|---:|---|---|---|---|
| Node A | 1876 BC | Nexus | MT Regular; SP-215; subordinate Ramesside convergence field by dependency | Life Setting; Entry / Call node. The year-label is shared by multiple dependency-controlled node-classes. | Dependency-controlled structural anchor. |
| Node B | 1991/1776 BC | Tumbler | MT Regular; MT Adjusted; SP Regular; LXX Regular | Death Setting. `1991 BC` is MT Regular Abraham Death; `1776 BC` is MT Adjusted / SP Regular / LXX Regular Abraham Death. | Death-tumbler slash-pair. |

Source-history note:
The legacy source labeled Node B as `LXX; SP Regular`. Final handling preserves that source-history while spelling out the active slash-pair members: `1991 BC` for MT Regular and `1776 BC` for adjusted / SP / LXX death-state.

### 1.1 Node A arithmetic checks

Active state:
Node A / `1876 BC` year-label.

| Active state | Calculation | Result | Arithmetic status | Claim-status |
|---|---|---:|---|---|
| MT Regular Entry from primary Exodus | `1446 + 430` | 1876 BC | checks | Arithmetic fact; dependency-controlled by `File_16`. |
| MT Adjusted / SP-215 Abraham Call | `1951 − 75` | 1876 BC | checks | Arithmetic fact. |
| SP Regular Abraham Call | `1951 − 75` | 1876 BC | checks | Arithmetic fact. |
| SP-215 shift from MT Regular Call | `2091 − 215` | 1876 BC | checks | Arithmetic fact; variant-state witness. |
| subordinate Ramesside convergence path | `1231 + 430 + 215` | 1876 BC | checks | Dependency-controlled arithmetic witness from `File_16`. |

Dependency note:
`1876 BC` is locally the Nexus of Entry / Call in `File_01`. `File_16`, §2 controls the broader convergence proof. `File_01` preserves the arithmetic witnesses without reproducing the full proof.

Claim-status note:
The local argument does not require all `1876 BC` witnesses to be identical events. It requires only that `1876 BC` remain a shared year-label whose node-class is stated by active tradition and dependency.

### 1.2 Node B arithmetic checks

Active state:
Node B / `1991/1776 BC` death slash-pair.

| Active state | Calculation | Result | Arithmetic status | Claim-status |
|---|---|---:|---|---|
| MT Regular Abraham Death | `2166 − 175` | 1991 BC | checks | Arithmetic fact. |
| MT Adjusted / SP Regular / LXX Regular Abraham Death | `1951 − 175` | 1776 BC | checks | Arithmetic fact. |
| SP-215 shift from MT Regular Death | `1991 − 215` | 1776 BC | checks | Arithmetic fact; variant-state witness. |
| relationship between slash-pair members | `1991 − 1776` | 215 | checks; not a range | Arithmetic fact; modal-state relation. |

Claim-status note:
Node B’s local argument role is to preserve the death-tumbler slash-pair. The LXX label is no longer a blocking audit item; it is classified as the LXX Regular witness to the `1776 BC` death-state.

## 2. Chronological Matrix

All matrix dates are BC unless otherwise labeled. The matrix is anchored to the primary Exodus state at `1446 BC`.

State-control note:
The regular and adjusted Abraham rows are biography states. The cumulative, BJ, and SOR rows are placed in a separate register table so their node-classes are not forced into the ordinary Birth / Call / Death matrix.

### 2.1 Primary Abraham biography rows

| Tradition / state | Birth node | Call at 75 node | Death at 175 node | Logic / variant | Arithmetic check | Argument role |
|---|---:|---:|---:|---|---|---|
| MT Regular | 2166 BC | 2091 BC | 1991 BC | Standard MT: Terah 70 state; Egypt = 430. | `2166 − 2091 = 75`; `2166 − 1991 = 175`; `1876 − 1446 = 430`. | Primary regular biography state; base for 720, 390 cross-modal comparison, and 2160 Messianic Week. |
| MT Adjusted | 1951 BC | 1876 BC | 1776 BC | SP-215 / Sojourn 215 axiom: aligns Call with Jacob’s Entry. | `1951 − 1876 = 75`; `1951 − 1776 = 175`; `2166 − 215 = 1951`; `2091 − 215 = 1876`; `1991 − 215 = 1776`. | Adjusted biography state; supports Node A and Node B without replacing MT Regular. |
| SP Regular | 1951 BC | 1876 BC | 1776 BC | Standard SP: Terah lives 145 years. | `1951 − 1876 = 75`; `1951 − 1776 = 175`. | Comparative SP witness. |
| LXX Regular death-tumbler witness | 1951 BC | 1876 BC | 1776 BC | LXX / Acts-associated death-tumbler witness. | `1951 − 1876 = 75`; `1951 − 1776 = 175`. | Comparative LXX witness to the `1776 BC` death-state. |

Arithmetic note:
The MT Regular `Egypt = 430` line is checked through the Entry node, not through Abraham’s MT Regular Call node. The relevant calculation is `1876 − 1446 = 430`.

Source-history note:
The legacy MT Regular row compressed this as `Terah 70 +60`. In current repository terminology, `+60 Terah` is a separate structural variator and is not made load-bearing inside the MT Regular biography row.

Claim-status note:
The biography rows are textual / tradition-state data with arithmetic facts attached. Interpretive claims in later sections must cite the active row before using its dates.

### 2.2 Cumulative and comparative registers

The following rows are separated from the ordinary Abraham biography table because they use cumulative, BJ, or SOR register logic.

| Register | Primary node | Secondary node | Target / endpoint | Function | Arithmetic check | Status |
|---|---:|---:|---:|---|---|---|
| MT Cumulative, Moses register | 2435 BC | 2260 BC | 1406 BC | Abraham cumulative node to lifespan-close / successor node; cumulative trunk ends at Moses’ death. | `2435 − 2260 = 175`. | Local pair checks; `12600` remains dependency-controlled by `File_51a` and Restart Capsule v11.15. |
| MT Cumulative, Aaron register | 2438 BC | 2263 BC | +3.5 | Aaron / priestly cumulative display labels. | `2438 − 2263 = 175`; integer labels differ from Moses row by 3. | Exact underlying Aaron/Moses offset remains `+3.5`; display-state note active. |
| BJ / Jubilees | 1981 BC | 1906 BC | 1806 BC; 1406 BC | Abraham Birth, Call, Death, and Conquest target in the Abrahamic 500/400 conquestal frame. | `1981 − 1906 = 75`; `1981 − 1806 = 175`; `1906 − 1406 = 500`; `1806 − 1406 = 400`. | Expanded because dependency evidence supplies all three Abrahamic biography cells. |
| SOR / Seder Olam | 1946 BC | 1846 BC | 1446 BC | SOR low-bound register: Abraham node to Isaac / 400-year node to Exodus. | `1946 − 100 = 1846`; `1846 − 1446 = 400`. | Retained as comparative low-bound register; Abraham Call and Death are not inserted. Extension labels (July 2026, `File_20` §7A): Creation week `3901–3894 BC`; Year 6 `3896` (`2450 = 49 × 50`); endpoint `3894` (`2448`); apparent head `3926` (`2520`); Flood `2238`; Sarah `1936` (`490`); Entry `1656 BC` (`210` in Egypt). MT-sub-variant classification per `File_20`. |

Dependency note:
The cumulative `12600` statement is not re-proved inside `File_01`. This file preserves the Abraham-local pair and points the cumulative trunk to `File_51a` and Restart Capsule v11.15.

Display-state note:
The Aaron row preserves `+3.5` as the source’s Priestly Offset. The printed integer date labels `2438 BC` and `2263 BC` remain display labels; they do not reduce the exact underlying offset to `+3` or `+4`.

Claim-status note:
The BJ row is expanded because its 500/400 conquestal frame is structurally active. The SOR row remains a low-bound comparative register because the source supplies Abraham and Isaac/Exodus frame logic but does not require a full Abraham Birth / Call / Death biography row.

Shared-label note — Benjamin `1906 BC` (bounded addition):
The BJ Call label `1906 BC` is also the Regular-page birth-label of Benjamin, born in Jacob's hundredth year (`2006 − 100 = 1906`; Gen. 35:16–18) — the son-at-100 reprising Abraham's 99/100 circumcision-to-Isaac pattern, with the two hundredth-year sons standing `2066 − 1906 = 160 = 4 × 40` apart. The two `1906` states are distinct: BJ Call (this register) and Benjamin birth-label (page-table state); do not collapse. Claim-status: textual datum (Gen. 35 sequence; the hundredth-year framing as structural inference from the page derivation); arithmetic fact (the spans); typological reading (the son-at-100 parallel).

## 3. The Logic Engine

### 3.1 The 720-Year Fractal

Active state:
MT Regular Abraham Birth to primary Exodus anchor.

Node-class:
Birth node to Exodus anchor.

Operator label:
same-side BC span.

Arithmetic fact:

```markdown
2166 − 1446 = 720
```

Factorization:

```markdown
720 = 2 × 360
```

Cosmic scaling retained from source:

```markdown
720 × 360 = 259200
```

Arithmetic status:
The same-side span and scaling calculations check.

Modal-state note:
This section uses the MT Regular Abraham Birth state, not the adjusted / SP / LXX Abraham Birth state.

Claim-status note:
The `720` span is an arithmetic fact. The connection to `2 × 360` is a structural inference within the prophetic-year register. The scaling to `259200` Sumerian years is retained as a structural scaling note, not as a separate historical chronology.

### 3.2 The Sarah Harmonic: The New Eve

Active state:
MT Year-6 / prime-chronology Year-6 Adam state, `4116 BC`, with Sarah’s MT Regular birth at `2156 BC`.

Node-class:
matriarchal harmonic using Adam Year-6 state and Sarah’s MT Regular birth.

Anchor:
Sarah’s Birth is 10 years after Abraham in the MT Regular biography state.

Arithmetic fact:

```markdown
2166 − 10 = 2156 BC
```

Adam-to-Sarah span:

```markdown
4116 − 2156 = 1960
```

Harmonic:

```markdown
1960 = 40 × 49
```

Theological note:
Sarah represents the completion, `40`, of the Jubilee promise, `49`, initiated at Adam.

Typological reading:
The source’s “New Eve” language is preserved as a typological reading. It explains the theological meaning of Sarah’s placement but does not carry the arithmetic burden.

Claim-status note:
The `1960 = 40 × 49` harmonic is an arithmetic fact under the active Adam Year-6 state and Sarah MT Regular birth state. The Sarah / New Eve claim is a theological and typological note, not the arithmetic basis of the section.

### 3.3 The Rome Harmonic

Active state:
subordinate Ramesside Exodus state.

Node-class:
subordinate Exodus anchor to NT Rome target.

Operator label:
civil cross-axis span.

Calculation retained from source:

```markdown
1231 BC to AD 60 = 1290 years
```

Civil-span verification:

```markdown
1231 + 60 − 1 = 1290
```

Arithmetic status:
The cross-axis span checks under the repository civil-span operator.

Dependency note:
The `1231 BC` anchor is controlled by `File_05`. The Danielic / Quadruple Lock register is controlled by `File_16`, §7. The local arithmetic is civil cross-axis arithmetic, not Mirror arithmetic.

Modal-state note:
The Rome Harmonic uses the subordinate Ramesside Exodus state. It does not replace the primary `1446 BC` matrix anchor.

Claim-status note:
The `1290` span is an arithmetic fact under civil cross-axis counting. The Daniel 12 identification is dependency-controlled and should be treated as structural / prophetic interpretation, not as a local proof supplied by `File_01` alone.

## 4. The 390-Year Harmonic

Active state:
cross-modal Abraham harmonic.

Node-class:
MT Regular Birth node combined with adjusted / SP / LXX Death node.

Operator label:
same-side BC span across modal states.

Arithmetic fact:

```markdown
2166 − 1776 = 390
```

Theological note:
This span encapsulates the Ezekiel 4:5 “Iniquity of Israel” span within the Patriarch’s life.

Arithmetic status:
The subtraction checks. The state remains cross-modal because it combines `2166 BC` from the MT Regular Abraham Birth state with `1776 BC` from the adjusted / SP / LXX death state.

Machine Guard restated:
This calculation must not be described as Abraham’s single-state lifespan. Abraham’s ordinary lifespan in each biography row remains 175 years.

Claim-status note:
The `390` value is an arithmetic fact across modal states. Its Ezekiel 4:5 interpretation is a theological and typological reading. The argument should say that the harmonic “encapsulates” or “echoes” the Ezekiel span, not that Abraham’s literal lifespan is 390 years.

## 5. The Messianic Week: The 2160 Harmonic

Classification:
Prophetic Scaling / Creation Mirror.

Argument-status note:
The source label `Creation Mirror` is preserved as source-retained local Creation-pattern language. In `File_01`, it does not function as File_00 foundational Mirror protocol, Protocol 1 Mirror, Protocol 2 Mirror, Mirror coordinate-completion, or Mirror corroboration. The local argument is a same-side BC prophetic-scaling argument.

Target:
`6 BC`, Birth of Christ.

### 5.1 The Six-Day Span

Active state:
MT Regular Abraham Birth to Messianic target.

Node-class:
patriarchal birth anchor to Christological target.

Operator label:
same-side BC span.

Arithmetic fact:

```markdown
2166 − 6 = 2160
```

Factorization:

```markdown
2160 = 6 × 360
```

Theological note:
The history from the Father of Faith to the Author of Faith is structured as Six Prophetic Days.

Arithmetic status:
The same-side BC span and factorization check.

Claim-status note:
The `2160` span is an arithmetic fact. The Six Prophetic Days framing is a structural inference and theological reading. The Father of Faith / Author of Faith language is preserved as theological interpretation, not as the arithmetic basis.

### 5.2 The Ministry Shift: +30

Active state:
Messianic Week state with `+30` Ministry Shift.

Node-class:
Christological target shifted into ministry display-state.

Logic:
The Prophetic Week requires a Ministry Shift of `30` years, acting as the Fractal Leap Month of `30` days.

Calculation retained from source:

```markdown
6 BC + 30 years = AD 25/26
```

Civil-span check to `AD 25`:

```markdown
6 + 25 − 1 = 30
```

Civil-span check to `AD 26`:

```markdown
6 + 26 − 1 = 31
```

Arithmetic status:
`AD 25` is the exact civil +30 landing from `6 BC`. `AD 26` is not the exact civil +30 landing under `BC + AD − 1`; it remains a calendar or ministry display member of the slash-pair.

Display-state note:
`AD 25/26` is retained as a ministry slash-pair / display-state. `AD 25` is the exact civil +30 landing; `AD 26` remains a calendar or ministry display member.

Claim-status note:
The `+30` Ministry Shift is a structural inference derived from the Messianic Week state. Its “Fractal Leap Month” language is source-retained theological / symbolic framing and should not be used to override the civil-span arithmetic.

## 6. Cross-Reference Index

| Function | Legacy source reference | Current controlling reference | Scope |
|---|---|---|---|
| Axiom Definition | `File_00`, Section 2.B | `File_00 Final`, §§3.2–3.4.1 | `+60 Terah`; SP-215 / Sojourn 215 variant logic |
| Anchor Definition | `File_05` | `File_05` | Exodus `1446 BC` primary state; `1231 BC` subordinate Ramesside state |
| Convergence Proof | `File_16`, §2 | `File_16`, §2 | `1876 BC` Convergence |
| Cainan macro-vector | `File_16`, §3 / §7; `File_18`, §1.3.8 | `File_16` / `File_18` focused-pressure-tested update | Category B / B6 Cainan macro-vector dependency; no local Abraham biography row |
| Prophetic Logic | `File_16`, §7 | `File_16`, §7 | Daniel Quadruple Lock |

Dependency note:
The legacy source cites `File_00`, Section 2.B for `+60 Terah` and SP-215 / Sojourn 215 variant logic. In the revised `File_00 Final`, the same material is controlled by §§3.2–3.4.1: Structural Variators, the Sojourn 215 / Exodus 215 distinction, and `+60 Terah`.

Dependency note:
`File_05` controls the Exodus anchor field in this file: `1446 BC` remains primary, while `1231 BC` remains subordinate Ramesside.

Dependency note:
`File_16` controls the broader `1876 BC` convergence field and the Daniel Quadruple Lock. `File_01` preserves those dependencies locally but does not re-prove them.

Claim-status note:
The Cross-Reference Index functions as dependency control. It does not add new local arguments; it identifies which file must supply the governing proof, operator, or anchor definition.

## 7. Final dependency-boundary notes

No author-decision issue remains open in this Final source.

The following notes remain active for future cross-file revision discipline:

| Item | Final handling |
|---|---|
| Node B legacy wording | Resolved by source-history note; `1776 BC` is treated as the adjusted / SP / LXX death-state. |
| `Terah 70 +60` | Resolved by source-history note; `+60 Terah` remains a separate structural variator. |
| Cumulative `12600` | Dependency-controlled by `File_51a` and Restart Capsule v11.15; not locally re-proved in `File_01`. |
| Aaron `+3.5` | Exact offset preserved; `2438 BC` and `2263 BC` remain integer display labels. |
| BJ / Jubilees | Expanded to Birth `1981 BC`, Call `1906 BC`, Death `1806 BC`, Conquest `1406 BC`. |
| SOR / Seder Olam | Retained as low-bound register: Abraham `1946 BC`; Isaac / 400-year node `1846 BC`; Exodus `1446 BC`. |
| Sarah harmonic | Active Adam state resolved as `4116 BC`; Sarah birth `2156 BC`; span `1960`. |
| `AD 25/26` | Slash-pair / display-state retained; `AD 25` exact civil +30 landing; `AD 26` display member. |
| `Creation Mirror` | Source-retained Creation-pattern language; not File_00 Mirror protocol. |

## Revision and archive note

This public-clean Markdown source removes only archival amendment history, pass logs, pressure-test repetition, pointer-refresh records, and obsolete replacement-workflow notes.

Detailed revision history is preserved in the `Repository_Change_Archive`.

Final pressure-test status is preserved in the file header. Detailed pressure-test records are archived rather than repeated in the public file body.

No arithmetic, anchors, modal states, node-classes, sign conventions, operators, slash-pairs, ranges, envelopes, Mirror protocols, Machine Guards, claim-status labels, theological claims, or dependency boundaries are changed.
